/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars*/
import * as $protobuf from "protobufjs/minimal";

// Common aliases
const $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;

// Exported root namespace
const $root = $protobuf.roots["default"] || ($protobuf.roots["default"] = {});

export const dmcn = $root.dmcn = (() => {

    /**
     * Namespace dmcn.
     * @exports dmcn
     * @namespace
     */
    const dmcn = {};

    dmcn.identity = (function() {

        /**
         * Namespace identity.
         * @memberof dmcn
         * @namespace
         */
        const identity = {};

        /**
         * VerificationTier enum.
         * @name dmcn.identity.VerificationTier
         * @enum {number}
         * @property {number} VERIFICATION_TIER_UNVERIFIED=0 VERIFICATION_TIER_UNVERIFIED value
         * @property {number} VERIFICATION_TIER_DOMAIN_DNS=2 VERIFICATION_TIER_DOMAIN_DNS value
         * @property {number} VERIFICATION_TIER_DANE=3 VERIFICATION_TIER_DANE value
         */
        identity.VerificationTier = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "VERIFICATION_TIER_UNVERIFIED"] = 0;
            values[valuesById[2] = "VERIFICATION_TIER_DOMAIN_DNS"] = 2;
            values[valuesById[3] = "VERIFICATION_TIER_DANE"] = 3;
            return values;
        })();

        /**
         * AttestationType enum.
         * @name dmcn.identity.AttestationType
         * @enum {number}
         * @property {number} ATTESTATION_TYPE_UNSPECIFIED=0 ATTESTATION_TYPE_UNSPECIFIED value
         * @property {number} ATTESTATION_TYPE_IN_PERSON=1 ATTESTATION_TYPE_IN_PERSON value
         * @property {number} ATTESTATION_TYPE_FINGERPRINT=2 ATTESTATION_TYPE_FINGERPRINT value
         * @property {number} ATTESTATION_TYPE_NETWORK=3 ATTESTATION_TYPE_NETWORK value
         * @property {number} ATTESTATION_TYPE_ORGANISATIONAL=4 ATTESTATION_TYPE_ORGANISATIONAL value
         */
        identity.AttestationType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "ATTESTATION_TYPE_UNSPECIFIED"] = 0;
            values[valuesById[1] = "ATTESTATION_TYPE_IN_PERSON"] = 1;
            values[valuesById[2] = "ATTESTATION_TYPE_FINGERPRINT"] = 2;
            values[valuesById[3] = "ATTESTATION_TYPE_NETWORK"] = 3;
            values[valuesById[4] = "ATTESTATION_TYPE_ORGANISATIONAL"] = 4;
            return values;
        })();

        identity.AttestationRecord = (function() {

            /**
             * Properties of an AttestationRecord.
             * @memberof dmcn.identity
             * @interface IAttestationRecord
             * @property {string|null} [attesterAddress] AttestationRecord attesterAddress
             * @property {Uint8Array|null} [attesterPubkey] AttestationRecord attesterPubkey
             * @property {string|null} [subjectAddress] AttestationRecord subjectAddress
             * @property {Uint8Array|null} [subjectPubkey] AttestationRecord subjectPubkey
             * @property {dmcn.identity.AttestationType|null} [attestationType] AttestationRecord attestationType
             * @property {number|Long|null} [attestedAt] AttestationRecord attestedAt
             * @property {number|Long|null} [expiresAt] AttestationRecord expiresAt
             * @property {Uint8Array|null} [signature] AttestationRecord signature
             */

            /**
             * Constructs a new AttestationRecord.
             * @memberof dmcn.identity
             * @classdesc Represents an AttestationRecord.
             * @implements IAttestationRecord
             * @constructor
             * @param {dmcn.identity.IAttestationRecord=} [properties] Properties to set
             */
            function AttestationRecord(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AttestationRecord attesterAddress.
             * @member {string} attesterAddress
             * @memberof dmcn.identity.AttestationRecord
             * @instance
             */
            AttestationRecord.prototype.attesterAddress = "";

            /**
             * AttestationRecord attesterPubkey.
             * @member {Uint8Array} attesterPubkey
             * @memberof dmcn.identity.AttestationRecord
             * @instance
             */
            AttestationRecord.prototype.attesterPubkey = $util.newBuffer([]);

            /**
             * AttestationRecord subjectAddress.
             * @member {string} subjectAddress
             * @memberof dmcn.identity.AttestationRecord
             * @instance
             */
            AttestationRecord.prototype.subjectAddress = "";

            /**
             * AttestationRecord subjectPubkey.
             * @member {Uint8Array} subjectPubkey
             * @memberof dmcn.identity.AttestationRecord
             * @instance
             */
            AttestationRecord.prototype.subjectPubkey = $util.newBuffer([]);

            /**
             * AttestationRecord attestationType.
             * @member {dmcn.identity.AttestationType} attestationType
             * @memberof dmcn.identity.AttestationRecord
             * @instance
             */
            AttestationRecord.prototype.attestationType = 0;

            /**
             * AttestationRecord attestedAt.
             * @member {number|Long} attestedAt
             * @memberof dmcn.identity.AttestationRecord
             * @instance
             */
            AttestationRecord.prototype.attestedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * AttestationRecord expiresAt.
             * @member {number|Long} expiresAt
             * @memberof dmcn.identity.AttestationRecord
             * @instance
             */
            AttestationRecord.prototype.expiresAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * AttestationRecord signature.
             * @member {Uint8Array} signature
             * @memberof dmcn.identity.AttestationRecord
             * @instance
             */
            AttestationRecord.prototype.signature = $util.newBuffer([]);

            /**
             * Creates a new AttestationRecord instance using the specified properties.
             * @function create
             * @memberof dmcn.identity.AttestationRecord
             * @static
             * @param {dmcn.identity.IAttestationRecord=} [properties] Properties to set
             * @returns {dmcn.identity.AttestationRecord} AttestationRecord instance
             */
            AttestationRecord.create = function create(properties) {
                return new AttestationRecord(properties);
            };

            /**
             * Encodes the specified AttestationRecord message. Does not implicitly {@link dmcn.identity.AttestationRecord.verify|verify} messages.
             * @function encode
             * @memberof dmcn.identity.AttestationRecord
             * @static
             * @param {dmcn.identity.IAttestationRecord} message AttestationRecord message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AttestationRecord.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.attesterAddress != null && Object.hasOwnProperty.call(message, "attesterAddress"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.attesterAddress);
                if (message.attesterPubkey != null && Object.hasOwnProperty.call(message, "attesterPubkey"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.attesterPubkey);
                if (message.subjectAddress != null && Object.hasOwnProperty.call(message, "subjectAddress"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.subjectAddress);
                if (message.subjectPubkey != null && Object.hasOwnProperty.call(message, "subjectPubkey"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.subjectPubkey);
                if (message.attestationType != null && Object.hasOwnProperty.call(message, "attestationType"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int32(message.attestationType);
                if (message.attestedAt != null && Object.hasOwnProperty.call(message, "attestedAt"))
                    writer.uint32(/* id 6, wireType 0 =*/48).int64(message.attestedAt);
                if (message.expiresAt != null && Object.hasOwnProperty.call(message, "expiresAt"))
                    writer.uint32(/* id 7, wireType 0 =*/56).int64(message.expiresAt);
                if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                    writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.signature);
                return writer;
            };

            /**
             * Encodes the specified AttestationRecord message, length delimited. Does not implicitly {@link dmcn.identity.AttestationRecord.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.identity.AttestationRecord
             * @static
             * @param {dmcn.identity.IAttestationRecord} message AttestationRecord message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AttestationRecord.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes an AttestationRecord message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.identity.AttestationRecord
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.identity.AttestationRecord} AttestationRecord
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AttestationRecord.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.identity.AttestationRecord();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.attesterAddress = reader.string();
                            break;
                        }
                    case 2: {
                            message.attesterPubkey = reader.bytes();
                            break;
                        }
                    case 3: {
                            message.subjectAddress = reader.string();
                            break;
                        }
                    case 4: {
                            message.subjectPubkey = reader.bytes();
                            break;
                        }
                    case 5: {
                            message.attestationType = reader.int32();
                            break;
                        }
                    case 6: {
                            message.attestedAt = reader.int64();
                            break;
                        }
                    case 7: {
                            message.expiresAt = reader.int64();
                            break;
                        }
                    case 8: {
                            message.signature = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an AttestationRecord message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.identity.AttestationRecord
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.identity.AttestationRecord} AttestationRecord
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AttestationRecord.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AttestationRecord message.
             * @function verify
             * @memberof dmcn.identity.AttestationRecord
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AttestationRecord.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.attesterAddress != null && Object.hasOwnProperty.call(message, "attesterAddress"))
                    if (!$util.isString(message.attesterAddress))
                        return "attesterAddress: string expected";
                if (message.attesterPubkey != null && Object.hasOwnProperty.call(message, "attesterPubkey"))
                    if (!(message.attesterPubkey && typeof message.attesterPubkey.length === "number" || $util.isString(message.attesterPubkey)))
                        return "attesterPubkey: buffer expected";
                if (message.subjectAddress != null && Object.hasOwnProperty.call(message, "subjectAddress"))
                    if (!$util.isString(message.subjectAddress))
                        return "subjectAddress: string expected";
                if (message.subjectPubkey != null && Object.hasOwnProperty.call(message, "subjectPubkey"))
                    if (!(message.subjectPubkey && typeof message.subjectPubkey.length === "number" || $util.isString(message.subjectPubkey)))
                        return "subjectPubkey: buffer expected";
                if (message.attestationType != null && Object.hasOwnProperty.call(message, "attestationType"))
                    switch (message.attestationType) {
                    default:
                        return "attestationType: enum value expected";
                    case 0:
                    case 1:
                    case 2:
                    case 3:
                    case 4:
                        break;
                    }
                if (message.attestedAt != null && Object.hasOwnProperty.call(message, "attestedAt"))
                    if (!$util.isInteger(message.attestedAt) && !(message.attestedAt && $util.isInteger(message.attestedAt.low) && $util.isInteger(message.attestedAt.high)))
                        return "attestedAt: integer|Long expected";
                if (message.expiresAt != null && Object.hasOwnProperty.call(message, "expiresAt"))
                    if (!$util.isInteger(message.expiresAt) && !(message.expiresAt && $util.isInteger(message.expiresAt.low) && $util.isInteger(message.expiresAt.high)))
                        return "expiresAt: integer|Long expected";
                if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                    if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                        return "signature: buffer expected";
                return null;
            };

            /**
             * Creates an AttestationRecord message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.identity.AttestationRecord
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.identity.AttestationRecord} AttestationRecord
             */
            AttestationRecord.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.identity.AttestationRecord)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.identity.AttestationRecord: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.identity.AttestationRecord();
                if (object.attesterAddress != null)
                    message.attesterAddress = String(object.attesterAddress);
                if (object.attesterPubkey != null)
                    if (typeof object.attesterPubkey === "string")
                        $util.base64.decode(object.attesterPubkey, message.attesterPubkey = $util.newBuffer($util.base64.length(object.attesterPubkey)), 0);
                    else if (object.attesterPubkey.length >= 0)
                        message.attesterPubkey = object.attesterPubkey;
                if (object.subjectAddress != null)
                    message.subjectAddress = String(object.subjectAddress);
                if (object.subjectPubkey != null)
                    if (typeof object.subjectPubkey === "string")
                        $util.base64.decode(object.subjectPubkey, message.subjectPubkey = $util.newBuffer($util.base64.length(object.subjectPubkey)), 0);
                    else if (object.subjectPubkey.length >= 0)
                        message.subjectPubkey = object.subjectPubkey;
                switch (object.attestationType) {
                default:
                    if (typeof object.attestationType === "number") {
                        message.attestationType = object.attestationType;
                        break;
                    }
                    break;
                case "ATTESTATION_TYPE_UNSPECIFIED":
                case 0:
                    message.attestationType = 0;
                    break;
                case "ATTESTATION_TYPE_IN_PERSON":
                case 1:
                    message.attestationType = 1;
                    break;
                case "ATTESTATION_TYPE_FINGERPRINT":
                case 2:
                    message.attestationType = 2;
                    break;
                case "ATTESTATION_TYPE_NETWORK":
                case 3:
                    message.attestationType = 3;
                    break;
                case "ATTESTATION_TYPE_ORGANISATIONAL":
                case 4:
                    message.attestationType = 4;
                    break;
                }
                if (object.attestedAt != null)
                    if ($util.Long)
                        message.attestedAt = $util.Long.fromValue(object.attestedAt, false);
                    else if (typeof object.attestedAt === "string")
                        message.attestedAt = parseInt(object.attestedAt, 10);
                    else if (typeof object.attestedAt === "number")
                        message.attestedAt = object.attestedAt;
                    else if (typeof object.attestedAt === "object")
                        message.attestedAt = new $util.LongBits(object.attestedAt.low >>> 0, object.attestedAt.high >>> 0).toNumber();
                if (object.expiresAt != null)
                    if ($util.Long)
                        message.expiresAt = $util.Long.fromValue(object.expiresAt, false);
                    else if (typeof object.expiresAt === "string")
                        message.expiresAt = parseInt(object.expiresAt, 10);
                    else if (typeof object.expiresAt === "number")
                        message.expiresAt = object.expiresAt;
                    else if (typeof object.expiresAt === "object")
                        message.expiresAt = new $util.LongBits(object.expiresAt.low >>> 0, object.expiresAt.high >>> 0).toNumber();
                if (object.signature != null)
                    if (typeof object.signature === "string")
                        $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                    else if (object.signature.length >= 0)
                        message.signature = object.signature;
                return message;
            };

            /**
             * Creates a plain object from an AttestationRecord message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.identity.AttestationRecord
             * @static
             * @param {dmcn.identity.AttestationRecord} message AttestationRecord
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AttestationRecord.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.attesterAddress = "";
                    if (options.bytes === String)
                        object.attesterPubkey = "";
                    else {
                        object.attesterPubkey = [];
                        if (options.bytes !== Array)
                            object.attesterPubkey = $util.newBuffer(object.attesterPubkey);
                    }
                    object.subjectAddress = "";
                    if (options.bytes === String)
                        object.subjectPubkey = "";
                    else {
                        object.subjectPubkey = [];
                        if (options.bytes !== Array)
                            object.subjectPubkey = $util.newBuffer(object.subjectPubkey);
                    }
                    object.attestationType = options.enums === String ? "ATTESTATION_TYPE_UNSPECIFIED" : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.attestedAt = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.attestedAt = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.expiresAt = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.expiresAt = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if (options.bytes === String)
                        object.signature = "";
                    else {
                        object.signature = [];
                        if (options.bytes !== Array)
                            object.signature = $util.newBuffer(object.signature);
                    }
                }
                if (message.attesterAddress != null && Object.hasOwnProperty.call(message, "attesterAddress"))
                    object.attesterAddress = message.attesterAddress;
                if (message.attesterPubkey != null && Object.hasOwnProperty.call(message, "attesterPubkey"))
                    object.attesterPubkey = options.bytes === String ? $util.base64.encode(message.attesterPubkey, 0, message.attesterPubkey.length) : options.bytes === Array ? Array.prototype.slice.call(message.attesterPubkey) : message.attesterPubkey;
                if (message.subjectAddress != null && Object.hasOwnProperty.call(message, "subjectAddress"))
                    object.subjectAddress = message.subjectAddress;
                if (message.subjectPubkey != null && Object.hasOwnProperty.call(message, "subjectPubkey"))
                    object.subjectPubkey = options.bytes === String ? $util.base64.encode(message.subjectPubkey, 0, message.subjectPubkey.length) : options.bytes === Array ? Array.prototype.slice.call(message.subjectPubkey) : message.subjectPubkey;
                if (message.attestationType != null && Object.hasOwnProperty.call(message, "attestationType"))
                    object.attestationType = options.enums === String ? $root.dmcn.identity.AttestationType[message.attestationType] === undefined ? message.attestationType : $root.dmcn.identity.AttestationType[message.attestationType] : message.attestationType;
                if (message.attestedAt != null && Object.hasOwnProperty.call(message, "attestedAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.attestedAt = typeof message.attestedAt === "number" ? BigInt(message.attestedAt) : $util.Long.fromBits(message.attestedAt.low >>> 0, message.attestedAt.high >>> 0, false).toBigInt();
                    else if (typeof message.attestedAt === "number")
                        object.attestedAt = options.longs === String ? String(message.attestedAt) : message.attestedAt;
                    else
                        object.attestedAt = options.longs === String ? $util.Long.prototype.toString.call(message.attestedAt) : options.longs === Number ? new $util.LongBits(message.attestedAt.low >>> 0, message.attestedAt.high >>> 0).toNumber() : message.attestedAt;
                if (message.expiresAt != null && Object.hasOwnProperty.call(message, "expiresAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.expiresAt = typeof message.expiresAt === "number" ? BigInt(message.expiresAt) : $util.Long.fromBits(message.expiresAt.low >>> 0, message.expiresAt.high >>> 0, false).toBigInt();
                    else if (typeof message.expiresAt === "number")
                        object.expiresAt = options.longs === String ? String(message.expiresAt) : message.expiresAt;
                    else
                        object.expiresAt = options.longs === String ? $util.Long.prototype.toString.call(message.expiresAt) : options.longs === Number ? new $util.LongBits(message.expiresAt.low >>> 0, message.expiresAt.high >>> 0).toNumber() : message.expiresAt;
                if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                    object.signature = options.bytes === String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === Array ? Array.prototype.slice.call(message.signature) : message.signature;
                return object;
            };

            /**
             * Converts this AttestationRecord to JSON.
             * @function toJSON
             * @memberof dmcn.identity.AttestationRecord
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AttestationRecord.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AttestationRecord
             * @function getTypeUrl
             * @memberof dmcn.identity.AttestationRecord
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AttestationRecord.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.identity.AttestationRecord";
            };

            return AttestationRecord;
        })();

        identity.IdentityRecord = (function() {

            /**
             * Properties of an IdentityRecord.
             * @memberof dmcn.identity
             * @interface IIdentityRecord
             * @property {number|null} [version] IdentityRecord version
             * @property {string|null} [address] IdentityRecord address
             * @property {Uint8Array|null} [ed25519PublicKey] IdentityRecord ed25519PublicKey
             * @property {Uint8Array|null} [x25519PublicKey] IdentityRecord x25519PublicKey
             * @property {number|Long|null} [createdAt] IdentityRecord createdAt
             * @property {number|Long|null} [expiresAt] IdentityRecord expiresAt
             * @property {Array.<string>|null} [relayHints] IdentityRecord relayHints
             * @property {dmcn.identity.VerificationTier|null} [verificationTier] IdentityRecord verificationTier
             * @property {Array.<dmcn.identity.IAttestationRecord>|null} [attestations] IdentityRecord attestations
             * @property {Uint8Array|null} [selfSignature] IdentityRecord selfSignature
             * @property {boolean|null} [bridgeCapability] IdentityRecord bridgeCapability
             * @property {Uint8Array|null} [domainCountersignature] IdentityRecord domainCountersignature
             * @property {number|Long|null} [domainCountersignedAt] IdentityRecord domainCountersignedAt
             * @property {Uint8Array|null} [domainCountersignerPubkey] IdentityRecord domainCountersignerPubkey
             * @property {boolean|null} [requireOnion] IdentityRecord requireOnion
             * @property {dmcn.identity.ICredential|null} [addressCredential] IdentityRecord addressCredential
             * @property {dmcn.identity.ICredential|null} [routingCredential] IdentityRecord routingCredential
             * @property {number|Long|null} [revision] IdentityRecord revision
             * @property {Array.<dmcn.identity.ICredential>|null} [operatorCredentials] IdentityRecord operatorCredentials
             */

            /**
             * Constructs a new IdentityRecord.
             * @memberof dmcn.identity
             * @classdesc Represents an IdentityRecord.
             * @implements IIdentityRecord
             * @constructor
             * @param {dmcn.identity.IIdentityRecord=} [properties] Properties to set
             */
            function IdentityRecord(properties) {
                this.relayHints = [];
                this.attestations = [];
                this.operatorCredentials = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * IdentityRecord version.
             * @member {number} version
             * @memberof dmcn.identity.IdentityRecord
             * @instance
             */
            IdentityRecord.prototype.version = 0;

            /**
             * IdentityRecord address.
             * @member {string} address
             * @memberof dmcn.identity.IdentityRecord
             * @instance
             */
            IdentityRecord.prototype.address = "";

            /**
             * IdentityRecord ed25519PublicKey.
             * @member {Uint8Array} ed25519PublicKey
             * @memberof dmcn.identity.IdentityRecord
             * @instance
             */
            IdentityRecord.prototype.ed25519PublicKey = $util.newBuffer([]);

            /**
             * IdentityRecord x25519PublicKey.
             * @member {Uint8Array} x25519PublicKey
             * @memberof dmcn.identity.IdentityRecord
             * @instance
             */
            IdentityRecord.prototype.x25519PublicKey = $util.newBuffer([]);

            /**
             * IdentityRecord createdAt.
             * @member {number|Long} createdAt
             * @memberof dmcn.identity.IdentityRecord
             * @instance
             */
            IdentityRecord.prototype.createdAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * IdentityRecord expiresAt.
             * @member {number|Long} expiresAt
             * @memberof dmcn.identity.IdentityRecord
             * @instance
             */
            IdentityRecord.prototype.expiresAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * IdentityRecord relayHints.
             * @member {Array.<string>} relayHints
             * @memberof dmcn.identity.IdentityRecord
             * @instance
             */
            IdentityRecord.prototype.relayHints = $util.emptyArray;

            /**
             * IdentityRecord verificationTier.
             * @member {dmcn.identity.VerificationTier} verificationTier
             * @memberof dmcn.identity.IdentityRecord
             * @instance
             */
            IdentityRecord.prototype.verificationTier = 0;

            /**
             * IdentityRecord attestations.
             * @member {Array.<dmcn.identity.IAttestationRecord>} attestations
             * @memberof dmcn.identity.IdentityRecord
             * @instance
             */
            IdentityRecord.prototype.attestations = $util.emptyArray;

            /**
             * IdentityRecord selfSignature.
             * @member {Uint8Array} selfSignature
             * @memberof dmcn.identity.IdentityRecord
             * @instance
             */
            IdentityRecord.prototype.selfSignature = $util.newBuffer([]);

            /**
             * IdentityRecord bridgeCapability.
             * @member {boolean} bridgeCapability
             * @memberof dmcn.identity.IdentityRecord
             * @instance
             */
            IdentityRecord.prototype.bridgeCapability = false;

            /**
             * IdentityRecord domainCountersignature.
             * @member {Uint8Array} domainCountersignature
             * @memberof dmcn.identity.IdentityRecord
             * @instance
             */
            IdentityRecord.prototype.domainCountersignature = $util.newBuffer([]);

            /**
             * IdentityRecord domainCountersignedAt.
             * @member {number|Long} domainCountersignedAt
             * @memberof dmcn.identity.IdentityRecord
             * @instance
             */
            IdentityRecord.prototype.domainCountersignedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * IdentityRecord domainCountersignerPubkey.
             * @member {Uint8Array} domainCountersignerPubkey
             * @memberof dmcn.identity.IdentityRecord
             * @instance
             */
            IdentityRecord.prototype.domainCountersignerPubkey = $util.newBuffer([]);

            /**
             * IdentityRecord requireOnion.
             * @member {boolean} requireOnion
             * @memberof dmcn.identity.IdentityRecord
             * @instance
             */
            IdentityRecord.prototype.requireOnion = false;

            /**
             * IdentityRecord addressCredential.
             * @member {dmcn.identity.ICredential|null|undefined} addressCredential
             * @memberof dmcn.identity.IdentityRecord
             * @instance
             */
            IdentityRecord.prototype.addressCredential = null;

            /**
             * IdentityRecord routingCredential.
             * @member {dmcn.identity.ICredential|null|undefined} routingCredential
             * @memberof dmcn.identity.IdentityRecord
             * @instance
             */
            IdentityRecord.prototype.routingCredential = null;

            /**
             * IdentityRecord revision.
             * @member {number|Long} revision
             * @memberof dmcn.identity.IdentityRecord
             * @instance
             */
            IdentityRecord.prototype.revision = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * IdentityRecord operatorCredentials.
             * @member {Array.<dmcn.identity.ICredential>} operatorCredentials
             * @memberof dmcn.identity.IdentityRecord
             * @instance
             */
            IdentityRecord.prototype.operatorCredentials = $util.emptyArray;

            /**
             * Creates a new IdentityRecord instance using the specified properties.
             * @function create
             * @memberof dmcn.identity.IdentityRecord
             * @static
             * @param {dmcn.identity.IIdentityRecord=} [properties] Properties to set
             * @returns {dmcn.identity.IdentityRecord} IdentityRecord instance
             */
            IdentityRecord.create = function create(properties) {
                return new IdentityRecord(properties);
            };

            /**
             * Encodes the specified IdentityRecord message. Does not implicitly {@link dmcn.identity.IdentityRecord.verify|verify} messages.
             * @function encode
             * @memberof dmcn.identity.IdentityRecord
             * @static
             * @param {dmcn.identity.IIdentityRecord} message IdentityRecord message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            IdentityRecord.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.version);
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.address);
                if (message.ed25519PublicKey != null && Object.hasOwnProperty.call(message, "ed25519PublicKey"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.ed25519PublicKey);
                if (message.x25519PublicKey != null && Object.hasOwnProperty.call(message, "x25519PublicKey"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.x25519PublicKey);
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.createdAt);
                if (message.expiresAt != null && Object.hasOwnProperty.call(message, "expiresAt"))
                    writer.uint32(/* id 6, wireType 0 =*/48).int64(message.expiresAt);
                if (message.relayHints != null && message.relayHints.length)
                    for (let i = 0; i < message.relayHints.length; ++i)
                        writer.uint32(/* id 7, wireType 2 =*/58).string(message.relayHints[i]);
                if (message.verificationTier != null && Object.hasOwnProperty.call(message, "verificationTier"))
                    writer.uint32(/* id 8, wireType 0 =*/64).int32(message.verificationTier);
                if (message.attestations != null && message.attestations.length)
                    for (let i = 0; i < message.attestations.length; ++i)
                        $root.dmcn.identity.AttestationRecord.encode(message.attestations[i], writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
                if (message.selfSignature != null && Object.hasOwnProperty.call(message, "selfSignature"))
                    writer.uint32(/* id 10, wireType 2 =*/82).bytes(message.selfSignature);
                if (message.bridgeCapability != null && Object.hasOwnProperty.call(message, "bridgeCapability"))
                    writer.uint32(/* id 19, wireType 0 =*/152).bool(message.bridgeCapability);
                if (message.domainCountersignature != null && Object.hasOwnProperty.call(message, "domainCountersignature"))
                    writer.uint32(/* id 20, wireType 2 =*/162).bytes(message.domainCountersignature);
                if (message.domainCountersignedAt != null && Object.hasOwnProperty.call(message, "domainCountersignedAt"))
                    writer.uint32(/* id 21, wireType 0 =*/168).int64(message.domainCountersignedAt);
                if (message.domainCountersignerPubkey != null && Object.hasOwnProperty.call(message, "domainCountersignerPubkey"))
                    writer.uint32(/* id 22, wireType 2 =*/178).bytes(message.domainCountersignerPubkey);
                if (message.requireOnion != null && Object.hasOwnProperty.call(message, "requireOnion"))
                    writer.uint32(/* id 23, wireType 0 =*/184).bool(message.requireOnion);
                if (message.addressCredential != null && Object.hasOwnProperty.call(message, "addressCredential"))
                    $root.dmcn.identity.Credential.encode(message.addressCredential, writer.uint32(/* id 24, wireType 2 =*/194).fork(), q + 1).ldelim();
                if (message.routingCredential != null && Object.hasOwnProperty.call(message, "routingCredential"))
                    $root.dmcn.identity.Credential.encode(message.routingCredential, writer.uint32(/* id 25, wireType 2 =*/202).fork(), q + 1).ldelim();
                if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                    writer.uint32(/* id 26, wireType 0 =*/208).uint64(message.revision);
                if (message.operatorCredentials != null && message.operatorCredentials.length)
                    for (let i = 0; i < message.operatorCredentials.length; ++i)
                        $root.dmcn.identity.Credential.encode(message.operatorCredentials[i], writer.uint32(/* id 28, wireType 2 =*/226).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified IdentityRecord message, length delimited. Does not implicitly {@link dmcn.identity.IdentityRecord.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.identity.IdentityRecord
             * @static
             * @param {dmcn.identity.IIdentityRecord} message IdentityRecord message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            IdentityRecord.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes an IdentityRecord message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.identity.IdentityRecord
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.identity.IdentityRecord} IdentityRecord
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            IdentityRecord.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.identity.IdentityRecord();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.version = reader.uint32();
                            break;
                        }
                    case 2: {
                            message.address = reader.string();
                            break;
                        }
                    case 3: {
                            message.ed25519PublicKey = reader.bytes();
                            break;
                        }
                    case 4: {
                            message.x25519PublicKey = reader.bytes();
                            break;
                        }
                    case 5: {
                            message.createdAt = reader.int64();
                            break;
                        }
                    case 6: {
                            message.expiresAt = reader.int64();
                            break;
                        }
                    case 7: {
                            if (!(message.relayHints && message.relayHints.length))
                                message.relayHints = [];
                            message.relayHints.push(reader.string());
                            break;
                        }
                    case 8: {
                            message.verificationTier = reader.int32();
                            break;
                        }
                    case 9: {
                            if (!(message.attestations && message.attestations.length))
                                message.attestations = [];
                            message.attestations.push($root.dmcn.identity.AttestationRecord.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 10: {
                            message.selfSignature = reader.bytes();
                            break;
                        }
                    case 19: {
                            message.bridgeCapability = reader.bool();
                            break;
                        }
                    case 20: {
                            message.domainCountersignature = reader.bytes();
                            break;
                        }
                    case 21: {
                            message.domainCountersignedAt = reader.int64();
                            break;
                        }
                    case 22: {
                            message.domainCountersignerPubkey = reader.bytes();
                            break;
                        }
                    case 23: {
                            message.requireOnion = reader.bool();
                            break;
                        }
                    case 24: {
                            message.addressCredential = $root.dmcn.identity.Credential.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 25: {
                            message.routingCredential = $root.dmcn.identity.Credential.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 26: {
                            message.revision = reader.uint64();
                            break;
                        }
                    case 28: {
                            if (!(message.operatorCredentials && message.operatorCredentials.length))
                                message.operatorCredentials = [];
                            message.operatorCredentials.push($root.dmcn.identity.Credential.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an IdentityRecord message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.identity.IdentityRecord
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.identity.IdentityRecord} IdentityRecord
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            IdentityRecord.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an IdentityRecord message.
             * @function verify
             * @memberof dmcn.identity.IdentityRecord
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            IdentityRecord.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    if (!$util.isInteger(message.version))
                        return "version: integer expected";
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    if (!$util.isString(message.address))
                        return "address: string expected";
                if (message.ed25519PublicKey != null && Object.hasOwnProperty.call(message, "ed25519PublicKey"))
                    if (!(message.ed25519PublicKey && typeof message.ed25519PublicKey.length === "number" || $util.isString(message.ed25519PublicKey)))
                        return "ed25519PublicKey: buffer expected";
                if (message.x25519PublicKey != null && Object.hasOwnProperty.call(message, "x25519PublicKey"))
                    if (!(message.x25519PublicKey && typeof message.x25519PublicKey.length === "number" || $util.isString(message.x25519PublicKey)))
                        return "x25519PublicKey: buffer expected";
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
                        return "createdAt: integer|Long expected";
                if (message.expiresAt != null && Object.hasOwnProperty.call(message, "expiresAt"))
                    if (!$util.isInteger(message.expiresAt) && !(message.expiresAt && $util.isInteger(message.expiresAt.low) && $util.isInteger(message.expiresAt.high)))
                        return "expiresAt: integer|Long expected";
                if (message.relayHints != null && Object.hasOwnProperty.call(message, "relayHints")) {
                    if (!Array.isArray(message.relayHints))
                        return "relayHints: array expected";
                    for (let i = 0; i < message.relayHints.length; ++i)
                        if (!$util.isString(message.relayHints[i]))
                            return "relayHints: string[] expected";
                }
                if (message.verificationTier != null && Object.hasOwnProperty.call(message, "verificationTier"))
                    switch (message.verificationTier) {
                    default:
                        return "verificationTier: enum value expected";
                    case 0:
                    case 2:
                    case 3:
                        break;
                    }
                if (message.attestations != null && Object.hasOwnProperty.call(message, "attestations")) {
                    if (!Array.isArray(message.attestations))
                        return "attestations: array expected";
                    for (let i = 0; i < message.attestations.length; ++i) {
                        let error = $root.dmcn.identity.AttestationRecord.verify(message.attestations[i], long + 1);
                        if (error)
                            return "attestations." + error;
                    }
                }
                if (message.selfSignature != null && Object.hasOwnProperty.call(message, "selfSignature"))
                    if (!(message.selfSignature && typeof message.selfSignature.length === "number" || $util.isString(message.selfSignature)))
                        return "selfSignature: buffer expected";
                if (message.bridgeCapability != null && Object.hasOwnProperty.call(message, "bridgeCapability"))
                    if (typeof message.bridgeCapability !== "boolean")
                        return "bridgeCapability: boolean expected";
                if (message.domainCountersignature != null && Object.hasOwnProperty.call(message, "domainCountersignature"))
                    if (!(message.domainCountersignature && typeof message.domainCountersignature.length === "number" || $util.isString(message.domainCountersignature)))
                        return "domainCountersignature: buffer expected";
                if (message.domainCountersignedAt != null && Object.hasOwnProperty.call(message, "domainCountersignedAt"))
                    if (!$util.isInteger(message.domainCountersignedAt) && !(message.domainCountersignedAt && $util.isInteger(message.domainCountersignedAt.low) && $util.isInteger(message.domainCountersignedAt.high)))
                        return "domainCountersignedAt: integer|Long expected";
                if (message.domainCountersignerPubkey != null && Object.hasOwnProperty.call(message, "domainCountersignerPubkey"))
                    if (!(message.domainCountersignerPubkey && typeof message.domainCountersignerPubkey.length === "number" || $util.isString(message.domainCountersignerPubkey)))
                        return "domainCountersignerPubkey: buffer expected";
                if (message.requireOnion != null && Object.hasOwnProperty.call(message, "requireOnion"))
                    if (typeof message.requireOnion !== "boolean")
                        return "requireOnion: boolean expected";
                if (message.addressCredential != null && Object.hasOwnProperty.call(message, "addressCredential")) {
                    let error = $root.dmcn.identity.Credential.verify(message.addressCredential, long + 1);
                    if (error)
                        return "addressCredential." + error;
                }
                if (message.routingCredential != null && Object.hasOwnProperty.call(message, "routingCredential")) {
                    let error = $root.dmcn.identity.Credential.verify(message.routingCredential, long + 1);
                    if (error)
                        return "routingCredential." + error;
                }
                if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                    if (!$util.isInteger(message.revision) && !(message.revision && $util.isInteger(message.revision.low) && $util.isInteger(message.revision.high)))
                        return "revision: integer|Long expected";
                if (message.operatorCredentials != null && Object.hasOwnProperty.call(message, "operatorCredentials")) {
                    if (!Array.isArray(message.operatorCredentials))
                        return "operatorCredentials: array expected";
                    for (let i = 0; i < message.operatorCredentials.length; ++i) {
                        let error = $root.dmcn.identity.Credential.verify(message.operatorCredentials[i], long + 1);
                        if (error)
                            return "operatorCredentials." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an IdentityRecord message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.identity.IdentityRecord
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.identity.IdentityRecord} IdentityRecord
             */
            IdentityRecord.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.identity.IdentityRecord)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.identity.IdentityRecord: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.identity.IdentityRecord();
                if (object.version != null)
                    message.version = object.version >>> 0;
                if (object.address != null)
                    message.address = String(object.address);
                if (object.ed25519PublicKey != null)
                    if (typeof object.ed25519PublicKey === "string")
                        $util.base64.decode(object.ed25519PublicKey, message.ed25519PublicKey = $util.newBuffer($util.base64.length(object.ed25519PublicKey)), 0);
                    else if (object.ed25519PublicKey.length >= 0)
                        message.ed25519PublicKey = object.ed25519PublicKey;
                if (object.x25519PublicKey != null)
                    if (typeof object.x25519PublicKey === "string")
                        $util.base64.decode(object.x25519PublicKey, message.x25519PublicKey = $util.newBuffer($util.base64.length(object.x25519PublicKey)), 0);
                    else if (object.x25519PublicKey.length >= 0)
                        message.x25519PublicKey = object.x25519PublicKey;
                if (object.createdAt != null)
                    if ($util.Long)
                        message.createdAt = $util.Long.fromValue(object.createdAt, false);
                    else if (typeof object.createdAt === "string")
                        message.createdAt = parseInt(object.createdAt, 10);
                    else if (typeof object.createdAt === "number")
                        message.createdAt = object.createdAt;
                    else if (typeof object.createdAt === "object")
                        message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
                if (object.expiresAt != null)
                    if ($util.Long)
                        message.expiresAt = $util.Long.fromValue(object.expiresAt, false);
                    else if (typeof object.expiresAt === "string")
                        message.expiresAt = parseInt(object.expiresAt, 10);
                    else if (typeof object.expiresAt === "number")
                        message.expiresAt = object.expiresAt;
                    else if (typeof object.expiresAt === "object")
                        message.expiresAt = new $util.LongBits(object.expiresAt.low >>> 0, object.expiresAt.high >>> 0).toNumber();
                if (object.relayHints) {
                    if (!Array.isArray(object.relayHints))
                        throw TypeError(".dmcn.identity.IdentityRecord.relayHints: array expected");
                    message.relayHints = [];
                    for (let i = 0; i < object.relayHints.length; ++i)
                        message.relayHints[i] = String(object.relayHints[i]);
                }
                switch (object.verificationTier) {
                default:
                    if (typeof object.verificationTier === "number") {
                        message.verificationTier = object.verificationTier;
                        break;
                    }
                    break;
                case "VERIFICATION_TIER_UNVERIFIED":
                case 0:
                    message.verificationTier = 0;
                    break;
                case "VERIFICATION_TIER_DOMAIN_DNS":
                case 2:
                    message.verificationTier = 2;
                    break;
                case "VERIFICATION_TIER_DANE":
                case 3:
                    message.verificationTier = 3;
                    break;
                }
                if (object.attestations) {
                    if (!Array.isArray(object.attestations))
                        throw TypeError(".dmcn.identity.IdentityRecord.attestations: array expected");
                    message.attestations = [];
                    for (let i = 0; i < object.attestations.length; ++i) {
                        if (!$util.isObject(object.attestations[i]))
                            throw TypeError(".dmcn.identity.IdentityRecord.attestations: object expected");
                        message.attestations[i] = $root.dmcn.identity.AttestationRecord.fromObject(object.attestations[i], long + 1);
                    }
                }
                if (object.selfSignature != null)
                    if (typeof object.selfSignature === "string")
                        $util.base64.decode(object.selfSignature, message.selfSignature = $util.newBuffer($util.base64.length(object.selfSignature)), 0);
                    else if (object.selfSignature.length >= 0)
                        message.selfSignature = object.selfSignature;
                if (object.bridgeCapability != null)
                    message.bridgeCapability = Boolean(object.bridgeCapability);
                if (object.domainCountersignature != null)
                    if (typeof object.domainCountersignature === "string")
                        $util.base64.decode(object.domainCountersignature, message.domainCountersignature = $util.newBuffer($util.base64.length(object.domainCountersignature)), 0);
                    else if (object.domainCountersignature.length >= 0)
                        message.domainCountersignature = object.domainCountersignature;
                if (object.domainCountersignedAt != null)
                    if ($util.Long)
                        message.domainCountersignedAt = $util.Long.fromValue(object.domainCountersignedAt, false);
                    else if (typeof object.domainCountersignedAt === "string")
                        message.domainCountersignedAt = parseInt(object.domainCountersignedAt, 10);
                    else if (typeof object.domainCountersignedAt === "number")
                        message.domainCountersignedAt = object.domainCountersignedAt;
                    else if (typeof object.domainCountersignedAt === "object")
                        message.domainCountersignedAt = new $util.LongBits(object.domainCountersignedAt.low >>> 0, object.domainCountersignedAt.high >>> 0).toNumber();
                if (object.domainCountersignerPubkey != null)
                    if (typeof object.domainCountersignerPubkey === "string")
                        $util.base64.decode(object.domainCountersignerPubkey, message.domainCountersignerPubkey = $util.newBuffer($util.base64.length(object.domainCountersignerPubkey)), 0);
                    else if (object.domainCountersignerPubkey.length >= 0)
                        message.domainCountersignerPubkey = object.domainCountersignerPubkey;
                if (object.requireOnion != null)
                    message.requireOnion = Boolean(object.requireOnion);
                if (object.addressCredential != null) {
                    if (!$util.isObject(object.addressCredential))
                        throw TypeError(".dmcn.identity.IdentityRecord.addressCredential: object expected");
                    message.addressCredential = $root.dmcn.identity.Credential.fromObject(object.addressCredential, long + 1);
                }
                if (object.routingCredential != null) {
                    if (!$util.isObject(object.routingCredential))
                        throw TypeError(".dmcn.identity.IdentityRecord.routingCredential: object expected");
                    message.routingCredential = $root.dmcn.identity.Credential.fromObject(object.routingCredential, long + 1);
                }
                if (object.revision != null)
                    if ($util.Long)
                        message.revision = $util.Long.fromValue(object.revision, true);
                    else if (typeof object.revision === "string")
                        message.revision = parseInt(object.revision, 10);
                    else if (typeof object.revision === "number")
                        message.revision = object.revision;
                    else if (typeof object.revision === "object")
                        message.revision = new $util.LongBits(object.revision.low >>> 0, object.revision.high >>> 0).toNumber(true);
                if (object.operatorCredentials) {
                    if (!Array.isArray(object.operatorCredentials))
                        throw TypeError(".dmcn.identity.IdentityRecord.operatorCredentials: array expected");
                    message.operatorCredentials = [];
                    for (let i = 0; i < object.operatorCredentials.length; ++i) {
                        if (!$util.isObject(object.operatorCredentials[i]))
                            throw TypeError(".dmcn.identity.IdentityRecord.operatorCredentials: object expected");
                        message.operatorCredentials[i] = $root.dmcn.identity.Credential.fromObject(object.operatorCredentials[i], long + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from an IdentityRecord message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.identity.IdentityRecord
             * @static
             * @param {dmcn.identity.IdentityRecord} message IdentityRecord
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            IdentityRecord.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.relayHints = [];
                    object.attestations = [];
                    object.operatorCredentials = [];
                }
                if (options.defaults) {
                    object.version = 0;
                    object.address = "";
                    if (options.bytes === String)
                        object.ed25519PublicKey = "";
                    else {
                        object.ed25519PublicKey = [];
                        if (options.bytes !== Array)
                            object.ed25519PublicKey = $util.newBuffer(object.ed25519PublicKey);
                    }
                    if (options.bytes === String)
                        object.x25519PublicKey = "";
                    else {
                        object.x25519PublicKey = [];
                        if (options.bytes !== Array)
                            object.x25519PublicKey = $util.newBuffer(object.x25519PublicKey);
                    }
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.createdAt = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.createdAt = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.expiresAt = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.expiresAt = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    object.verificationTier = options.enums === String ? "VERIFICATION_TIER_UNVERIFIED" : 0;
                    if (options.bytes === String)
                        object.selfSignature = "";
                    else {
                        object.selfSignature = [];
                        if (options.bytes !== Array)
                            object.selfSignature = $util.newBuffer(object.selfSignature);
                    }
                    object.bridgeCapability = false;
                    if (options.bytes === String)
                        object.domainCountersignature = "";
                    else {
                        object.domainCountersignature = [];
                        if (options.bytes !== Array)
                            object.domainCountersignature = $util.newBuffer(object.domainCountersignature);
                    }
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.domainCountersignedAt = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.domainCountersignedAt = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if (options.bytes === String)
                        object.domainCountersignerPubkey = "";
                    else {
                        object.domainCountersignerPubkey = [];
                        if (options.bytes !== Array)
                            object.domainCountersignerPubkey = $util.newBuffer(object.domainCountersignerPubkey);
                    }
                    object.requireOnion = false;
                    object.addressCredential = null;
                    object.routingCredential = null;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.revision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.revision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                }
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    object.version = message.version;
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    object.address = message.address;
                if (message.ed25519PublicKey != null && Object.hasOwnProperty.call(message, "ed25519PublicKey"))
                    object.ed25519PublicKey = options.bytes === String ? $util.base64.encode(message.ed25519PublicKey, 0, message.ed25519PublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.ed25519PublicKey) : message.ed25519PublicKey;
                if (message.x25519PublicKey != null && Object.hasOwnProperty.call(message, "x25519PublicKey"))
                    object.x25519PublicKey = options.bytes === String ? $util.base64.encode(message.x25519PublicKey, 0, message.x25519PublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.x25519PublicKey) : message.x25519PublicKey;
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.createdAt = typeof message.createdAt === "number" ? BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
                    else if (typeof message.createdAt === "number")
                        object.createdAt = options.longs === String ? String(message.createdAt) : message.createdAt;
                    else
                        object.createdAt = options.longs === String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
                if (message.expiresAt != null && Object.hasOwnProperty.call(message, "expiresAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.expiresAt = typeof message.expiresAt === "number" ? BigInt(message.expiresAt) : $util.Long.fromBits(message.expiresAt.low >>> 0, message.expiresAt.high >>> 0, false).toBigInt();
                    else if (typeof message.expiresAt === "number")
                        object.expiresAt = options.longs === String ? String(message.expiresAt) : message.expiresAt;
                    else
                        object.expiresAt = options.longs === String ? $util.Long.prototype.toString.call(message.expiresAt) : options.longs === Number ? new $util.LongBits(message.expiresAt.low >>> 0, message.expiresAt.high >>> 0).toNumber() : message.expiresAt;
                if (message.relayHints && message.relayHints.length) {
                    object.relayHints = [];
                    for (let j = 0; j < message.relayHints.length; ++j)
                        object.relayHints[j] = message.relayHints[j];
                }
                if (message.verificationTier != null && Object.hasOwnProperty.call(message, "verificationTier"))
                    object.verificationTier = options.enums === String ? $root.dmcn.identity.VerificationTier[message.verificationTier] === undefined ? message.verificationTier : $root.dmcn.identity.VerificationTier[message.verificationTier] : message.verificationTier;
                if (message.attestations && message.attestations.length) {
                    object.attestations = [];
                    for (let j = 0; j < message.attestations.length; ++j)
                        object.attestations[j] = $root.dmcn.identity.AttestationRecord.toObject(message.attestations[j], options, q + 1);
                }
                if (message.selfSignature != null && Object.hasOwnProperty.call(message, "selfSignature"))
                    object.selfSignature = options.bytes === String ? $util.base64.encode(message.selfSignature, 0, message.selfSignature.length) : options.bytes === Array ? Array.prototype.slice.call(message.selfSignature) : message.selfSignature;
                if (message.bridgeCapability != null && Object.hasOwnProperty.call(message, "bridgeCapability"))
                    object.bridgeCapability = message.bridgeCapability;
                if (message.domainCountersignature != null && Object.hasOwnProperty.call(message, "domainCountersignature"))
                    object.domainCountersignature = options.bytes === String ? $util.base64.encode(message.domainCountersignature, 0, message.domainCountersignature.length) : options.bytes === Array ? Array.prototype.slice.call(message.domainCountersignature) : message.domainCountersignature;
                if (message.domainCountersignedAt != null && Object.hasOwnProperty.call(message, "domainCountersignedAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.domainCountersignedAt = typeof message.domainCountersignedAt === "number" ? BigInt(message.domainCountersignedAt) : $util.Long.fromBits(message.domainCountersignedAt.low >>> 0, message.domainCountersignedAt.high >>> 0, false).toBigInt();
                    else if (typeof message.domainCountersignedAt === "number")
                        object.domainCountersignedAt = options.longs === String ? String(message.domainCountersignedAt) : message.domainCountersignedAt;
                    else
                        object.domainCountersignedAt = options.longs === String ? $util.Long.prototype.toString.call(message.domainCountersignedAt) : options.longs === Number ? new $util.LongBits(message.domainCountersignedAt.low >>> 0, message.domainCountersignedAt.high >>> 0).toNumber() : message.domainCountersignedAt;
                if (message.domainCountersignerPubkey != null && Object.hasOwnProperty.call(message, "domainCountersignerPubkey"))
                    object.domainCountersignerPubkey = options.bytes === String ? $util.base64.encode(message.domainCountersignerPubkey, 0, message.domainCountersignerPubkey.length) : options.bytes === Array ? Array.prototype.slice.call(message.domainCountersignerPubkey) : message.domainCountersignerPubkey;
                if (message.requireOnion != null && Object.hasOwnProperty.call(message, "requireOnion"))
                    object.requireOnion = message.requireOnion;
                if (message.addressCredential != null && Object.hasOwnProperty.call(message, "addressCredential"))
                    object.addressCredential = $root.dmcn.identity.Credential.toObject(message.addressCredential, options, q + 1);
                if (message.routingCredential != null && Object.hasOwnProperty.call(message, "routingCredential"))
                    object.routingCredential = $root.dmcn.identity.Credential.toObject(message.routingCredential, options, q + 1);
                if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.revision = typeof message.revision === "number" ? BigInt(message.revision) : $util.Long.fromBits(message.revision.low >>> 0, message.revision.high >>> 0, true).toBigInt();
                    else if (typeof message.revision === "number")
                        object.revision = options.longs === String ? String(message.revision) : message.revision;
                    else
                        object.revision = options.longs === String ? $util.Long.prototype.toString.call(message.revision) : options.longs === Number ? new $util.LongBits(message.revision.low >>> 0, message.revision.high >>> 0).toNumber(true) : message.revision;
                if (message.operatorCredentials && message.operatorCredentials.length) {
                    object.operatorCredentials = [];
                    for (let j = 0; j < message.operatorCredentials.length; ++j)
                        object.operatorCredentials[j] = $root.dmcn.identity.Credential.toObject(message.operatorCredentials[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this IdentityRecord to JSON.
             * @function toJSON
             * @memberof dmcn.identity.IdentityRecord
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            IdentityRecord.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for IdentityRecord
             * @function getTypeUrl
             * @memberof dmcn.identity.IdentityRecord
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            IdentityRecord.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.identity.IdentityRecord";
            };

            return IdentityRecord;
        })();

        identity.AuthorityKey = (function() {

            /**
             * Properties of an AuthorityKey.
             * @memberof dmcn.identity
             * @interface IAuthorityKey
             * @property {Uint8Array|null} [ed25519PublicKey] AuthorityKey ed25519PublicKey
             * @property {Uint8Array|null} [x25519PublicKey] AuthorityKey x25519PublicKey
             * @property {number|Long|null} [effectiveFrom] AuthorityKey effectiveFrom
             * @property {number|null} [rotationReason] AuthorityKey rotationReason
             */

            /**
             * Constructs a new AuthorityKey.
             * @memberof dmcn.identity
             * @classdesc Represents an AuthorityKey.
             * @implements IAuthorityKey
             * @constructor
             * @param {dmcn.identity.IAuthorityKey=} [properties] Properties to set
             */
            function AuthorityKey(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AuthorityKey ed25519PublicKey.
             * @member {Uint8Array} ed25519PublicKey
             * @memberof dmcn.identity.AuthorityKey
             * @instance
             */
            AuthorityKey.prototype.ed25519PublicKey = $util.newBuffer([]);

            /**
             * AuthorityKey x25519PublicKey.
             * @member {Uint8Array} x25519PublicKey
             * @memberof dmcn.identity.AuthorityKey
             * @instance
             */
            AuthorityKey.prototype.x25519PublicKey = $util.newBuffer([]);

            /**
             * AuthorityKey effectiveFrom.
             * @member {number|Long} effectiveFrom
             * @memberof dmcn.identity.AuthorityKey
             * @instance
             */
            AuthorityKey.prototype.effectiveFrom = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * AuthorityKey rotationReason.
             * @member {number} rotationReason
             * @memberof dmcn.identity.AuthorityKey
             * @instance
             */
            AuthorityKey.prototype.rotationReason = 0;

            /**
             * Creates a new AuthorityKey instance using the specified properties.
             * @function create
             * @memberof dmcn.identity.AuthorityKey
             * @static
             * @param {dmcn.identity.IAuthorityKey=} [properties] Properties to set
             * @returns {dmcn.identity.AuthorityKey} AuthorityKey instance
             */
            AuthorityKey.create = function create(properties) {
                return new AuthorityKey(properties);
            };

            /**
             * Encodes the specified AuthorityKey message. Does not implicitly {@link dmcn.identity.AuthorityKey.verify|verify} messages.
             * @function encode
             * @memberof dmcn.identity.AuthorityKey
             * @static
             * @param {dmcn.identity.IAuthorityKey} message AuthorityKey message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AuthorityKey.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.ed25519PublicKey != null && Object.hasOwnProperty.call(message, "ed25519PublicKey"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.ed25519PublicKey);
                if (message.x25519PublicKey != null && Object.hasOwnProperty.call(message, "x25519PublicKey"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.x25519PublicKey);
                if (message.effectiveFrom != null && Object.hasOwnProperty.call(message, "effectiveFrom"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.effectiveFrom);
                if (message.rotationReason != null && Object.hasOwnProperty.call(message, "rotationReason"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.rotationReason);
                return writer;
            };

            /**
             * Encodes the specified AuthorityKey message, length delimited. Does not implicitly {@link dmcn.identity.AuthorityKey.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.identity.AuthorityKey
             * @static
             * @param {dmcn.identity.IAuthorityKey} message AuthorityKey message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AuthorityKey.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes an AuthorityKey message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.identity.AuthorityKey
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.identity.AuthorityKey} AuthorityKey
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AuthorityKey.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.identity.AuthorityKey();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.ed25519PublicKey = reader.bytes();
                            break;
                        }
                    case 2: {
                            message.x25519PublicKey = reader.bytes();
                            break;
                        }
                    case 3: {
                            message.effectiveFrom = reader.int64();
                            break;
                        }
                    case 4: {
                            message.rotationReason = reader.uint32();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an AuthorityKey message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.identity.AuthorityKey
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.identity.AuthorityKey} AuthorityKey
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AuthorityKey.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AuthorityKey message.
             * @function verify
             * @memberof dmcn.identity.AuthorityKey
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AuthorityKey.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.ed25519PublicKey != null && Object.hasOwnProperty.call(message, "ed25519PublicKey"))
                    if (!(message.ed25519PublicKey && typeof message.ed25519PublicKey.length === "number" || $util.isString(message.ed25519PublicKey)))
                        return "ed25519PublicKey: buffer expected";
                if (message.x25519PublicKey != null && Object.hasOwnProperty.call(message, "x25519PublicKey"))
                    if (!(message.x25519PublicKey && typeof message.x25519PublicKey.length === "number" || $util.isString(message.x25519PublicKey)))
                        return "x25519PublicKey: buffer expected";
                if (message.effectiveFrom != null && Object.hasOwnProperty.call(message, "effectiveFrom"))
                    if (!$util.isInteger(message.effectiveFrom) && !(message.effectiveFrom && $util.isInteger(message.effectiveFrom.low) && $util.isInteger(message.effectiveFrom.high)))
                        return "effectiveFrom: integer|Long expected";
                if (message.rotationReason != null && Object.hasOwnProperty.call(message, "rotationReason"))
                    if (!$util.isInteger(message.rotationReason))
                        return "rotationReason: integer expected";
                return null;
            };

            /**
             * Creates an AuthorityKey message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.identity.AuthorityKey
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.identity.AuthorityKey} AuthorityKey
             */
            AuthorityKey.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.identity.AuthorityKey)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.identity.AuthorityKey: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.identity.AuthorityKey();
                if (object.ed25519PublicKey != null)
                    if (typeof object.ed25519PublicKey === "string")
                        $util.base64.decode(object.ed25519PublicKey, message.ed25519PublicKey = $util.newBuffer($util.base64.length(object.ed25519PublicKey)), 0);
                    else if (object.ed25519PublicKey.length >= 0)
                        message.ed25519PublicKey = object.ed25519PublicKey;
                if (object.x25519PublicKey != null)
                    if (typeof object.x25519PublicKey === "string")
                        $util.base64.decode(object.x25519PublicKey, message.x25519PublicKey = $util.newBuffer($util.base64.length(object.x25519PublicKey)), 0);
                    else if (object.x25519PublicKey.length >= 0)
                        message.x25519PublicKey = object.x25519PublicKey;
                if (object.effectiveFrom != null)
                    if ($util.Long)
                        message.effectiveFrom = $util.Long.fromValue(object.effectiveFrom, false);
                    else if (typeof object.effectiveFrom === "string")
                        message.effectiveFrom = parseInt(object.effectiveFrom, 10);
                    else if (typeof object.effectiveFrom === "number")
                        message.effectiveFrom = object.effectiveFrom;
                    else if (typeof object.effectiveFrom === "object")
                        message.effectiveFrom = new $util.LongBits(object.effectiveFrom.low >>> 0, object.effectiveFrom.high >>> 0).toNumber();
                if (object.rotationReason != null)
                    message.rotationReason = object.rotationReason >>> 0;
                return message;
            };

            /**
             * Creates a plain object from an AuthorityKey message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.identity.AuthorityKey
             * @static
             * @param {dmcn.identity.AuthorityKey} message AuthorityKey
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AuthorityKey.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    if (options.bytes === String)
                        object.ed25519PublicKey = "";
                    else {
                        object.ed25519PublicKey = [];
                        if (options.bytes !== Array)
                            object.ed25519PublicKey = $util.newBuffer(object.ed25519PublicKey);
                    }
                    if (options.bytes === String)
                        object.x25519PublicKey = "";
                    else {
                        object.x25519PublicKey = [];
                        if (options.bytes !== Array)
                            object.x25519PublicKey = $util.newBuffer(object.x25519PublicKey);
                    }
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.effectiveFrom = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.effectiveFrom = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    object.rotationReason = 0;
                }
                if (message.ed25519PublicKey != null && Object.hasOwnProperty.call(message, "ed25519PublicKey"))
                    object.ed25519PublicKey = options.bytes === String ? $util.base64.encode(message.ed25519PublicKey, 0, message.ed25519PublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.ed25519PublicKey) : message.ed25519PublicKey;
                if (message.x25519PublicKey != null && Object.hasOwnProperty.call(message, "x25519PublicKey"))
                    object.x25519PublicKey = options.bytes === String ? $util.base64.encode(message.x25519PublicKey, 0, message.x25519PublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.x25519PublicKey) : message.x25519PublicKey;
                if (message.effectiveFrom != null && Object.hasOwnProperty.call(message, "effectiveFrom"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.effectiveFrom = typeof message.effectiveFrom === "number" ? BigInt(message.effectiveFrom) : $util.Long.fromBits(message.effectiveFrom.low >>> 0, message.effectiveFrom.high >>> 0, false).toBigInt();
                    else if (typeof message.effectiveFrom === "number")
                        object.effectiveFrom = options.longs === String ? String(message.effectiveFrom) : message.effectiveFrom;
                    else
                        object.effectiveFrom = options.longs === String ? $util.Long.prototype.toString.call(message.effectiveFrom) : options.longs === Number ? new $util.LongBits(message.effectiveFrom.low >>> 0, message.effectiveFrom.high >>> 0).toNumber() : message.effectiveFrom;
                if (message.rotationReason != null && Object.hasOwnProperty.call(message, "rotationReason"))
                    object.rotationReason = message.rotationReason;
                return object;
            };

            /**
             * Converts this AuthorityKey to JSON.
             * @function toJSON
             * @memberof dmcn.identity.AuthorityKey
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AuthorityKey.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AuthorityKey
             * @function getTypeUrl
             * @memberof dmcn.identity.AuthorityKey
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AuthorityKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.identity.AuthorityKey";
            };

            return AuthorityKey;
        })();

        identity.SubAuthority = (function() {

            /**
             * Properties of a SubAuthority.
             * @memberof dmcn.identity
             * @interface ISubAuthority
             * @property {Uint8Array|null} [ed25519PublicKey] SubAuthority ed25519PublicKey
             * @property {string|null} [scope] SubAuthority scope
             * @property {number|Long|null} [effectiveFrom] SubAuthority effectiveFrom
             * @property {number|Long|null} [effectiveUntil] SubAuthority effectiveUntil
             * @property {number|null} [permissions] SubAuthority permissions
             */

            /**
             * Constructs a new SubAuthority.
             * @memberof dmcn.identity
             * @classdesc Represents a SubAuthority.
             * @implements ISubAuthority
             * @constructor
             * @param {dmcn.identity.ISubAuthority=} [properties] Properties to set
             */
            function SubAuthority(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * SubAuthority ed25519PublicKey.
             * @member {Uint8Array} ed25519PublicKey
             * @memberof dmcn.identity.SubAuthority
             * @instance
             */
            SubAuthority.prototype.ed25519PublicKey = $util.newBuffer([]);

            /**
             * SubAuthority scope.
             * @member {string} scope
             * @memberof dmcn.identity.SubAuthority
             * @instance
             */
            SubAuthority.prototype.scope = "";

            /**
             * SubAuthority effectiveFrom.
             * @member {number|Long} effectiveFrom
             * @memberof dmcn.identity.SubAuthority
             * @instance
             */
            SubAuthority.prototype.effectiveFrom = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * SubAuthority effectiveUntil.
             * @member {number|Long} effectiveUntil
             * @memberof dmcn.identity.SubAuthority
             * @instance
             */
            SubAuthority.prototype.effectiveUntil = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * SubAuthority permissions.
             * @member {number} permissions
             * @memberof dmcn.identity.SubAuthority
             * @instance
             */
            SubAuthority.prototype.permissions = 0;

            /**
             * Creates a new SubAuthority instance using the specified properties.
             * @function create
             * @memberof dmcn.identity.SubAuthority
             * @static
             * @param {dmcn.identity.ISubAuthority=} [properties] Properties to set
             * @returns {dmcn.identity.SubAuthority} SubAuthority instance
             */
            SubAuthority.create = function create(properties) {
                return new SubAuthority(properties);
            };

            /**
             * Encodes the specified SubAuthority message. Does not implicitly {@link dmcn.identity.SubAuthority.verify|verify} messages.
             * @function encode
             * @memberof dmcn.identity.SubAuthority
             * @static
             * @param {dmcn.identity.ISubAuthority} message SubAuthority message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SubAuthority.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.ed25519PublicKey != null && Object.hasOwnProperty.call(message, "ed25519PublicKey"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.ed25519PublicKey);
                if (message.scope != null && Object.hasOwnProperty.call(message, "scope"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.scope);
                if (message.effectiveFrom != null && Object.hasOwnProperty.call(message, "effectiveFrom"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.effectiveFrom);
                if (message.effectiveUntil != null && Object.hasOwnProperty.call(message, "effectiveUntil"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int64(message.effectiveUntil);
                if (message.permissions != null && Object.hasOwnProperty.call(message, "permissions"))
                    writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.permissions);
                return writer;
            };

            /**
             * Encodes the specified SubAuthority message, length delimited. Does not implicitly {@link dmcn.identity.SubAuthority.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.identity.SubAuthority
             * @static
             * @param {dmcn.identity.ISubAuthority} message SubAuthority message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SubAuthority.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a SubAuthority message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.identity.SubAuthority
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.identity.SubAuthority} SubAuthority
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SubAuthority.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.identity.SubAuthority();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.ed25519PublicKey = reader.bytes();
                            break;
                        }
                    case 2: {
                            message.scope = reader.string();
                            break;
                        }
                    case 3: {
                            message.effectiveFrom = reader.int64();
                            break;
                        }
                    case 4: {
                            message.effectiveUntil = reader.int64();
                            break;
                        }
                    case 5: {
                            message.permissions = reader.uint32();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a SubAuthority message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.identity.SubAuthority
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.identity.SubAuthority} SubAuthority
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SubAuthority.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SubAuthority message.
             * @function verify
             * @memberof dmcn.identity.SubAuthority
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SubAuthority.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.ed25519PublicKey != null && Object.hasOwnProperty.call(message, "ed25519PublicKey"))
                    if (!(message.ed25519PublicKey && typeof message.ed25519PublicKey.length === "number" || $util.isString(message.ed25519PublicKey)))
                        return "ed25519PublicKey: buffer expected";
                if (message.scope != null && Object.hasOwnProperty.call(message, "scope"))
                    if (!$util.isString(message.scope))
                        return "scope: string expected";
                if (message.effectiveFrom != null && Object.hasOwnProperty.call(message, "effectiveFrom"))
                    if (!$util.isInteger(message.effectiveFrom) && !(message.effectiveFrom && $util.isInteger(message.effectiveFrom.low) && $util.isInteger(message.effectiveFrom.high)))
                        return "effectiveFrom: integer|Long expected";
                if (message.effectiveUntil != null && Object.hasOwnProperty.call(message, "effectiveUntil"))
                    if (!$util.isInteger(message.effectiveUntil) && !(message.effectiveUntil && $util.isInteger(message.effectiveUntil.low) && $util.isInteger(message.effectiveUntil.high)))
                        return "effectiveUntil: integer|Long expected";
                if (message.permissions != null && Object.hasOwnProperty.call(message, "permissions"))
                    if (!$util.isInteger(message.permissions))
                        return "permissions: integer expected";
                return null;
            };

            /**
             * Creates a SubAuthority message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.identity.SubAuthority
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.identity.SubAuthority} SubAuthority
             */
            SubAuthority.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.identity.SubAuthority)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.identity.SubAuthority: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.identity.SubAuthority();
                if (object.ed25519PublicKey != null)
                    if (typeof object.ed25519PublicKey === "string")
                        $util.base64.decode(object.ed25519PublicKey, message.ed25519PublicKey = $util.newBuffer($util.base64.length(object.ed25519PublicKey)), 0);
                    else if (object.ed25519PublicKey.length >= 0)
                        message.ed25519PublicKey = object.ed25519PublicKey;
                if (object.scope != null)
                    message.scope = String(object.scope);
                if (object.effectiveFrom != null)
                    if ($util.Long)
                        message.effectiveFrom = $util.Long.fromValue(object.effectiveFrom, false);
                    else if (typeof object.effectiveFrom === "string")
                        message.effectiveFrom = parseInt(object.effectiveFrom, 10);
                    else if (typeof object.effectiveFrom === "number")
                        message.effectiveFrom = object.effectiveFrom;
                    else if (typeof object.effectiveFrom === "object")
                        message.effectiveFrom = new $util.LongBits(object.effectiveFrom.low >>> 0, object.effectiveFrom.high >>> 0).toNumber();
                if (object.effectiveUntil != null)
                    if ($util.Long)
                        message.effectiveUntil = $util.Long.fromValue(object.effectiveUntil, false);
                    else if (typeof object.effectiveUntil === "string")
                        message.effectiveUntil = parseInt(object.effectiveUntil, 10);
                    else if (typeof object.effectiveUntil === "number")
                        message.effectiveUntil = object.effectiveUntil;
                    else if (typeof object.effectiveUntil === "object")
                        message.effectiveUntil = new $util.LongBits(object.effectiveUntil.low >>> 0, object.effectiveUntil.high >>> 0).toNumber();
                if (object.permissions != null)
                    message.permissions = object.permissions >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a SubAuthority message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.identity.SubAuthority
             * @static
             * @param {dmcn.identity.SubAuthority} message SubAuthority
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SubAuthority.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    if (options.bytes === String)
                        object.ed25519PublicKey = "";
                    else {
                        object.ed25519PublicKey = [];
                        if (options.bytes !== Array)
                            object.ed25519PublicKey = $util.newBuffer(object.ed25519PublicKey);
                    }
                    object.scope = "";
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.effectiveFrom = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.effectiveFrom = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.effectiveUntil = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.effectiveUntil = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    object.permissions = 0;
                }
                if (message.ed25519PublicKey != null && Object.hasOwnProperty.call(message, "ed25519PublicKey"))
                    object.ed25519PublicKey = options.bytes === String ? $util.base64.encode(message.ed25519PublicKey, 0, message.ed25519PublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.ed25519PublicKey) : message.ed25519PublicKey;
                if (message.scope != null && Object.hasOwnProperty.call(message, "scope"))
                    object.scope = message.scope;
                if (message.effectiveFrom != null && Object.hasOwnProperty.call(message, "effectiveFrom"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.effectiveFrom = typeof message.effectiveFrom === "number" ? BigInt(message.effectiveFrom) : $util.Long.fromBits(message.effectiveFrom.low >>> 0, message.effectiveFrom.high >>> 0, false).toBigInt();
                    else if (typeof message.effectiveFrom === "number")
                        object.effectiveFrom = options.longs === String ? String(message.effectiveFrom) : message.effectiveFrom;
                    else
                        object.effectiveFrom = options.longs === String ? $util.Long.prototype.toString.call(message.effectiveFrom) : options.longs === Number ? new $util.LongBits(message.effectiveFrom.low >>> 0, message.effectiveFrom.high >>> 0).toNumber() : message.effectiveFrom;
                if (message.effectiveUntil != null && Object.hasOwnProperty.call(message, "effectiveUntil"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.effectiveUntil = typeof message.effectiveUntil === "number" ? BigInt(message.effectiveUntil) : $util.Long.fromBits(message.effectiveUntil.low >>> 0, message.effectiveUntil.high >>> 0, false).toBigInt();
                    else if (typeof message.effectiveUntil === "number")
                        object.effectiveUntil = options.longs === String ? String(message.effectiveUntil) : message.effectiveUntil;
                    else
                        object.effectiveUntil = options.longs === String ? $util.Long.prototype.toString.call(message.effectiveUntil) : options.longs === Number ? new $util.LongBits(message.effectiveUntil.low >>> 0, message.effectiveUntil.high >>> 0).toNumber() : message.effectiveUntil;
                if (message.permissions != null && Object.hasOwnProperty.call(message, "permissions"))
                    object.permissions = message.permissions;
                return object;
            };

            /**
             * Converts this SubAuthority to JSON.
             * @function toJSON
             * @memberof dmcn.identity.SubAuthority
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SubAuthority.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for SubAuthority
             * @function getTypeUrl
             * @memberof dmcn.identity.SubAuthority
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            SubAuthority.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.identity.SubAuthority";
            };

            return SubAuthority;
        })();

        identity.DomainAuthorityRecord = (function() {

            /**
             * Properties of a DomainAuthorityRecord.
             * @memberof dmcn.identity
             * @interface IDomainAuthorityRecord
             * @property {number|null} [version] DomainAuthorityRecord version
             * @property {string|null} [domain] DomainAuthorityRecord domain
             * @property {Uint8Array|null} [authorityEd25519PublicKey] DomainAuthorityRecord authorityEd25519PublicKey
             * @property {Uint8Array|null} [authorityX25519PublicKey] DomainAuthorityRecord authorityX25519PublicKey
             * @property {number|Long|null} [authorityEffectiveFrom] DomainAuthorityRecord authorityEffectiveFrom
             * @property {Array.<dmcn.identity.IAuthorityKey>|null} [supersededKeys] DomainAuthorityRecord supersededKeys
             * @property {Array.<dmcn.identity.ISubAuthority>|null} [subAuthorities] DomainAuthorityRecord subAuthorities
             * @property {number|null} [policyFlags] DomainAuthorityRecord policyFlags
             * @property {number|Long|null} [createdAt] DomainAuthorityRecord createdAt
             * @property {number|Long|null} [revision] DomainAuthorityRecord revision
             * @property {Uint8Array|null} [selfSignature] DomainAuthorityRecord selfSignature
             * @property {Array.<dmcn.identity.ICredential>|null} [authorityCredentials] DomainAuthorityRecord authorityCredentials
             * @property {Array.<string>|null} [reservedLocalParts] DomainAuthorityRecord reservedLocalParts
             * @property {string|null} [fleetDomain] DomainAuthorityRecord fleetDomain
             */

            /**
             * Constructs a new DomainAuthorityRecord.
             * @memberof dmcn.identity
             * @classdesc Represents a DomainAuthorityRecord.
             * @implements IDomainAuthorityRecord
             * @constructor
             * @param {dmcn.identity.IDomainAuthorityRecord=} [properties] Properties to set
             */
            function DomainAuthorityRecord(properties) {
                this.supersededKeys = [];
                this.subAuthorities = [];
                this.authorityCredentials = [];
                this.reservedLocalParts = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * DomainAuthorityRecord version.
             * @member {number} version
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @instance
             */
            DomainAuthorityRecord.prototype.version = 0;

            /**
             * DomainAuthorityRecord domain.
             * @member {string} domain
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @instance
             */
            DomainAuthorityRecord.prototype.domain = "";

            /**
             * DomainAuthorityRecord authorityEd25519PublicKey.
             * @member {Uint8Array} authorityEd25519PublicKey
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @instance
             */
            DomainAuthorityRecord.prototype.authorityEd25519PublicKey = $util.newBuffer([]);

            /**
             * DomainAuthorityRecord authorityX25519PublicKey.
             * @member {Uint8Array} authorityX25519PublicKey
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @instance
             */
            DomainAuthorityRecord.prototype.authorityX25519PublicKey = $util.newBuffer([]);

            /**
             * DomainAuthorityRecord authorityEffectiveFrom.
             * @member {number|Long} authorityEffectiveFrom
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @instance
             */
            DomainAuthorityRecord.prototype.authorityEffectiveFrom = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * DomainAuthorityRecord supersededKeys.
             * @member {Array.<dmcn.identity.IAuthorityKey>} supersededKeys
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @instance
             */
            DomainAuthorityRecord.prototype.supersededKeys = $util.emptyArray;

            /**
             * DomainAuthorityRecord subAuthorities.
             * @member {Array.<dmcn.identity.ISubAuthority>} subAuthorities
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @instance
             */
            DomainAuthorityRecord.prototype.subAuthorities = $util.emptyArray;

            /**
             * DomainAuthorityRecord policyFlags.
             * @member {number} policyFlags
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @instance
             */
            DomainAuthorityRecord.prototype.policyFlags = 0;

            /**
             * DomainAuthorityRecord createdAt.
             * @member {number|Long} createdAt
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @instance
             */
            DomainAuthorityRecord.prototype.createdAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * DomainAuthorityRecord revision.
             * @member {number|Long} revision
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @instance
             */
            DomainAuthorityRecord.prototype.revision = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * DomainAuthorityRecord selfSignature.
             * @member {Uint8Array} selfSignature
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @instance
             */
            DomainAuthorityRecord.prototype.selfSignature = $util.newBuffer([]);

            /**
             * DomainAuthorityRecord authorityCredentials.
             * @member {Array.<dmcn.identity.ICredential>} authorityCredentials
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @instance
             */
            DomainAuthorityRecord.prototype.authorityCredentials = $util.emptyArray;

            /**
             * DomainAuthorityRecord reservedLocalParts.
             * @member {Array.<string>} reservedLocalParts
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @instance
             */
            DomainAuthorityRecord.prototype.reservedLocalParts = $util.emptyArray;

            /**
             * DomainAuthorityRecord fleetDomain.
             * @member {string} fleetDomain
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @instance
             */
            DomainAuthorityRecord.prototype.fleetDomain = "";

            /**
             * Creates a new DomainAuthorityRecord instance using the specified properties.
             * @function create
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @static
             * @param {dmcn.identity.IDomainAuthorityRecord=} [properties] Properties to set
             * @returns {dmcn.identity.DomainAuthorityRecord} DomainAuthorityRecord instance
             */
            DomainAuthorityRecord.create = function create(properties) {
                return new DomainAuthorityRecord(properties);
            };

            /**
             * Encodes the specified DomainAuthorityRecord message. Does not implicitly {@link dmcn.identity.DomainAuthorityRecord.verify|verify} messages.
             * @function encode
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @static
             * @param {dmcn.identity.IDomainAuthorityRecord} message DomainAuthorityRecord message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DomainAuthorityRecord.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.version);
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.domain);
                if (message.authorityEd25519PublicKey != null && Object.hasOwnProperty.call(message, "authorityEd25519PublicKey"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.authorityEd25519PublicKey);
                if (message.authorityX25519PublicKey != null && Object.hasOwnProperty.call(message, "authorityX25519PublicKey"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.authorityX25519PublicKey);
                if (message.authorityEffectiveFrom != null && Object.hasOwnProperty.call(message, "authorityEffectiveFrom"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.authorityEffectiveFrom);
                if (message.supersededKeys != null && message.supersededKeys.length)
                    for (let i = 0; i < message.supersededKeys.length; ++i)
                        $root.dmcn.identity.AuthorityKey.encode(message.supersededKeys[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
                if (message.subAuthorities != null && message.subAuthorities.length)
                    for (let i = 0; i < message.subAuthorities.length; ++i)
                        $root.dmcn.identity.SubAuthority.encode(message.subAuthorities[i], writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
                if (message.policyFlags != null && Object.hasOwnProperty.call(message, "policyFlags"))
                    writer.uint32(/* id 8, wireType 0 =*/64).uint32(message.policyFlags);
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    writer.uint32(/* id 9, wireType 0 =*/72).int64(message.createdAt);
                if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                    writer.uint32(/* id 10, wireType 0 =*/80).uint64(message.revision);
                if (message.selfSignature != null && Object.hasOwnProperty.call(message, "selfSignature"))
                    writer.uint32(/* id 11, wireType 2 =*/90).bytes(message.selfSignature);
                if (message.authorityCredentials != null && message.authorityCredentials.length)
                    for (let i = 0; i < message.authorityCredentials.length; ++i)
                        $root.dmcn.identity.Credential.encode(message.authorityCredentials[i], writer.uint32(/* id 12, wireType 2 =*/98).fork(), q + 1).ldelim();
                if (message.reservedLocalParts != null && message.reservedLocalParts.length)
                    for (let i = 0; i < message.reservedLocalParts.length; ++i)
                        writer.uint32(/* id 13, wireType 2 =*/106).string(message.reservedLocalParts[i]);
                if (message.fleetDomain != null && Object.hasOwnProperty.call(message, "fleetDomain"))
                    writer.uint32(/* id 14, wireType 2 =*/114).string(message.fleetDomain);
                return writer;
            };

            /**
             * Encodes the specified DomainAuthorityRecord message, length delimited. Does not implicitly {@link dmcn.identity.DomainAuthorityRecord.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @static
             * @param {dmcn.identity.IDomainAuthorityRecord} message DomainAuthorityRecord message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DomainAuthorityRecord.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a DomainAuthorityRecord message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.identity.DomainAuthorityRecord} DomainAuthorityRecord
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DomainAuthorityRecord.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.identity.DomainAuthorityRecord();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.version = reader.uint32();
                            break;
                        }
                    case 2: {
                            message.domain = reader.string();
                            break;
                        }
                    case 3: {
                            message.authorityEd25519PublicKey = reader.bytes();
                            break;
                        }
                    case 4: {
                            message.authorityX25519PublicKey = reader.bytes();
                            break;
                        }
                    case 5: {
                            message.authorityEffectiveFrom = reader.int64();
                            break;
                        }
                    case 6: {
                            if (!(message.supersededKeys && message.supersededKeys.length))
                                message.supersededKeys = [];
                            message.supersededKeys.push($root.dmcn.identity.AuthorityKey.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 7: {
                            if (!(message.subAuthorities && message.subAuthorities.length))
                                message.subAuthorities = [];
                            message.subAuthorities.push($root.dmcn.identity.SubAuthority.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 8: {
                            message.policyFlags = reader.uint32();
                            break;
                        }
                    case 9: {
                            message.createdAt = reader.int64();
                            break;
                        }
                    case 10: {
                            message.revision = reader.uint64();
                            break;
                        }
                    case 11: {
                            message.selfSignature = reader.bytes();
                            break;
                        }
                    case 12: {
                            if (!(message.authorityCredentials && message.authorityCredentials.length))
                                message.authorityCredentials = [];
                            message.authorityCredentials.push($root.dmcn.identity.Credential.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 13: {
                            if (!(message.reservedLocalParts && message.reservedLocalParts.length))
                                message.reservedLocalParts = [];
                            message.reservedLocalParts.push(reader.string());
                            break;
                        }
                    case 14: {
                            message.fleetDomain = reader.string();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a DomainAuthorityRecord message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.identity.DomainAuthorityRecord} DomainAuthorityRecord
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DomainAuthorityRecord.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DomainAuthorityRecord message.
             * @function verify
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DomainAuthorityRecord.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    if (!$util.isInteger(message.version))
                        return "version: integer expected";
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    if (!$util.isString(message.domain))
                        return "domain: string expected";
                if (message.authorityEd25519PublicKey != null && Object.hasOwnProperty.call(message, "authorityEd25519PublicKey"))
                    if (!(message.authorityEd25519PublicKey && typeof message.authorityEd25519PublicKey.length === "number" || $util.isString(message.authorityEd25519PublicKey)))
                        return "authorityEd25519PublicKey: buffer expected";
                if (message.authorityX25519PublicKey != null && Object.hasOwnProperty.call(message, "authorityX25519PublicKey"))
                    if (!(message.authorityX25519PublicKey && typeof message.authorityX25519PublicKey.length === "number" || $util.isString(message.authorityX25519PublicKey)))
                        return "authorityX25519PublicKey: buffer expected";
                if (message.authorityEffectiveFrom != null && Object.hasOwnProperty.call(message, "authorityEffectiveFrom"))
                    if (!$util.isInteger(message.authorityEffectiveFrom) && !(message.authorityEffectiveFrom && $util.isInteger(message.authorityEffectiveFrom.low) && $util.isInteger(message.authorityEffectiveFrom.high)))
                        return "authorityEffectiveFrom: integer|Long expected";
                if (message.supersededKeys != null && Object.hasOwnProperty.call(message, "supersededKeys")) {
                    if (!Array.isArray(message.supersededKeys))
                        return "supersededKeys: array expected";
                    for (let i = 0; i < message.supersededKeys.length; ++i) {
                        let error = $root.dmcn.identity.AuthorityKey.verify(message.supersededKeys[i], long + 1);
                        if (error)
                            return "supersededKeys." + error;
                    }
                }
                if (message.subAuthorities != null && Object.hasOwnProperty.call(message, "subAuthorities")) {
                    if (!Array.isArray(message.subAuthorities))
                        return "subAuthorities: array expected";
                    for (let i = 0; i < message.subAuthorities.length; ++i) {
                        let error = $root.dmcn.identity.SubAuthority.verify(message.subAuthorities[i], long + 1);
                        if (error)
                            return "subAuthorities." + error;
                    }
                }
                if (message.policyFlags != null && Object.hasOwnProperty.call(message, "policyFlags"))
                    if (!$util.isInteger(message.policyFlags))
                        return "policyFlags: integer expected";
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
                        return "createdAt: integer|Long expected";
                if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                    if (!$util.isInteger(message.revision) && !(message.revision && $util.isInteger(message.revision.low) && $util.isInteger(message.revision.high)))
                        return "revision: integer|Long expected";
                if (message.selfSignature != null && Object.hasOwnProperty.call(message, "selfSignature"))
                    if (!(message.selfSignature && typeof message.selfSignature.length === "number" || $util.isString(message.selfSignature)))
                        return "selfSignature: buffer expected";
                if (message.authorityCredentials != null && Object.hasOwnProperty.call(message, "authorityCredentials")) {
                    if (!Array.isArray(message.authorityCredentials))
                        return "authorityCredentials: array expected";
                    for (let i = 0; i < message.authorityCredentials.length; ++i) {
                        let error = $root.dmcn.identity.Credential.verify(message.authorityCredentials[i], long + 1);
                        if (error)
                            return "authorityCredentials." + error;
                    }
                }
                if (message.reservedLocalParts != null && Object.hasOwnProperty.call(message, "reservedLocalParts")) {
                    if (!Array.isArray(message.reservedLocalParts))
                        return "reservedLocalParts: array expected";
                    for (let i = 0; i < message.reservedLocalParts.length; ++i)
                        if (!$util.isString(message.reservedLocalParts[i]))
                            return "reservedLocalParts: string[] expected";
                }
                if (message.fleetDomain != null && Object.hasOwnProperty.call(message, "fleetDomain"))
                    if (!$util.isString(message.fleetDomain))
                        return "fleetDomain: string expected";
                return null;
            };

            /**
             * Creates a DomainAuthorityRecord message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.identity.DomainAuthorityRecord} DomainAuthorityRecord
             */
            DomainAuthorityRecord.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.identity.DomainAuthorityRecord)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.identity.DomainAuthorityRecord: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.identity.DomainAuthorityRecord();
                if (object.version != null)
                    message.version = object.version >>> 0;
                if (object.domain != null)
                    message.domain = String(object.domain);
                if (object.authorityEd25519PublicKey != null)
                    if (typeof object.authorityEd25519PublicKey === "string")
                        $util.base64.decode(object.authorityEd25519PublicKey, message.authorityEd25519PublicKey = $util.newBuffer($util.base64.length(object.authorityEd25519PublicKey)), 0);
                    else if (object.authorityEd25519PublicKey.length >= 0)
                        message.authorityEd25519PublicKey = object.authorityEd25519PublicKey;
                if (object.authorityX25519PublicKey != null)
                    if (typeof object.authorityX25519PublicKey === "string")
                        $util.base64.decode(object.authorityX25519PublicKey, message.authorityX25519PublicKey = $util.newBuffer($util.base64.length(object.authorityX25519PublicKey)), 0);
                    else if (object.authorityX25519PublicKey.length >= 0)
                        message.authorityX25519PublicKey = object.authorityX25519PublicKey;
                if (object.authorityEffectiveFrom != null)
                    if ($util.Long)
                        message.authorityEffectiveFrom = $util.Long.fromValue(object.authorityEffectiveFrom, false);
                    else if (typeof object.authorityEffectiveFrom === "string")
                        message.authorityEffectiveFrom = parseInt(object.authorityEffectiveFrom, 10);
                    else if (typeof object.authorityEffectiveFrom === "number")
                        message.authorityEffectiveFrom = object.authorityEffectiveFrom;
                    else if (typeof object.authorityEffectiveFrom === "object")
                        message.authorityEffectiveFrom = new $util.LongBits(object.authorityEffectiveFrom.low >>> 0, object.authorityEffectiveFrom.high >>> 0).toNumber();
                if (object.supersededKeys) {
                    if (!Array.isArray(object.supersededKeys))
                        throw TypeError(".dmcn.identity.DomainAuthorityRecord.supersededKeys: array expected");
                    message.supersededKeys = [];
                    for (let i = 0; i < object.supersededKeys.length; ++i) {
                        if (!$util.isObject(object.supersededKeys[i]))
                            throw TypeError(".dmcn.identity.DomainAuthorityRecord.supersededKeys: object expected");
                        message.supersededKeys[i] = $root.dmcn.identity.AuthorityKey.fromObject(object.supersededKeys[i], long + 1);
                    }
                }
                if (object.subAuthorities) {
                    if (!Array.isArray(object.subAuthorities))
                        throw TypeError(".dmcn.identity.DomainAuthorityRecord.subAuthorities: array expected");
                    message.subAuthorities = [];
                    for (let i = 0; i < object.subAuthorities.length; ++i) {
                        if (!$util.isObject(object.subAuthorities[i]))
                            throw TypeError(".dmcn.identity.DomainAuthorityRecord.subAuthorities: object expected");
                        message.subAuthorities[i] = $root.dmcn.identity.SubAuthority.fromObject(object.subAuthorities[i], long + 1);
                    }
                }
                if (object.policyFlags != null)
                    message.policyFlags = object.policyFlags >>> 0;
                if (object.createdAt != null)
                    if ($util.Long)
                        message.createdAt = $util.Long.fromValue(object.createdAt, false);
                    else if (typeof object.createdAt === "string")
                        message.createdAt = parseInt(object.createdAt, 10);
                    else if (typeof object.createdAt === "number")
                        message.createdAt = object.createdAt;
                    else if (typeof object.createdAt === "object")
                        message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
                if (object.revision != null)
                    if ($util.Long)
                        message.revision = $util.Long.fromValue(object.revision, true);
                    else if (typeof object.revision === "string")
                        message.revision = parseInt(object.revision, 10);
                    else if (typeof object.revision === "number")
                        message.revision = object.revision;
                    else if (typeof object.revision === "object")
                        message.revision = new $util.LongBits(object.revision.low >>> 0, object.revision.high >>> 0).toNumber(true);
                if (object.selfSignature != null)
                    if (typeof object.selfSignature === "string")
                        $util.base64.decode(object.selfSignature, message.selfSignature = $util.newBuffer($util.base64.length(object.selfSignature)), 0);
                    else if (object.selfSignature.length >= 0)
                        message.selfSignature = object.selfSignature;
                if (object.authorityCredentials) {
                    if (!Array.isArray(object.authorityCredentials))
                        throw TypeError(".dmcn.identity.DomainAuthorityRecord.authorityCredentials: array expected");
                    message.authorityCredentials = [];
                    for (let i = 0; i < object.authorityCredentials.length; ++i) {
                        if (!$util.isObject(object.authorityCredentials[i]))
                            throw TypeError(".dmcn.identity.DomainAuthorityRecord.authorityCredentials: object expected");
                        message.authorityCredentials[i] = $root.dmcn.identity.Credential.fromObject(object.authorityCredentials[i], long + 1);
                    }
                }
                if (object.reservedLocalParts) {
                    if (!Array.isArray(object.reservedLocalParts))
                        throw TypeError(".dmcn.identity.DomainAuthorityRecord.reservedLocalParts: array expected");
                    message.reservedLocalParts = [];
                    for (let i = 0; i < object.reservedLocalParts.length; ++i)
                        message.reservedLocalParts[i] = String(object.reservedLocalParts[i]);
                }
                if (object.fleetDomain != null)
                    message.fleetDomain = String(object.fleetDomain);
                return message;
            };

            /**
             * Creates a plain object from a DomainAuthorityRecord message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @static
             * @param {dmcn.identity.DomainAuthorityRecord} message DomainAuthorityRecord
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DomainAuthorityRecord.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.supersededKeys = [];
                    object.subAuthorities = [];
                    object.authorityCredentials = [];
                    object.reservedLocalParts = [];
                }
                if (options.defaults) {
                    object.version = 0;
                    object.domain = "";
                    if (options.bytes === String)
                        object.authorityEd25519PublicKey = "";
                    else {
                        object.authorityEd25519PublicKey = [];
                        if (options.bytes !== Array)
                            object.authorityEd25519PublicKey = $util.newBuffer(object.authorityEd25519PublicKey);
                    }
                    if (options.bytes === String)
                        object.authorityX25519PublicKey = "";
                    else {
                        object.authorityX25519PublicKey = [];
                        if (options.bytes !== Array)
                            object.authorityX25519PublicKey = $util.newBuffer(object.authorityX25519PublicKey);
                    }
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.authorityEffectiveFrom = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.authorityEffectiveFrom = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    object.policyFlags = 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.createdAt = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.createdAt = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.revision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.revision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if (options.bytes === String)
                        object.selfSignature = "";
                    else {
                        object.selfSignature = [];
                        if (options.bytes !== Array)
                            object.selfSignature = $util.newBuffer(object.selfSignature);
                    }
                    object.fleetDomain = "";
                }
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    object.version = message.version;
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    object.domain = message.domain;
                if (message.authorityEd25519PublicKey != null && Object.hasOwnProperty.call(message, "authorityEd25519PublicKey"))
                    object.authorityEd25519PublicKey = options.bytes === String ? $util.base64.encode(message.authorityEd25519PublicKey, 0, message.authorityEd25519PublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.authorityEd25519PublicKey) : message.authorityEd25519PublicKey;
                if (message.authorityX25519PublicKey != null && Object.hasOwnProperty.call(message, "authorityX25519PublicKey"))
                    object.authorityX25519PublicKey = options.bytes === String ? $util.base64.encode(message.authorityX25519PublicKey, 0, message.authorityX25519PublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.authorityX25519PublicKey) : message.authorityX25519PublicKey;
                if (message.authorityEffectiveFrom != null && Object.hasOwnProperty.call(message, "authorityEffectiveFrom"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.authorityEffectiveFrom = typeof message.authorityEffectiveFrom === "number" ? BigInt(message.authorityEffectiveFrom) : $util.Long.fromBits(message.authorityEffectiveFrom.low >>> 0, message.authorityEffectiveFrom.high >>> 0, false).toBigInt();
                    else if (typeof message.authorityEffectiveFrom === "number")
                        object.authorityEffectiveFrom = options.longs === String ? String(message.authorityEffectiveFrom) : message.authorityEffectiveFrom;
                    else
                        object.authorityEffectiveFrom = options.longs === String ? $util.Long.prototype.toString.call(message.authorityEffectiveFrom) : options.longs === Number ? new $util.LongBits(message.authorityEffectiveFrom.low >>> 0, message.authorityEffectiveFrom.high >>> 0).toNumber() : message.authorityEffectiveFrom;
                if (message.supersededKeys && message.supersededKeys.length) {
                    object.supersededKeys = [];
                    for (let j = 0; j < message.supersededKeys.length; ++j)
                        object.supersededKeys[j] = $root.dmcn.identity.AuthorityKey.toObject(message.supersededKeys[j], options, q + 1);
                }
                if (message.subAuthorities && message.subAuthorities.length) {
                    object.subAuthorities = [];
                    for (let j = 0; j < message.subAuthorities.length; ++j)
                        object.subAuthorities[j] = $root.dmcn.identity.SubAuthority.toObject(message.subAuthorities[j], options, q + 1);
                }
                if (message.policyFlags != null && Object.hasOwnProperty.call(message, "policyFlags"))
                    object.policyFlags = message.policyFlags;
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.createdAt = typeof message.createdAt === "number" ? BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
                    else if (typeof message.createdAt === "number")
                        object.createdAt = options.longs === String ? String(message.createdAt) : message.createdAt;
                    else
                        object.createdAt = options.longs === String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
                if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.revision = typeof message.revision === "number" ? BigInt(message.revision) : $util.Long.fromBits(message.revision.low >>> 0, message.revision.high >>> 0, true).toBigInt();
                    else if (typeof message.revision === "number")
                        object.revision = options.longs === String ? String(message.revision) : message.revision;
                    else
                        object.revision = options.longs === String ? $util.Long.prototype.toString.call(message.revision) : options.longs === Number ? new $util.LongBits(message.revision.low >>> 0, message.revision.high >>> 0).toNumber(true) : message.revision;
                if (message.selfSignature != null && Object.hasOwnProperty.call(message, "selfSignature"))
                    object.selfSignature = options.bytes === String ? $util.base64.encode(message.selfSignature, 0, message.selfSignature.length) : options.bytes === Array ? Array.prototype.slice.call(message.selfSignature) : message.selfSignature;
                if (message.authorityCredentials && message.authorityCredentials.length) {
                    object.authorityCredentials = [];
                    for (let j = 0; j < message.authorityCredentials.length; ++j)
                        object.authorityCredentials[j] = $root.dmcn.identity.Credential.toObject(message.authorityCredentials[j], options, q + 1);
                }
                if (message.reservedLocalParts && message.reservedLocalParts.length) {
                    object.reservedLocalParts = [];
                    for (let j = 0; j < message.reservedLocalParts.length; ++j)
                        object.reservedLocalParts[j] = message.reservedLocalParts[j];
                }
                if (message.fleetDomain != null && Object.hasOwnProperty.call(message, "fleetDomain"))
                    object.fleetDomain = message.fleetDomain;
                return object;
            };

            /**
             * Converts this DomainAuthorityRecord to JSON.
             * @function toJSON
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DomainAuthorityRecord.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for DomainAuthorityRecord
             * @function getTypeUrl
             * @memberof dmcn.identity.DomainAuthorityRecord
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            DomainAuthorityRecord.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.identity.DomainAuthorityRecord";
            };

            return DomainAuthorityRecord;
        })();

        identity.FleetNode = (function() {

            /**
             * Properties of a FleetNode.
             * @memberof dmcn.identity
             * @interface IFleetNode
             * @property {string|null} [peerId] FleetNode peerId
             * @property {Array.<string>|null} [multiaddrs] FleetNode multiaddrs
             * @property {Array.<string>|null} [roles] FleetNode roles
             */

            /**
             * Constructs a new FleetNode.
             * @memberof dmcn.identity
             * @classdesc Represents a FleetNode.
             * @implements IFleetNode
             * @constructor
             * @param {dmcn.identity.IFleetNode=} [properties] Properties to set
             */
            function FleetNode(properties) {
                this.multiaddrs = [];
                this.roles = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * FleetNode peerId.
             * @member {string} peerId
             * @memberof dmcn.identity.FleetNode
             * @instance
             */
            FleetNode.prototype.peerId = "";

            /**
             * FleetNode multiaddrs.
             * @member {Array.<string>} multiaddrs
             * @memberof dmcn.identity.FleetNode
             * @instance
             */
            FleetNode.prototype.multiaddrs = $util.emptyArray;

            /**
             * FleetNode roles.
             * @member {Array.<string>} roles
             * @memberof dmcn.identity.FleetNode
             * @instance
             */
            FleetNode.prototype.roles = $util.emptyArray;

            /**
             * Creates a new FleetNode instance using the specified properties.
             * @function create
             * @memberof dmcn.identity.FleetNode
             * @static
             * @param {dmcn.identity.IFleetNode=} [properties] Properties to set
             * @returns {dmcn.identity.FleetNode} FleetNode instance
             */
            FleetNode.create = function create(properties) {
                return new FleetNode(properties);
            };

            /**
             * Encodes the specified FleetNode message. Does not implicitly {@link dmcn.identity.FleetNode.verify|verify} messages.
             * @function encode
             * @memberof dmcn.identity.FleetNode
             * @static
             * @param {dmcn.identity.IFleetNode} message FleetNode message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FleetNode.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.peerId != null && Object.hasOwnProperty.call(message, "peerId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.peerId);
                if (message.multiaddrs != null && message.multiaddrs.length)
                    for (let i = 0; i < message.multiaddrs.length; ++i)
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.multiaddrs[i]);
                if (message.roles != null && message.roles.length)
                    for (let i = 0; i < message.roles.length; ++i)
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.roles[i]);
                return writer;
            };

            /**
             * Encodes the specified FleetNode message, length delimited. Does not implicitly {@link dmcn.identity.FleetNode.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.identity.FleetNode
             * @static
             * @param {dmcn.identity.IFleetNode} message FleetNode message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FleetNode.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a FleetNode message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.identity.FleetNode
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.identity.FleetNode} FleetNode
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FleetNode.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.identity.FleetNode();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.peerId = reader.string();
                            break;
                        }
                    case 2: {
                            if (!(message.multiaddrs && message.multiaddrs.length))
                                message.multiaddrs = [];
                            message.multiaddrs.push(reader.string());
                            break;
                        }
                    case 3: {
                            if (!(message.roles && message.roles.length))
                                message.roles = [];
                            message.roles.push(reader.string());
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a FleetNode message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.identity.FleetNode
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.identity.FleetNode} FleetNode
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FleetNode.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FleetNode message.
             * @function verify
             * @memberof dmcn.identity.FleetNode
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FleetNode.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.peerId != null && Object.hasOwnProperty.call(message, "peerId"))
                    if (!$util.isString(message.peerId))
                        return "peerId: string expected";
                if (message.multiaddrs != null && Object.hasOwnProperty.call(message, "multiaddrs")) {
                    if (!Array.isArray(message.multiaddrs))
                        return "multiaddrs: array expected";
                    for (let i = 0; i < message.multiaddrs.length; ++i)
                        if (!$util.isString(message.multiaddrs[i]))
                            return "multiaddrs: string[] expected";
                }
                if (message.roles != null && Object.hasOwnProperty.call(message, "roles")) {
                    if (!Array.isArray(message.roles))
                        return "roles: array expected";
                    for (let i = 0; i < message.roles.length; ++i)
                        if (!$util.isString(message.roles[i]))
                            return "roles: string[] expected";
                }
                return null;
            };

            /**
             * Creates a FleetNode message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.identity.FleetNode
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.identity.FleetNode} FleetNode
             */
            FleetNode.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.identity.FleetNode)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.identity.FleetNode: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.identity.FleetNode();
                if (object.peerId != null)
                    message.peerId = String(object.peerId);
                if (object.multiaddrs) {
                    if (!Array.isArray(object.multiaddrs))
                        throw TypeError(".dmcn.identity.FleetNode.multiaddrs: array expected");
                    message.multiaddrs = [];
                    for (let i = 0; i < object.multiaddrs.length; ++i)
                        message.multiaddrs[i] = String(object.multiaddrs[i]);
                }
                if (object.roles) {
                    if (!Array.isArray(object.roles))
                        throw TypeError(".dmcn.identity.FleetNode.roles: array expected");
                    message.roles = [];
                    for (let i = 0; i < object.roles.length; ++i)
                        message.roles[i] = String(object.roles[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a FleetNode message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.identity.FleetNode
             * @static
             * @param {dmcn.identity.FleetNode} message FleetNode
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FleetNode.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.multiaddrs = [];
                    object.roles = [];
                }
                if (options.defaults)
                    object.peerId = "";
                if (message.peerId != null && Object.hasOwnProperty.call(message, "peerId"))
                    object.peerId = message.peerId;
                if (message.multiaddrs && message.multiaddrs.length) {
                    object.multiaddrs = [];
                    for (let j = 0; j < message.multiaddrs.length; ++j)
                        object.multiaddrs[j] = message.multiaddrs[j];
                }
                if (message.roles && message.roles.length) {
                    object.roles = [];
                    for (let j = 0; j < message.roles.length; ++j)
                        object.roles[j] = message.roles[j];
                }
                return object;
            };

            /**
             * Converts this FleetNode to JSON.
             * @function toJSON
             * @memberof dmcn.identity.FleetNode
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FleetNode.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for FleetNode
             * @function getTypeUrl
             * @memberof dmcn.identity.FleetNode
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            FleetNode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.identity.FleetNode";
            };

            return FleetNode;
        })();

        identity.FleetRoster = (function() {

            /**
             * Properties of a FleetRoster.
             * @memberof dmcn.identity
             * @interface IFleetRoster
             * @property {number|null} [version] FleetRoster version
             * @property {string|null} [fleetDomain] FleetRoster fleetDomain
             * @property {Array.<dmcn.identity.IFleetNode>|null} [nodes] FleetRoster nodes
             * @property {number|Long|null} [revision] FleetRoster revision
             * @property {number|Long|null} [createdAt] FleetRoster createdAt
             * @property {Uint8Array|null} [selfSignature] FleetRoster selfSignature
             */

            /**
             * Constructs a new FleetRoster.
             * @memberof dmcn.identity
             * @classdesc Represents a FleetRoster.
             * @implements IFleetRoster
             * @constructor
             * @param {dmcn.identity.IFleetRoster=} [properties] Properties to set
             */
            function FleetRoster(properties) {
                this.nodes = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * FleetRoster version.
             * @member {number} version
             * @memberof dmcn.identity.FleetRoster
             * @instance
             */
            FleetRoster.prototype.version = 0;

            /**
             * FleetRoster fleetDomain.
             * @member {string} fleetDomain
             * @memberof dmcn.identity.FleetRoster
             * @instance
             */
            FleetRoster.prototype.fleetDomain = "";

            /**
             * FleetRoster nodes.
             * @member {Array.<dmcn.identity.IFleetNode>} nodes
             * @memberof dmcn.identity.FleetRoster
             * @instance
             */
            FleetRoster.prototype.nodes = $util.emptyArray;

            /**
             * FleetRoster revision.
             * @member {number|Long} revision
             * @memberof dmcn.identity.FleetRoster
             * @instance
             */
            FleetRoster.prototype.revision = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * FleetRoster createdAt.
             * @member {number|Long} createdAt
             * @memberof dmcn.identity.FleetRoster
             * @instance
             */
            FleetRoster.prototype.createdAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * FleetRoster selfSignature.
             * @member {Uint8Array} selfSignature
             * @memberof dmcn.identity.FleetRoster
             * @instance
             */
            FleetRoster.prototype.selfSignature = $util.newBuffer([]);

            /**
             * Creates a new FleetRoster instance using the specified properties.
             * @function create
             * @memberof dmcn.identity.FleetRoster
             * @static
             * @param {dmcn.identity.IFleetRoster=} [properties] Properties to set
             * @returns {dmcn.identity.FleetRoster} FleetRoster instance
             */
            FleetRoster.create = function create(properties) {
                return new FleetRoster(properties);
            };

            /**
             * Encodes the specified FleetRoster message. Does not implicitly {@link dmcn.identity.FleetRoster.verify|verify} messages.
             * @function encode
             * @memberof dmcn.identity.FleetRoster
             * @static
             * @param {dmcn.identity.IFleetRoster} message FleetRoster message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FleetRoster.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.version);
                if (message.fleetDomain != null && Object.hasOwnProperty.call(message, "fleetDomain"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.fleetDomain);
                if (message.nodes != null && message.nodes.length)
                    for (let i = 0; i < message.nodes.length; ++i)
                        $root.dmcn.identity.FleetNode.encode(message.nodes[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.revision);
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.createdAt);
                if (message.selfSignature != null && Object.hasOwnProperty.call(message, "selfSignature"))
                    writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.selfSignature);
                return writer;
            };

            /**
             * Encodes the specified FleetRoster message, length delimited. Does not implicitly {@link dmcn.identity.FleetRoster.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.identity.FleetRoster
             * @static
             * @param {dmcn.identity.IFleetRoster} message FleetRoster message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FleetRoster.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a FleetRoster message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.identity.FleetRoster
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.identity.FleetRoster} FleetRoster
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FleetRoster.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.identity.FleetRoster();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.version = reader.uint32();
                            break;
                        }
                    case 2: {
                            message.fleetDomain = reader.string();
                            break;
                        }
                    case 3: {
                            if (!(message.nodes && message.nodes.length))
                                message.nodes = [];
                            message.nodes.push($root.dmcn.identity.FleetNode.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 4: {
                            message.revision = reader.uint64();
                            break;
                        }
                    case 5: {
                            message.createdAt = reader.int64();
                            break;
                        }
                    case 6: {
                            message.selfSignature = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a FleetRoster message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.identity.FleetRoster
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.identity.FleetRoster} FleetRoster
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FleetRoster.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FleetRoster message.
             * @function verify
             * @memberof dmcn.identity.FleetRoster
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FleetRoster.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    if (!$util.isInteger(message.version))
                        return "version: integer expected";
                if (message.fleetDomain != null && Object.hasOwnProperty.call(message, "fleetDomain"))
                    if (!$util.isString(message.fleetDomain))
                        return "fleetDomain: string expected";
                if (message.nodes != null && Object.hasOwnProperty.call(message, "nodes")) {
                    if (!Array.isArray(message.nodes))
                        return "nodes: array expected";
                    for (let i = 0; i < message.nodes.length; ++i) {
                        let error = $root.dmcn.identity.FleetNode.verify(message.nodes[i], long + 1);
                        if (error)
                            return "nodes." + error;
                    }
                }
                if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                    if (!$util.isInteger(message.revision) && !(message.revision && $util.isInteger(message.revision.low) && $util.isInteger(message.revision.high)))
                        return "revision: integer|Long expected";
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
                        return "createdAt: integer|Long expected";
                if (message.selfSignature != null && Object.hasOwnProperty.call(message, "selfSignature"))
                    if (!(message.selfSignature && typeof message.selfSignature.length === "number" || $util.isString(message.selfSignature)))
                        return "selfSignature: buffer expected";
                return null;
            };

            /**
             * Creates a FleetRoster message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.identity.FleetRoster
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.identity.FleetRoster} FleetRoster
             */
            FleetRoster.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.identity.FleetRoster)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.identity.FleetRoster: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.identity.FleetRoster();
                if (object.version != null)
                    message.version = object.version >>> 0;
                if (object.fleetDomain != null)
                    message.fleetDomain = String(object.fleetDomain);
                if (object.nodes) {
                    if (!Array.isArray(object.nodes))
                        throw TypeError(".dmcn.identity.FleetRoster.nodes: array expected");
                    message.nodes = [];
                    for (let i = 0; i < object.nodes.length; ++i) {
                        if (!$util.isObject(object.nodes[i]))
                            throw TypeError(".dmcn.identity.FleetRoster.nodes: object expected");
                        message.nodes[i] = $root.dmcn.identity.FleetNode.fromObject(object.nodes[i], long + 1);
                    }
                }
                if (object.revision != null)
                    if ($util.Long)
                        message.revision = $util.Long.fromValue(object.revision, true);
                    else if (typeof object.revision === "string")
                        message.revision = parseInt(object.revision, 10);
                    else if (typeof object.revision === "number")
                        message.revision = object.revision;
                    else if (typeof object.revision === "object")
                        message.revision = new $util.LongBits(object.revision.low >>> 0, object.revision.high >>> 0).toNumber(true);
                if (object.createdAt != null)
                    if ($util.Long)
                        message.createdAt = $util.Long.fromValue(object.createdAt, false);
                    else if (typeof object.createdAt === "string")
                        message.createdAt = parseInt(object.createdAt, 10);
                    else if (typeof object.createdAt === "number")
                        message.createdAt = object.createdAt;
                    else if (typeof object.createdAt === "object")
                        message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
                if (object.selfSignature != null)
                    if (typeof object.selfSignature === "string")
                        $util.base64.decode(object.selfSignature, message.selfSignature = $util.newBuffer($util.base64.length(object.selfSignature)), 0);
                    else if (object.selfSignature.length >= 0)
                        message.selfSignature = object.selfSignature;
                return message;
            };

            /**
             * Creates a plain object from a FleetRoster message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.identity.FleetRoster
             * @static
             * @param {dmcn.identity.FleetRoster} message FleetRoster
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FleetRoster.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.nodes = [];
                if (options.defaults) {
                    object.version = 0;
                    object.fleetDomain = "";
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.revision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.revision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.createdAt = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.createdAt = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if (options.bytes === String)
                        object.selfSignature = "";
                    else {
                        object.selfSignature = [];
                        if (options.bytes !== Array)
                            object.selfSignature = $util.newBuffer(object.selfSignature);
                    }
                }
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    object.version = message.version;
                if (message.fleetDomain != null && Object.hasOwnProperty.call(message, "fleetDomain"))
                    object.fleetDomain = message.fleetDomain;
                if (message.nodes && message.nodes.length) {
                    object.nodes = [];
                    for (let j = 0; j < message.nodes.length; ++j)
                        object.nodes[j] = $root.dmcn.identity.FleetNode.toObject(message.nodes[j], options, q + 1);
                }
                if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.revision = typeof message.revision === "number" ? BigInt(message.revision) : $util.Long.fromBits(message.revision.low >>> 0, message.revision.high >>> 0, true).toBigInt();
                    else if (typeof message.revision === "number")
                        object.revision = options.longs === String ? String(message.revision) : message.revision;
                    else
                        object.revision = options.longs === String ? $util.Long.prototype.toString.call(message.revision) : options.longs === Number ? new $util.LongBits(message.revision.low >>> 0, message.revision.high >>> 0).toNumber(true) : message.revision;
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.createdAt = typeof message.createdAt === "number" ? BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
                    else if (typeof message.createdAt === "number")
                        object.createdAt = options.longs === String ? String(message.createdAt) : message.createdAt;
                    else
                        object.createdAt = options.longs === String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
                if (message.selfSignature != null && Object.hasOwnProperty.call(message, "selfSignature"))
                    object.selfSignature = options.bytes === String ? $util.base64.encode(message.selfSignature, 0, message.selfSignature.length) : options.bytes === Array ? Array.prototype.slice.call(message.selfSignature) : message.selfSignature;
                return object;
            };

            /**
             * Converts this FleetRoster to JSON.
             * @function toJSON
             * @memberof dmcn.identity.FleetRoster
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FleetRoster.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for FleetRoster
             * @function getTypeUrl
             * @memberof dmcn.identity.FleetRoster
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            FleetRoster.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.identity.FleetRoster";
            };

            return FleetRoster;
        })();

        identity.Credential = (function() {

            /**
             * Properties of a Credential.
             * @memberof dmcn.identity
             * @interface ICredential
             * @property {number|null} [version] Credential version
             * @property {Uint8Array|null} [subject] Credential subject
             * @property {string|null} [domain] Credential domain
             * @property {string|null} [address] Credential address
             * @property {Array.<string>|null} [roles] Credential roles
             * @property {Array.<string>|null} [grants] Credential grants
             * @property {Object.<string,string>|null} [attributes] Credential attributes
             * @property {number|Long|null} [issuedAt] Credential issuedAt
             * @property {number|Long|null} [notAfter] Credential notAfter
             * @property {string|null} [scope] Credential scope
             * @property {Uint8Array|null} [issuerPub] Credential issuerPub
             * @property {Uint8Array|null} [signature] Credential signature
             * @property {Array.<string>|null} [relayHints] Credential relayHints
             * @property {number|Long|null} [effectiveFrom] Credential effectiveFrom
             */

            /**
             * Constructs a new Credential.
             * @memberof dmcn.identity
             * @classdesc Represents a Credential.
             * @implements ICredential
             * @constructor
             * @param {dmcn.identity.ICredential=} [properties] Properties to set
             */
            function Credential(properties) {
                this.roles = [];
                this.grants = [];
                this.attributes = {};
                this.relayHints = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Credential version.
             * @member {number} version
             * @memberof dmcn.identity.Credential
             * @instance
             */
            Credential.prototype.version = 0;

            /**
             * Credential subject.
             * @member {Uint8Array} subject
             * @memberof dmcn.identity.Credential
             * @instance
             */
            Credential.prototype.subject = $util.newBuffer([]);

            /**
             * Credential domain.
             * @member {string} domain
             * @memberof dmcn.identity.Credential
             * @instance
             */
            Credential.prototype.domain = "";

            /**
             * Credential address.
             * @member {string} address
             * @memberof dmcn.identity.Credential
             * @instance
             */
            Credential.prototype.address = "";

            /**
             * Credential roles.
             * @member {Array.<string>} roles
             * @memberof dmcn.identity.Credential
             * @instance
             */
            Credential.prototype.roles = $util.emptyArray;

            /**
             * Credential grants.
             * @member {Array.<string>} grants
             * @memberof dmcn.identity.Credential
             * @instance
             */
            Credential.prototype.grants = $util.emptyArray;

            /**
             * Credential attributes.
             * @member {Object.<string,string>} attributes
             * @memberof dmcn.identity.Credential
             * @instance
             */
            Credential.prototype.attributes = $util.emptyObject;

            /**
             * Credential issuedAt.
             * @member {number|Long} issuedAt
             * @memberof dmcn.identity.Credential
             * @instance
             */
            Credential.prototype.issuedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * Credential notAfter.
             * @member {number|Long} notAfter
             * @memberof dmcn.identity.Credential
             * @instance
             */
            Credential.prototype.notAfter = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * Credential scope.
             * @member {string} scope
             * @memberof dmcn.identity.Credential
             * @instance
             */
            Credential.prototype.scope = "";

            /**
             * Credential issuerPub.
             * @member {Uint8Array} issuerPub
             * @memberof dmcn.identity.Credential
             * @instance
             */
            Credential.prototype.issuerPub = $util.newBuffer([]);

            /**
             * Credential signature.
             * @member {Uint8Array} signature
             * @memberof dmcn.identity.Credential
             * @instance
             */
            Credential.prototype.signature = $util.newBuffer([]);

            /**
             * Credential relayHints.
             * @member {Array.<string>} relayHints
             * @memberof dmcn.identity.Credential
             * @instance
             */
            Credential.prototype.relayHints = $util.emptyArray;

            /**
             * Credential effectiveFrom.
             * @member {number|Long} effectiveFrom
             * @memberof dmcn.identity.Credential
             * @instance
             */
            Credential.prototype.effectiveFrom = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * Creates a new Credential instance using the specified properties.
             * @function create
             * @memberof dmcn.identity.Credential
             * @static
             * @param {dmcn.identity.ICredential=} [properties] Properties to set
             * @returns {dmcn.identity.Credential} Credential instance
             */
            Credential.create = function create(properties) {
                return new Credential(properties);
            };

            /**
             * Encodes the specified Credential message. Does not implicitly {@link dmcn.identity.Credential.verify|verify} messages.
             * @function encode
             * @memberof dmcn.identity.Credential
             * @static
             * @param {dmcn.identity.ICredential} message Credential message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Credential.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.version);
                if (message.subject != null && Object.hasOwnProperty.call(message, "subject"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.subject);
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.domain);
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.address);
                if (message.roles != null && message.roles.length)
                    for (let i = 0; i < message.roles.length; ++i)
                        writer.uint32(/* id 5, wireType 2 =*/42).string(message.roles[i]);
                if (message.grants != null && message.grants.length)
                    for (let i = 0; i < message.grants.length; ++i)
                        writer.uint32(/* id 6, wireType 2 =*/50).string(message.grants[i]);
                if (message.attributes != null && Object.hasOwnProperty.call(message, "attributes"))
                    for (let keys = Object.keys(message.attributes), i = 0; i < keys.length; ++i)
                        writer.uint32(/* id 7, wireType 2 =*/58).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.attributes[keys[i]]).ldelim();
                if (message.issuedAt != null && Object.hasOwnProperty.call(message, "issuedAt"))
                    writer.uint32(/* id 8, wireType 0 =*/64).int64(message.issuedAt);
                if (message.notAfter != null && Object.hasOwnProperty.call(message, "notAfter"))
                    writer.uint32(/* id 9, wireType 0 =*/72).int64(message.notAfter);
                if (message.scope != null && Object.hasOwnProperty.call(message, "scope"))
                    writer.uint32(/* id 10, wireType 2 =*/82).string(message.scope);
                if (message.issuerPub != null && Object.hasOwnProperty.call(message, "issuerPub"))
                    writer.uint32(/* id 11, wireType 2 =*/90).bytes(message.issuerPub);
                if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                    writer.uint32(/* id 12, wireType 2 =*/98).bytes(message.signature);
                if (message.relayHints != null && message.relayHints.length)
                    for (let i = 0; i < message.relayHints.length; ++i)
                        writer.uint32(/* id 13, wireType 2 =*/106).string(message.relayHints[i]);
                if (message.effectiveFrom != null && Object.hasOwnProperty.call(message, "effectiveFrom"))
                    writer.uint32(/* id 14, wireType 0 =*/112).int64(message.effectiveFrom);
                return writer;
            };

            /**
             * Encodes the specified Credential message, length delimited. Does not implicitly {@link dmcn.identity.Credential.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.identity.Credential
             * @static
             * @param {dmcn.identity.ICredential} message Credential message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Credential.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a Credential message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.identity.Credential
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.identity.Credential} Credential
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Credential.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.identity.Credential(), key, value;
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.version = reader.uint32();
                            break;
                        }
                    case 2: {
                            message.subject = reader.bytes();
                            break;
                        }
                    case 3: {
                            message.domain = reader.string();
                            break;
                        }
                    case 4: {
                            message.address = reader.string();
                            break;
                        }
                    case 5: {
                            if (!(message.roles && message.roles.length))
                                message.roles = [];
                            message.roles.push(reader.string());
                            break;
                        }
                    case 6: {
                            if (!(message.grants && message.grants.length))
                                message.grants = [];
                            message.grants.push(reader.string());
                            break;
                        }
                    case 7: {
                            if (message.attributes === $util.emptyObject)
                                message.attributes = {};
                            let end2 = reader.uint32() + reader.pos;
                            key = "";
                            value = "";
                            while (reader.pos < end2) {
                                let tag2 = reader.uint32();
                                switch (tag2 >>> 3) {
                                case 1:
                                    key = reader.string();
                                    break;
                                case 2:
                                    value = reader.string();
                                    break;
                                default:
                                    reader.skipType(tag2 & 7, long);
                                    break;
                                }
                            }
                            if (key === "__proto__")
                                $util.makeProp(message.attributes, key);
                            message.attributes[key] = value;
                            break;
                        }
                    case 8: {
                            message.issuedAt = reader.int64();
                            break;
                        }
                    case 9: {
                            message.notAfter = reader.int64();
                            break;
                        }
                    case 10: {
                            message.scope = reader.string();
                            break;
                        }
                    case 11: {
                            message.issuerPub = reader.bytes();
                            break;
                        }
                    case 12: {
                            message.signature = reader.bytes();
                            break;
                        }
                    case 13: {
                            if (!(message.relayHints && message.relayHints.length))
                                message.relayHints = [];
                            message.relayHints.push(reader.string());
                            break;
                        }
                    case 14: {
                            message.effectiveFrom = reader.int64();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a Credential message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.identity.Credential
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.identity.Credential} Credential
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Credential.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Credential message.
             * @function verify
             * @memberof dmcn.identity.Credential
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Credential.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    if (!$util.isInteger(message.version))
                        return "version: integer expected";
                if (message.subject != null && Object.hasOwnProperty.call(message, "subject"))
                    if (!(message.subject && typeof message.subject.length === "number" || $util.isString(message.subject)))
                        return "subject: buffer expected";
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    if (!$util.isString(message.domain))
                        return "domain: string expected";
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    if (!$util.isString(message.address))
                        return "address: string expected";
                if (message.roles != null && Object.hasOwnProperty.call(message, "roles")) {
                    if (!Array.isArray(message.roles))
                        return "roles: array expected";
                    for (let i = 0; i < message.roles.length; ++i)
                        if (!$util.isString(message.roles[i]))
                            return "roles: string[] expected";
                }
                if (message.grants != null && Object.hasOwnProperty.call(message, "grants")) {
                    if (!Array.isArray(message.grants))
                        return "grants: array expected";
                    for (let i = 0; i < message.grants.length; ++i)
                        if (!$util.isString(message.grants[i]))
                            return "grants: string[] expected";
                }
                if (message.attributes != null && Object.hasOwnProperty.call(message, "attributes")) {
                    if (!$util.isObject(message.attributes))
                        return "attributes: object expected";
                    let key = Object.keys(message.attributes);
                    for (let i = 0; i < key.length; ++i)
                        if (!$util.isString(message.attributes[key[i]]))
                            return "attributes: string{k:string} expected";
                }
                if (message.issuedAt != null && Object.hasOwnProperty.call(message, "issuedAt"))
                    if (!$util.isInteger(message.issuedAt) && !(message.issuedAt && $util.isInteger(message.issuedAt.low) && $util.isInteger(message.issuedAt.high)))
                        return "issuedAt: integer|Long expected";
                if (message.notAfter != null && Object.hasOwnProperty.call(message, "notAfter"))
                    if (!$util.isInteger(message.notAfter) && !(message.notAfter && $util.isInteger(message.notAfter.low) && $util.isInteger(message.notAfter.high)))
                        return "notAfter: integer|Long expected";
                if (message.scope != null && Object.hasOwnProperty.call(message, "scope"))
                    if (!$util.isString(message.scope))
                        return "scope: string expected";
                if (message.issuerPub != null && Object.hasOwnProperty.call(message, "issuerPub"))
                    if (!(message.issuerPub && typeof message.issuerPub.length === "number" || $util.isString(message.issuerPub)))
                        return "issuerPub: buffer expected";
                if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                    if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                        return "signature: buffer expected";
                if (message.relayHints != null && Object.hasOwnProperty.call(message, "relayHints")) {
                    if (!Array.isArray(message.relayHints))
                        return "relayHints: array expected";
                    for (let i = 0; i < message.relayHints.length; ++i)
                        if (!$util.isString(message.relayHints[i]))
                            return "relayHints: string[] expected";
                }
                if (message.effectiveFrom != null && Object.hasOwnProperty.call(message, "effectiveFrom"))
                    if (!$util.isInteger(message.effectiveFrom) && !(message.effectiveFrom && $util.isInteger(message.effectiveFrom.low) && $util.isInteger(message.effectiveFrom.high)))
                        return "effectiveFrom: integer|Long expected";
                return null;
            };

            /**
             * Creates a Credential message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.identity.Credential
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.identity.Credential} Credential
             */
            Credential.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.identity.Credential)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.identity.Credential: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.identity.Credential();
                if (object.version != null)
                    message.version = object.version >>> 0;
                if (object.subject != null)
                    if (typeof object.subject === "string")
                        $util.base64.decode(object.subject, message.subject = $util.newBuffer($util.base64.length(object.subject)), 0);
                    else if (object.subject.length >= 0)
                        message.subject = object.subject;
                if (object.domain != null)
                    message.domain = String(object.domain);
                if (object.address != null)
                    message.address = String(object.address);
                if (object.roles) {
                    if (!Array.isArray(object.roles))
                        throw TypeError(".dmcn.identity.Credential.roles: array expected");
                    message.roles = [];
                    for (let i = 0; i < object.roles.length; ++i)
                        message.roles[i] = String(object.roles[i]);
                }
                if (object.grants) {
                    if (!Array.isArray(object.grants))
                        throw TypeError(".dmcn.identity.Credential.grants: array expected");
                    message.grants = [];
                    for (let i = 0; i < object.grants.length; ++i)
                        message.grants[i] = String(object.grants[i]);
                }
                if (object.attributes) {
                    if (!$util.isObject(object.attributes))
                        throw TypeError(".dmcn.identity.Credential.attributes: object expected");
                    message.attributes = {};
                    for (let keys = Object.keys(object.attributes), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.attributes, keys[i]);
                        message.attributes[keys[i]] = String(object.attributes[keys[i]]);
                    }
                }
                if (object.issuedAt != null)
                    if ($util.Long)
                        message.issuedAt = $util.Long.fromValue(object.issuedAt, false);
                    else if (typeof object.issuedAt === "string")
                        message.issuedAt = parseInt(object.issuedAt, 10);
                    else if (typeof object.issuedAt === "number")
                        message.issuedAt = object.issuedAt;
                    else if (typeof object.issuedAt === "object")
                        message.issuedAt = new $util.LongBits(object.issuedAt.low >>> 0, object.issuedAt.high >>> 0).toNumber();
                if (object.notAfter != null)
                    if ($util.Long)
                        message.notAfter = $util.Long.fromValue(object.notAfter, false);
                    else if (typeof object.notAfter === "string")
                        message.notAfter = parseInt(object.notAfter, 10);
                    else if (typeof object.notAfter === "number")
                        message.notAfter = object.notAfter;
                    else if (typeof object.notAfter === "object")
                        message.notAfter = new $util.LongBits(object.notAfter.low >>> 0, object.notAfter.high >>> 0).toNumber();
                if (object.scope != null)
                    message.scope = String(object.scope);
                if (object.issuerPub != null)
                    if (typeof object.issuerPub === "string")
                        $util.base64.decode(object.issuerPub, message.issuerPub = $util.newBuffer($util.base64.length(object.issuerPub)), 0);
                    else if (object.issuerPub.length >= 0)
                        message.issuerPub = object.issuerPub;
                if (object.signature != null)
                    if (typeof object.signature === "string")
                        $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                    else if (object.signature.length >= 0)
                        message.signature = object.signature;
                if (object.relayHints) {
                    if (!Array.isArray(object.relayHints))
                        throw TypeError(".dmcn.identity.Credential.relayHints: array expected");
                    message.relayHints = [];
                    for (let i = 0; i < object.relayHints.length; ++i)
                        message.relayHints[i] = String(object.relayHints[i]);
                }
                if (object.effectiveFrom != null)
                    if ($util.Long)
                        message.effectiveFrom = $util.Long.fromValue(object.effectiveFrom, false);
                    else if (typeof object.effectiveFrom === "string")
                        message.effectiveFrom = parseInt(object.effectiveFrom, 10);
                    else if (typeof object.effectiveFrom === "number")
                        message.effectiveFrom = object.effectiveFrom;
                    else if (typeof object.effectiveFrom === "object")
                        message.effectiveFrom = new $util.LongBits(object.effectiveFrom.low >>> 0, object.effectiveFrom.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a Credential message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.identity.Credential
             * @static
             * @param {dmcn.identity.Credential} message Credential
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Credential.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.roles = [];
                    object.grants = [];
                    object.relayHints = [];
                }
                if (options.objects || options.defaults)
                    object.attributes = {};
                if (options.defaults) {
                    object.version = 0;
                    if (options.bytes === String)
                        object.subject = "";
                    else {
                        object.subject = [];
                        if (options.bytes !== Array)
                            object.subject = $util.newBuffer(object.subject);
                    }
                    object.domain = "";
                    object.address = "";
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.issuedAt = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.issuedAt = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.notAfter = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.notAfter = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    object.scope = "";
                    if (options.bytes === String)
                        object.issuerPub = "";
                    else {
                        object.issuerPub = [];
                        if (options.bytes !== Array)
                            object.issuerPub = $util.newBuffer(object.issuerPub);
                    }
                    if (options.bytes === String)
                        object.signature = "";
                    else {
                        object.signature = [];
                        if (options.bytes !== Array)
                            object.signature = $util.newBuffer(object.signature);
                    }
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.effectiveFrom = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.effectiveFrom = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                }
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    object.version = message.version;
                if (message.subject != null && Object.hasOwnProperty.call(message, "subject"))
                    object.subject = options.bytes === String ? $util.base64.encode(message.subject, 0, message.subject.length) : options.bytes === Array ? Array.prototype.slice.call(message.subject) : message.subject;
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    object.domain = message.domain;
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    object.address = message.address;
                if (message.roles && message.roles.length) {
                    object.roles = [];
                    for (let j = 0; j < message.roles.length; ++j)
                        object.roles[j] = message.roles[j];
                }
                if (message.grants && message.grants.length) {
                    object.grants = [];
                    for (let j = 0; j < message.grants.length; ++j)
                        object.grants[j] = message.grants[j];
                }
                let keys2;
                if (message.attributes && (keys2 = Object.keys(message.attributes)).length) {
                    object.attributes = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.attributes, keys2[j]);
                        object.attributes[keys2[j]] = message.attributes[keys2[j]];
                    }
                }
                if (message.issuedAt != null && Object.hasOwnProperty.call(message, "issuedAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.issuedAt = typeof message.issuedAt === "number" ? BigInt(message.issuedAt) : $util.Long.fromBits(message.issuedAt.low >>> 0, message.issuedAt.high >>> 0, false).toBigInt();
                    else if (typeof message.issuedAt === "number")
                        object.issuedAt = options.longs === String ? String(message.issuedAt) : message.issuedAt;
                    else
                        object.issuedAt = options.longs === String ? $util.Long.prototype.toString.call(message.issuedAt) : options.longs === Number ? new $util.LongBits(message.issuedAt.low >>> 0, message.issuedAt.high >>> 0).toNumber() : message.issuedAt;
                if (message.notAfter != null && Object.hasOwnProperty.call(message, "notAfter"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.notAfter = typeof message.notAfter === "number" ? BigInt(message.notAfter) : $util.Long.fromBits(message.notAfter.low >>> 0, message.notAfter.high >>> 0, false).toBigInt();
                    else if (typeof message.notAfter === "number")
                        object.notAfter = options.longs === String ? String(message.notAfter) : message.notAfter;
                    else
                        object.notAfter = options.longs === String ? $util.Long.prototype.toString.call(message.notAfter) : options.longs === Number ? new $util.LongBits(message.notAfter.low >>> 0, message.notAfter.high >>> 0).toNumber() : message.notAfter;
                if (message.scope != null && Object.hasOwnProperty.call(message, "scope"))
                    object.scope = message.scope;
                if (message.issuerPub != null && Object.hasOwnProperty.call(message, "issuerPub"))
                    object.issuerPub = options.bytes === String ? $util.base64.encode(message.issuerPub, 0, message.issuerPub.length) : options.bytes === Array ? Array.prototype.slice.call(message.issuerPub) : message.issuerPub;
                if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                    object.signature = options.bytes === String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === Array ? Array.prototype.slice.call(message.signature) : message.signature;
                if (message.relayHints && message.relayHints.length) {
                    object.relayHints = [];
                    for (let j = 0; j < message.relayHints.length; ++j)
                        object.relayHints[j] = message.relayHints[j];
                }
                if (message.effectiveFrom != null && Object.hasOwnProperty.call(message, "effectiveFrom"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.effectiveFrom = typeof message.effectiveFrom === "number" ? BigInt(message.effectiveFrom) : $util.Long.fromBits(message.effectiveFrom.low >>> 0, message.effectiveFrom.high >>> 0, false).toBigInt();
                    else if (typeof message.effectiveFrom === "number")
                        object.effectiveFrom = options.longs === String ? String(message.effectiveFrom) : message.effectiveFrom;
                    else
                        object.effectiveFrom = options.longs === String ? $util.Long.prototype.toString.call(message.effectiveFrom) : options.longs === Number ? new $util.LongBits(message.effectiveFrom.low >>> 0, message.effectiveFrom.high >>> 0).toNumber() : message.effectiveFrom;
                return object;
            };

            /**
             * Converts this Credential to JSON.
             * @function toJSON
             * @memberof dmcn.identity.Credential
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Credential.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Credential
             * @function getTypeUrl
             * @memberof dmcn.identity.Credential
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Credential.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.identity.Credential";
            };

            return Credential;
        })();

        identity.CredentialBlock = (function() {

            /**
             * Properties of a CredentialBlock.
             * @memberof dmcn.identity
             * @interface ICredentialBlock
             * @property {Uint8Array|null} [pubkey] CredentialBlock pubkey
             * @property {number|Long|null} [effectiveFrom] CredentialBlock effectiveFrom
             * @property {boolean|null} [compromised] CredentialBlock compromised
             * @property {number|Long|null} [compromisedSince] CredentialBlock compromisedSince
             * @property {number|Long|null} [createdAt] CredentialBlock createdAt
             */

            /**
             * Constructs a new CredentialBlock.
             * @memberof dmcn.identity
             * @classdesc Represents a CredentialBlock.
             * @implements ICredentialBlock
             * @constructor
             * @param {dmcn.identity.ICredentialBlock=} [properties] Properties to set
             */
            function CredentialBlock(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * CredentialBlock pubkey.
             * @member {Uint8Array} pubkey
             * @memberof dmcn.identity.CredentialBlock
             * @instance
             */
            CredentialBlock.prototype.pubkey = $util.newBuffer([]);

            /**
             * CredentialBlock effectiveFrom.
             * @member {number|Long} effectiveFrom
             * @memberof dmcn.identity.CredentialBlock
             * @instance
             */
            CredentialBlock.prototype.effectiveFrom = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * CredentialBlock compromised.
             * @member {boolean} compromised
             * @memberof dmcn.identity.CredentialBlock
             * @instance
             */
            CredentialBlock.prototype.compromised = false;

            /**
             * CredentialBlock compromisedSince.
             * @member {number|Long} compromisedSince
             * @memberof dmcn.identity.CredentialBlock
             * @instance
             */
            CredentialBlock.prototype.compromisedSince = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * CredentialBlock createdAt.
             * @member {number|Long} createdAt
             * @memberof dmcn.identity.CredentialBlock
             * @instance
             */
            CredentialBlock.prototype.createdAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * Creates a new CredentialBlock instance using the specified properties.
             * @function create
             * @memberof dmcn.identity.CredentialBlock
             * @static
             * @param {dmcn.identity.ICredentialBlock=} [properties] Properties to set
             * @returns {dmcn.identity.CredentialBlock} CredentialBlock instance
             */
            CredentialBlock.create = function create(properties) {
                return new CredentialBlock(properties);
            };

            /**
             * Encodes the specified CredentialBlock message. Does not implicitly {@link dmcn.identity.CredentialBlock.verify|verify} messages.
             * @function encode
             * @memberof dmcn.identity.CredentialBlock
             * @static
             * @param {dmcn.identity.ICredentialBlock} message CredentialBlock message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CredentialBlock.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.pubkey != null && Object.hasOwnProperty.call(message, "pubkey"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.pubkey);
                if (message.effectiveFrom != null && Object.hasOwnProperty.call(message, "effectiveFrom"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.effectiveFrom);
                if (message.compromised != null && Object.hasOwnProperty.call(message, "compromised"))
                    writer.uint32(/* id 3, wireType 0 =*/24).bool(message.compromised);
                if (message.compromisedSince != null && Object.hasOwnProperty.call(message, "compromisedSince"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int64(message.compromisedSince);
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.createdAt);
                return writer;
            };

            /**
             * Encodes the specified CredentialBlock message, length delimited. Does not implicitly {@link dmcn.identity.CredentialBlock.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.identity.CredentialBlock
             * @static
             * @param {dmcn.identity.ICredentialBlock} message CredentialBlock message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CredentialBlock.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a CredentialBlock message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.identity.CredentialBlock
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.identity.CredentialBlock} CredentialBlock
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CredentialBlock.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.identity.CredentialBlock();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.pubkey = reader.bytes();
                            break;
                        }
                    case 2: {
                            message.effectiveFrom = reader.int64();
                            break;
                        }
                    case 3: {
                            message.compromised = reader.bool();
                            break;
                        }
                    case 4: {
                            message.compromisedSince = reader.int64();
                            break;
                        }
                    case 5: {
                            message.createdAt = reader.int64();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a CredentialBlock message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.identity.CredentialBlock
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.identity.CredentialBlock} CredentialBlock
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CredentialBlock.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CredentialBlock message.
             * @function verify
             * @memberof dmcn.identity.CredentialBlock
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CredentialBlock.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.pubkey != null && Object.hasOwnProperty.call(message, "pubkey"))
                    if (!(message.pubkey && typeof message.pubkey.length === "number" || $util.isString(message.pubkey)))
                        return "pubkey: buffer expected";
                if (message.effectiveFrom != null && Object.hasOwnProperty.call(message, "effectiveFrom"))
                    if (!$util.isInteger(message.effectiveFrom) && !(message.effectiveFrom && $util.isInteger(message.effectiveFrom.low) && $util.isInteger(message.effectiveFrom.high)))
                        return "effectiveFrom: integer|Long expected";
                if (message.compromised != null && Object.hasOwnProperty.call(message, "compromised"))
                    if (typeof message.compromised !== "boolean")
                        return "compromised: boolean expected";
                if (message.compromisedSince != null && Object.hasOwnProperty.call(message, "compromisedSince"))
                    if (!$util.isInteger(message.compromisedSince) && !(message.compromisedSince && $util.isInteger(message.compromisedSince.low) && $util.isInteger(message.compromisedSince.high)))
                        return "compromisedSince: integer|Long expected";
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
                        return "createdAt: integer|Long expected";
                return null;
            };

            /**
             * Creates a CredentialBlock message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.identity.CredentialBlock
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.identity.CredentialBlock} CredentialBlock
             */
            CredentialBlock.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.identity.CredentialBlock)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.identity.CredentialBlock: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.identity.CredentialBlock();
                if (object.pubkey != null)
                    if (typeof object.pubkey === "string")
                        $util.base64.decode(object.pubkey, message.pubkey = $util.newBuffer($util.base64.length(object.pubkey)), 0);
                    else if (object.pubkey.length >= 0)
                        message.pubkey = object.pubkey;
                if (object.effectiveFrom != null)
                    if ($util.Long)
                        message.effectiveFrom = $util.Long.fromValue(object.effectiveFrom, false);
                    else if (typeof object.effectiveFrom === "string")
                        message.effectiveFrom = parseInt(object.effectiveFrom, 10);
                    else if (typeof object.effectiveFrom === "number")
                        message.effectiveFrom = object.effectiveFrom;
                    else if (typeof object.effectiveFrom === "object")
                        message.effectiveFrom = new $util.LongBits(object.effectiveFrom.low >>> 0, object.effectiveFrom.high >>> 0).toNumber();
                if (object.compromised != null)
                    message.compromised = Boolean(object.compromised);
                if (object.compromisedSince != null)
                    if ($util.Long)
                        message.compromisedSince = $util.Long.fromValue(object.compromisedSince, false);
                    else if (typeof object.compromisedSince === "string")
                        message.compromisedSince = parseInt(object.compromisedSince, 10);
                    else if (typeof object.compromisedSince === "number")
                        message.compromisedSince = object.compromisedSince;
                    else if (typeof object.compromisedSince === "object")
                        message.compromisedSince = new $util.LongBits(object.compromisedSince.low >>> 0, object.compromisedSince.high >>> 0).toNumber();
                if (object.createdAt != null)
                    if ($util.Long)
                        message.createdAt = $util.Long.fromValue(object.createdAt, false);
                    else if (typeof object.createdAt === "string")
                        message.createdAt = parseInt(object.createdAt, 10);
                    else if (typeof object.createdAt === "number")
                        message.createdAt = object.createdAt;
                    else if (typeof object.createdAt === "object")
                        message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a CredentialBlock message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.identity.CredentialBlock
             * @static
             * @param {dmcn.identity.CredentialBlock} message CredentialBlock
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CredentialBlock.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    if (options.bytes === String)
                        object.pubkey = "";
                    else {
                        object.pubkey = [];
                        if (options.bytes !== Array)
                            object.pubkey = $util.newBuffer(object.pubkey);
                    }
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.effectiveFrom = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.effectiveFrom = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    object.compromised = false;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.compromisedSince = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.compromisedSince = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.createdAt = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.createdAt = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                }
                if (message.pubkey != null && Object.hasOwnProperty.call(message, "pubkey"))
                    object.pubkey = options.bytes === String ? $util.base64.encode(message.pubkey, 0, message.pubkey.length) : options.bytes === Array ? Array.prototype.slice.call(message.pubkey) : message.pubkey;
                if (message.effectiveFrom != null && Object.hasOwnProperty.call(message, "effectiveFrom"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.effectiveFrom = typeof message.effectiveFrom === "number" ? BigInt(message.effectiveFrom) : $util.Long.fromBits(message.effectiveFrom.low >>> 0, message.effectiveFrom.high >>> 0, false).toBigInt();
                    else if (typeof message.effectiveFrom === "number")
                        object.effectiveFrom = options.longs === String ? String(message.effectiveFrom) : message.effectiveFrom;
                    else
                        object.effectiveFrom = options.longs === String ? $util.Long.prototype.toString.call(message.effectiveFrom) : options.longs === Number ? new $util.LongBits(message.effectiveFrom.low >>> 0, message.effectiveFrom.high >>> 0).toNumber() : message.effectiveFrom;
                if (message.compromised != null && Object.hasOwnProperty.call(message, "compromised"))
                    object.compromised = message.compromised;
                if (message.compromisedSince != null && Object.hasOwnProperty.call(message, "compromisedSince"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.compromisedSince = typeof message.compromisedSince === "number" ? BigInt(message.compromisedSince) : $util.Long.fromBits(message.compromisedSince.low >>> 0, message.compromisedSince.high >>> 0, false).toBigInt();
                    else if (typeof message.compromisedSince === "number")
                        object.compromisedSince = options.longs === String ? String(message.compromisedSince) : message.compromisedSince;
                    else
                        object.compromisedSince = options.longs === String ? $util.Long.prototype.toString.call(message.compromisedSince) : options.longs === Number ? new $util.LongBits(message.compromisedSince.low >>> 0, message.compromisedSince.high >>> 0).toNumber() : message.compromisedSince;
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.createdAt = typeof message.createdAt === "number" ? BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
                    else if (typeof message.createdAt === "number")
                        object.createdAt = options.longs === String ? String(message.createdAt) : message.createdAt;
                    else
                        object.createdAt = options.longs === String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
                return object;
            };

            /**
             * Converts this CredentialBlock to JSON.
             * @function toJSON
             * @memberof dmcn.identity.CredentialBlock
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CredentialBlock.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for CredentialBlock
             * @function getTypeUrl
             * @memberof dmcn.identity.CredentialBlock
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            CredentialBlock.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.identity.CredentialBlock";
            };

            return CredentialBlock;
        })();

        identity.CredentialBlockList = (function() {

            /**
             * Properties of a CredentialBlockList.
             * @memberof dmcn.identity
             * @interface ICredentialBlockList
             * @property {string|null} [domain] CredentialBlockList domain
             * @property {Array.<dmcn.identity.ICredentialBlock>|null} [blocks] CredentialBlockList blocks
             * @property {number|Long|null} [revision] CredentialBlockList revision
             * @property {Uint8Array|null} [signature] CredentialBlockList signature
             */

            /**
             * Constructs a new CredentialBlockList.
             * @memberof dmcn.identity
             * @classdesc Represents a CredentialBlockList.
             * @implements ICredentialBlockList
             * @constructor
             * @param {dmcn.identity.ICredentialBlockList=} [properties] Properties to set
             */
            function CredentialBlockList(properties) {
                this.blocks = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * CredentialBlockList domain.
             * @member {string} domain
             * @memberof dmcn.identity.CredentialBlockList
             * @instance
             */
            CredentialBlockList.prototype.domain = "";

            /**
             * CredentialBlockList blocks.
             * @member {Array.<dmcn.identity.ICredentialBlock>} blocks
             * @memberof dmcn.identity.CredentialBlockList
             * @instance
             */
            CredentialBlockList.prototype.blocks = $util.emptyArray;

            /**
             * CredentialBlockList revision.
             * @member {number|Long} revision
             * @memberof dmcn.identity.CredentialBlockList
             * @instance
             */
            CredentialBlockList.prototype.revision = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * CredentialBlockList signature.
             * @member {Uint8Array} signature
             * @memberof dmcn.identity.CredentialBlockList
             * @instance
             */
            CredentialBlockList.prototype.signature = $util.newBuffer([]);

            /**
             * Creates a new CredentialBlockList instance using the specified properties.
             * @function create
             * @memberof dmcn.identity.CredentialBlockList
             * @static
             * @param {dmcn.identity.ICredentialBlockList=} [properties] Properties to set
             * @returns {dmcn.identity.CredentialBlockList} CredentialBlockList instance
             */
            CredentialBlockList.create = function create(properties) {
                return new CredentialBlockList(properties);
            };

            /**
             * Encodes the specified CredentialBlockList message. Does not implicitly {@link dmcn.identity.CredentialBlockList.verify|verify} messages.
             * @function encode
             * @memberof dmcn.identity.CredentialBlockList
             * @static
             * @param {dmcn.identity.ICredentialBlockList} message CredentialBlockList message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CredentialBlockList.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.domain);
                if (message.blocks != null && message.blocks.length)
                    for (let i = 0; i < message.blocks.length; ++i)
                        $root.dmcn.identity.CredentialBlock.encode(message.blocks[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                    writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.revision);
                if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.signature);
                return writer;
            };

            /**
             * Encodes the specified CredentialBlockList message, length delimited. Does not implicitly {@link dmcn.identity.CredentialBlockList.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.identity.CredentialBlockList
             * @static
             * @param {dmcn.identity.ICredentialBlockList} message CredentialBlockList message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CredentialBlockList.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a CredentialBlockList message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.identity.CredentialBlockList
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.identity.CredentialBlockList} CredentialBlockList
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CredentialBlockList.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.identity.CredentialBlockList();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.domain = reader.string();
                            break;
                        }
                    case 2: {
                            if (!(message.blocks && message.blocks.length))
                                message.blocks = [];
                            message.blocks.push($root.dmcn.identity.CredentialBlock.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 3: {
                            message.revision = reader.uint64();
                            break;
                        }
                    case 4: {
                            message.signature = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a CredentialBlockList message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.identity.CredentialBlockList
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.identity.CredentialBlockList} CredentialBlockList
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CredentialBlockList.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CredentialBlockList message.
             * @function verify
             * @memberof dmcn.identity.CredentialBlockList
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CredentialBlockList.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    if (!$util.isString(message.domain))
                        return "domain: string expected";
                if (message.blocks != null && Object.hasOwnProperty.call(message, "blocks")) {
                    if (!Array.isArray(message.blocks))
                        return "blocks: array expected";
                    for (let i = 0; i < message.blocks.length; ++i) {
                        let error = $root.dmcn.identity.CredentialBlock.verify(message.blocks[i], long + 1);
                        if (error)
                            return "blocks." + error;
                    }
                }
                if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                    if (!$util.isInteger(message.revision) && !(message.revision && $util.isInteger(message.revision.low) && $util.isInteger(message.revision.high)))
                        return "revision: integer|Long expected";
                if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                    if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                        return "signature: buffer expected";
                return null;
            };

            /**
             * Creates a CredentialBlockList message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.identity.CredentialBlockList
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.identity.CredentialBlockList} CredentialBlockList
             */
            CredentialBlockList.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.identity.CredentialBlockList)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.identity.CredentialBlockList: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.identity.CredentialBlockList();
                if (object.domain != null)
                    message.domain = String(object.domain);
                if (object.blocks) {
                    if (!Array.isArray(object.blocks))
                        throw TypeError(".dmcn.identity.CredentialBlockList.blocks: array expected");
                    message.blocks = [];
                    for (let i = 0; i < object.blocks.length; ++i) {
                        if (!$util.isObject(object.blocks[i]))
                            throw TypeError(".dmcn.identity.CredentialBlockList.blocks: object expected");
                        message.blocks[i] = $root.dmcn.identity.CredentialBlock.fromObject(object.blocks[i], long + 1);
                    }
                }
                if (object.revision != null)
                    if ($util.Long)
                        message.revision = $util.Long.fromValue(object.revision, true);
                    else if (typeof object.revision === "string")
                        message.revision = parseInt(object.revision, 10);
                    else if (typeof object.revision === "number")
                        message.revision = object.revision;
                    else if (typeof object.revision === "object")
                        message.revision = new $util.LongBits(object.revision.low >>> 0, object.revision.high >>> 0).toNumber(true);
                if (object.signature != null)
                    if (typeof object.signature === "string")
                        $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                    else if (object.signature.length >= 0)
                        message.signature = object.signature;
                return message;
            };

            /**
             * Creates a plain object from a CredentialBlockList message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.identity.CredentialBlockList
             * @static
             * @param {dmcn.identity.CredentialBlockList} message CredentialBlockList
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CredentialBlockList.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.blocks = [];
                if (options.defaults) {
                    object.domain = "";
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.revision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.revision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if (options.bytes === String)
                        object.signature = "";
                    else {
                        object.signature = [];
                        if (options.bytes !== Array)
                            object.signature = $util.newBuffer(object.signature);
                    }
                }
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    object.domain = message.domain;
                if (message.blocks && message.blocks.length) {
                    object.blocks = [];
                    for (let j = 0; j < message.blocks.length; ++j)
                        object.blocks[j] = $root.dmcn.identity.CredentialBlock.toObject(message.blocks[j], options, q + 1);
                }
                if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.revision = typeof message.revision === "number" ? BigInt(message.revision) : $util.Long.fromBits(message.revision.low >>> 0, message.revision.high >>> 0, true).toBigInt();
                    else if (typeof message.revision === "number")
                        object.revision = options.longs === String ? String(message.revision) : message.revision;
                    else
                        object.revision = options.longs === String ? $util.Long.prototype.toString.call(message.revision) : options.longs === Number ? new $util.LongBits(message.revision.low >>> 0, message.revision.high >>> 0).toNumber(true) : message.revision;
                if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                    object.signature = options.bytes === String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === Array ? Array.prototype.slice.call(message.signature) : message.signature;
                return object;
            };

            /**
             * Converts this CredentialBlockList to JSON.
             * @function toJSON
             * @memberof dmcn.identity.CredentialBlockList
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CredentialBlockList.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for CredentialBlockList
             * @function getTypeUrl
             * @memberof dmcn.identity.CredentialBlockList
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            CredentialBlockList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.identity.CredentialBlockList";
            };

            return CredentialBlockList;
        })();

        identity.CredentialBundle = (function() {

            /**
             * Properties of a CredentialBundle.
             * @memberof dmcn.identity
             * @interface ICredentialBundle
             * @property {dmcn.identity.ICredential|null} [credential] CredentialBundle credential
             * @property {dmcn.identity.IDomainAuthorityRecord|null} [dar] CredentialBundle dar
             */

            /**
             * Constructs a new CredentialBundle.
             * @memberof dmcn.identity
             * @classdesc Represents a CredentialBundle.
             * @implements ICredentialBundle
             * @constructor
             * @param {dmcn.identity.ICredentialBundle=} [properties] Properties to set
             */
            function CredentialBundle(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * CredentialBundle credential.
             * @member {dmcn.identity.ICredential|null|undefined} credential
             * @memberof dmcn.identity.CredentialBundle
             * @instance
             */
            CredentialBundle.prototype.credential = null;

            /**
             * CredentialBundle dar.
             * @member {dmcn.identity.IDomainAuthorityRecord|null|undefined} dar
             * @memberof dmcn.identity.CredentialBundle
             * @instance
             */
            CredentialBundle.prototype.dar = null;

            /**
             * Creates a new CredentialBundle instance using the specified properties.
             * @function create
             * @memberof dmcn.identity.CredentialBundle
             * @static
             * @param {dmcn.identity.ICredentialBundle=} [properties] Properties to set
             * @returns {dmcn.identity.CredentialBundle} CredentialBundle instance
             */
            CredentialBundle.create = function create(properties) {
                return new CredentialBundle(properties);
            };

            /**
             * Encodes the specified CredentialBundle message. Does not implicitly {@link dmcn.identity.CredentialBundle.verify|verify} messages.
             * @function encode
             * @memberof dmcn.identity.CredentialBundle
             * @static
             * @param {dmcn.identity.ICredentialBundle} message CredentialBundle message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CredentialBundle.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.credential != null && Object.hasOwnProperty.call(message, "credential"))
                    $root.dmcn.identity.Credential.encode(message.credential, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.dar != null && Object.hasOwnProperty.call(message, "dar"))
                    $root.dmcn.identity.DomainAuthorityRecord.encode(message.dar, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified CredentialBundle message, length delimited. Does not implicitly {@link dmcn.identity.CredentialBundle.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.identity.CredentialBundle
             * @static
             * @param {dmcn.identity.ICredentialBundle} message CredentialBundle message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CredentialBundle.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a CredentialBundle message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.identity.CredentialBundle
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.identity.CredentialBundle} CredentialBundle
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CredentialBundle.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.identity.CredentialBundle();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.credential = $root.dmcn.identity.Credential.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 2: {
                            message.dar = $root.dmcn.identity.DomainAuthorityRecord.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a CredentialBundle message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.identity.CredentialBundle
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.identity.CredentialBundle} CredentialBundle
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CredentialBundle.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CredentialBundle message.
             * @function verify
             * @memberof dmcn.identity.CredentialBundle
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CredentialBundle.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.credential != null && Object.hasOwnProperty.call(message, "credential")) {
                    let error = $root.dmcn.identity.Credential.verify(message.credential, long + 1);
                    if (error)
                        return "credential." + error;
                }
                if (message.dar != null && Object.hasOwnProperty.call(message, "dar")) {
                    let error = $root.dmcn.identity.DomainAuthorityRecord.verify(message.dar, long + 1);
                    if (error)
                        return "dar." + error;
                }
                return null;
            };

            /**
             * Creates a CredentialBundle message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.identity.CredentialBundle
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.identity.CredentialBundle} CredentialBundle
             */
            CredentialBundle.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.identity.CredentialBundle)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.identity.CredentialBundle: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.identity.CredentialBundle();
                if (object.credential != null) {
                    if (!$util.isObject(object.credential))
                        throw TypeError(".dmcn.identity.CredentialBundle.credential: object expected");
                    message.credential = $root.dmcn.identity.Credential.fromObject(object.credential, long + 1);
                }
                if (object.dar != null) {
                    if (!$util.isObject(object.dar))
                        throw TypeError(".dmcn.identity.CredentialBundle.dar: object expected");
                    message.dar = $root.dmcn.identity.DomainAuthorityRecord.fromObject(object.dar, long + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a CredentialBundle message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.identity.CredentialBundle
             * @static
             * @param {dmcn.identity.CredentialBundle} message CredentialBundle
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CredentialBundle.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.credential = null;
                    object.dar = null;
                }
                if (message.credential != null && Object.hasOwnProperty.call(message, "credential"))
                    object.credential = $root.dmcn.identity.Credential.toObject(message.credential, options, q + 1);
                if (message.dar != null && Object.hasOwnProperty.call(message, "dar"))
                    object.dar = $root.dmcn.identity.DomainAuthorityRecord.toObject(message.dar, options, q + 1);
                return object;
            };

            /**
             * Converts this CredentialBundle to JSON.
             * @function toJSON
             * @memberof dmcn.identity.CredentialBundle
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CredentialBundle.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for CredentialBundle
             * @function getTypeUrl
             * @memberof dmcn.identity.CredentialBundle
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            CredentialBundle.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.identity.CredentialBundle";
            };

            return CredentialBundle;
        })();

        identity.JoinRequest = (function() {

            /**
             * Properties of a JoinRequest.
             * @memberof dmcn.identity
             * @interface IJoinRequest
             * @property {dmcn.identity.ICredential|null} [credential] JoinRequest credential
             * @property {dmcn.identity.IDomainAuthorityRecord|null} [dar] JoinRequest dar
             * @property {Array.<dmcn.identity.ICredentialBundle>|null} [bundles] JoinRequest bundles
             */

            /**
             * Constructs a new JoinRequest.
             * @memberof dmcn.identity
             * @classdesc Represents a JoinRequest.
             * @implements IJoinRequest
             * @constructor
             * @param {dmcn.identity.IJoinRequest=} [properties] Properties to set
             */
            function JoinRequest(properties) {
                this.bundles = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * JoinRequest credential.
             * @member {dmcn.identity.ICredential|null|undefined} credential
             * @memberof dmcn.identity.JoinRequest
             * @instance
             */
            JoinRequest.prototype.credential = null;

            /**
             * JoinRequest dar.
             * @member {dmcn.identity.IDomainAuthorityRecord|null|undefined} dar
             * @memberof dmcn.identity.JoinRequest
             * @instance
             */
            JoinRequest.prototype.dar = null;

            /**
             * JoinRequest bundles.
             * @member {Array.<dmcn.identity.ICredentialBundle>} bundles
             * @memberof dmcn.identity.JoinRequest
             * @instance
             */
            JoinRequest.prototype.bundles = $util.emptyArray;

            /**
             * Creates a new JoinRequest instance using the specified properties.
             * @function create
             * @memberof dmcn.identity.JoinRequest
             * @static
             * @param {dmcn.identity.IJoinRequest=} [properties] Properties to set
             * @returns {dmcn.identity.JoinRequest} JoinRequest instance
             */
            JoinRequest.create = function create(properties) {
                return new JoinRequest(properties);
            };

            /**
             * Encodes the specified JoinRequest message. Does not implicitly {@link dmcn.identity.JoinRequest.verify|verify} messages.
             * @function encode
             * @memberof dmcn.identity.JoinRequest
             * @static
             * @param {dmcn.identity.IJoinRequest} message JoinRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            JoinRequest.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.credential != null && Object.hasOwnProperty.call(message, "credential"))
                    $root.dmcn.identity.Credential.encode(message.credential, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.dar != null && Object.hasOwnProperty.call(message, "dar"))
                    $root.dmcn.identity.DomainAuthorityRecord.encode(message.dar, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.bundles != null && message.bundles.length)
                    for (let i = 0; i < message.bundles.length; ++i)
                        $root.dmcn.identity.CredentialBundle.encode(message.bundles[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified JoinRequest message, length delimited. Does not implicitly {@link dmcn.identity.JoinRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.identity.JoinRequest
             * @static
             * @param {dmcn.identity.IJoinRequest} message JoinRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            JoinRequest.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a JoinRequest message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.identity.JoinRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.identity.JoinRequest} JoinRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            JoinRequest.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.identity.JoinRequest();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.credential = $root.dmcn.identity.Credential.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 2: {
                            message.dar = $root.dmcn.identity.DomainAuthorityRecord.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 3: {
                            if (!(message.bundles && message.bundles.length))
                                message.bundles = [];
                            message.bundles.push($root.dmcn.identity.CredentialBundle.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a JoinRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.identity.JoinRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.identity.JoinRequest} JoinRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            JoinRequest.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a JoinRequest message.
             * @function verify
             * @memberof dmcn.identity.JoinRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            JoinRequest.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.credential != null && Object.hasOwnProperty.call(message, "credential")) {
                    let error = $root.dmcn.identity.Credential.verify(message.credential, long + 1);
                    if (error)
                        return "credential." + error;
                }
                if (message.dar != null && Object.hasOwnProperty.call(message, "dar")) {
                    let error = $root.dmcn.identity.DomainAuthorityRecord.verify(message.dar, long + 1);
                    if (error)
                        return "dar." + error;
                }
                if (message.bundles != null && Object.hasOwnProperty.call(message, "bundles")) {
                    if (!Array.isArray(message.bundles))
                        return "bundles: array expected";
                    for (let i = 0; i < message.bundles.length; ++i) {
                        let error = $root.dmcn.identity.CredentialBundle.verify(message.bundles[i], long + 1);
                        if (error)
                            return "bundles." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a JoinRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.identity.JoinRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.identity.JoinRequest} JoinRequest
             */
            JoinRequest.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.identity.JoinRequest)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.identity.JoinRequest: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.identity.JoinRequest();
                if (object.credential != null) {
                    if (!$util.isObject(object.credential))
                        throw TypeError(".dmcn.identity.JoinRequest.credential: object expected");
                    message.credential = $root.dmcn.identity.Credential.fromObject(object.credential, long + 1);
                }
                if (object.dar != null) {
                    if (!$util.isObject(object.dar))
                        throw TypeError(".dmcn.identity.JoinRequest.dar: object expected");
                    message.dar = $root.dmcn.identity.DomainAuthorityRecord.fromObject(object.dar, long + 1);
                }
                if (object.bundles) {
                    if (!Array.isArray(object.bundles))
                        throw TypeError(".dmcn.identity.JoinRequest.bundles: array expected");
                    message.bundles = [];
                    for (let i = 0; i < object.bundles.length; ++i) {
                        if (!$util.isObject(object.bundles[i]))
                            throw TypeError(".dmcn.identity.JoinRequest.bundles: object expected");
                        message.bundles[i] = $root.dmcn.identity.CredentialBundle.fromObject(object.bundles[i], long + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a JoinRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.identity.JoinRequest
             * @static
             * @param {dmcn.identity.JoinRequest} message JoinRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            JoinRequest.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.bundles = [];
                if (options.defaults) {
                    object.credential = null;
                    object.dar = null;
                }
                if (message.credential != null && Object.hasOwnProperty.call(message, "credential"))
                    object.credential = $root.dmcn.identity.Credential.toObject(message.credential, options, q + 1);
                if (message.dar != null && Object.hasOwnProperty.call(message, "dar"))
                    object.dar = $root.dmcn.identity.DomainAuthorityRecord.toObject(message.dar, options, q + 1);
                if (message.bundles && message.bundles.length) {
                    object.bundles = [];
                    for (let j = 0; j < message.bundles.length; ++j)
                        object.bundles[j] = $root.dmcn.identity.CredentialBundle.toObject(message.bundles[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this JoinRequest to JSON.
             * @function toJSON
             * @memberof dmcn.identity.JoinRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            JoinRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for JoinRequest
             * @function getTypeUrl
             * @memberof dmcn.identity.JoinRequest
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            JoinRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.identity.JoinRequest";
            };

            return JoinRequest;
        })();

        identity.JoinResponse = (function() {

            /**
             * Properties of a JoinResponse.
             * @memberof dmcn.identity
             * @interface IJoinResponse
             * @property {boolean|null} [accepted] JoinResponse accepted
             * @property {string|null} [error] JoinResponse error
             * @property {dmcn.identity.ICredential|null} [credential] JoinResponse credential
             * @property {dmcn.identity.IDomainAuthorityRecord|null} [dar] JoinResponse dar
             * @property {Array.<dmcn.identity.ICredentialBundle>|null} [bundles] JoinResponse bundles
             */

            /**
             * Constructs a new JoinResponse.
             * @memberof dmcn.identity
             * @classdesc Represents a JoinResponse.
             * @implements IJoinResponse
             * @constructor
             * @param {dmcn.identity.IJoinResponse=} [properties] Properties to set
             */
            function JoinResponse(properties) {
                this.bundles = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * JoinResponse accepted.
             * @member {boolean} accepted
             * @memberof dmcn.identity.JoinResponse
             * @instance
             */
            JoinResponse.prototype.accepted = false;

            /**
             * JoinResponse error.
             * @member {string} error
             * @memberof dmcn.identity.JoinResponse
             * @instance
             */
            JoinResponse.prototype.error = "";

            /**
             * JoinResponse credential.
             * @member {dmcn.identity.ICredential|null|undefined} credential
             * @memberof dmcn.identity.JoinResponse
             * @instance
             */
            JoinResponse.prototype.credential = null;

            /**
             * JoinResponse dar.
             * @member {dmcn.identity.IDomainAuthorityRecord|null|undefined} dar
             * @memberof dmcn.identity.JoinResponse
             * @instance
             */
            JoinResponse.prototype.dar = null;

            /**
             * JoinResponse bundles.
             * @member {Array.<dmcn.identity.ICredentialBundle>} bundles
             * @memberof dmcn.identity.JoinResponse
             * @instance
             */
            JoinResponse.prototype.bundles = $util.emptyArray;

            /**
             * Creates a new JoinResponse instance using the specified properties.
             * @function create
             * @memberof dmcn.identity.JoinResponse
             * @static
             * @param {dmcn.identity.IJoinResponse=} [properties] Properties to set
             * @returns {dmcn.identity.JoinResponse} JoinResponse instance
             */
            JoinResponse.create = function create(properties) {
                return new JoinResponse(properties);
            };

            /**
             * Encodes the specified JoinResponse message. Does not implicitly {@link dmcn.identity.JoinResponse.verify|verify} messages.
             * @function encode
             * @memberof dmcn.identity.JoinResponse
             * @static
             * @param {dmcn.identity.IJoinResponse} message JoinResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            JoinResponse.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.accepted != null && Object.hasOwnProperty.call(message, "accepted"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.accepted);
                if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.error);
                if (message.credential != null && Object.hasOwnProperty.call(message, "credential"))
                    $root.dmcn.identity.Credential.encode(message.credential, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.dar != null && Object.hasOwnProperty.call(message, "dar"))
                    $root.dmcn.identity.DomainAuthorityRecord.encode(message.dar, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.bundles != null && message.bundles.length)
                    for (let i = 0; i < message.bundles.length; ++i)
                        $root.dmcn.identity.CredentialBundle.encode(message.bundles[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified JoinResponse message, length delimited. Does not implicitly {@link dmcn.identity.JoinResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.identity.JoinResponse
             * @static
             * @param {dmcn.identity.IJoinResponse} message JoinResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            JoinResponse.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a JoinResponse message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.identity.JoinResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.identity.JoinResponse} JoinResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            JoinResponse.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.identity.JoinResponse();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.accepted = reader.bool();
                            break;
                        }
                    case 2: {
                            message.error = reader.string();
                            break;
                        }
                    case 3: {
                            message.credential = $root.dmcn.identity.Credential.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 4: {
                            message.dar = $root.dmcn.identity.DomainAuthorityRecord.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 5: {
                            if (!(message.bundles && message.bundles.length))
                                message.bundles = [];
                            message.bundles.push($root.dmcn.identity.CredentialBundle.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a JoinResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.identity.JoinResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.identity.JoinResponse} JoinResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            JoinResponse.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a JoinResponse message.
             * @function verify
             * @memberof dmcn.identity.JoinResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            JoinResponse.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.accepted != null && Object.hasOwnProperty.call(message, "accepted"))
                    if (typeof message.accepted !== "boolean")
                        return "accepted: boolean expected";
                if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                    if (!$util.isString(message.error))
                        return "error: string expected";
                if (message.credential != null && Object.hasOwnProperty.call(message, "credential")) {
                    let error = $root.dmcn.identity.Credential.verify(message.credential, long + 1);
                    if (error)
                        return "credential." + error;
                }
                if (message.dar != null && Object.hasOwnProperty.call(message, "dar")) {
                    let error = $root.dmcn.identity.DomainAuthorityRecord.verify(message.dar, long + 1);
                    if (error)
                        return "dar." + error;
                }
                if (message.bundles != null && Object.hasOwnProperty.call(message, "bundles")) {
                    if (!Array.isArray(message.bundles))
                        return "bundles: array expected";
                    for (let i = 0; i < message.bundles.length; ++i) {
                        let error = $root.dmcn.identity.CredentialBundle.verify(message.bundles[i], long + 1);
                        if (error)
                            return "bundles." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a JoinResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.identity.JoinResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.identity.JoinResponse} JoinResponse
             */
            JoinResponse.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.identity.JoinResponse)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.identity.JoinResponse: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.identity.JoinResponse();
                if (object.accepted != null)
                    message.accepted = Boolean(object.accepted);
                if (object.error != null)
                    message.error = String(object.error);
                if (object.credential != null) {
                    if (!$util.isObject(object.credential))
                        throw TypeError(".dmcn.identity.JoinResponse.credential: object expected");
                    message.credential = $root.dmcn.identity.Credential.fromObject(object.credential, long + 1);
                }
                if (object.dar != null) {
                    if (!$util.isObject(object.dar))
                        throw TypeError(".dmcn.identity.JoinResponse.dar: object expected");
                    message.dar = $root.dmcn.identity.DomainAuthorityRecord.fromObject(object.dar, long + 1);
                }
                if (object.bundles) {
                    if (!Array.isArray(object.bundles))
                        throw TypeError(".dmcn.identity.JoinResponse.bundles: array expected");
                    message.bundles = [];
                    for (let i = 0; i < object.bundles.length; ++i) {
                        if (!$util.isObject(object.bundles[i]))
                            throw TypeError(".dmcn.identity.JoinResponse.bundles: object expected");
                        message.bundles[i] = $root.dmcn.identity.CredentialBundle.fromObject(object.bundles[i], long + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a JoinResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.identity.JoinResponse
             * @static
             * @param {dmcn.identity.JoinResponse} message JoinResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            JoinResponse.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.bundles = [];
                if (options.defaults) {
                    object.accepted = false;
                    object.error = "";
                    object.credential = null;
                    object.dar = null;
                }
                if (message.accepted != null && Object.hasOwnProperty.call(message, "accepted"))
                    object.accepted = message.accepted;
                if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                    object.error = message.error;
                if (message.credential != null && Object.hasOwnProperty.call(message, "credential"))
                    object.credential = $root.dmcn.identity.Credential.toObject(message.credential, options, q + 1);
                if (message.dar != null && Object.hasOwnProperty.call(message, "dar"))
                    object.dar = $root.dmcn.identity.DomainAuthorityRecord.toObject(message.dar, options, q + 1);
                if (message.bundles && message.bundles.length) {
                    object.bundles = [];
                    for (let j = 0; j < message.bundles.length; ++j)
                        object.bundles[j] = $root.dmcn.identity.CredentialBundle.toObject(message.bundles[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this JoinResponse to JSON.
             * @function toJSON
             * @memberof dmcn.identity.JoinResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            JoinResponse.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for JoinResponse
             * @function getTypeUrl
             * @memberof dmcn.identity.JoinResponse
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            JoinResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.identity.JoinResponse";
            };

            return JoinResponse;
        })();

        identity.RemovedBinding = (function() {

            /**
             * Properties of a RemovedBinding.
             * @memberof dmcn.identity
             * @interface IRemovedBinding
             * @property {Uint8Array|null} [ed25519PublicKey] RemovedBinding ed25519PublicKey
             * @property {number|Long|null} [removedAt] RemovedBinding removedAt
             */

            /**
             * Constructs a new RemovedBinding.
             * @memberof dmcn.identity
             * @classdesc Represents a RemovedBinding.
             * @implements IRemovedBinding
             * @constructor
             * @param {dmcn.identity.IRemovedBinding=} [properties] Properties to set
             */
            function RemovedBinding(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * RemovedBinding ed25519PublicKey.
             * @member {Uint8Array} ed25519PublicKey
             * @memberof dmcn.identity.RemovedBinding
             * @instance
             */
            RemovedBinding.prototype.ed25519PublicKey = $util.newBuffer([]);

            /**
             * RemovedBinding removedAt.
             * @member {number|Long} removedAt
             * @memberof dmcn.identity.RemovedBinding
             * @instance
             */
            RemovedBinding.prototype.removedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * Creates a new RemovedBinding instance using the specified properties.
             * @function create
             * @memberof dmcn.identity.RemovedBinding
             * @static
             * @param {dmcn.identity.IRemovedBinding=} [properties] Properties to set
             * @returns {dmcn.identity.RemovedBinding} RemovedBinding instance
             */
            RemovedBinding.create = function create(properties) {
                return new RemovedBinding(properties);
            };

            /**
             * Encodes the specified RemovedBinding message. Does not implicitly {@link dmcn.identity.RemovedBinding.verify|verify} messages.
             * @function encode
             * @memberof dmcn.identity.RemovedBinding
             * @static
             * @param {dmcn.identity.IRemovedBinding} message RemovedBinding message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RemovedBinding.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.ed25519PublicKey != null && Object.hasOwnProperty.call(message, "ed25519PublicKey"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.ed25519PublicKey);
                if (message.removedAt != null && Object.hasOwnProperty.call(message, "removedAt"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.removedAt);
                return writer;
            };

            /**
             * Encodes the specified RemovedBinding message, length delimited. Does not implicitly {@link dmcn.identity.RemovedBinding.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.identity.RemovedBinding
             * @static
             * @param {dmcn.identity.IRemovedBinding} message RemovedBinding message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RemovedBinding.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a RemovedBinding message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.identity.RemovedBinding
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.identity.RemovedBinding} RemovedBinding
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RemovedBinding.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.identity.RemovedBinding();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.ed25519PublicKey = reader.bytes();
                            break;
                        }
                    case 2: {
                            message.removedAt = reader.int64();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a RemovedBinding message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.identity.RemovedBinding
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.identity.RemovedBinding} RemovedBinding
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RemovedBinding.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a RemovedBinding message.
             * @function verify
             * @memberof dmcn.identity.RemovedBinding
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RemovedBinding.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.ed25519PublicKey != null && Object.hasOwnProperty.call(message, "ed25519PublicKey"))
                    if (!(message.ed25519PublicKey && typeof message.ed25519PublicKey.length === "number" || $util.isString(message.ed25519PublicKey)))
                        return "ed25519PublicKey: buffer expected";
                if (message.removedAt != null && Object.hasOwnProperty.call(message, "removedAt"))
                    if (!$util.isInteger(message.removedAt) && !(message.removedAt && $util.isInteger(message.removedAt.low) && $util.isInteger(message.removedAt.high)))
                        return "removedAt: integer|Long expected";
                return null;
            };

            /**
             * Creates a RemovedBinding message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.identity.RemovedBinding
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.identity.RemovedBinding} RemovedBinding
             */
            RemovedBinding.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.identity.RemovedBinding)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.identity.RemovedBinding: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.identity.RemovedBinding();
                if (object.ed25519PublicKey != null)
                    if (typeof object.ed25519PublicKey === "string")
                        $util.base64.decode(object.ed25519PublicKey, message.ed25519PublicKey = $util.newBuffer($util.base64.length(object.ed25519PublicKey)), 0);
                    else if (object.ed25519PublicKey.length >= 0)
                        message.ed25519PublicKey = object.ed25519PublicKey;
                if (object.removedAt != null)
                    if ($util.Long)
                        message.removedAt = $util.Long.fromValue(object.removedAt, false);
                    else if (typeof object.removedAt === "string")
                        message.removedAt = parseInt(object.removedAt, 10);
                    else if (typeof object.removedAt === "number")
                        message.removedAt = object.removedAt;
                    else if (typeof object.removedAt === "object")
                        message.removedAt = new $util.LongBits(object.removedAt.low >>> 0, object.removedAt.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a RemovedBinding message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.identity.RemovedBinding
             * @static
             * @param {dmcn.identity.RemovedBinding} message RemovedBinding
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RemovedBinding.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    if (options.bytes === String)
                        object.ed25519PublicKey = "";
                    else {
                        object.ed25519PublicKey = [];
                        if (options.bytes !== Array)
                            object.ed25519PublicKey = $util.newBuffer(object.ed25519PublicKey);
                    }
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.removedAt = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.removedAt = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                }
                if (message.ed25519PublicKey != null && Object.hasOwnProperty.call(message, "ed25519PublicKey"))
                    object.ed25519PublicKey = options.bytes === String ? $util.base64.encode(message.ed25519PublicKey, 0, message.ed25519PublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.ed25519PublicKey) : message.ed25519PublicKey;
                if (message.removedAt != null && Object.hasOwnProperty.call(message, "removedAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.removedAt = typeof message.removedAt === "number" ? BigInt(message.removedAt) : $util.Long.fromBits(message.removedAt.low >>> 0, message.removedAt.high >>> 0, false).toBigInt();
                    else if (typeof message.removedAt === "number")
                        object.removedAt = options.longs === String ? String(message.removedAt) : message.removedAt;
                    else
                        object.removedAt = options.longs === String ? $util.Long.prototype.toString.call(message.removedAt) : options.longs === Number ? new $util.LongBits(message.removedAt.low >>> 0, message.removedAt.high >>> 0).toNumber() : message.removedAt;
                return object;
            };

            /**
             * Converts this RemovedBinding to JSON.
             * @function toJSON
             * @memberof dmcn.identity.RemovedBinding
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RemovedBinding.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for RemovedBinding
             * @function getTypeUrl
             * @memberof dmcn.identity.RemovedBinding
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            RemovedBinding.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.identity.RemovedBinding";
            };

            return RemovedBinding;
        })();

        identity.AddressRemovalRecord = (function() {

            /**
             * Properties of an AddressRemovalRecord.
             * @memberof dmcn.identity
             * @interface IAddressRemovalRecord
             * @property {number|null} [version] AddressRemovalRecord version
             * @property {string|null} [domain] AddressRemovalRecord domain
             * @property {string|null} [address] AddressRemovalRecord address
             * @property {Array.<dmcn.identity.IRemovedBinding>|null} [removedBindings] AddressRemovalRecord removedBindings
             * @property {number|Long|null} [revision] AddressRemovalRecord revision
             * @property {number|Long|null} [createdAt] AddressRemovalRecord createdAt
             * @property {Uint8Array|null} [selfSignature] AddressRemovalRecord selfSignature
             */

            /**
             * Constructs a new AddressRemovalRecord.
             * @memberof dmcn.identity
             * @classdesc Represents an AddressRemovalRecord.
             * @implements IAddressRemovalRecord
             * @constructor
             * @param {dmcn.identity.IAddressRemovalRecord=} [properties] Properties to set
             */
            function AddressRemovalRecord(properties) {
                this.removedBindings = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AddressRemovalRecord version.
             * @member {number} version
             * @memberof dmcn.identity.AddressRemovalRecord
             * @instance
             */
            AddressRemovalRecord.prototype.version = 0;

            /**
             * AddressRemovalRecord domain.
             * @member {string} domain
             * @memberof dmcn.identity.AddressRemovalRecord
             * @instance
             */
            AddressRemovalRecord.prototype.domain = "";

            /**
             * AddressRemovalRecord address.
             * @member {string} address
             * @memberof dmcn.identity.AddressRemovalRecord
             * @instance
             */
            AddressRemovalRecord.prototype.address = "";

            /**
             * AddressRemovalRecord removedBindings.
             * @member {Array.<dmcn.identity.IRemovedBinding>} removedBindings
             * @memberof dmcn.identity.AddressRemovalRecord
             * @instance
             */
            AddressRemovalRecord.prototype.removedBindings = $util.emptyArray;

            /**
             * AddressRemovalRecord revision.
             * @member {number|Long} revision
             * @memberof dmcn.identity.AddressRemovalRecord
             * @instance
             */
            AddressRemovalRecord.prototype.revision = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * AddressRemovalRecord createdAt.
             * @member {number|Long} createdAt
             * @memberof dmcn.identity.AddressRemovalRecord
             * @instance
             */
            AddressRemovalRecord.prototype.createdAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * AddressRemovalRecord selfSignature.
             * @member {Uint8Array} selfSignature
             * @memberof dmcn.identity.AddressRemovalRecord
             * @instance
             */
            AddressRemovalRecord.prototype.selfSignature = $util.newBuffer([]);

            /**
             * Creates a new AddressRemovalRecord instance using the specified properties.
             * @function create
             * @memberof dmcn.identity.AddressRemovalRecord
             * @static
             * @param {dmcn.identity.IAddressRemovalRecord=} [properties] Properties to set
             * @returns {dmcn.identity.AddressRemovalRecord} AddressRemovalRecord instance
             */
            AddressRemovalRecord.create = function create(properties) {
                return new AddressRemovalRecord(properties);
            };

            /**
             * Encodes the specified AddressRemovalRecord message. Does not implicitly {@link dmcn.identity.AddressRemovalRecord.verify|verify} messages.
             * @function encode
             * @memberof dmcn.identity.AddressRemovalRecord
             * @static
             * @param {dmcn.identity.IAddressRemovalRecord} message AddressRemovalRecord message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AddressRemovalRecord.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.version);
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.domain);
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.address);
                if (message.removedBindings != null && message.removedBindings.length)
                    for (let i = 0; i < message.removedBindings.length; ++i)
                        $root.dmcn.identity.RemovedBinding.encode(message.removedBindings[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                    writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.revision);
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    writer.uint32(/* id 6, wireType 0 =*/48).int64(message.createdAt);
                if (message.selfSignature != null && Object.hasOwnProperty.call(message, "selfSignature"))
                    writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.selfSignature);
                return writer;
            };

            /**
             * Encodes the specified AddressRemovalRecord message, length delimited. Does not implicitly {@link dmcn.identity.AddressRemovalRecord.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.identity.AddressRemovalRecord
             * @static
             * @param {dmcn.identity.IAddressRemovalRecord} message AddressRemovalRecord message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AddressRemovalRecord.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes an AddressRemovalRecord message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.identity.AddressRemovalRecord
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.identity.AddressRemovalRecord} AddressRemovalRecord
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AddressRemovalRecord.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.identity.AddressRemovalRecord();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.version = reader.uint32();
                            break;
                        }
                    case 2: {
                            message.domain = reader.string();
                            break;
                        }
                    case 3: {
                            message.address = reader.string();
                            break;
                        }
                    case 4: {
                            if (!(message.removedBindings && message.removedBindings.length))
                                message.removedBindings = [];
                            message.removedBindings.push($root.dmcn.identity.RemovedBinding.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 5: {
                            message.revision = reader.uint64();
                            break;
                        }
                    case 6: {
                            message.createdAt = reader.int64();
                            break;
                        }
                    case 7: {
                            message.selfSignature = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an AddressRemovalRecord message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.identity.AddressRemovalRecord
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.identity.AddressRemovalRecord} AddressRemovalRecord
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AddressRemovalRecord.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AddressRemovalRecord message.
             * @function verify
             * @memberof dmcn.identity.AddressRemovalRecord
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AddressRemovalRecord.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    if (!$util.isInteger(message.version))
                        return "version: integer expected";
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    if (!$util.isString(message.domain))
                        return "domain: string expected";
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    if (!$util.isString(message.address))
                        return "address: string expected";
                if (message.removedBindings != null && Object.hasOwnProperty.call(message, "removedBindings")) {
                    if (!Array.isArray(message.removedBindings))
                        return "removedBindings: array expected";
                    for (let i = 0; i < message.removedBindings.length; ++i) {
                        let error = $root.dmcn.identity.RemovedBinding.verify(message.removedBindings[i], long + 1);
                        if (error)
                            return "removedBindings." + error;
                    }
                }
                if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                    if (!$util.isInteger(message.revision) && !(message.revision && $util.isInteger(message.revision.low) && $util.isInteger(message.revision.high)))
                        return "revision: integer|Long expected";
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
                        return "createdAt: integer|Long expected";
                if (message.selfSignature != null && Object.hasOwnProperty.call(message, "selfSignature"))
                    if (!(message.selfSignature && typeof message.selfSignature.length === "number" || $util.isString(message.selfSignature)))
                        return "selfSignature: buffer expected";
                return null;
            };

            /**
             * Creates an AddressRemovalRecord message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.identity.AddressRemovalRecord
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.identity.AddressRemovalRecord} AddressRemovalRecord
             */
            AddressRemovalRecord.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.identity.AddressRemovalRecord)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.identity.AddressRemovalRecord: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.identity.AddressRemovalRecord();
                if (object.version != null)
                    message.version = object.version >>> 0;
                if (object.domain != null)
                    message.domain = String(object.domain);
                if (object.address != null)
                    message.address = String(object.address);
                if (object.removedBindings) {
                    if (!Array.isArray(object.removedBindings))
                        throw TypeError(".dmcn.identity.AddressRemovalRecord.removedBindings: array expected");
                    message.removedBindings = [];
                    for (let i = 0; i < object.removedBindings.length; ++i) {
                        if (!$util.isObject(object.removedBindings[i]))
                            throw TypeError(".dmcn.identity.AddressRemovalRecord.removedBindings: object expected");
                        message.removedBindings[i] = $root.dmcn.identity.RemovedBinding.fromObject(object.removedBindings[i], long + 1);
                    }
                }
                if (object.revision != null)
                    if ($util.Long)
                        message.revision = $util.Long.fromValue(object.revision, true);
                    else if (typeof object.revision === "string")
                        message.revision = parseInt(object.revision, 10);
                    else if (typeof object.revision === "number")
                        message.revision = object.revision;
                    else if (typeof object.revision === "object")
                        message.revision = new $util.LongBits(object.revision.low >>> 0, object.revision.high >>> 0).toNumber(true);
                if (object.createdAt != null)
                    if ($util.Long)
                        message.createdAt = $util.Long.fromValue(object.createdAt, false);
                    else if (typeof object.createdAt === "string")
                        message.createdAt = parseInt(object.createdAt, 10);
                    else if (typeof object.createdAt === "number")
                        message.createdAt = object.createdAt;
                    else if (typeof object.createdAt === "object")
                        message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
                if (object.selfSignature != null)
                    if (typeof object.selfSignature === "string")
                        $util.base64.decode(object.selfSignature, message.selfSignature = $util.newBuffer($util.base64.length(object.selfSignature)), 0);
                    else if (object.selfSignature.length >= 0)
                        message.selfSignature = object.selfSignature;
                return message;
            };

            /**
             * Creates a plain object from an AddressRemovalRecord message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.identity.AddressRemovalRecord
             * @static
             * @param {dmcn.identity.AddressRemovalRecord} message AddressRemovalRecord
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AddressRemovalRecord.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.removedBindings = [];
                if (options.defaults) {
                    object.version = 0;
                    object.domain = "";
                    object.address = "";
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.revision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.revision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.createdAt = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.createdAt = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if (options.bytes === String)
                        object.selfSignature = "";
                    else {
                        object.selfSignature = [];
                        if (options.bytes !== Array)
                            object.selfSignature = $util.newBuffer(object.selfSignature);
                    }
                }
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    object.version = message.version;
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    object.domain = message.domain;
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    object.address = message.address;
                if (message.removedBindings && message.removedBindings.length) {
                    object.removedBindings = [];
                    for (let j = 0; j < message.removedBindings.length; ++j)
                        object.removedBindings[j] = $root.dmcn.identity.RemovedBinding.toObject(message.removedBindings[j], options, q + 1);
                }
                if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.revision = typeof message.revision === "number" ? BigInt(message.revision) : $util.Long.fromBits(message.revision.low >>> 0, message.revision.high >>> 0, true).toBigInt();
                    else if (typeof message.revision === "number")
                        object.revision = options.longs === String ? String(message.revision) : message.revision;
                    else
                        object.revision = options.longs === String ? $util.Long.prototype.toString.call(message.revision) : options.longs === Number ? new $util.LongBits(message.revision.low >>> 0, message.revision.high >>> 0).toNumber(true) : message.revision;
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.createdAt = typeof message.createdAt === "number" ? BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
                    else if (typeof message.createdAt === "number")
                        object.createdAt = options.longs === String ? String(message.createdAt) : message.createdAt;
                    else
                        object.createdAt = options.longs === String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
                if (message.selfSignature != null && Object.hasOwnProperty.call(message, "selfSignature"))
                    object.selfSignature = options.bytes === String ? $util.base64.encode(message.selfSignature, 0, message.selfSignature.length) : options.bytes === Array ? Array.prototype.slice.call(message.selfSignature) : message.selfSignature;
                return object;
            };

            /**
             * Converts this AddressRemovalRecord to JSON.
             * @function toJSON
             * @memberof dmcn.identity.AddressRemovalRecord
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AddressRemovalRecord.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AddressRemovalRecord
             * @function getTypeUrl
             * @memberof dmcn.identity.AddressRemovalRecord
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AddressRemovalRecord.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.identity.AddressRemovalRecord";
            };

            return AddressRemovalRecord;
        })();

        identity.CompromisedKey = (function() {

            /**
             * Properties of a CompromisedKey.
             * @memberof dmcn.identity
             * @interface ICompromisedKey
             * @property {Uint8Array|null} [ed25519PublicKey] CompromisedKey ed25519PublicKey
             * @property {number|Long|null} [compromisedAt] CompromisedKey compromisedAt
             * @property {number|Long|null} [retentionUntil] CompromisedKey retentionUntil
             */

            /**
             * Constructs a new CompromisedKey.
             * @memberof dmcn.identity
             * @classdesc Represents a CompromisedKey.
             * @implements ICompromisedKey
             * @constructor
             * @param {dmcn.identity.ICompromisedKey=} [properties] Properties to set
             */
            function CompromisedKey(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * CompromisedKey ed25519PublicKey.
             * @member {Uint8Array} ed25519PublicKey
             * @memberof dmcn.identity.CompromisedKey
             * @instance
             */
            CompromisedKey.prototype.ed25519PublicKey = $util.newBuffer([]);

            /**
             * CompromisedKey compromisedAt.
             * @member {number|Long} compromisedAt
             * @memberof dmcn.identity.CompromisedKey
             * @instance
             */
            CompromisedKey.prototype.compromisedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * CompromisedKey retentionUntil.
             * @member {number|Long} retentionUntil
             * @memberof dmcn.identity.CompromisedKey
             * @instance
             */
            CompromisedKey.prototype.retentionUntil = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * Creates a new CompromisedKey instance using the specified properties.
             * @function create
             * @memberof dmcn.identity.CompromisedKey
             * @static
             * @param {dmcn.identity.ICompromisedKey=} [properties] Properties to set
             * @returns {dmcn.identity.CompromisedKey} CompromisedKey instance
             */
            CompromisedKey.create = function create(properties) {
                return new CompromisedKey(properties);
            };

            /**
             * Encodes the specified CompromisedKey message. Does not implicitly {@link dmcn.identity.CompromisedKey.verify|verify} messages.
             * @function encode
             * @memberof dmcn.identity.CompromisedKey
             * @static
             * @param {dmcn.identity.ICompromisedKey} message CompromisedKey message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CompromisedKey.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.ed25519PublicKey != null && Object.hasOwnProperty.call(message, "ed25519PublicKey"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.ed25519PublicKey);
                if (message.compromisedAt != null && Object.hasOwnProperty.call(message, "compromisedAt"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.compromisedAt);
                if (message.retentionUntil != null && Object.hasOwnProperty.call(message, "retentionUntil"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.retentionUntil);
                return writer;
            };

            /**
             * Encodes the specified CompromisedKey message, length delimited. Does not implicitly {@link dmcn.identity.CompromisedKey.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.identity.CompromisedKey
             * @static
             * @param {dmcn.identity.ICompromisedKey} message CompromisedKey message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CompromisedKey.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a CompromisedKey message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.identity.CompromisedKey
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.identity.CompromisedKey} CompromisedKey
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CompromisedKey.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.identity.CompromisedKey();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.ed25519PublicKey = reader.bytes();
                            break;
                        }
                    case 2: {
                            message.compromisedAt = reader.int64();
                            break;
                        }
                    case 3: {
                            message.retentionUntil = reader.int64();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a CompromisedKey message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.identity.CompromisedKey
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.identity.CompromisedKey} CompromisedKey
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CompromisedKey.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CompromisedKey message.
             * @function verify
             * @memberof dmcn.identity.CompromisedKey
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CompromisedKey.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.ed25519PublicKey != null && Object.hasOwnProperty.call(message, "ed25519PublicKey"))
                    if (!(message.ed25519PublicKey && typeof message.ed25519PublicKey.length === "number" || $util.isString(message.ed25519PublicKey)))
                        return "ed25519PublicKey: buffer expected";
                if (message.compromisedAt != null && Object.hasOwnProperty.call(message, "compromisedAt"))
                    if (!$util.isInteger(message.compromisedAt) && !(message.compromisedAt && $util.isInteger(message.compromisedAt.low) && $util.isInteger(message.compromisedAt.high)))
                        return "compromisedAt: integer|Long expected";
                if (message.retentionUntil != null && Object.hasOwnProperty.call(message, "retentionUntil"))
                    if (!$util.isInteger(message.retentionUntil) && !(message.retentionUntil && $util.isInteger(message.retentionUntil.low) && $util.isInteger(message.retentionUntil.high)))
                        return "retentionUntil: integer|Long expected";
                return null;
            };

            /**
             * Creates a CompromisedKey message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.identity.CompromisedKey
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.identity.CompromisedKey} CompromisedKey
             */
            CompromisedKey.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.identity.CompromisedKey)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.identity.CompromisedKey: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.identity.CompromisedKey();
                if (object.ed25519PublicKey != null)
                    if (typeof object.ed25519PublicKey === "string")
                        $util.base64.decode(object.ed25519PublicKey, message.ed25519PublicKey = $util.newBuffer($util.base64.length(object.ed25519PublicKey)), 0);
                    else if (object.ed25519PublicKey.length >= 0)
                        message.ed25519PublicKey = object.ed25519PublicKey;
                if (object.compromisedAt != null)
                    if ($util.Long)
                        message.compromisedAt = $util.Long.fromValue(object.compromisedAt, false);
                    else if (typeof object.compromisedAt === "string")
                        message.compromisedAt = parseInt(object.compromisedAt, 10);
                    else if (typeof object.compromisedAt === "number")
                        message.compromisedAt = object.compromisedAt;
                    else if (typeof object.compromisedAt === "object")
                        message.compromisedAt = new $util.LongBits(object.compromisedAt.low >>> 0, object.compromisedAt.high >>> 0).toNumber();
                if (object.retentionUntil != null)
                    if ($util.Long)
                        message.retentionUntil = $util.Long.fromValue(object.retentionUntil, false);
                    else if (typeof object.retentionUntil === "string")
                        message.retentionUntil = parseInt(object.retentionUntil, 10);
                    else if (typeof object.retentionUntil === "number")
                        message.retentionUntil = object.retentionUntil;
                    else if (typeof object.retentionUntil === "object")
                        message.retentionUntil = new $util.LongBits(object.retentionUntil.low >>> 0, object.retentionUntil.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a CompromisedKey message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.identity.CompromisedKey
             * @static
             * @param {dmcn.identity.CompromisedKey} message CompromisedKey
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CompromisedKey.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    if (options.bytes === String)
                        object.ed25519PublicKey = "";
                    else {
                        object.ed25519PublicKey = [];
                        if (options.bytes !== Array)
                            object.ed25519PublicKey = $util.newBuffer(object.ed25519PublicKey);
                    }
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.compromisedAt = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.compromisedAt = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.retentionUntil = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.retentionUntil = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                }
                if (message.ed25519PublicKey != null && Object.hasOwnProperty.call(message, "ed25519PublicKey"))
                    object.ed25519PublicKey = options.bytes === String ? $util.base64.encode(message.ed25519PublicKey, 0, message.ed25519PublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.ed25519PublicKey) : message.ed25519PublicKey;
                if (message.compromisedAt != null && Object.hasOwnProperty.call(message, "compromisedAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.compromisedAt = typeof message.compromisedAt === "number" ? BigInt(message.compromisedAt) : $util.Long.fromBits(message.compromisedAt.low >>> 0, message.compromisedAt.high >>> 0, false).toBigInt();
                    else if (typeof message.compromisedAt === "number")
                        object.compromisedAt = options.longs === String ? String(message.compromisedAt) : message.compromisedAt;
                    else
                        object.compromisedAt = options.longs === String ? $util.Long.prototype.toString.call(message.compromisedAt) : options.longs === Number ? new $util.LongBits(message.compromisedAt.low >>> 0, message.compromisedAt.high >>> 0).toNumber() : message.compromisedAt;
                if (message.retentionUntil != null && Object.hasOwnProperty.call(message, "retentionUntil"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.retentionUntil = typeof message.retentionUntil === "number" ? BigInt(message.retentionUntil) : $util.Long.fromBits(message.retentionUntil.low >>> 0, message.retentionUntil.high >>> 0, false).toBigInt();
                    else if (typeof message.retentionUntil === "number")
                        object.retentionUntil = options.longs === String ? String(message.retentionUntil) : message.retentionUntil;
                    else
                        object.retentionUntil = options.longs === String ? $util.Long.prototype.toString.call(message.retentionUntil) : options.longs === Number ? new $util.LongBits(message.retentionUntil.low >>> 0, message.retentionUntil.high >>> 0).toNumber() : message.retentionUntil;
                return object;
            };

            /**
             * Converts this CompromisedKey to JSON.
             * @function toJSON
             * @memberof dmcn.identity.CompromisedKey
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CompromisedKey.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for CompromisedKey
             * @function getTypeUrl
             * @memberof dmcn.identity.CompromisedKey
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            CompromisedKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.identity.CompromisedKey";
            };

            return CompromisedKey;
        })();

        identity.RelayDescriptor = (function() {

            /**
             * Properties of a RelayDescriptor.
             * @memberof dmcn.identity
             * @interface IRelayDescriptor
             * @property {string|null} [peerId] RelayDescriptor peerId
             * @property {Uint8Array|null} [x25519PublicKey] RelayDescriptor x25519PublicKey
             * @property {Array.<string>|null} [multiaddrs] RelayDescriptor multiaddrs
             * @property {number|Long|null} [createdAt] RelayDescriptor createdAt
             * @property {number|Long|null} [revision] RelayDescriptor revision
             * @property {Uint8Array|null} [signature] RelayDescriptor signature
             * @property {string|null} [domain] RelayDescriptor domain
             * @property {Uint8Array|null} [domainCountersignature] RelayDescriptor domainCountersignature
             * @property {number|Long|null} [domainCountersignedAt] RelayDescriptor domainCountersignedAt
             * @property {Uint8Array|null} [domainCountersignerPubkey] RelayDescriptor domainCountersignerPubkey
             * @property {dmcn.identity.ICredential|null} [credential] RelayDescriptor credential
             */

            /**
             * Constructs a new RelayDescriptor.
             * @memberof dmcn.identity
             * @classdesc Represents a RelayDescriptor.
             * @implements IRelayDescriptor
             * @constructor
             * @param {dmcn.identity.IRelayDescriptor=} [properties] Properties to set
             */
            function RelayDescriptor(properties) {
                this.multiaddrs = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * RelayDescriptor peerId.
             * @member {string} peerId
             * @memberof dmcn.identity.RelayDescriptor
             * @instance
             */
            RelayDescriptor.prototype.peerId = "";

            /**
             * RelayDescriptor x25519PublicKey.
             * @member {Uint8Array} x25519PublicKey
             * @memberof dmcn.identity.RelayDescriptor
             * @instance
             */
            RelayDescriptor.prototype.x25519PublicKey = $util.newBuffer([]);

            /**
             * RelayDescriptor multiaddrs.
             * @member {Array.<string>} multiaddrs
             * @memberof dmcn.identity.RelayDescriptor
             * @instance
             */
            RelayDescriptor.prototype.multiaddrs = $util.emptyArray;

            /**
             * RelayDescriptor createdAt.
             * @member {number|Long} createdAt
             * @memberof dmcn.identity.RelayDescriptor
             * @instance
             */
            RelayDescriptor.prototype.createdAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * RelayDescriptor revision.
             * @member {number|Long} revision
             * @memberof dmcn.identity.RelayDescriptor
             * @instance
             */
            RelayDescriptor.prototype.revision = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * RelayDescriptor signature.
             * @member {Uint8Array} signature
             * @memberof dmcn.identity.RelayDescriptor
             * @instance
             */
            RelayDescriptor.prototype.signature = $util.newBuffer([]);

            /**
             * RelayDescriptor domain.
             * @member {string} domain
             * @memberof dmcn.identity.RelayDescriptor
             * @instance
             */
            RelayDescriptor.prototype.domain = "";

            /**
             * RelayDescriptor domainCountersignature.
             * @member {Uint8Array} domainCountersignature
             * @memberof dmcn.identity.RelayDescriptor
             * @instance
             */
            RelayDescriptor.prototype.domainCountersignature = $util.newBuffer([]);

            /**
             * RelayDescriptor domainCountersignedAt.
             * @member {number|Long} domainCountersignedAt
             * @memberof dmcn.identity.RelayDescriptor
             * @instance
             */
            RelayDescriptor.prototype.domainCountersignedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * RelayDescriptor domainCountersignerPubkey.
             * @member {Uint8Array} domainCountersignerPubkey
             * @memberof dmcn.identity.RelayDescriptor
             * @instance
             */
            RelayDescriptor.prototype.domainCountersignerPubkey = $util.newBuffer([]);

            /**
             * RelayDescriptor credential.
             * @member {dmcn.identity.ICredential|null|undefined} credential
             * @memberof dmcn.identity.RelayDescriptor
             * @instance
             */
            RelayDescriptor.prototype.credential = null;

            /**
             * Creates a new RelayDescriptor instance using the specified properties.
             * @function create
             * @memberof dmcn.identity.RelayDescriptor
             * @static
             * @param {dmcn.identity.IRelayDescriptor=} [properties] Properties to set
             * @returns {dmcn.identity.RelayDescriptor} RelayDescriptor instance
             */
            RelayDescriptor.create = function create(properties) {
                return new RelayDescriptor(properties);
            };

            /**
             * Encodes the specified RelayDescriptor message. Does not implicitly {@link dmcn.identity.RelayDescriptor.verify|verify} messages.
             * @function encode
             * @memberof dmcn.identity.RelayDescriptor
             * @static
             * @param {dmcn.identity.IRelayDescriptor} message RelayDescriptor message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RelayDescriptor.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.peerId != null && Object.hasOwnProperty.call(message, "peerId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.peerId);
                if (message.x25519PublicKey != null && Object.hasOwnProperty.call(message, "x25519PublicKey"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.x25519PublicKey);
                if (message.multiaddrs != null && message.multiaddrs.length)
                    for (let i = 0; i < message.multiaddrs.length; ++i)
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.multiaddrs[i]);
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int64(message.createdAt);
                if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                    writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.revision);
                if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                    writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.signature);
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.domain);
                if (message.domainCountersignature != null && Object.hasOwnProperty.call(message, "domainCountersignature"))
                    writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.domainCountersignature);
                if (message.domainCountersignedAt != null && Object.hasOwnProperty.call(message, "domainCountersignedAt"))
                    writer.uint32(/* id 9, wireType 0 =*/72).int64(message.domainCountersignedAt);
                if (message.domainCountersignerPubkey != null && Object.hasOwnProperty.call(message, "domainCountersignerPubkey"))
                    writer.uint32(/* id 10, wireType 2 =*/82).bytes(message.domainCountersignerPubkey);
                if (message.credential != null && Object.hasOwnProperty.call(message, "credential"))
                    $root.dmcn.identity.Credential.encode(message.credential, writer.uint32(/* id 11, wireType 2 =*/90).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified RelayDescriptor message, length delimited. Does not implicitly {@link dmcn.identity.RelayDescriptor.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.identity.RelayDescriptor
             * @static
             * @param {dmcn.identity.IRelayDescriptor} message RelayDescriptor message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RelayDescriptor.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a RelayDescriptor message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.identity.RelayDescriptor
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.identity.RelayDescriptor} RelayDescriptor
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RelayDescriptor.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.identity.RelayDescriptor();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.peerId = reader.string();
                            break;
                        }
                    case 2: {
                            message.x25519PublicKey = reader.bytes();
                            break;
                        }
                    case 3: {
                            if (!(message.multiaddrs && message.multiaddrs.length))
                                message.multiaddrs = [];
                            message.multiaddrs.push(reader.string());
                            break;
                        }
                    case 4: {
                            message.createdAt = reader.int64();
                            break;
                        }
                    case 5: {
                            message.revision = reader.uint64();
                            break;
                        }
                    case 6: {
                            message.signature = reader.bytes();
                            break;
                        }
                    case 7: {
                            message.domain = reader.string();
                            break;
                        }
                    case 8: {
                            message.domainCountersignature = reader.bytes();
                            break;
                        }
                    case 9: {
                            message.domainCountersignedAt = reader.int64();
                            break;
                        }
                    case 10: {
                            message.domainCountersignerPubkey = reader.bytes();
                            break;
                        }
                    case 11: {
                            message.credential = $root.dmcn.identity.Credential.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a RelayDescriptor message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.identity.RelayDescriptor
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.identity.RelayDescriptor} RelayDescriptor
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RelayDescriptor.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a RelayDescriptor message.
             * @function verify
             * @memberof dmcn.identity.RelayDescriptor
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RelayDescriptor.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.peerId != null && Object.hasOwnProperty.call(message, "peerId"))
                    if (!$util.isString(message.peerId))
                        return "peerId: string expected";
                if (message.x25519PublicKey != null && Object.hasOwnProperty.call(message, "x25519PublicKey"))
                    if (!(message.x25519PublicKey && typeof message.x25519PublicKey.length === "number" || $util.isString(message.x25519PublicKey)))
                        return "x25519PublicKey: buffer expected";
                if (message.multiaddrs != null && Object.hasOwnProperty.call(message, "multiaddrs")) {
                    if (!Array.isArray(message.multiaddrs))
                        return "multiaddrs: array expected";
                    for (let i = 0; i < message.multiaddrs.length; ++i)
                        if (!$util.isString(message.multiaddrs[i]))
                            return "multiaddrs: string[] expected";
                }
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
                        return "createdAt: integer|Long expected";
                if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                    if (!$util.isInteger(message.revision) && !(message.revision && $util.isInteger(message.revision.low) && $util.isInteger(message.revision.high)))
                        return "revision: integer|Long expected";
                if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                    if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                        return "signature: buffer expected";
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    if (!$util.isString(message.domain))
                        return "domain: string expected";
                if (message.domainCountersignature != null && Object.hasOwnProperty.call(message, "domainCountersignature"))
                    if (!(message.domainCountersignature && typeof message.domainCountersignature.length === "number" || $util.isString(message.domainCountersignature)))
                        return "domainCountersignature: buffer expected";
                if (message.domainCountersignedAt != null && Object.hasOwnProperty.call(message, "domainCountersignedAt"))
                    if (!$util.isInteger(message.domainCountersignedAt) && !(message.domainCountersignedAt && $util.isInteger(message.domainCountersignedAt.low) && $util.isInteger(message.domainCountersignedAt.high)))
                        return "domainCountersignedAt: integer|Long expected";
                if (message.domainCountersignerPubkey != null && Object.hasOwnProperty.call(message, "domainCountersignerPubkey"))
                    if (!(message.domainCountersignerPubkey && typeof message.domainCountersignerPubkey.length === "number" || $util.isString(message.domainCountersignerPubkey)))
                        return "domainCountersignerPubkey: buffer expected";
                if (message.credential != null && Object.hasOwnProperty.call(message, "credential")) {
                    let error = $root.dmcn.identity.Credential.verify(message.credential, long + 1);
                    if (error)
                        return "credential." + error;
                }
                return null;
            };

            /**
             * Creates a RelayDescriptor message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.identity.RelayDescriptor
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.identity.RelayDescriptor} RelayDescriptor
             */
            RelayDescriptor.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.identity.RelayDescriptor)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.identity.RelayDescriptor: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.identity.RelayDescriptor();
                if (object.peerId != null)
                    message.peerId = String(object.peerId);
                if (object.x25519PublicKey != null)
                    if (typeof object.x25519PublicKey === "string")
                        $util.base64.decode(object.x25519PublicKey, message.x25519PublicKey = $util.newBuffer($util.base64.length(object.x25519PublicKey)), 0);
                    else if (object.x25519PublicKey.length >= 0)
                        message.x25519PublicKey = object.x25519PublicKey;
                if (object.multiaddrs) {
                    if (!Array.isArray(object.multiaddrs))
                        throw TypeError(".dmcn.identity.RelayDescriptor.multiaddrs: array expected");
                    message.multiaddrs = [];
                    for (let i = 0; i < object.multiaddrs.length; ++i)
                        message.multiaddrs[i] = String(object.multiaddrs[i]);
                }
                if (object.createdAt != null)
                    if ($util.Long)
                        message.createdAt = $util.Long.fromValue(object.createdAt, false);
                    else if (typeof object.createdAt === "string")
                        message.createdAt = parseInt(object.createdAt, 10);
                    else if (typeof object.createdAt === "number")
                        message.createdAt = object.createdAt;
                    else if (typeof object.createdAt === "object")
                        message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
                if (object.revision != null)
                    if ($util.Long)
                        message.revision = $util.Long.fromValue(object.revision, true);
                    else if (typeof object.revision === "string")
                        message.revision = parseInt(object.revision, 10);
                    else if (typeof object.revision === "number")
                        message.revision = object.revision;
                    else if (typeof object.revision === "object")
                        message.revision = new $util.LongBits(object.revision.low >>> 0, object.revision.high >>> 0).toNumber(true);
                if (object.signature != null)
                    if (typeof object.signature === "string")
                        $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                    else if (object.signature.length >= 0)
                        message.signature = object.signature;
                if (object.domain != null)
                    message.domain = String(object.domain);
                if (object.domainCountersignature != null)
                    if (typeof object.domainCountersignature === "string")
                        $util.base64.decode(object.domainCountersignature, message.domainCountersignature = $util.newBuffer($util.base64.length(object.domainCountersignature)), 0);
                    else if (object.domainCountersignature.length >= 0)
                        message.domainCountersignature = object.domainCountersignature;
                if (object.domainCountersignedAt != null)
                    if ($util.Long)
                        message.domainCountersignedAt = $util.Long.fromValue(object.domainCountersignedAt, false);
                    else if (typeof object.domainCountersignedAt === "string")
                        message.domainCountersignedAt = parseInt(object.domainCountersignedAt, 10);
                    else if (typeof object.domainCountersignedAt === "number")
                        message.domainCountersignedAt = object.domainCountersignedAt;
                    else if (typeof object.domainCountersignedAt === "object")
                        message.domainCountersignedAt = new $util.LongBits(object.domainCountersignedAt.low >>> 0, object.domainCountersignedAt.high >>> 0).toNumber();
                if (object.domainCountersignerPubkey != null)
                    if (typeof object.domainCountersignerPubkey === "string")
                        $util.base64.decode(object.domainCountersignerPubkey, message.domainCountersignerPubkey = $util.newBuffer($util.base64.length(object.domainCountersignerPubkey)), 0);
                    else if (object.domainCountersignerPubkey.length >= 0)
                        message.domainCountersignerPubkey = object.domainCountersignerPubkey;
                if (object.credential != null) {
                    if (!$util.isObject(object.credential))
                        throw TypeError(".dmcn.identity.RelayDescriptor.credential: object expected");
                    message.credential = $root.dmcn.identity.Credential.fromObject(object.credential, long + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a RelayDescriptor message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.identity.RelayDescriptor
             * @static
             * @param {dmcn.identity.RelayDescriptor} message RelayDescriptor
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RelayDescriptor.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.multiaddrs = [];
                if (options.defaults) {
                    object.peerId = "";
                    if (options.bytes === String)
                        object.x25519PublicKey = "";
                    else {
                        object.x25519PublicKey = [];
                        if (options.bytes !== Array)
                            object.x25519PublicKey = $util.newBuffer(object.x25519PublicKey);
                    }
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.createdAt = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.createdAt = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.revision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.revision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if (options.bytes === String)
                        object.signature = "";
                    else {
                        object.signature = [];
                        if (options.bytes !== Array)
                            object.signature = $util.newBuffer(object.signature);
                    }
                    object.domain = "";
                    if (options.bytes === String)
                        object.domainCountersignature = "";
                    else {
                        object.domainCountersignature = [];
                        if (options.bytes !== Array)
                            object.domainCountersignature = $util.newBuffer(object.domainCountersignature);
                    }
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.domainCountersignedAt = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.domainCountersignedAt = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if (options.bytes === String)
                        object.domainCountersignerPubkey = "";
                    else {
                        object.domainCountersignerPubkey = [];
                        if (options.bytes !== Array)
                            object.domainCountersignerPubkey = $util.newBuffer(object.domainCountersignerPubkey);
                    }
                    object.credential = null;
                }
                if (message.peerId != null && Object.hasOwnProperty.call(message, "peerId"))
                    object.peerId = message.peerId;
                if (message.x25519PublicKey != null && Object.hasOwnProperty.call(message, "x25519PublicKey"))
                    object.x25519PublicKey = options.bytes === String ? $util.base64.encode(message.x25519PublicKey, 0, message.x25519PublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.x25519PublicKey) : message.x25519PublicKey;
                if (message.multiaddrs && message.multiaddrs.length) {
                    object.multiaddrs = [];
                    for (let j = 0; j < message.multiaddrs.length; ++j)
                        object.multiaddrs[j] = message.multiaddrs[j];
                }
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.createdAt = typeof message.createdAt === "number" ? BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
                    else if (typeof message.createdAt === "number")
                        object.createdAt = options.longs === String ? String(message.createdAt) : message.createdAt;
                    else
                        object.createdAt = options.longs === String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
                if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.revision = typeof message.revision === "number" ? BigInt(message.revision) : $util.Long.fromBits(message.revision.low >>> 0, message.revision.high >>> 0, true).toBigInt();
                    else if (typeof message.revision === "number")
                        object.revision = options.longs === String ? String(message.revision) : message.revision;
                    else
                        object.revision = options.longs === String ? $util.Long.prototype.toString.call(message.revision) : options.longs === Number ? new $util.LongBits(message.revision.low >>> 0, message.revision.high >>> 0).toNumber(true) : message.revision;
                if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                    object.signature = options.bytes === String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === Array ? Array.prototype.slice.call(message.signature) : message.signature;
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    object.domain = message.domain;
                if (message.domainCountersignature != null && Object.hasOwnProperty.call(message, "domainCountersignature"))
                    object.domainCountersignature = options.bytes === String ? $util.base64.encode(message.domainCountersignature, 0, message.domainCountersignature.length) : options.bytes === Array ? Array.prototype.slice.call(message.domainCountersignature) : message.domainCountersignature;
                if (message.domainCountersignedAt != null && Object.hasOwnProperty.call(message, "domainCountersignedAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.domainCountersignedAt = typeof message.domainCountersignedAt === "number" ? BigInt(message.domainCountersignedAt) : $util.Long.fromBits(message.domainCountersignedAt.low >>> 0, message.domainCountersignedAt.high >>> 0, false).toBigInt();
                    else if (typeof message.domainCountersignedAt === "number")
                        object.domainCountersignedAt = options.longs === String ? String(message.domainCountersignedAt) : message.domainCountersignedAt;
                    else
                        object.domainCountersignedAt = options.longs === String ? $util.Long.prototype.toString.call(message.domainCountersignedAt) : options.longs === Number ? new $util.LongBits(message.domainCountersignedAt.low >>> 0, message.domainCountersignedAt.high >>> 0).toNumber() : message.domainCountersignedAt;
                if (message.domainCountersignerPubkey != null && Object.hasOwnProperty.call(message, "domainCountersignerPubkey"))
                    object.domainCountersignerPubkey = options.bytes === String ? $util.base64.encode(message.domainCountersignerPubkey, 0, message.domainCountersignerPubkey.length) : options.bytes === Array ? Array.prototype.slice.call(message.domainCountersignerPubkey) : message.domainCountersignerPubkey;
                if (message.credential != null && Object.hasOwnProperty.call(message, "credential"))
                    object.credential = $root.dmcn.identity.Credential.toObject(message.credential, options, q + 1);
                return object;
            };

            /**
             * Converts this RelayDescriptor to JSON.
             * @function toJSON
             * @memberof dmcn.identity.RelayDescriptor
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RelayDescriptor.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for RelayDescriptor
             * @function getTypeUrl
             * @memberof dmcn.identity.RelayDescriptor
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            RelayDescriptor.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.identity.RelayDescriptor";
            };

            return RelayDescriptor;
        })();

        identity.KeyCompromiseRecord = (function() {

            /**
             * Properties of a KeyCompromiseRecord.
             * @memberof dmcn.identity
             * @interface IKeyCompromiseRecord
             * @property {number|null} [version] KeyCompromiseRecord version
             * @property {string|null} [domain] KeyCompromiseRecord domain
             * @property {Array.<dmcn.identity.ICompromisedKey>|null} [compromisedKeys] KeyCompromiseRecord compromisedKeys
             * @property {number|Long|null} [revision] KeyCompromiseRecord revision
             * @property {number|Long|null} [createdAt] KeyCompromiseRecord createdAt
             * @property {Uint8Array|null} [selfSignature] KeyCompromiseRecord selfSignature
             */

            /**
             * Constructs a new KeyCompromiseRecord.
             * @memberof dmcn.identity
             * @classdesc Represents a KeyCompromiseRecord.
             * @implements IKeyCompromiseRecord
             * @constructor
             * @param {dmcn.identity.IKeyCompromiseRecord=} [properties] Properties to set
             */
            function KeyCompromiseRecord(properties) {
                this.compromisedKeys = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * KeyCompromiseRecord version.
             * @member {number} version
             * @memberof dmcn.identity.KeyCompromiseRecord
             * @instance
             */
            KeyCompromiseRecord.prototype.version = 0;

            /**
             * KeyCompromiseRecord domain.
             * @member {string} domain
             * @memberof dmcn.identity.KeyCompromiseRecord
             * @instance
             */
            KeyCompromiseRecord.prototype.domain = "";

            /**
             * KeyCompromiseRecord compromisedKeys.
             * @member {Array.<dmcn.identity.ICompromisedKey>} compromisedKeys
             * @memberof dmcn.identity.KeyCompromiseRecord
             * @instance
             */
            KeyCompromiseRecord.prototype.compromisedKeys = $util.emptyArray;

            /**
             * KeyCompromiseRecord revision.
             * @member {number|Long} revision
             * @memberof dmcn.identity.KeyCompromiseRecord
             * @instance
             */
            KeyCompromiseRecord.prototype.revision = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * KeyCompromiseRecord createdAt.
             * @member {number|Long} createdAt
             * @memberof dmcn.identity.KeyCompromiseRecord
             * @instance
             */
            KeyCompromiseRecord.prototype.createdAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * KeyCompromiseRecord selfSignature.
             * @member {Uint8Array} selfSignature
             * @memberof dmcn.identity.KeyCompromiseRecord
             * @instance
             */
            KeyCompromiseRecord.prototype.selfSignature = $util.newBuffer([]);

            /**
             * Creates a new KeyCompromiseRecord instance using the specified properties.
             * @function create
             * @memberof dmcn.identity.KeyCompromiseRecord
             * @static
             * @param {dmcn.identity.IKeyCompromiseRecord=} [properties] Properties to set
             * @returns {dmcn.identity.KeyCompromiseRecord} KeyCompromiseRecord instance
             */
            KeyCompromiseRecord.create = function create(properties) {
                return new KeyCompromiseRecord(properties);
            };

            /**
             * Encodes the specified KeyCompromiseRecord message. Does not implicitly {@link dmcn.identity.KeyCompromiseRecord.verify|verify} messages.
             * @function encode
             * @memberof dmcn.identity.KeyCompromiseRecord
             * @static
             * @param {dmcn.identity.IKeyCompromiseRecord} message KeyCompromiseRecord message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            KeyCompromiseRecord.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.version);
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.domain);
                if (message.compromisedKeys != null && message.compromisedKeys.length)
                    for (let i = 0; i < message.compromisedKeys.length; ++i)
                        $root.dmcn.identity.CompromisedKey.encode(message.compromisedKeys[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.revision);
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.createdAt);
                if (message.selfSignature != null && Object.hasOwnProperty.call(message, "selfSignature"))
                    writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.selfSignature);
                return writer;
            };

            /**
             * Encodes the specified KeyCompromiseRecord message, length delimited. Does not implicitly {@link dmcn.identity.KeyCompromiseRecord.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.identity.KeyCompromiseRecord
             * @static
             * @param {dmcn.identity.IKeyCompromiseRecord} message KeyCompromiseRecord message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            KeyCompromiseRecord.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a KeyCompromiseRecord message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.identity.KeyCompromiseRecord
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.identity.KeyCompromiseRecord} KeyCompromiseRecord
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            KeyCompromiseRecord.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.identity.KeyCompromiseRecord();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.version = reader.uint32();
                            break;
                        }
                    case 2: {
                            message.domain = reader.string();
                            break;
                        }
                    case 3: {
                            if (!(message.compromisedKeys && message.compromisedKeys.length))
                                message.compromisedKeys = [];
                            message.compromisedKeys.push($root.dmcn.identity.CompromisedKey.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 4: {
                            message.revision = reader.uint64();
                            break;
                        }
                    case 5: {
                            message.createdAt = reader.int64();
                            break;
                        }
                    case 6: {
                            message.selfSignature = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a KeyCompromiseRecord message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.identity.KeyCompromiseRecord
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.identity.KeyCompromiseRecord} KeyCompromiseRecord
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            KeyCompromiseRecord.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a KeyCompromiseRecord message.
             * @function verify
             * @memberof dmcn.identity.KeyCompromiseRecord
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            KeyCompromiseRecord.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    if (!$util.isInteger(message.version))
                        return "version: integer expected";
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    if (!$util.isString(message.domain))
                        return "domain: string expected";
                if (message.compromisedKeys != null && Object.hasOwnProperty.call(message, "compromisedKeys")) {
                    if (!Array.isArray(message.compromisedKeys))
                        return "compromisedKeys: array expected";
                    for (let i = 0; i < message.compromisedKeys.length; ++i) {
                        let error = $root.dmcn.identity.CompromisedKey.verify(message.compromisedKeys[i], long + 1);
                        if (error)
                            return "compromisedKeys." + error;
                    }
                }
                if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                    if (!$util.isInteger(message.revision) && !(message.revision && $util.isInteger(message.revision.low) && $util.isInteger(message.revision.high)))
                        return "revision: integer|Long expected";
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
                        return "createdAt: integer|Long expected";
                if (message.selfSignature != null && Object.hasOwnProperty.call(message, "selfSignature"))
                    if (!(message.selfSignature && typeof message.selfSignature.length === "number" || $util.isString(message.selfSignature)))
                        return "selfSignature: buffer expected";
                return null;
            };

            /**
             * Creates a KeyCompromiseRecord message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.identity.KeyCompromiseRecord
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.identity.KeyCompromiseRecord} KeyCompromiseRecord
             */
            KeyCompromiseRecord.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.identity.KeyCompromiseRecord)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.identity.KeyCompromiseRecord: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.identity.KeyCompromiseRecord();
                if (object.version != null)
                    message.version = object.version >>> 0;
                if (object.domain != null)
                    message.domain = String(object.domain);
                if (object.compromisedKeys) {
                    if (!Array.isArray(object.compromisedKeys))
                        throw TypeError(".dmcn.identity.KeyCompromiseRecord.compromisedKeys: array expected");
                    message.compromisedKeys = [];
                    for (let i = 0; i < object.compromisedKeys.length; ++i) {
                        if (!$util.isObject(object.compromisedKeys[i]))
                            throw TypeError(".dmcn.identity.KeyCompromiseRecord.compromisedKeys: object expected");
                        message.compromisedKeys[i] = $root.dmcn.identity.CompromisedKey.fromObject(object.compromisedKeys[i], long + 1);
                    }
                }
                if (object.revision != null)
                    if ($util.Long)
                        message.revision = $util.Long.fromValue(object.revision, true);
                    else if (typeof object.revision === "string")
                        message.revision = parseInt(object.revision, 10);
                    else if (typeof object.revision === "number")
                        message.revision = object.revision;
                    else if (typeof object.revision === "object")
                        message.revision = new $util.LongBits(object.revision.low >>> 0, object.revision.high >>> 0).toNumber(true);
                if (object.createdAt != null)
                    if ($util.Long)
                        message.createdAt = $util.Long.fromValue(object.createdAt, false);
                    else if (typeof object.createdAt === "string")
                        message.createdAt = parseInt(object.createdAt, 10);
                    else if (typeof object.createdAt === "number")
                        message.createdAt = object.createdAt;
                    else if (typeof object.createdAt === "object")
                        message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
                if (object.selfSignature != null)
                    if (typeof object.selfSignature === "string")
                        $util.base64.decode(object.selfSignature, message.selfSignature = $util.newBuffer($util.base64.length(object.selfSignature)), 0);
                    else if (object.selfSignature.length >= 0)
                        message.selfSignature = object.selfSignature;
                return message;
            };

            /**
             * Creates a plain object from a KeyCompromiseRecord message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.identity.KeyCompromiseRecord
             * @static
             * @param {dmcn.identity.KeyCompromiseRecord} message KeyCompromiseRecord
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            KeyCompromiseRecord.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.compromisedKeys = [];
                if (options.defaults) {
                    object.version = 0;
                    object.domain = "";
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.revision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.revision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.createdAt = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.createdAt = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if (options.bytes === String)
                        object.selfSignature = "";
                    else {
                        object.selfSignature = [];
                        if (options.bytes !== Array)
                            object.selfSignature = $util.newBuffer(object.selfSignature);
                    }
                }
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    object.version = message.version;
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    object.domain = message.domain;
                if (message.compromisedKeys && message.compromisedKeys.length) {
                    object.compromisedKeys = [];
                    for (let j = 0; j < message.compromisedKeys.length; ++j)
                        object.compromisedKeys[j] = $root.dmcn.identity.CompromisedKey.toObject(message.compromisedKeys[j], options, q + 1);
                }
                if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.revision = typeof message.revision === "number" ? BigInt(message.revision) : $util.Long.fromBits(message.revision.low >>> 0, message.revision.high >>> 0, true).toBigInt();
                    else if (typeof message.revision === "number")
                        object.revision = options.longs === String ? String(message.revision) : message.revision;
                    else
                        object.revision = options.longs === String ? $util.Long.prototype.toString.call(message.revision) : options.longs === Number ? new $util.LongBits(message.revision.low >>> 0, message.revision.high >>> 0).toNumber(true) : message.revision;
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.createdAt = typeof message.createdAt === "number" ? BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
                    else if (typeof message.createdAt === "number")
                        object.createdAt = options.longs === String ? String(message.createdAt) : message.createdAt;
                    else
                        object.createdAt = options.longs === String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
                if (message.selfSignature != null && Object.hasOwnProperty.call(message, "selfSignature"))
                    object.selfSignature = options.bytes === String ? $util.base64.encode(message.selfSignature, 0, message.selfSignature.length) : options.bytes === Array ? Array.prototype.slice.call(message.selfSignature) : message.selfSignature;
                return object;
            };

            /**
             * Converts this KeyCompromiseRecord to JSON.
             * @function toJSON
             * @memberof dmcn.identity.KeyCompromiseRecord
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            KeyCompromiseRecord.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for KeyCompromiseRecord
             * @function getTypeUrl
             * @memberof dmcn.identity.KeyCompromiseRecord
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            KeyCompromiseRecord.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.identity.KeyCompromiseRecord";
            };

            return KeyCompromiseRecord;
        })();

        return identity;
    })();

    dmcn.message = (function() {

        /**
         * Namespace message.
         * @memberof dmcn
         * @namespace
         */
        const message = {};

        message.MessageBody = (function() {

            /**
             * Properties of a MessageBody.
             * @memberof dmcn.message
             * @interface IMessageBody
             * @property {string|null} [contentType] MessageBody contentType
             * @property {Uint8Array|null} [content] MessageBody content
             */

            /**
             * Constructs a new MessageBody.
             * @memberof dmcn.message
             * @classdesc Represents a MessageBody.
             * @implements IMessageBody
             * @constructor
             * @param {dmcn.message.IMessageBody=} [properties] Properties to set
             */
            function MessageBody(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MessageBody contentType.
             * @member {string} contentType
             * @memberof dmcn.message.MessageBody
             * @instance
             */
            MessageBody.prototype.contentType = "";

            /**
             * MessageBody content.
             * @member {Uint8Array} content
             * @memberof dmcn.message.MessageBody
             * @instance
             */
            MessageBody.prototype.content = $util.newBuffer([]);

            /**
             * Creates a new MessageBody instance using the specified properties.
             * @function create
             * @memberof dmcn.message.MessageBody
             * @static
             * @param {dmcn.message.IMessageBody=} [properties] Properties to set
             * @returns {dmcn.message.MessageBody} MessageBody instance
             */
            MessageBody.create = function create(properties) {
                return new MessageBody(properties);
            };

            /**
             * Encodes the specified MessageBody message. Does not implicitly {@link dmcn.message.MessageBody.verify|verify} messages.
             * @function encode
             * @memberof dmcn.message.MessageBody
             * @static
             * @param {dmcn.message.IMessageBody} message MessageBody message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MessageBody.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.contentType != null && Object.hasOwnProperty.call(message, "contentType"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.contentType);
                if (message.content != null && Object.hasOwnProperty.call(message, "content"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.content);
                return writer;
            };

            /**
             * Encodes the specified MessageBody message, length delimited. Does not implicitly {@link dmcn.message.MessageBody.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.message.MessageBody
             * @static
             * @param {dmcn.message.IMessageBody} message MessageBody message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MessageBody.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a MessageBody message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.message.MessageBody
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.message.MessageBody} MessageBody
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MessageBody.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.message.MessageBody();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.contentType = reader.string();
                            break;
                        }
                    case 2: {
                            message.content = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a MessageBody message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.message.MessageBody
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.message.MessageBody} MessageBody
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MessageBody.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MessageBody message.
             * @function verify
             * @memberof dmcn.message.MessageBody
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MessageBody.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.contentType != null && Object.hasOwnProperty.call(message, "contentType"))
                    if (!$util.isString(message.contentType))
                        return "contentType: string expected";
                if (message.content != null && Object.hasOwnProperty.call(message, "content"))
                    if (!(message.content && typeof message.content.length === "number" || $util.isString(message.content)))
                        return "content: buffer expected";
                return null;
            };

            /**
             * Creates a MessageBody message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.message.MessageBody
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.message.MessageBody} MessageBody
             */
            MessageBody.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.message.MessageBody)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.message.MessageBody: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.message.MessageBody();
                if (object.contentType != null)
                    message.contentType = String(object.contentType);
                if (object.content != null)
                    if (typeof object.content === "string")
                        $util.base64.decode(object.content, message.content = $util.newBuffer($util.base64.length(object.content)), 0);
                    else if (object.content.length >= 0)
                        message.content = object.content;
                return message;
            };

            /**
             * Creates a plain object from a MessageBody message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.message.MessageBody
             * @static
             * @param {dmcn.message.MessageBody} message MessageBody
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MessageBody.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.contentType = "";
                    if (options.bytes === String)
                        object.content = "";
                    else {
                        object.content = [];
                        if (options.bytes !== Array)
                            object.content = $util.newBuffer(object.content);
                    }
                }
                if (message.contentType != null && Object.hasOwnProperty.call(message, "contentType"))
                    object.contentType = message.contentType;
                if (message.content != null && Object.hasOwnProperty.call(message, "content"))
                    object.content = options.bytes === String ? $util.base64.encode(message.content, 0, message.content.length) : options.bytes === Array ? Array.prototype.slice.call(message.content) : message.content;
                return object;
            };

            /**
             * Converts this MessageBody to JSON.
             * @function toJSON
             * @memberof dmcn.message.MessageBody
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MessageBody.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MessageBody
             * @function getTypeUrl
             * @memberof dmcn.message.MessageBody
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MessageBody.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.message.MessageBody";
            };

            return MessageBody;
        })();

        message.AttachmentRecord = (function() {

            /**
             * Properties of an AttachmentRecord.
             * @memberof dmcn.message
             * @interface IAttachmentRecord
             * @property {Uint8Array|null} [attachmentId] AttachmentRecord attachmentId
             * @property {string|null} [filename] AttachmentRecord filename
             * @property {string|null} [contentType] AttachmentRecord contentType
             * @property {number|Long|null} [sizeBytes] AttachmentRecord sizeBytes
             * @property {Uint8Array|null} [contentHash] AttachmentRecord contentHash
             * @property {Uint8Array|null} [content] AttachmentRecord content
             */

            /**
             * Constructs a new AttachmentRecord.
             * @memberof dmcn.message
             * @classdesc Represents an AttachmentRecord.
             * @implements IAttachmentRecord
             * @constructor
             * @param {dmcn.message.IAttachmentRecord=} [properties] Properties to set
             */
            function AttachmentRecord(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AttachmentRecord attachmentId.
             * @member {Uint8Array} attachmentId
             * @memberof dmcn.message.AttachmentRecord
             * @instance
             */
            AttachmentRecord.prototype.attachmentId = $util.newBuffer([]);

            /**
             * AttachmentRecord filename.
             * @member {string} filename
             * @memberof dmcn.message.AttachmentRecord
             * @instance
             */
            AttachmentRecord.prototype.filename = "";

            /**
             * AttachmentRecord contentType.
             * @member {string} contentType
             * @memberof dmcn.message.AttachmentRecord
             * @instance
             */
            AttachmentRecord.prototype.contentType = "";

            /**
             * AttachmentRecord sizeBytes.
             * @member {number|Long} sizeBytes
             * @memberof dmcn.message.AttachmentRecord
             * @instance
             */
            AttachmentRecord.prototype.sizeBytes = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * AttachmentRecord contentHash.
             * @member {Uint8Array} contentHash
             * @memberof dmcn.message.AttachmentRecord
             * @instance
             */
            AttachmentRecord.prototype.contentHash = $util.newBuffer([]);

            /**
             * AttachmentRecord content.
             * @member {Uint8Array} content
             * @memberof dmcn.message.AttachmentRecord
             * @instance
             */
            AttachmentRecord.prototype.content = $util.newBuffer([]);

            /**
             * Creates a new AttachmentRecord instance using the specified properties.
             * @function create
             * @memberof dmcn.message.AttachmentRecord
             * @static
             * @param {dmcn.message.IAttachmentRecord=} [properties] Properties to set
             * @returns {dmcn.message.AttachmentRecord} AttachmentRecord instance
             */
            AttachmentRecord.create = function create(properties) {
                return new AttachmentRecord(properties);
            };

            /**
             * Encodes the specified AttachmentRecord message. Does not implicitly {@link dmcn.message.AttachmentRecord.verify|verify} messages.
             * @function encode
             * @memberof dmcn.message.AttachmentRecord
             * @static
             * @param {dmcn.message.IAttachmentRecord} message AttachmentRecord message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AttachmentRecord.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.attachmentId != null && Object.hasOwnProperty.call(message, "attachmentId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.attachmentId);
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.filename);
                if (message.contentType != null && Object.hasOwnProperty.call(message, "contentType"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.contentType);
                if (message.sizeBytes != null && Object.hasOwnProperty.call(message, "sizeBytes"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.sizeBytes);
                if (message.contentHash != null && Object.hasOwnProperty.call(message, "contentHash"))
                    writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.contentHash);
                if (message.content != null && Object.hasOwnProperty.call(message, "content"))
                    writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.content);
                return writer;
            };

            /**
             * Encodes the specified AttachmentRecord message, length delimited. Does not implicitly {@link dmcn.message.AttachmentRecord.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.message.AttachmentRecord
             * @static
             * @param {dmcn.message.IAttachmentRecord} message AttachmentRecord message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AttachmentRecord.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes an AttachmentRecord message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.message.AttachmentRecord
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.message.AttachmentRecord} AttachmentRecord
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AttachmentRecord.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.message.AttachmentRecord();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.attachmentId = reader.bytes();
                            break;
                        }
                    case 2: {
                            message.filename = reader.string();
                            break;
                        }
                    case 3: {
                            message.contentType = reader.string();
                            break;
                        }
                    case 4: {
                            message.sizeBytes = reader.uint64();
                            break;
                        }
                    case 5: {
                            message.contentHash = reader.bytes();
                            break;
                        }
                    case 6: {
                            message.content = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an AttachmentRecord message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.message.AttachmentRecord
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.message.AttachmentRecord} AttachmentRecord
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AttachmentRecord.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AttachmentRecord message.
             * @function verify
             * @memberof dmcn.message.AttachmentRecord
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AttachmentRecord.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.attachmentId != null && Object.hasOwnProperty.call(message, "attachmentId"))
                    if (!(message.attachmentId && typeof message.attachmentId.length === "number" || $util.isString(message.attachmentId)))
                        return "attachmentId: buffer expected";
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    if (!$util.isString(message.filename))
                        return "filename: string expected";
                if (message.contentType != null && Object.hasOwnProperty.call(message, "contentType"))
                    if (!$util.isString(message.contentType))
                        return "contentType: string expected";
                if (message.sizeBytes != null && Object.hasOwnProperty.call(message, "sizeBytes"))
                    if (!$util.isInteger(message.sizeBytes) && !(message.sizeBytes && $util.isInteger(message.sizeBytes.low) && $util.isInteger(message.sizeBytes.high)))
                        return "sizeBytes: integer|Long expected";
                if (message.contentHash != null && Object.hasOwnProperty.call(message, "contentHash"))
                    if (!(message.contentHash && typeof message.contentHash.length === "number" || $util.isString(message.contentHash)))
                        return "contentHash: buffer expected";
                if (message.content != null && Object.hasOwnProperty.call(message, "content"))
                    if (!(message.content && typeof message.content.length === "number" || $util.isString(message.content)))
                        return "content: buffer expected";
                return null;
            };

            /**
             * Creates an AttachmentRecord message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.message.AttachmentRecord
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.message.AttachmentRecord} AttachmentRecord
             */
            AttachmentRecord.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.message.AttachmentRecord)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.message.AttachmentRecord: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.message.AttachmentRecord();
                if (object.attachmentId != null)
                    if (typeof object.attachmentId === "string")
                        $util.base64.decode(object.attachmentId, message.attachmentId = $util.newBuffer($util.base64.length(object.attachmentId)), 0);
                    else if (object.attachmentId.length >= 0)
                        message.attachmentId = object.attachmentId;
                if (object.filename != null)
                    message.filename = String(object.filename);
                if (object.contentType != null)
                    message.contentType = String(object.contentType);
                if (object.sizeBytes != null)
                    if ($util.Long)
                        message.sizeBytes = $util.Long.fromValue(object.sizeBytes, true);
                    else if (typeof object.sizeBytes === "string")
                        message.sizeBytes = parseInt(object.sizeBytes, 10);
                    else if (typeof object.sizeBytes === "number")
                        message.sizeBytes = object.sizeBytes;
                    else if (typeof object.sizeBytes === "object")
                        message.sizeBytes = new $util.LongBits(object.sizeBytes.low >>> 0, object.sizeBytes.high >>> 0).toNumber(true);
                if (object.contentHash != null)
                    if (typeof object.contentHash === "string")
                        $util.base64.decode(object.contentHash, message.contentHash = $util.newBuffer($util.base64.length(object.contentHash)), 0);
                    else if (object.contentHash.length >= 0)
                        message.contentHash = object.contentHash;
                if (object.content != null)
                    if (typeof object.content === "string")
                        $util.base64.decode(object.content, message.content = $util.newBuffer($util.base64.length(object.content)), 0);
                    else if (object.content.length >= 0)
                        message.content = object.content;
                return message;
            };

            /**
             * Creates a plain object from an AttachmentRecord message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.message.AttachmentRecord
             * @static
             * @param {dmcn.message.AttachmentRecord} message AttachmentRecord
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AttachmentRecord.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    if (options.bytes === String)
                        object.attachmentId = "";
                    else {
                        object.attachmentId = [];
                        if (options.bytes !== Array)
                            object.attachmentId = $util.newBuffer(object.attachmentId);
                    }
                    object.filename = "";
                    object.contentType = "";
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.sizeBytes = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.sizeBytes = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if (options.bytes === String)
                        object.contentHash = "";
                    else {
                        object.contentHash = [];
                        if (options.bytes !== Array)
                            object.contentHash = $util.newBuffer(object.contentHash);
                    }
                    if (options.bytes === String)
                        object.content = "";
                    else {
                        object.content = [];
                        if (options.bytes !== Array)
                            object.content = $util.newBuffer(object.content);
                    }
                }
                if (message.attachmentId != null && Object.hasOwnProperty.call(message, "attachmentId"))
                    object.attachmentId = options.bytes === String ? $util.base64.encode(message.attachmentId, 0, message.attachmentId.length) : options.bytes === Array ? Array.prototype.slice.call(message.attachmentId) : message.attachmentId;
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    object.filename = message.filename;
                if (message.contentType != null && Object.hasOwnProperty.call(message, "contentType"))
                    object.contentType = message.contentType;
                if (message.sizeBytes != null && Object.hasOwnProperty.call(message, "sizeBytes"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.sizeBytes = typeof message.sizeBytes === "number" ? BigInt(message.sizeBytes) : $util.Long.fromBits(message.sizeBytes.low >>> 0, message.sizeBytes.high >>> 0, true).toBigInt();
                    else if (typeof message.sizeBytes === "number")
                        object.sizeBytes = options.longs === String ? String(message.sizeBytes) : message.sizeBytes;
                    else
                        object.sizeBytes = options.longs === String ? $util.Long.prototype.toString.call(message.sizeBytes) : options.longs === Number ? new $util.LongBits(message.sizeBytes.low >>> 0, message.sizeBytes.high >>> 0).toNumber(true) : message.sizeBytes;
                if (message.contentHash != null && Object.hasOwnProperty.call(message, "contentHash"))
                    object.contentHash = options.bytes === String ? $util.base64.encode(message.contentHash, 0, message.contentHash.length) : options.bytes === Array ? Array.prototype.slice.call(message.contentHash) : message.contentHash;
                if (message.content != null && Object.hasOwnProperty.call(message, "content"))
                    object.content = options.bytes === String ? $util.base64.encode(message.content, 0, message.content.length) : options.bytes === Array ? Array.prototype.slice.call(message.content) : message.content;
                return object;
            };

            /**
             * Converts this AttachmentRecord to JSON.
             * @function toJSON
             * @memberof dmcn.message.AttachmentRecord
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AttachmentRecord.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AttachmentRecord
             * @function getTypeUrl
             * @memberof dmcn.message.AttachmentRecord
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AttachmentRecord.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.message.AttachmentRecord";
            };

            return AttachmentRecord;
        })();

        message.PlaintextMessage = (function() {

            /**
             * Properties of a PlaintextMessage.
             * @memberof dmcn.message
             * @interface IPlaintextMessage
             * @property {number|null} [version] PlaintextMessage version
             * @property {Uint8Array|null} [messageId] PlaintextMessage messageId
             * @property {Uint8Array|null} [threadId] PlaintextMessage threadId
             * @property {string|null} [senderAddress] PlaintextMessage senderAddress
             * @property {Uint8Array|null} [senderPublicKey] PlaintextMessage senderPublicKey
             * @property {string|null} [recipientAddress] PlaintextMessage recipientAddress
             * @property {number|Long|null} [sentAt] PlaintextMessage sentAt
             * @property {string|null} [subject] PlaintextMessage subject
             * @property {dmcn.message.IMessageBody|null} [body] PlaintextMessage body
             * @property {Array.<dmcn.message.IAttachmentRecord>|null} [attachments] PlaintextMessage attachments
             * @property {Uint8Array|null} [replyToId] PlaintextMessage replyToId
             */

            /**
             * Constructs a new PlaintextMessage.
             * @memberof dmcn.message
             * @classdesc Represents a PlaintextMessage.
             * @implements IPlaintextMessage
             * @constructor
             * @param {dmcn.message.IPlaintextMessage=} [properties] Properties to set
             */
            function PlaintextMessage(properties) {
                this.attachments = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PlaintextMessage version.
             * @member {number} version
             * @memberof dmcn.message.PlaintextMessage
             * @instance
             */
            PlaintextMessage.prototype.version = 0;

            /**
             * PlaintextMessage messageId.
             * @member {Uint8Array} messageId
             * @memberof dmcn.message.PlaintextMessage
             * @instance
             */
            PlaintextMessage.prototype.messageId = $util.newBuffer([]);

            /**
             * PlaintextMessage threadId.
             * @member {Uint8Array} threadId
             * @memberof dmcn.message.PlaintextMessage
             * @instance
             */
            PlaintextMessage.prototype.threadId = $util.newBuffer([]);

            /**
             * PlaintextMessage senderAddress.
             * @member {string} senderAddress
             * @memberof dmcn.message.PlaintextMessage
             * @instance
             */
            PlaintextMessage.prototype.senderAddress = "";

            /**
             * PlaintextMessage senderPublicKey.
             * @member {Uint8Array} senderPublicKey
             * @memberof dmcn.message.PlaintextMessage
             * @instance
             */
            PlaintextMessage.prototype.senderPublicKey = $util.newBuffer([]);

            /**
             * PlaintextMessage recipientAddress.
             * @member {string} recipientAddress
             * @memberof dmcn.message.PlaintextMessage
             * @instance
             */
            PlaintextMessage.prototype.recipientAddress = "";

            /**
             * PlaintextMessage sentAt.
             * @member {number|Long} sentAt
             * @memberof dmcn.message.PlaintextMessage
             * @instance
             */
            PlaintextMessage.prototype.sentAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * PlaintextMessage subject.
             * @member {string} subject
             * @memberof dmcn.message.PlaintextMessage
             * @instance
             */
            PlaintextMessage.prototype.subject = "";

            /**
             * PlaintextMessage body.
             * @member {dmcn.message.IMessageBody|null|undefined} body
             * @memberof dmcn.message.PlaintextMessage
             * @instance
             */
            PlaintextMessage.prototype.body = null;

            /**
             * PlaintextMessage attachments.
             * @member {Array.<dmcn.message.IAttachmentRecord>} attachments
             * @memberof dmcn.message.PlaintextMessage
             * @instance
             */
            PlaintextMessage.prototype.attachments = $util.emptyArray;

            /**
             * PlaintextMessage replyToId.
             * @member {Uint8Array} replyToId
             * @memberof dmcn.message.PlaintextMessage
             * @instance
             */
            PlaintextMessage.prototype.replyToId = $util.newBuffer([]);

            /**
             * Creates a new PlaintextMessage instance using the specified properties.
             * @function create
             * @memberof dmcn.message.PlaintextMessage
             * @static
             * @param {dmcn.message.IPlaintextMessage=} [properties] Properties to set
             * @returns {dmcn.message.PlaintextMessage} PlaintextMessage instance
             */
            PlaintextMessage.create = function create(properties) {
                return new PlaintextMessage(properties);
            };

            /**
             * Encodes the specified PlaintextMessage message. Does not implicitly {@link dmcn.message.PlaintextMessage.verify|verify} messages.
             * @function encode
             * @memberof dmcn.message.PlaintextMessage
             * @static
             * @param {dmcn.message.IPlaintextMessage} message PlaintextMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PlaintextMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.version);
                if (message.messageId != null && Object.hasOwnProperty.call(message, "messageId"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.messageId);
                if (message.threadId != null && Object.hasOwnProperty.call(message, "threadId"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.threadId);
                if (message.senderAddress != null && Object.hasOwnProperty.call(message, "senderAddress"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.senderAddress);
                if (message.senderPublicKey != null && Object.hasOwnProperty.call(message, "senderPublicKey"))
                    writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.senderPublicKey);
                if (message.recipientAddress != null && Object.hasOwnProperty.call(message, "recipientAddress"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.recipientAddress);
                if (message.sentAt != null && Object.hasOwnProperty.call(message, "sentAt"))
                    writer.uint32(/* id 7, wireType 0 =*/56).int64(message.sentAt);
                if (message.subject != null && Object.hasOwnProperty.call(message, "subject"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.subject);
                if (message.body != null && Object.hasOwnProperty.call(message, "body"))
                    $root.dmcn.message.MessageBody.encode(message.body, writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
                if (message.attachments != null && message.attachments.length)
                    for (let i = 0; i < message.attachments.length; ++i)
                        $root.dmcn.message.AttachmentRecord.encode(message.attachments[i], writer.uint32(/* id 10, wireType 2 =*/82).fork(), q + 1).ldelim();
                if (message.replyToId != null && Object.hasOwnProperty.call(message, "replyToId"))
                    writer.uint32(/* id 11, wireType 2 =*/90).bytes(message.replyToId);
                return writer;
            };

            /**
             * Encodes the specified PlaintextMessage message, length delimited. Does not implicitly {@link dmcn.message.PlaintextMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.message.PlaintextMessage
             * @static
             * @param {dmcn.message.IPlaintextMessage} message PlaintextMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PlaintextMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a PlaintextMessage message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.message.PlaintextMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.message.PlaintextMessage} PlaintextMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PlaintextMessage.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.message.PlaintextMessage();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.version = reader.uint32();
                            break;
                        }
                    case 2: {
                            message.messageId = reader.bytes();
                            break;
                        }
                    case 3: {
                            message.threadId = reader.bytes();
                            break;
                        }
                    case 4: {
                            message.senderAddress = reader.string();
                            break;
                        }
                    case 5: {
                            message.senderPublicKey = reader.bytes();
                            break;
                        }
                    case 6: {
                            message.recipientAddress = reader.string();
                            break;
                        }
                    case 7: {
                            message.sentAt = reader.int64();
                            break;
                        }
                    case 8: {
                            message.subject = reader.string();
                            break;
                        }
                    case 9: {
                            message.body = $root.dmcn.message.MessageBody.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 10: {
                            if (!(message.attachments && message.attachments.length))
                                message.attachments = [];
                            message.attachments.push($root.dmcn.message.AttachmentRecord.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 11: {
                            message.replyToId = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a PlaintextMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.message.PlaintextMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.message.PlaintextMessage} PlaintextMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PlaintextMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PlaintextMessage message.
             * @function verify
             * @memberof dmcn.message.PlaintextMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PlaintextMessage.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    if (!$util.isInteger(message.version))
                        return "version: integer expected";
                if (message.messageId != null && Object.hasOwnProperty.call(message, "messageId"))
                    if (!(message.messageId && typeof message.messageId.length === "number" || $util.isString(message.messageId)))
                        return "messageId: buffer expected";
                if (message.threadId != null && Object.hasOwnProperty.call(message, "threadId"))
                    if (!(message.threadId && typeof message.threadId.length === "number" || $util.isString(message.threadId)))
                        return "threadId: buffer expected";
                if (message.senderAddress != null && Object.hasOwnProperty.call(message, "senderAddress"))
                    if (!$util.isString(message.senderAddress))
                        return "senderAddress: string expected";
                if (message.senderPublicKey != null && Object.hasOwnProperty.call(message, "senderPublicKey"))
                    if (!(message.senderPublicKey && typeof message.senderPublicKey.length === "number" || $util.isString(message.senderPublicKey)))
                        return "senderPublicKey: buffer expected";
                if (message.recipientAddress != null && Object.hasOwnProperty.call(message, "recipientAddress"))
                    if (!$util.isString(message.recipientAddress))
                        return "recipientAddress: string expected";
                if (message.sentAt != null && Object.hasOwnProperty.call(message, "sentAt"))
                    if (!$util.isInteger(message.sentAt) && !(message.sentAt && $util.isInteger(message.sentAt.low) && $util.isInteger(message.sentAt.high)))
                        return "sentAt: integer|Long expected";
                if (message.subject != null && Object.hasOwnProperty.call(message, "subject"))
                    if (!$util.isString(message.subject))
                        return "subject: string expected";
                if (message.body != null && Object.hasOwnProperty.call(message, "body")) {
                    let error = $root.dmcn.message.MessageBody.verify(message.body, long + 1);
                    if (error)
                        return "body." + error;
                }
                if (message.attachments != null && Object.hasOwnProperty.call(message, "attachments")) {
                    if (!Array.isArray(message.attachments))
                        return "attachments: array expected";
                    for (let i = 0; i < message.attachments.length; ++i) {
                        let error = $root.dmcn.message.AttachmentRecord.verify(message.attachments[i], long + 1);
                        if (error)
                            return "attachments." + error;
                    }
                }
                if (message.replyToId != null && Object.hasOwnProperty.call(message, "replyToId"))
                    if (!(message.replyToId && typeof message.replyToId.length === "number" || $util.isString(message.replyToId)))
                        return "replyToId: buffer expected";
                return null;
            };

            /**
             * Creates a PlaintextMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.message.PlaintextMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.message.PlaintextMessage} PlaintextMessage
             */
            PlaintextMessage.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.message.PlaintextMessage)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.message.PlaintextMessage: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.message.PlaintextMessage();
                if (object.version != null)
                    message.version = object.version >>> 0;
                if (object.messageId != null)
                    if (typeof object.messageId === "string")
                        $util.base64.decode(object.messageId, message.messageId = $util.newBuffer($util.base64.length(object.messageId)), 0);
                    else if (object.messageId.length >= 0)
                        message.messageId = object.messageId;
                if (object.threadId != null)
                    if (typeof object.threadId === "string")
                        $util.base64.decode(object.threadId, message.threadId = $util.newBuffer($util.base64.length(object.threadId)), 0);
                    else if (object.threadId.length >= 0)
                        message.threadId = object.threadId;
                if (object.senderAddress != null)
                    message.senderAddress = String(object.senderAddress);
                if (object.senderPublicKey != null)
                    if (typeof object.senderPublicKey === "string")
                        $util.base64.decode(object.senderPublicKey, message.senderPublicKey = $util.newBuffer($util.base64.length(object.senderPublicKey)), 0);
                    else if (object.senderPublicKey.length >= 0)
                        message.senderPublicKey = object.senderPublicKey;
                if (object.recipientAddress != null)
                    message.recipientAddress = String(object.recipientAddress);
                if (object.sentAt != null)
                    if ($util.Long)
                        message.sentAt = $util.Long.fromValue(object.sentAt, false);
                    else if (typeof object.sentAt === "string")
                        message.sentAt = parseInt(object.sentAt, 10);
                    else if (typeof object.sentAt === "number")
                        message.sentAt = object.sentAt;
                    else if (typeof object.sentAt === "object")
                        message.sentAt = new $util.LongBits(object.sentAt.low >>> 0, object.sentAt.high >>> 0).toNumber();
                if (object.subject != null)
                    message.subject = String(object.subject);
                if (object.body != null) {
                    if (!$util.isObject(object.body))
                        throw TypeError(".dmcn.message.PlaintextMessage.body: object expected");
                    message.body = $root.dmcn.message.MessageBody.fromObject(object.body, long + 1);
                }
                if (object.attachments) {
                    if (!Array.isArray(object.attachments))
                        throw TypeError(".dmcn.message.PlaintextMessage.attachments: array expected");
                    message.attachments = [];
                    for (let i = 0; i < object.attachments.length; ++i) {
                        if (!$util.isObject(object.attachments[i]))
                            throw TypeError(".dmcn.message.PlaintextMessage.attachments: object expected");
                        message.attachments[i] = $root.dmcn.message.AttachmentRecord.fromObject(object.attachments[i], long + 1);
                    }
                }
                if (object.replyToId != null)
                    if (typeof object.replyToId === "string")
                        $util.base64.decode(object.replyToId, message.replyToId = $util.newBuffer($util.base64.length(object.replyToId)), 0);
                    else if (object.replyToId.length >= 0)
                        message.replyToId = object.replyToId;
                return message;
            };

            /**
             * Creates a plain object from a PlaintextMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.message.PlaintextMessage
             * @static
             * @param {dmcn.message.PlaintextMessage} message PlaintextMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PlaintextMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.attachments = [];
                if (options.defaults) {
                    object.version = 0;
                    if (options.bytes === String)
                        object.messageId = "";
                    else {
                        object.messageId = [];
                        if (options.bytes !== Array)
                            object.messageId = $util.newBuffer(object.messageId);
                    }
                    if (options.bytes === String)
                        object.threadId = "";
                    else {
                        object.threadId = [];
                        if (options.bytes !== Array)
                            object.threadId = $util.newBuffer(object.threadId);
                    }
                    object.senderAddress = "";
                    if (options.bytes === String)
                        object.senderPublicKey = "";
                    else {
                        object.senderPublicKey = [];
                        if (options.bytes !== Array)
                            object.senderPublicKey = $util.newBuffer(object.senderPublicKey);
                    }
                    object.recipientAddress = "";
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.sentAt = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.sentAt = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    object.subject = "";
                    object.body = null;
                    if (options.bytes === String)
                        object.replyToId = "";
                    else {
                        object.replyToId = [];
                        if (options.bytes !== Array)
                            object.replyToId = $util.newBuffer(object.replyToId);
                    }
                }
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    object.version = message.version;
                if (message.messageId != null && Object.hasOwnProperty.call(message, "messageId"))
                    object.messageId = options.bytes === String ? $util.base64.encode(message.messageId, 0, message.messageId.length) : options.bytes === Array ? Array.prototype.slice.call(message.messageId) : message.messageId;
                if (message.threadId != null && Object.hasOwnProperty.call(message, "threadId"))
                    object.threadId = options.bytes === String ? $util.base64.encode(message.threadId, 0, message.threadId.length) : options.bytes === Array ? Array.prototype.slice.call(message.threadId) : message.threadId;
                if (message.senderAddress != null && Object.hasOwnProperty.call(message, "senderAddress"))
                    object.senderAddress = message.senderAddress;
                if (message.senderPublicKey != null && Object.hasOwnProperty.call(message, "senderPublicKey"))
                    object.senderPublicKey = options.bytes === String ? $util.base64.encode(message.senderPublicKey, 0, message.senderPublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.senderPublicKey) : message.senderPublicKey;
                if (message.recipientAddress != null && Object.hasOwnProperty.call(message, "recipientAddress"))
                    object.recipientAddress = message.recipientAddress;
                if (message.sentAt != null && Object.hasOwnProperty.call(message, "sentAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.sentAt = typeof message.sentAt === "number" ? BigInt(message.sentAt) : $util.Long.fromBits(message.sentAt.low >>> 0, message.sentAt.high >>> 0, false).toBigInt();
                    else if (typeof message.sentAt === "number")
                        object.sentAt = options.longs === String ? String(message.sentAt) : message.sentAt;
                    else
                        object.sentAt = options.longs === String ? $util.Long.prototype.toString.call(message.sentAt) : options.longs === Number ? new $util.LongBits(message.sentAt.low >>> 0, message.sentAt.high >>> 0).toNumber() : message.sentAt;
                if (message.subject != null && Object.hasOwnProperty.call(message, "subject"))
                    object.subject = message.subject;
                if (message.body != null && Object.hasOwnProperty.call(message, "body"))
                    object.body = $root.dmcn.message.MessageBody.toObject(message.body, options, q + 1);
                if (message.attachments && message.attachments.length) {
                    object.attachments = [];
                    for (let j = 0; j < message.attachments.length; ++j)
                        object.attachments[j] = $root.dmcn.message.AttachmentRecord.toObject(message.attachments[j], options, q + 1);
                }
                if (message.replyToId != null && Object.hasOwnProperty.call(message, "replyToId"))
                    object.replyToId = options.bytes === String ? $util.base64.encode(message.replyToId, 0, message.replyToId.length) : options.bytes === Array ? Array.prototype.slice.call(message.replyToId) : message.replyToId;
                return object;
            };

            /**
             * Converts this PlaintextMessage to JSON.
             * @function toJSON
             * @memberof dmcn.message.PlaintextMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PlaintextMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PlaintextMessage
             * @function getTypeUrl
             * @memberof dmcn.message.PlaintextMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PlaintextMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.message.PlaintextMessage";
            };

            return PlaintextMessage;
        })();

        message.SignedMessage = (function() {

            /**
             * Properties of a SignedMessage.
             * @memberof dmcn.message
             * @interface ISignedMessage
             * @property {dmcn.message.IPlaintextMessage|null} [plaintext] SignedMessage plaintext
             * @property {Uint8Array|null} [senderSignature] SignedMessage senderSignature
             */

            /**
             * Constructs a new SignedMessage.
             * @memberof dmcn.message
             * @classdesc Represents a SignedMessage.
             * @implements ISignedMessage
             * @constructor
             * @param {dmcn.message.ISignedMessage=} [properties] Properties to set
             */
            function SignedMessage(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * SignedMessage plaintext.
             * @member {dmcn.message.IPlaintextMessage|null|undefined} plaintext
             * @memberof dmcn.message.SignedMessage
             * @instance
             */
            SignedMessage.prototype.plaintext = null;

            /**
             * SignedMessage senderSignature.
             * @member {Uint8Array} senderSignature
             * @memberof dmcn.message.SignedMessage
             * @instance
             */
            SignedMessage.prototype.senderSignature = $util.newBuffer([]);

            /**
             * Creates a new SignedMessage instance using the specified properties.
             * @function create
             * @memberof dmcn.message.SignedMessage
             * @static
             * @param {dmcn.message.ISignedMessage=} [properties] Properties to set
             * @returns {dmcn.message.SignedMessage} SignedMessage instance
             */
            SignedMessage.create = function create(properties) {
                return new SignedMessage(properties);
            };

            /**
             * Encodes the specified SignedMessage message. Does not implicitly {@link dmcn.message.SignedMessage.verify|verify} messages.
             * @function encode
             * @memberof dmcn.message.SignedMessage
             * @static
             * @param {dmcn.message.ISignedMessage} message SignedMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SignedMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.plaintext != null && Object.hasOwnProperty.call(message, "plaintext"))
                    $root.dmcn.message.PlaintextMessage.encode(message.plaintext, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.senderSignature != null && Object.hasOwnProperty.call(message, "senderSignature"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.senderSignature);
                return writer;
            };

            /**
             * Encodes the specified SignedMessage message, length delimited. Does not implicitly {@link dmcn.message.SignedMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.message.SignedMessage
             * @static
             * @param {dmcn.message.ISignedMessage} message SignedMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SignedMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a SignedMessage message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.message.SignedMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.message.SignedMessage} SignedMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SignedMessage.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.message.SignedMessage();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.plaintext = $root.dmcn.message.PlaintextMessage.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 2: {
                            message.senderSignature = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a SignedMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.message.SignedMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.message.SignedMessage} SignedMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SignedMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SignedMessage message.
             * @function verify
             * @memberof dmcn.message.SignedMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SignedMessage.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.plaintext != null && Object.hasOwnProperty.call(message, "plaintext")) {
                    let error = $root.dmcn.message.PlaintextMessage.verify(message.plaintext, long + 1);
                    if (error)
                        return "plaintext." + error;
                }
                if (message.senderSignature != null && Object.hasOwnProperty.call(message, "senderSignature"))
                    if (!(message.senderSignature && typeof message.senderSignature.length === "number" || $util.isString(message.senderSignature)))
                        return "senderSignature: buffer expected";
                return null;
            };

            /**
             * Creates a SignedMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.message.SignedMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.message.SignedMessage} SignedMessage
             */
            SignedMessage.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.message.SignedMessage)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.message.SignedMessage: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.message.SignedMessage();
                if (object.plaintext != null) {
                    if (!$util.isObject(object.plaintext))
                        throw TypeError(".dmcn.message.SignedMessage.plaintext: object expected");
                    message.plaintext = $root.dmcn.message.PlaintextMessage.fromObject(object.plaintext, long + 1);
                }
                if (object.senderSignature != null)
                    if (typeof object.senderSignature === "string")
                        $util.base64.decode(object.senderSignature, message.senderSignature = $util.newBuffer($util.base64.length(object.senderSignature)), 0);
                    else if (object.senderSignature.length >= 0)
                        message.senderSignature = object.senderSignature;
                return message;
            };

            /**
             * Creates a plain object from a SignedMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.message.SignedMessage
             * @static
             * @param {dmcn.message.SignedMessage} message SignedMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SignedMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.plaintext = null;
                    if (options.bytes === String)
                        object.senderSignature = "";
                    else {
                        object.senderSignature = [];
                        if (options.bytes !== Array)
                            object.senderSignature = $util.newBuffer(object.senderSignature);
                    }
                }
                if (message.plaintext != null && Object.hasOwnProperty.call(message, "plaintext"))
                    object.plaintext = $root.dmcn.message.PlaintextMessage.toObject(message.plaintext, options, q + 1);
                if (message.senderSignature != null && Object.hasOwnProperty.call(message, "senderSignature"))
                    object.senderSignature = options.bytes === String ? $util.base64.encode(message.senderSignature, 0, message.senderSignature.length) : options.bytes === Array ? Array.prototype.slice.call(message.senderSignature) : message.senderSignature;
                return object;
            };

            /**
             * Converts this SignedMessage to JSON.
             * @function toJSON
             * @memberof dmcn.message.SignedMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SignedMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for SignedMessage
             * @function getTypeUrl
             * @memberof dmcn.message.SignedMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            SignedMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.message.SignedMessage";
            };

            return SignedMessage;
        })();

        message.MessageHeader = (function() {

            /**
             * Properties of a MessageHeader.
             * @memberof dmcn.message
             * @interface IMessageHeader
             * @property {number|null} [version] MessageHeader version
             * @property {Uint8Array|null} [messageId] MessageHeader messageId
             * @property {Uint8Array|null} [threadId] MessageHeader threadId
             * @property {string|null} [senderAddress] MessageHeader senderAddress
             * @property {Uint8Array|null} [senderPublicKey] MessageHeader senderPublicKey
             * @property {string|null} [recipientAddress] MessageHeader recipientAddress
             * @property {number|Long|null} [sentAt] MessageHeader sentAt
             * @property {string|null} [subject] MessageHeader subject
             * @property {number|null} [attachmentCount] MessageHeader attachmentCount
             * @property {number|Long|null} [bodySize] MessageHeader bodySize
             * @property {string|null} [snippet] MessageHeader snippet
             * @property {Uint8Array|null} [replyToId] MessageHeader replyToId
             * @property {Uint8Array|null} [bodyHash] MessageHeader bodyHash
             * @property {Uint8Array|null} [bodyContentAddress] MessageHeader bodyContentAddress
             * @property {Array.<string>|null} [to] MessageHeader to
             * @property {Array.<string>|null} [cc] MessageHeader cc
             * @property {Array.<string>|null} [bcc] MessageHeader bcc
             */

            /**
             * Constructs a new MessageHeader.
             * @memberof dmcn.message
             * @classdesc Represents a MessageHeader.
             * @implements IMessageHeader
             * @constructor
             * @param {dmcn.message.IMessageHeader=} [properties] Properties to set
             */
            function MessageHeader(properties) {
                this.to = [];
                this.cc = [];
                this.bcc = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MessageHeader version.
             * @member {number} version
             * @memberof dmcn.message.MessageHeader
             * @instance
             */
            MessageHeader.prototype.version = 0;

            /**
             * MessageHeader messageId.
             * @member {Uint8Array} messageId
             * @memberof dmcn.message.MessageHeader
             * @instance
             */
            MessageHeader.prototype.messageId = $util.newBuffer([]);

            /**
             * MessageHeader threadId.
             * @member {Uint8Array} threadId
             * @memberof dmcn.message.MessageHeader
             * @instance
             */
            MessageHeader.prototype.threadId = $util.newBuffer([]);

            /**
             * MessageHeader senderAddress.
             * @member {string} senderAddress
             * @memberof dmcn.message.MessageHeader
             * @instance
             */
            MessageHeader.prototype.senderAddress = "";

            /**
             * MessageHeader senderPublicKey.
             * @member {Uint8Array} senderPublicKey
             * @memberof dmcn.message.MessageHeader
             * @instance
             */
            MessageHeader.prototype.senderPublicKey = $util.newBuffer([]);

            /**
             * MessageHeader recipientAddress.
             * @member {string} recipientAddress
             * @memberof dmcn.message.MessageHeader
             * @instance
             */
            MessageHeader.prototype.recipientAddress = "";

            /**
             * MessageHeader sentAt.
             * @member {number|Long} sentAt
             * @memberof dmcn.message.MessageHeader
             * @instance
             */
            MessageHeader.prototype.sentAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * MessageHeader subject.
             * @member {string} subject
             * @memberof dmcn.message.MessageHeader
             * @instance
             */
            MessageHeader.prototype.subject = "";

            /**
             * MessageHeader attachmentCount.
             * @member {number} attachmentCount
             * @memberof dmcn.message.MessageHeader
             * @instance
             */
            MessageHeader.prototype.attachmentCount = 0;

            /**
             * MessageHeader bodySize.
             * @member {number|Long} bodySize
             * @memberof dmcn.message.MessageHeader
             * @instance
             */
            MessageHeader.prototype.bodySize = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * MessageHeader snippet.
             * @member {string} snippet
             * @memberof dmcn.message.MessageHeader
             * @instance
             */
            MessageHeader.prototype.snippet = "";

            /**
             * MessageHeader replyToId.
             * @member {Uint8Array} replyToId
             * @memberof dmcn.message.MessageHeader
             * @instance
             */
            MessageHeader.prototype.replyToId = $util.newBuffer([]);

            /**
             * MessageHeader bodyHash.
             * @member {Uint8Array} bodyHash
             * @memberof dmcn.message.MessageHeader
             * @instance
             */
            MessageHeader.prototype.bodyHash = $util.newBuffer([]);

            /**
             * MessageHeader bodyContentAddress.
             * @member {Uint8Array} bodyContentAddress
             * @memberof dmcn.message.MessageHeader
             * @instance
             */
            MessageHeader.prototype.bodyContentAddress = $util.newBuffer([]);

            /**
             * MessageHeader to.
             * @member {Array.<string>} to
             * @memberof dmcn.message.MessageHeader
             * @instance
             */
            MessageHeader.prototype.to = $util.emptyArray;

            /**
             * MessageHeader cc.
             * @member {Array.<string>} cc
             * @memberof dmcn.message.MessageHeader
             * @instance
             */
            MessageHeader.prototype.cc = $util.emptyArray;

            /**
             * MessageHeader bcc.
             * @member {Array.<string>} bcc
             * @memberof dmcn.message.MessageHeader
             * @instance
             */
            MessageHeader.prototype.bcc = $util.emptyArray;

            /**
             * Creates a new MessageHeader instance using the specified properties.
             * @function create
             * @memberof dmcn.message.MessageHeader
             * @static
             * @param {dmcn.message.IMessageHeader=} [properties] Properties to set
             * @returns {dmcn.message.MessageHeader} MessageHeader instance
             */
            MessageHeader.create = function create(properties) {
                return new MessageHeader(properties);
            };

            /**
             * Encodes the specified MessageHeader message. Does not implicitly {@link dmcn.message.MessageHeader.verify|verify} messages.
             * @function encode
             * @memberof dmcn.message.MessageHeader
             * @static
             * @param {dmcn.message.IMessageHeader} message MessageHeader message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MessageHeader.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.version);
                if (message.messageId != null && Object.hasOwnProperty.call(message, "messageId"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.messageId);
                if (message.threadId != null && Object.hasOwnProperty.call(message, "threadId"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.threadId);
                if (message.senderAddress != null && Object.hasOwnProperty.call(message, "senderAddress"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.senderAddress);
                if (message.senderPublicKey != null && Object.hasOwnProperty.call(message, "senderPublicKey"))
                    writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.senderPublicKey);
                if (message.recipientAddress != null && Object.hasOwnProperty.call(message, "recipientAddress"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.recipientAddress);
                if (message.sentAt != null && Object.hasOwnProperty.call(message, "sentAt"))
                    writer.uint32(/* id 7, wireType 0 =*/56).int64(message.sentAt);
                if (message.subject != null && Object.hasOwnProperty.call(message, "subject"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.subject);
                if (message.attachmentCount != null && Object.hasOwnProperty.call(message, "attachmentCount"))
                    writer.uint32(/* id 9, wireType 0 =*/72).uint32(message.attachmentCount);
                if (message.bodySize != null && Object.hasOwnProperty.call(message, "bodySize"))
                    writer.uint32(/* id 10, wireType 0 =*/80).uint64(message.bodySize);
                if (message.snippet != null && Object.hasOwnProperty.call(message, "snippet"))
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.snippet);
                if (message.replyToId != null && Object.hasOwnProperty.call(message, "replyToId"))
                    writer.uint32(/* id 12, wireType 2 =*/98).bytes(message.replyToId);
                if (message.bodyHash != null && Object.hasOwnProperty.call(message, "bodyHash"))
                    writer.uint32(/* id 13, wireType 2 =*/106).bytes(message.bodyHash);
                if (message.bodyContentAddress != null && Object.hasOwnProperty.call(message, "bodyContentAddress"))
                    writer.uint32(/* id 14, wireType 2 =*/114).bytes(message.bodyContentAddress);
                if (message.to != null && message.to.length)
                    for (let i = 0; i < message.to.length; ++i)
                        writer.uint32(/* id 15, wireType 2 =*/122).string(message.to[i]);
                if (message.cc != null && message.cc.length)
                    for (let i = 0; i < message.cc.length; ++i)
                        writer.uint32(/* id 16, wireType 2 =*/130).string(message.cc[i]);
                if (message.bcc != null && message.bcc.length)
                    for (let i = 0; i < message.bcc.length; ++i)
                        writer.uint32(/* id 17, wireType 2 =*/138).string(message.bcc[i]);
                return writer;
            };

            /**
             * Encodes the specified MessageHeader message, length delimited. Does not implicitly {@link dmcn.message.MessageHeader.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.message.MessageHeader
             * @static
             * @param {dmcn.message.IMessageHeader} message MessageHeader message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MessageHeader.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a MessageHeader message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.message.MessageHeader
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.message.MessageHeader} MessageHeader
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MessageHeader.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.message.MessageHeader();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.version = reader.uint32();
                            break;
                        }
                    case 2: {
                            message.messageId = reader.bytes();
                            break;
                        }
                    case 3: {
                            message.threadId = reader.bytes();
                            break;
                        }
                    case 4: {
                            message.senderAddress = reader.string();
                            break;
                        }
                    case 5: {
                            message.senderPublicKey = reader.bytes();
                            break;
                        }
                    case 6: {
                            message.recipientAddress = reader.string();
                            break;
                        }
                    case 7: {
                            message.sentAt = reader.int64();
                            break;
                        }
                    case 8: {
                            message.subject = reader.string();
                            break;
                        }
                    case 9: {
                            message.attachmentCount = reader.uint32();
                            break;
                        }
                    case 10: {
                            message.bodySize = reader.uint64();
                            break;
                        }
                    case 11: {
                            message.snippet = reader.string();
                            break;
                        }
                    case 12: {
                            message.replyToId = reader.bytes();
                            break;
                        }
                    case 13: {
                            message.bodyHash = reader.bytes();
                            break;
                        }
                    case 14: {
                            message.bodyContentAddress = reader.bytes();
                            break;
                        }
                    case 15: {
                            if (!(message.to && message.to.length))
                                message.to = [];
                            message.to.push(reader.string());
                            break;
                        }
                    case 16: {
                            if (!(message.cc && message.cc.length))
                                message.cc = [];
                            message.cc.push(reader.string());
                            break;
                        }
                    case 17: {
                            if (!(message.bcc && message.bcc.length))
                                message.bcc = [];
                            message.bcc.push(reader.string());
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a MessageHeader message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.message.MessageHeader
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.message.MessageHeader} MessageHeader
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MessageHeader.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MessageHeader message.
             * @function verify
             * @memberof dmcn.message.MessageHeader
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MessageHeader.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    if (!$util.isInteger(message.version))
                        return "version: integer expected";
                if (message.messageId != null && Object.hasOwnProperty.call(message, "messageId"))
                    if (!(message.messageId && typeof message.messageId.length === "number" || $util.isString(message.messageId)))
                        return "messageId: buffer expected";
                if (message.threadId != null && Object.hasOwnProperty.call(message, "threadId"))
                    if (!(message.threadId && typeof message.threadId.length === "number" || $util.isString(message.threadId)))
                        return "threadId: buffer expected";
                if (message.senderAddress != null && Object.hasOwnProperty.call(message, "senderAddress"))
                    if (!$util.isString(message.senderAddress))
                        return "senderAddress: string expected";
                if (message.senderPublicKey != null && Object.hasOwnProperty.call(message, "senderPublicKey"))
                    if (!(message.senderPublicKey && typeof message.senderPublicKey.length === "number" || $util.isString(message.senderPublicKey)))
                        return "senderPublicKey: buffer expected";
                if (message.recipientAddress != null && Object.hasOwnProperty.call(message, "recipientAddress"))
                    if (!$util.isString(message.recipientAddress))
                        return "recipientAddress: string expected";
                if (message.sentAt != null && Object.hasOwnProperty.call(message, "sentAt"))
                    if (!$util.isInteger(message.sentAt) && !(message.sentAt && $util.isInteger(message.sentAt.low) && $util.isInteger(message.sentAt.high)))
                        return "sentAt: integer|Long expected";
                if (message.subject != null && Object.hasOwnProperty.call(message, "subject"))
                    if (!$util.isString(message.subject))
                        return "subject: string expected";
                if (message.attachmentCount != null && Object.hasOwnProperty.call(message, "attachmentCount"))
                    if (!$util.isInteger(message.attachmentCount))
                        return "attachmentCount: integer expected";
                if (message.bodySize != null && Object.hasOwnProperty.call(message, "bodySize"))
                    if (!$util.isInteger(message.bodySize) && !(message.bodySize && $util.isInteger(message.bodySize.low) && $util.isInteger(message.bodySize.high)))
                        return "bodySize: integer|Long expected";
                if (message.snippet != null && Object.hasOwnProperty.call(message, "snippet"))
                    if (!$util.isString(message.snippet))
                        return "snippet: string expected";
                if (message.replyToId != null && Object.hasOwnProperty.call(message, "replyToId"))
                    if (!(message.replyToId && typeof message.replyToId.length === "number" || $util.isString(message.replyToId)))
                        return "replyToId: buffer expected";
                if (message.bodyHash != null && Object.hasOwnProperty.call(message, "bodyHash"))
                    if (!(message.bodyHash && typeof message.bodyHash.length === "number" || $util.isString(message.bodyHash)))
                        return "bodyHash: buffer expected";
                if (message.bodyContentAddress != null && Object.hasOwnProperty.call(message, "bodyContentAddress"))
                    if (!(message.bodyContentAddress && typeof message.bodyContentAddress.length === "number" || $util.isString(message.bodyContentAddress)))
                        return "bodyContentAddress: buffer expected";
                if (message.to != null && Object.hasOwnProperty.call(message, "to")) {
                    if (!Array.isArray(message.to))
                        return "to: array expected";
                    for (let i = 0; i < message.to.length; ++i)
                        if (!$util.isString(message.to[i]))
                            return "to: string[] expected";
                }
                if (message.cc != null && Object.hasOwnProperty.call(message, "cc")) {
                    if (!Array.isArray(message.cc))
                        return "cc: array expected";
                    for (let i = 0; i < message.cc.length; ++i)
                        if (!$util.isString(message.cc[i]))
                            return "cc: string[] expected";
                }
                if (message.bcc != null && Object.hasOwnProperty.call(message, "bcc")) {
                    if (!Array.isArray(message.bcc))
                        return "bcc: array expected";
                    for (let i = 0; i < message.bcc.length; ++i)
                        if (!$util.isString(message.bcc[i]))
                            return "bcc: string[] expected";
                }
                return null;
            };

            /**
             * Creates a MessageHeader message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.message.MessageHeader
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.message.MessageHeader} MessageHeader
             */
            MessageHeader.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.message.MessageHeader)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.message.MessageHeader: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.message.MessageHeader();
                if (object.version != null)
                    message.version = object.version >>> 0;
                if (object.messageId != null)
                    if (typeof object.messageId === "string")
                        $util.base64.decode(object.messageId, message.messageId = $util.newBuffer($util.base64.length(object.messageId)), 0);
                    else if (object.messageId.length >= 0)
                        message.messageId = object.messageId;
                if (object.threadId != null)
                    if (typeof object.threadId === "string")
                        $util.base64.decode(object.threadId, message.threadId = $util.newBuffer($util.base64.length(object.threadId)), 0);
                    else if (object.threadId.length >= 0)
                        message.threadId = object.threadId;
                if (object.senderAddress != null)
                    message.senderAddress = String(object.senderAddress);
                if (object.senderPublicKey != null)
                    if (typeof object.senderPublicKey === "string")
                        $util.base64.decode(object.senderPublicKey, message.senderPublicKey = $util.newBuffer($util.base64.length(object.senderPublicKey)), 0);
                    else if (object.senderPublicKey.length >= 0)
                        message.senderPublicKey = object.senderPublicKey;
                if (object.recipientAddress != null)
                    message.recipientAddress = String(object.recipientAddress);
                if (object.sentAt != null)
                    if ($util.Long)
                        message.sentAt = $util.Long.fromValue(object.sentAt, false);
                    else if (typeof object.sentAt === "string")
                        message.sentAt = parseInt(object.sentAt, 10);
                    else if (typeof object.sentAt === "number")
                        message.sentAt = object.sentAt;
                    else if (typeof object.sentAt === "object")
                        message.sentAt = new $util.LongBits(object.sentAt.low >>> 0, object.sentAt.high >>> 0).toNumber();
                if (object.subject != null)
                    message.subject = String(object.subject);
                if (object.attachmentCount != null)
                    message.attachmentCount = object.attachmentCount >>> 0;
                if (object.bodySize != null)
                    if ($util.Long)
                        message.bodySize = $util.Long.fromValue(object.bodySize, true);
                    else if (typeof object.bodySize === "string")
                        message.bodySize = parseInt(object.bodySize, 10);
                    else if (typeof object.bodySize === "number")
                        message.bodySize = object.bodySize;
                    else if (typeof object.bodySize === "object")
                        message.bodySize = new $util.LongBits(object.bodySize.low >>> 0, object.bodySize.high >>> 0).toNumber(true);
                if (object.snippet != null)
                    message.snippet = String(object.snippet);
                if (object.replyToId != null)
                    if (typeof object.replyToId === "string")
                        $util.base64.decode(object.replyToId, message.replyToId = $util.newBuffer($util.base64.length(object.replyToId)), 0);
                    else if (object.replyToId.length >= 0)
                        message.replyToId = object.replyToId;
                if (object.bodyHash != null)
                    if (typeof object.bodyHash === "string")
                        $util.base64.decode(object.bodyHash, message.bodyHash = $util.newBuffer($util.base64.length(object.bodyHash)), 0);
                    else if (object.bodyHash.length >= 0)
                        message.bodyHash = object.bodyHash;
                if (object.bodyContentAddress != null)
                    if (typeof object.bodyContentAddress === "string")
                        $util.base64.decode(object.bodyContentAddress, message.bodyContentAddress = $util.newBuffer($util.base64.length(object.bodyContentAddress)), 0);
                    else if (object.bodyContentAddress.length >= 0)
                        message.bodyContentAddress = object.bodyContentAddress;
                if (object.to) {
                    if (!Array.isArray(object.to))
                        throw TypeError(".dmcn.message.MessageHeader.to: array expected");
                    message.to = [];
                    for (let i = 0; i < object.to.length; ++i)
                        message.to[i] = String(object.to[i]);
                }
                if (object.cc) {
                    if (!Array.isArray(object.cc))
                        throw TypeError(".dmcn.message.MessageHeader.cc: array expected");
                    message.cc = [];
                    for (let i = 0; i < object.cc.length; ++i)
                        message.cc[i] = String(object.cc[i]);
                }
                if (object.bcc) {
                    if (!Array.isArray(object.bcc))
                        throw TypeError(".dmcn.message.MessageHeader.bcc: array expected");
                    message.bcc = [];
                    for (let i = 0; i < object.bcc.length; ++i)
                        message.bcc[i] = String(object.bcc[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a MessageHeader message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.message.MessageHeader
             * @static
             * @param {dmcn.message.MessageHeader} message MessageHeader
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MessageHeader.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.to = [];
                    object.cc = [];
                    object.bcc = [];
                }
                if (options.defaults) {
                    object.version = 0;
                    if (options.bytes === String)
                        object.messageId = "";
                    else {
                        object.messageId = [];
                        if (options.bytes !== Array)
                            object.messageId = $util.newBuffer(object.messageId);
                    }
                    if (options.bytes === String)
                        object.threadId = "";
                    else {
                        object.threadId = [];
                        if (options.bytes !== Array)
                            object.threadId = $util.newBuffer(object.threadId);
                    }
                    object.senderAddress = "";
                    if (options.bytes === String)
                        object.senderPublicKey = "";
                    else {
                        object.senderPublicKey = [];
                        if (options.bytes !== Array)
                            object.senderPublicKey = $util.newBuffer(object.senderPublicKey);
                    }
                    object.recipientAddress = "";
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.sentAt = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.sentAt = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    object.subject = "";
                    object.attachmentCount = 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.bodySize = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.bodySize = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    object.snippet = "";
                    if (options.bytes === String)
                        object.replyToId = "";
                    else {
                        object.replyToId = [];
                        if (options.bytes !== Array)
                            object.replyToId = $util.newBuffer(object.replyToId);
                    }
                    if (options.bytes === String)
                        object.bodyHash = "";
                    else {
                        object.bodyHash = [];
                        if (options.bytes !== Array)
                            object.bodyHash = $util.newBuffer(object.bodyHash);
                    }
                    if (options.bytes === String)
                        object.bodyContentAddress = "";
                    else {
                        object.bodyContentAddress = [];
                        if (options.bytes !== Array)
                            object.bodyContentAddress = $util.newBuffer(object.bodyContentAddress);
                    }
                }
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    object.version = message.version;
                if (message.messageId != null && Object.hasOwnProperty.call(message, "messageId"))
                    object.messageId = options.bytes === String ? $util.base64.encode(message.messageId, 0, message.messageId.length) : options.bytes === Array ? Array.prototype.slice.call(message.messageId) : message.messageId;
                if (message.threadId != null && Object.hasOwnProperty.call(message, "threadId"))
                    object.threadId = options.bytes === String ? $util.base64.encode(message.threadId, 0, message.threadId.length) : options.bytes === Array ? Array.prototype.slice.call(message.threadId) : message.threadId;
                if (message.senderAddress != null && Object.hasOwnProperty.call(message, "senderAddress"))
                    object.senderAddress = message.senderAddress;
                if (message.senderPublicKey != null && Object.hasOwnProperty.call(message, "senderPublicKey"))
                    object.senderPublicKey = options.bytes === String ? $util.base64.encode(message.senderPublicKey, 0, message.senderPublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.senderPublicKey) : message.senderPublicKey;
                if (message.recipientAddress != null && Object.hasOwnProperty.call(message, "recipientAddress"))
                    object.recipientAddress = message.recipientAddress;
                if (message.sentAt != null && Object.hasOwnProperty.call(message, "sentAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.sentAt = typeof message.sentAt === "number" ? BigInt(message.sentAt) : $util.Long.fromBits(message.sentAt.low >>> 0, message.sentAt.high >>> 0, false).toBigInt();
                    else if (typeof message.sentAt === "number")
                        object.sentAt = options.longs === String ? String(message.sentAt) : message.sentAt;
                    else
                        object.sentAt = options.longs === String ? $util.Long.prototype.toString.call(message.sentAt) : options.longs === Number ? new $util.LongBits(message.sentAt.low >>> 0, message.sentAt.high >>> 0).toNumber() : message.sentAt;
                if (message.subject != null && Object.hasOwnProperty.call(message, "subject"))
                    object.subject = message.subject;
                if (message.attachmentCount != null && Object.hasOwnProperty.call(message, "attachmentCount"))
                    object.attachmentCount = message.attachmentCount;
                if (message.bodySize != null && Object.hasOwnProperty.call(message, "bodySize"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.bodySize = typeof message.bodySize === "number" ? BigInt(message.bodySize) : $util.Long.fromBits(message.bodySize.low >>> 0, message.bodySize.high >>> 0, true).toBigInt();
                    else if (typeof message.bodySize === "number")
                        object.bodySize = options.longs === String ? String(message.bodySize) : message.bodySize;
                    else
                        object.bodySize = options.longs === String ? $util.Long.prototype.toString.call(message.bodySize) : options.longs === Number ? new $util.LongBits(message.bodySize.low >>> 0, message.bodySize.high >>> 0).toNumber(true) : message.bodySize;
                if (message.snippet != null && Object.hasOwnProperty.call(message, "snippet"))
                    object.snippet = message.snippet;
                if (message.replyToId != null && Object.hasOwnProperty.call(message, "replyToId"))
                    object.replyToId = options.bytes === String ? $util.base64.encode(message.replyToId, 0, message.replyToId.length) : options.bytes === Array ? Array.prototype.slice.call(message.replyToId) : message.replyToId;
                if (message.bodyHash != null && Object.hasOwnProperty.call(message, "bodyHash"))
                    object.bodyHash = options.bytes === String ? $util.base64.encode(message.bodyHash, 0, message.bodyHash.length) : options.bytes === Array ? Array.prototype.slice.call(message.bodyHash) : message.bodyHash;
                if (message.bodyContentAddress != null && Object.hasOwnProperty.call(message, "bodyContentAddress"))
                    object.bodyContentAddress = options.bytes === String ? $util.base64.encode(message.bodyContentAddress, 0, message.bodyContentAddress.length) : options.bytes === Array ? Array.prototype.slice.call(message.bodyContentAddress) : message.bodyContentAddress;
                if (message.to && message.to.length) {
                    object.to = [];
                    for (let j = 0; j < message.to.length; ++j)
                        object.to[j] = message.to[j];
                }
                if (message.cc && message.cc.length) {
                    object.cc = [];
                    for (let j = 0; j < message.cc.length; ++j)
                        object.cc[j] = message.cc[j];
                }
                if (message.bcc && message.bcc.length) {
                    object.bcc = [];
                    for (let j = 0; j < message.bcc.length; ++j)
                        object.bcc[j] = message.bcc[j];
                }
                return object;
            };

            /**
             * Converts this MessageHeader to JSON.
             * @function toJSON
             * @memberof dmcn.message.MessageHeader
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MessageHeader.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MessageHeader
             * @function getTypeUrl
             * @memberof dmcn.message.MessageHeader
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MessageHeader.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.message.MessageHeader";
            };

            return MessageHeader;
        })();

        message.SignedHeader = (function() {

            /**
             * Properties of a SignedHeader.
             * @memberof dmcn.message
             * @interface ISignedHeader
             * @property {dmcn.message.IMessageHeader|null} [header] SignedHeader header
             * @property {Uint8Array|null} [senderSignature] SignedHeader senderSignature
             */

            /**
             * Constructs a new SignedHeader.
             * @memberof dmcn.message
             * @classdesc Represents a SignedHeader.
             * @implements ISignedHeader
             * @constructor
             * @param {dmcn.message.ISignedHeader=} [properties] Properties to set
             */
            function SignedHeader(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * SignedHeader header.
             * @member {dmcn.message.IMessageHeader|null|undefined} header
             * @memberof dmcn.message.SignedHeader
             * @instance
             */
            SignedHeader.prototype.header = null;

            /**
             * SignedHeader senderSignature.
             * @member {Uint8Array} senderSignature
             * @memberof dmcn.message.SignedHeader
             * @instance
             */
            SignedHeader.prototype.senderSignature = $util.newBuffer([]);

            /**
             * Creates a new SignedHeader instance using the specified properties.
             * @function create
             * @memberof dmcn.message.SignedHeader
             * @static
             * @param {dmcn.message.ISignedHeader=} [properties] Properties to set
             * @returns {dmcn.message.SignedHeader} SignedHeader instance
             */
            SignedHeader.create = function create(properties) {
                return new SignedHeader(properties);
            };

            /**
             * Encodes the specified SignedHeader message. Does not implicitly {@link dmcn.message.SignedHeader.verify|verify} messages.
             * @function encode
             * @memberof dmcn.message.SignedHeader
             * @static
             * @param {dmcn.message.ISignedHeader} message SignedHeader message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SignedHeader.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.header != null && Object.hasOwnProperty.call(message, "header"))
                    $root.dmcn.message.MessageHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.senderSignature != null && Object.hasOwnProperty.call(message, "senderSignature"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.senderSignature);
                return writer;
            };

            /**
             * Encodes the specified SignedHeader message, length delimited. Does not implicitly {@link dmcn.message.SignedHeader.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.message.SignedHeader
             * @static
             * @param {dmcn.message.ISignedHeader} message SignedHeader message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SignedHeader.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a SignedHeader message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.message.SignedHeader
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.message.SignedHeader} SignedHeader
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SignedHeader.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.message.SignedHeader();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.header = $root.dmcn.message.MessageHeader.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 2: {
                            message.senderSignature = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a SignedHeader message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.message.SignedHeader
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.message.SignedHeader} SignedHeader
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SignedHeader.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SignedHeader message.
             * @function verify
             * @memberof dmcn.message.SignedHeader
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SignedHeader.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.header != null && Object.hasOwnProperty.call(message, "header")) {
                    let error = $root.dmcn.message.MessageHeader.verify(message.header, long + 1);
                    if (error)
                        return "header." + error;
                }
                if (message.senderSignature != null && Object.hasOwnProperty.call(message, "senderSignature"))
                    if (!(message.senderSignature && typeof message.senderSignature.length === "number" || $util.isString(message.senderSignature)))
                        return "senderSignature: buffer expected";
                return null;
            };

            /**
             * Creates a SignedHeader message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.message.SignedHeader
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.message.SignedHeader} SignedHeader
             */
            SignedHeader.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.message.SignedHeader)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.message.SignedHeader: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.message.SignedHeader();
                if (object.header != null) {
                    if (!$util.isObject(object.header))
                        throw TypeError(".dmcn.message.SignedHeader.header: object expected");
                    message.header = $root.dmcn.message.MessageHeader.fromObject(object.header, long + 1);
                }
                if (object.senderSignature != null)
                    if (typeof object.senderSignature === "string")
                        $util.base64.decode(object.senderSignature, message.senderSignature = $util.newBuffer($util.base64.length(object.senderSignature)), 0);
                    else if (object.senderSignature.length >= 0)
                        message.senderSignature = object.senderSignature;
                return message;
            };

            /**
             * Creates a plain object from a SignedHeader message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.message.SignedHeader
             * @static
             * @param {dmcn.message.SignedHeader} message SignedHeader
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SignedHeader.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.header = null;
                    if (options.bytes === String)
                        object.senderSignature = "";
                    else {
                        object.senderSignature = [];
                        if (options.bytes !== Array)
                            object.senderSignature = $util.newBuffer(object.senderSignature);
                    }
                }
                if (message.header != null && Object.hasOwnProperty.call(message, "header"))
                    object.header = $root.dmcn.message.MessageHeader.toObject(message.header, options, q + 1);
                if (message.senderSignature != null && Object.hasOwnProperty.call(message, "senderSignature"))
                    object.senderSignature = options.bytes === String ? $util.base64.encode(message.senderSignature, 0, message.senderSignature.length) : options.bytes === Array ? Array.prototype.slice.call(message.senderSignature) : message.senderSignature;
                return object;
            };

            /**
             * Converts this SignedHeader to JSON.
             * @function toJSON
             * @memberof dmcn.message.SignedHeader
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SignedHeader.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for SignedHeader
             * @function getTypeUrl
             * @memberof dmcn.message.SignedHeader
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            SignedHeader.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.message.SignedHeader";
            };

            return SignedHeader;
        })();

        message.MessageContent = (function() {

            /**
             * Properties of a MessageContent.
             * @memberof dmcn.message
             * @interface IMessageContent
             * @property {dmcn.message.IMessageBody|null} [body] MessageContent body
             * @property {Array.<dmcn.message.IAttachmentRecord>|null} [attachments] MessageContent attachments
             */

            /**
             * Constructs a new MessageContent.
             * @memberof dmcn.message
             * @classdesc Represents a MessageContent.
             * @implements IMessageContent
             * @constructor
             * @param {dmcn.message.IMessageContent=} [properties] Properties to set
             */
            function MessageContent(properties) {
                this.attachments = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MessageContent body.
             * @member {dmcn.message.IMessageBody|null|undefined} body
             * @memberof dmcn.message.MessageContent
             * @instance
             */
            MessageContent.prototype.body = null;

            /**
             * MessageContent attachments.
             * @member {Array.<dmcn.message.IAttachmentRecord>} attachments
             * @memberof dmcn.message.MessageContent
             * @instance
             */
            MessageContent.prototype.attachments = $util.emptyArray;

            /**
             * Creates a new MessageContent instance using the specified properties.
             * @function create
             * @memberof dmcn.message.MessageContent
             * @static
             * @param {dmcn.message.IMessageContent=} [properties] Properties to set
             * @returns {dmcn.message.MessageContent} MessageContent instance
             */
            MessageContent.create = function create(properties) {
                return new MessageContent(properties);
            };

            /**
             * Encodes the specified MessageContent message. Does not implicitly {@link dmcn.message.MessageContent.verify|verify} messages.
             * @function encode
             * @memberof dmcn.message.MessageContent
             * @static
             * @param {dmcn.message.IMessageContent} message MessageContent message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MessageContent.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.body != null && Object.hasOwnProperty.call(message, "body"))
                    $root.dmcn.message.MessageBody.encode(message.body, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.attachments != null && message.attachments.length)
                    for (let i = 0; i < message.attachments.length; ++i)
                        $root.dmcn.message.AttachmentRecord.encode(message.attachments[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified MessageContent message, length delimited. Does not implicitly {@link dmcn.message.MessageContent.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.message.MessageContent
             * @static
             * @param {dmcn.message.IMessageContent} message MessageContent message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MessageContent.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a MessageContent message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.message.MessageContent
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.message.MessageContent} MessageContent
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MessageContent.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.message.MessageContent();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.body = $root.dmcn.message.MessageBody.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 2: {
                            if (!(message.attachments && message.attachments.length))
                                message.attachments = [];
                            message.attachments.push($root.dmcn.message.AttachmentRecord.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a MessageContent message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.message.MessageContent
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.message.MessageContent} MessageContent
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MessageContent.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MessageContent message.
             * @function verify
             * @memberof dmcn.message.MessageContent
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MessageContent.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.body != null && Object.hasOwnProperty.call(message, "body")) {
                    let error = $root.dmcn.message.MessageBody.verify(message.body, long + 1);
                    if (error)
                        return "body." + error;
                }
                if (message.attachments != null && Object.hasOwnProperty.call(message, "attachments")) {
                    if (!Array.isArray(message.attachments))
                        return "attachments: array expected";
                    for (let i = 0; i < message.attachments.length; ++i) {
                        let error = $root.dmcn.message.AttachmentRecord.verify(message.attachments[i], long + 1);
                        if (error)
                            return "attachments." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a MessageContent message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.message.MessageContent
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.message.MessageContent} MessageContent
             */
            MessageContent.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.message.MessageContent)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.message.MessageContent: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.message.MessageContent();
                if (object.body != null) {
                    if (!$util.isObject(object.body))
                        throw TypeError(".dmcn.message.MessageContent.body: object expected");
                    message.body = $root.dmcn.message.MessageBody.fromObject(object.body, long + 1);
                }
                if (object.attachments) {
                    if (!Array.isArray(object.attachments))
                        throw TypeError(".dmcn.message.MessageContent.attachments: array expected");
                    message.attachments = [];
                    for (let i = 0; i < object.attachments.length; ++i) {
                        if (!$util.isObject(object.attachments[i]))
                            throw TypeError(".dmcn.message.MessageContent.attachments: object expected");
                        message.attachments[i] = $root.dmcn.message.AttachmentRecord.fromObject(object.attachments[i], long + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a MessageContent message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.message.MessageContent
             * @static
             * @param {dmcn.message.MessageContent} message MessageContent
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MessageContent.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.attachments = [];
                if (options.defaults)
                    object.body = null;
                if (message.body != null && Object.hasOwnProperty.call(message, "body"))
                    object.body = $root.dmcn.message.MessageBody.toObject(message.body, options, q + 1);
                if (message.attachments && message.attachments.length) {
                    object.attachments = [];
                    for (let j = 0; j < message.attachments.length; ++j)
                        object.attachments[j] = $root.dmcn.message.AttachmentRecord.toObject(message.attachments[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this MessageContent to JSON.
             * @function toJSON
             * @memberof dmcn.message.MessageContent
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MessageContent.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MessageContent
             * @function getTypeUrl
             * @memberof dmcn.message.MessageContent
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MessageContent.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.message.MessageContent";
            };

            return MessageContent;
        })();

        message.RecipientRecord = (function() {

            /**
             * Properties of a RecipientRecord.
             * @memberof dmcn.message
             * @interface IRecipientRecord
             * @property {Uint8Array|null} [deviceId] RecipientRecord deviceId
             * @property {Uint8Array|null} [recipientXPub] RecipientRecord recipientXPub
             * @property {Uint8Array|null} [ephemeralXPub] RecipientRecord ephemeralXPub
             * @property {Uint8Array|null} [wrappedCek] RecipientRecord wrappedCek
             * @property {Uint8Array|null} [cekNonce] RecipientRecord cekNonce
             * @property {Uint8Array|null} [cekTag] RecipientRecord cekTag
             */

            /**
             * Constructs a new RecipientRecord.
             * @memberof dmcn.message
             * @classdesc Represents a RecipientRecord.
             * @implements IRecipientRecord
             * @constructor
             * @param {dmcn.message.IRecipientRecord=} [properties] Properties to set
             */
            function RecipientRecord(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * RecipientRecord deviceId.
             * @member {Uint8Array} deviceId
             * @memberof dmcn.message.RecipientRecord
             * @instance
             */
            RecipientRecord.prototype.deviceId = $util.newBuffer([]);

            /**
             * RecipientRecord recipientXPub.
             * @member {Uint8Array} recipientXPub
             * @memberof dmcn.message.RecipientRecord
             * @instance
             */
            RecipientRecord.prototype.recipientXPub = $util.newBuffer([]);

            /**
             * RecipientRecord ephemeralXPub.
             * @member {Uint8Array} ephemeralXPub
             * @memberof dmcn.message.RecipientRecord
             * @instance
             */
            RecipientRecord.prototype.ephemeralXPub = $util.newBuffer([]);

            /**
             * RecipientRecord wrappedCek.
             * @member {Uint8Array} wrappedCek
             * @memberof dmcn.message.RecipientRecord
             * @instance
             */
            RecipientRecord.prototype.wrappedCek = $util.newBuffer([]);

            /**
             * RecipientRecord cekNonce.
             * @member {Uint8Array} cekNonce
             * @memberof dmcn.message.RecipientRecord
             * @instance
             */
            RecipientRecord.prototype.cekNonce = $util.newBuffer([]);

            /**
             * RecipientRecord cekTag.
             * @member {Uint8Array} cekTag
             * @memberof dmcn.message.RecipientRecord
             * @instance
             */
            RecipientRecord.prototype.cekTag = $util.newBuffer([]);

            /**
             * Creates a new RecipientRecord instance using the specified properties.
             * @function create
             * @memberof dmcn.message.RecipientRecord
             * @static
             * @param {dmcn.message.IRecipientRecord=} [properties] Properties to set
             * @returns {dmcn.message.RecipientRecord} RecipientRecord instance
             */
            RecipientRecord.create = function create(properties) {
                return new RecipientRecord(properties);
            };

            /**
             * Encodes the specified RecipientRecord message. Does not implicitly {@link dmcn.message.RecipientRecord.verify|verify} messages.
             * @function encode
             * @memberof dmcn.message.RecipientRecord
             * @static
             * @param {dmcn.message.IRecipientRecord} message RecipientRecord message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RecipientRecord.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.deviceId);
                if (message.recipientXPub != null && Object.hasOwnProperty.call(message, "recipientXPub"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.recipientXPub);
                if (message.ephemeralXPub != null && Object.hasOwnProperty.call(message, "ephemeralXPub"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.ephemeralXPub);
                if (message.wrappedCek != null && Object.hasOwnProperty.call(message, "wrappedCek"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.wrappedCek);
                if (message.cekNonce != null && Object.hasOwnProperty.call(message, "cekNonce"))
                    writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.cekNonce);
                if (message.cekTag != null && Object.hasOwnProperty.call(message, "cekTag"))
                    writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.cekTag);
                return writer;
            };

            /**
             * Encodes the specified RecipientRecord message, length delimited. Does not implicitly {@link dmcn.message.RecipientRecord.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.message.RecipientRecord
             * @static
             * @param {dmcn.message.IRecipientRecord} message RecipientRecord message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RecipientRecord.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a RecipientRecord message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.message.RecipientRecord
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.message.RecipientRecord} RecipientRecord
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RecipientRecord.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.message.RecipientRecord();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.deviceId = reader.bytes();
                            break;
                        }
                    case 2: {
                            message.recipientXPub = reader.bytes();
                            break;
                        }
                    case 3: {
                            message.ephemeralXPub = reader.bytes();
                            break;
                        }
                    case 4: {
                            message.wrappedCek = reader.bytes();
                            break;
                        }
                    case 5: {
                            message.cekNonce = reader.bytes();
                            break;
                        }
                    case 6: {
                            message.cekTag = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a RecipientRecord message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.message.RecipientRecord
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.message.RecipientRecord} RecipientRecord
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RecipientRecord.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a RecipientRecord message.
             * @function verify
             * @memberof dmcn.message.RecipientRecord
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RecipientRecord.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                    if (!(message.deviceId && typeof message.deviceId.length === "number" || $util.isString(message.deviceId)))
                        return "deviceId: buffer expected";
                if (message.recipientXPub != null && Object.hasOwnProperty.call(message, "recipientXPub"))
                    if (!(message.recipientXPub && typeof message.recipientXPub.length === "number" || $util.isString(message.recipientXPub)))
                        return "recipientXPub: buffer expected";
                if (message.ephemeralXPub != null && Object.hasOwnProperty.call(message, "ephemeralXPub"))
                    if (!(message.ephemeralXPub && typeof message.ephemeralXPub.length === "number" || $util.isString(message.ephemeralXPub)))
                        return "ephemeralXPub: buffer expected";
                if (message.wrappedCek != null && Object.hasOwnProperty.call(message, "wrappedCek"))
                    if (!(message.wrappedCek && typeof message.wrappedCek.length === "number" || $util.isString(message.wrappedCek)))
                        return "wrappedCek: buffer expected";
                if (message.cekNonce != null && Object.hasOwnProperty.call(message, "cekNonce"))
                    if (!(message.cekNonce && typeof message.cekNonce.length === "number" || $util.isString(message.cekNonce)))
                        return "cekNonce: buffer expected";
                if (message.cekTag != null && Object.hasOwnProperty.call(message, "cekTag"))
                    if (!(message.cekTag && typeof message.cekTag.length === "number" || $util.isString(message.cekTag)))
                        return "cekTag: buffer expected";
                return null;
            };

            /**
             * Creates a RecipientRecord message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.message.RecipientRecord
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.message.RecipientRecord} RecipientRecord
             */
            RecipientRecord.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.message.RecipientRecord)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.message.RecipientRecord: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.message.RecipientRecord();
                if (object.deviceId != null)
                    if (typeof object.deviceId === "string")
                        $util.base64.decode(object.deviceId, message.deviceId = $util.newBuffer($util.base64.length(object.deviceId)), 0);
                    else if (object.deviceId.length >= 0)
                        message.deviceId = object.deviceId;
                if (object.recipientXPub != null)
                    if (typeof object.recipientXPub === "string")
                        $util.base64.decode(object.recipientXPub, message.recipientXPub = $util.newBuffer($util.base64.length(object.recipientXPub)), 0);
                    else if (object.recipientXPub.length >= 0)
                        message.recipientXPub = object.recipientXPub;
                if (object.ephemeralXPub != null)
                    if (typeof object.ephemeralXPub === "string")
                        $util.base64.decode(object.ephemeralXPub, message.ephemeralXPub = $util.newBuffer($util.base64.length(object.ephemeralXPub)), 0);
                    else if (object.ephemeralXPub.length >= 0)
                        message.ephemeralXPub = object.ephemeralXPub;
                if (object.wrappedCek != null)
                    if (typeof object.wrappedCek === "string")
                        $util.base64.decode(object.wrappedCek, message.wrappedCek = $util.newBuffer($util.base64.length(object.wrappedCek)), 0);
                    else if (object.wrappedCek.length >= 0)
                        message.wrappedCek = object.wrappedCek;
                if (object.cekNonce != null)
                    if (typeof object.cekNonce === "string")
                        $util.base64.decode(object.cekNonce, message.cekNonce = $util.newBuffer($util.base64.length(object.cekNonce)), 0);
                    else if (object.cekNonce.length >= 0)
                        message.cekNonce = object.cekNonce;
                if (object.cekTag != null)
                    if (typeof object.cekTag === "string")
                        $util.base64.decode(object.cekTag, message.cekTag = $util.newBuffer($util.base64.length(object.cekTag)), 0);
                    else if (object.cekTag.length >= 0)
                        message.cekTag = object.cekTag;
                return message;
            };

            /**
             * Creates a plain object from a RecipientRecord message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.message.RecipientRecord
             * @static
             * @param {dmcn.message.RecipientRecord} message RecipientRecord
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RecipientRecord.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    if (options.bytes === String)
                        object.deviceId = "";
                    else {
                        object.deviceId = [];
                        if (options.bytes !== Array)
                            object.deviceId = $util.newBuffer(object.deviceId);
                    }
                    if (options.bytes === String)
                        object.recipientXPub = "";
                    else {
                        object.recipientXPub = [];
                        if (options.bytes !== Array)
                            object.recipientXPub = $util.newBuffer(object.recipientXPub);
                    }
                    if (options.bytes === String)
                        object.ephemeralXPub = "";
                    else {
                        object.ephemeralXPub = [];
                        if (options.bytes !== Array)
                            object.ephemeralXPub = $util.newBuffer(object.ephemeralXPub);
                    }
                    if (options.bytes === String)
                        object.wrappedCek = "";
                    else {
                        object.wrappedCek = [];
                        if (options.bytes !== Array)
                            object.wrappedCek = $util.newBuffer(object.wrappedCek);
                    }
                    if (options.bytes === String)
                        object.cekNonce = "";
                    else {
                        object.cekNonce = [];
                        if (options.bytes !== Array)
                            object.cekNonce = $util.newBuffer(object.cekNonce);
                    }
                    if (options.bytes === String)
                        object.cekTag = "";
                    else {
                        object.cekTag = [];
                        if (options.bytes !== Array)
                            object.cekTag = $util.newBuffer(object.cekTag);
                    }
                }
                if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                    object.deviceId = options.bytes === String ? $util.base64.encode(message.deviceId, 0, message.deviceId.length) : options.bytes === Array ? Array.prototype.slice.call(message.deviceId) : message.deviceId;
                if (message.recipientXPub != null && Object.hasOwnProperty.call(message, "recipientXPub"))
                    object.recipientXPub = options.bytes === String ? $util.base64.encode(message.recipientXPub, 0, message.recipientXPub.length) : options.bytes === Array ? Array.prototype.slice.call(message.recipientXPub) : message.recipientXPub;
                if (message.ephemeralXPub != null && Object.hasOwnProperty.call(message, "ephemeralXPub"))
                    object.ephemeralXPub = options.bytes === String ? $util.base64.encode(message.ephemeralXPub, 0, message.ephemeralXPub.length) : options.bytes === Array ? Array.prototype.slice.call(message.ephemeralXPub) : message.ephemeralXPub;
                if (message.wrappedCek != null && Object.hasOwnProperty.call(message, "wrappedCek"))
                    object.wrappedCek = options.bytes === String ? $util.base64.encode(message.wrappedCek, 0, message.wrappedCek.length) : options.bytes === Array ? Array.prototype.slice.call(message.wrappedCek) : message.wrappedCek;
                if (message.cekNonce != null && Object.hasOwnProperty.call(message, "cekNonce"))
                    object.cekNonce = options.bytes === String ? $util.base64.encode(message.cekNonce, 0, message.cekNonce.length) : options.bytes === Array ? Array.prototype.slice.call(message.cekNonce) : message.cekNonce;
                if (message.cekTag != null && Object.hasOwnProperty.call(message, "cekTag"))
                    object.cekTag = options.bytes === String ? $util.base64.encode(message.cekTag, 0, message.cekTag.length) : options.bytes === Array ? Array.prototype.slice.call(message.cekTag) : message.cekTag;
                return object;
            };

            /**
             * Converts this RecipientRecord to JSON.
             * @function toJSON
             * @memberof dmcn.message.RecipientRecord
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RecipientRecord.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for RecipientRecord
             * @function getTypeUrl
             * @memberof dmcn.message.RecipientRecord
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            RecipientRecord.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.message.RecipientRecord";
            };

            return RecipientRecord;
        })();

        message.EncryptedEnvelope = (function() {

            /**
             * Properties of an EncryptedEnvelope.
             * @memberof dmcn.message
             * @interface IEncryptedEnvelope
             * @property {number|null} [version] EncryptedEnvelope version
             * @property {Uint8Array|null} [messageId] EncryptedEnvelope messageId
             * @property {Array.<dmcn.message.IRecipientRecord>|null} [recipients] EncryptedEnvelope recipients
             * @property {Uint8Array|null} [encryptedPayload] EncryptedEnvelope encryptedPayload
             * @property {Uint8Array|null} [payloadNonce] EncryptedEnvelope payloadNonce
             * @property {Uint8Array|null} [payloadTag] EncryptedEnvelope payloadTag
             * @property {number|null} [payloadSizeClass] EncryptedEnvelope payloadSizeClass
             * @property {number|Long|null} [createdAt] EncryptedEnvelope createdAt
             * @property {Uint8Array|null} [ratchetPubKey] EncryptedEnvelope ratchetPubKey
             * @property {Uint8Array|null} [encryptedHeader] EncryptedEnvelope encryptedHeader
             * @property {Uint8Array|null} [headerNonce] EncryptedEnvelope headerNonce
             * @property {Uint8Array|null} [headerTag] EncryptedEnvelope headerTag
             * @property {number|null} [headerSizeClass] EncryptedEnvelope headerSizeClass
             * @property {Uint8Array|null} [encryptedBody] EncryptedEnvelope encryptedBody
             * @property {Uint8Array|null} [bodyNonce] EncryptedEnvelope bodyNonce
             * @property {Uint8Array|null} [bodyTag] EncryptedEnvelope bodyTag
             * @property {number|null} [bodySizeClass] EncryptedEnvelope bodySizeClass
             * @property {Uint8Array|null} [bodyContentAddress] EncryptedEnvelope bodyContentAddress
             */

            /**
             * Constructs a new EncryptedEnvelope.
             * @memberof dmcn.message
             * @classdesc Represents an EncryptedEnvelope.
             * @implements IEncryptedEnvelope
             * @constructor
             * @param {dmcn.message.IEncryptedEnvelope=} [properties] Properties to set
             */
            function EncryptedEnvelope(properties) {
                this.recipients = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * EncryptedEnvelope version.
             * @member {number} version
             * @memberof dmcn.message.EncryptedEnvelope
             * @instance
             */
            EncryptedEnvelope.prototype.version = 0;

            /**
             * EncryptedEnvelope messageId.
             * @member {Uint8Array} messageId
             * @memberof dmcn.message.EncryptedEnvelope
             * @instance
             */
            EncryptedEnvelope.prototype.messageId = $util.newBuffer([]);

            /**
             * EncryptedEnvelope recipients.
             * @member {Array.<dmcn.message.IRecipientRecord>} recipients
             * @memberof dmcn.message.EncryptedEnvelope
             * @instance
             */
            EncryptedEnvelope.prototype.recipients = $util.emptyArray;

            /**
             * EncryptedEnvelope encryptedPayload.
             * @member {Uint8Array} encryptedPayload
             * @memberof dmcn.message.EncryptedEnvelope
             * @instance
             */
            EncryptedEnvelope.prototype.encryptedPayload = $util.newBuffer([]);

            /**
             * EncryptedEnvelope payloadNonce.
             * @member {Uint8Array} payloadNonce
             * @memberof dmcn.message.EncryptedEnvelope
             * @instance
             */
            EncryptedEnvelope.prototype.payloadNonce = $util.newBuffer([]);

            /**
             * EncryptedEnvelope payloadTag.
             * @member {Uint8Array} payloadTag
             * @memberof dmcn.message.EncryptedEnvelope
             * @instance
             */
            EncryptedEnvelope.prototype.payloadTag = $util.newBuffer([]);

            /**
             * EncryptedEnvelope payloadSizeClass.
             * @member {number} payloadSizeClass
             * @memberof dmcn.message.EncryptedEnvelope
             * @instance
             */
            EncryptedEnvelope.prototype.payloadSizeClass = 0;

            /**
             * EncryptedEnvelope createdAt.
             * @member {number|Long} createdAt
             * @memberof dmcn.message.EncryptedEnvelope
             * @instance
             */
            EncryptedEnvelope.prototype.createdAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * EncryptedEnvelope ratchetPubKey.
             * @member {Uint8Array} ratchetPubKey
             * @memberof dmcn.message.EncryptedEnvelope
             * @instance
             */
            EncryptedEnvelope.prototype.ratchetPubKey = $util.newBuffer([]);

            /**
             * EncryptedEnvelope encryptedHeader.
             * @member {Uint8Array} encryptedHeader
             * @memberof dmcn.message.EncryptedEnvelope
             * @instance
             */
            EncryptedEnvelope.prototype.encryptedHeader = $util.newBuffer([]);

            /**
             * EncryptedEnvelope headerNonce.
             * @member {Uint8Array} headerNonce
             * @memberof dmcn.message.EncryptedEnvelope
             * @instance
             */
            EncryptedEnvelope.prototype.headerNonce = $util.newBuffer([]);

            /**
             * EncryptedEnvelope headerTag.
             * @member {Uint8Array} headerTag
             * @memberof dmcn.message.EncryptedEnvelope
             * @instance
             */
            EncryptedEnvelope.prototype.headerTag = $util.newBuffer([]);

            /**
             * EncryptedEnvelope headerSizeClass.
             * @member {number} headerSizeClass
             * @memberof dmcn.message.EncryptedEnvelope
             * @instance
             */
            EncryptedEnvelope.prototype.headerSizeClass = 0;

            /**
             * EncryptedEnvelope encryptedBody.
             * @member {Uint8Array} encryptedBody
             * @memberof dmcn.message.EncryptedEnvelope
             * @instance
             */
            EncryptedEnvelope.prototype.encryptedBody = $util.newBuffer([]);

            /**
             * EncryptedEnvelope bodyNonce.
             * @member {Uint8Array} bodyNonce
             * @memberof dmcn.message.EncryptedEnvelope
             * @instance
             */
            EncryptedEnvelope.prototype.bodyNonce = $util.newBuffer([]);

            /**
             * EncryptedEnvelope bodyTag.
             * @member {Uint8Array} bodyTag
             * @memberof dmcn.message.EncryptedEnvelope
             * @instance
             */
            EncryptedEnvelope.prototype.bodyTag = $util.newBuffer([]);

            /**
             * EncryptedEnvelope bodySizeClass.
             * @member {number} bodySizeClass
             * @memberof dmcn.message.EncryptedEnvelope
             * @instance
             */
            EncryptedEnvelope.prototype.bodySizeClass = 0;

            /**
             * EncryptedEnvelope bodyContentAddress.
             * @member {Uint8Array} bodyContentAddress
             * @memberof dmcn.message.EncryptedEnvelope
             * @instance
             */
            EncryptedEnvelope.prototype.bodyContentAddress = $util.newBuffer([]);

            /**
             * Creates a new EncryptedEnvelope instance using the specified properties.
             * @function create
             * @memberof dmcn.message.EncryptedEnvelope
             * @static
             * @param {dmcn.message.IEncryptedEnvelope=} [properties] Properties to set
             * @returns {dmcn.message.EncryptedEnvelope} EncryptedEnvelope instance
             */
            EncryptedEnvelope.create = function create(properties) {
                return new EncryptedEnvelope(properties);
            };

            /**
             * Encodes the specified EncryptedEnvelope message. Does not implicitly {@link dmcn.message.EncryptedEnvelope.verify|verify} messages.
             * @function encode
             * @memberof dmcn.message.EncryptedEnvelope
             * @static
             * @param {dmcn.message.IEncryptedEnvelope} message EncryptedEnvelope message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EncryptedEnvelope.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.version);
                if (message.messageId != null && Object.hasOwnProperty.call(message, "messageId"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.messageId);
                if (message.recipients != null && message.recipients.length)
                    for (let i = 0; i < message.recipients.length; ++i)
                        $root.dmcn.message.RecipientRecord.encode(message.recipients[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.encryptedPayload != null && Object.hasOwnProperty.call(message, "encryptedPayload"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.encryptedPayload);
                if (message.payloadNonce != null && Object.hasOwnProperty.call(message, "payloadNonce"))
                    writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.payloadNonce);
                if (message.payloadTag != null && Object.hasOwnProperty.call(message, "payloadTag"))
                    writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.payloadTag);
                if (message.payloadSizeClass != null && Object.hasOwnProperty.call(message, "payloadSizeClass"))
                    writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.payloadSizeClass);
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    writer.uint32(/* id 8, wireType 0 =*/64).int64(message.createdAt);
                if (message.ratchetPubKey != null && Object.hasOwnProperty.call(message, "ratchetPubKey"))
                    writer.uint32(/* id 9, wireType 2 =*/74).bytes(message.ratchetPubKey);
                if (message.encryptedHeader != null && Object.hasOwnProperty.call(message, "encryptedHeader"))
                    writer.uint32(/* id 10, wireType 2 =*/82).bytes(message.encryptedHeader);
                if (message.headerNonce != null && Object.hasOwnProperty.call(message, "headerNonce"))
                    writer.uint32(/* id 11, wireType 2 =*/90).bytes(message.headerNonce);
                if (message.headerTag != null && Object.hasOwnProperty.call(message, "headerTag"))
                    writer.uint32(/* id 12, wireType 2 =*/98).bytes(message.headerTag);
                if (message.headerSizeClass != null && Object.hasOwnProperty.call(message, "headerSizeClass"))
                    writer.uint32(/* id 13, wireType 0 =*/104).uint32(message.headerSizeClass);
                if (message.encryptedBody != null && Object.hasOwnProperty.call(message, "encryptedBody"))
                    writer.uint32(/* id 14, wireType 2 =*/114).bytes(message.encryptedBody);
                if (message.bodyNonce != null && Object.hasOwnProperty.call(message, "bodyNonce"))
                    writer.uint32(/* id 15, wireType 2 =*/122).bytes(message.bodyNonce);
                if (message.bodyTag != null && Object.hasOwnProperty.call(message, "bodyTag"))
                    writer.uint32(/* id 16, wireType 2 =*/130).bytes(message.bodyTag);
                if (message.bodySizeClass != null && Object.hasOwnProperty.call(message, "bodySizeClass"))
                    writer.uint32(/* id 17, wireType 0 =*/136).uint32(message.bodySizeClass);
                if (message.bodyContentAddress != null && Object.hasOwnProperty.call(message, "bodyContentAddress"))
                    writer.uint32(/* id 18, wireType 2 =*/146).bytes(message.bodyContentAddress);
                return writer;
            };

            /**
             * Encodes the specified EncryptedEnvelope message, length delimited. Does not implicitly {@link dmcn.message.EncryptedEnvelope.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.message.EncryptedEnvelope
             * @static
             * @param {dmcn.message.IEncryptedEnvelope} message EncryptedEnvelope message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EncryptedEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes an EncryptedEnvelope message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.message.EncryptedEnvelope
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.message.EncryptedEnvelope} EncryptedEnvelope
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EncryptedEnvelope.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.message.EncryptedEnvelope();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.version = reader.uint32();
                            break;
                        }
                    case 2: {
                            message.messageId = reader.bytes();
                            break;
                        }
                    case 3: {
                            if (!(message.recipients && message.recipients.length))
                                message.recipients = [];
                            message.recipients.push($root.dmcn.message.RecipientRecord.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 4: {
                            message.encryptedPayload = reader.bytes();
                            break;
                        }
                    case 5: {
                            message.payloadNonce = reader.bytes();
                            break;
                        }
                    case 6: {
                            message.payloadTag = reader.bytes();
                            break;
                        }
                    case 7: {
                            message.payloadSizeClass = reader.uint32();
                            break;
                        }
                    case 8: {
                            message.createdAt = reader.int64();
                            break;
                        }
                    case 9: {
                            message.ratchetPubKey = reader.bytes();
                            break;
                        }
                    case 10: {
                            message.encryptedHeader = reader.bytes();
                            break;
                        }
                    case 11: {
                            message.headerNonce = reader.bytes();
                            break;
                        }
                    case 12: {
                            message.headerTag = reader.bytes();
                            break;
                        }
                    case 13: {
                            message.headerSizeClass = reader.uint32();
                            break;
                        }
                    case 14: {
                            message.encryptedBody = reader.bytes();
                            break;
                        }
                    case 15: {
                            message.bodyNonce = reader.bytes();
                            break;
                        }
                    case 16: {
                            message.bodyTag = reader.bytes();
                            break;
                        }
                    case 17: {
                            message.bodySizeClass = reader.uint32();
                            break;
                        }
                    case 18: {
                            message.bodyContentAddress = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an EncryptedEnvelope message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.message.EncryptedEnvelope
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.message.EncryptedEnvelope} EncryptedEnvelope
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EncryptedEnvelope.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an EncryptedEnvelope message.
             * @function verify
             * @memberof dmcn.message.EncryptedEnvelope
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            EncryptedEnvelope.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    if (!$util.isInteger(message.version))
                        return "version: integer expected";
                if (message.messageId != null && Object.hasOwnProperty.call(message, "messageId"))
                    if (!(message.messageId && typeof message.messageId.length === "number" || $util.isString(message.messageId)))
                        return "messageId: buffer expected";
                if (message.recipients != null && Object.hasOwnProperty.call(message, "recipients")) {
                    if (!Array.isArray(message.recipients))
                        return "recipients: array expected";
                    for (let i = 0; i < message.recipients.length; ++i) {
                        let error = $root.dmcn.message.RecipientRecord.verify(message.recipients[i], long + 1);
                        if (error)
                            return "recipients." + error;
                    }
                }
                if (message.encryptedPayload != null && Object.hasOwnProperty.call(message, "encryptedPayload"))
                    if (!(message.encryptedPayload && typeof message.encryptedPayload.length === "number" || $util.isString(message.encryptedPayload)))
                        return "encryptedPayload: buffer expected";
                if (message.payloadNonce != null && Object.hasOwnProperty.call(message, "payloadNonce"))
                    if (!(message.payloadNonce && typeof message.payloadNonce.length === "number" || $util.isString(message.payloadNonce)))
                        return "payloadNonce: buffer expected";
                if (message.payloadTag != null && Object.hasOwnProperty.call(message, "payloadTag"))
                    if (!(message.payloadTag && typeof message.payloadTag.length === "number" || $util.isString(message.payloadTag)))
                        return "payloadTag: buffer expected";
                if (message.payloadSizeClass != null && Object.hasOwnProperty.call(message, "payloadSizeClass"))
                    if (!$util.isInteger(message.payloadSizeClass))
                        return "payloadSizeClass: integer expected";
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
                        return "createdAt: integer|Long expected";
                if (message.ratchetPubKey != null && Object.hasOwnProperty.call(message, "ratchetPubKey"))
                    if (!(message.ratchetPubKey && typeof message.ratchetPubKey.length === "number" || $util.isString(message.ratchetPubKey)))
                        return "ratchetPubKey: buffer expected";
                if (message.encryptedHeader != null && Object.hasOwnProperty.call(message, "encryptedHeader"))
                    if (!(message.encryptedHeader && typeof message.encryptedHeader.length === "number" || $util.isString(message.encryptedHeader)))
                        return "encryptedHeader: buffer expected";
                if (message.headerNonce != null && Object.hasOwnProperty.call(message, "headerNonce"))
                    if (!(message.headerNonce && typeof message.headerNonce.length === "number" || $util.isString(message.headerNonce)))
                        return "headerNonce: buffer expected";
                if (message.headerTag != null && Object.hasOwnProperty.call(message, "headerTag"))
                    if (!(message.headerTag && typeof message.headerTag.length === "number" || $util.isString(message.headerTag)))
                        return "headerTag: buffer expected";
                if (message.headerSizeClass != null && Object.hasOwnProperty.call(message, "headerSizeClass"))
                    if (!$util.isInteger(message.headerSizeClass))
                        return "headerSizeClass: integer expected";
                if (message.encryptedBody != null && Object.hasOwnProperty.call(message, "encryptedBody"))
                    if (!(message.encryptedBody && typeof message.encryptedBody.length === "number" || $util.isString(message.encryptedBody)))
                        return "encryptedBody: buffer expected";
                if (message.bodyNonce != null && Object.hasOwnProperty.call(message, "bodyNonce"))
                    if (!(message.bodyNonce && typeof message.bodyNonce.length === "number" || $util.isString(message.bodyNonce)))
                        return "bodyNonce: buffer expected";
                if (message.bodyTag != null && Object.hasOwnProperty.call(message, "bodyTag"))
                    if (!(message.bodyTag && typeof message.bodyTag.length === "number" || $util.isString(message.bodyTag)))
                        return "bodyTag: buffer expected";
                if (message.bodySizeClass != null && Object.hasOwnProperty.call(message, "bodySizeClass"))
                    if (!$util.isInteger(message.bodySizeClass))
                        return "bodySizeClass: integer expected";
                if (message.bodyContentAddress != null && Object.hasOwnProperty.call(message, "bodyContentAddress"))
                    if (!(message.bodyContentAddress && typeof message.bodyContentAddress.length === "number" || $util.isString(message.bodyContentAddress)))
                        return "bodyContentAddress: buffer expected";
                return null;
            };

            /**
             * Creates an EncryptedEnvelope message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.message.EncryptedEnvelope
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.message.EncryptedEnvelope} EncryptedEnvelope
             */
            EncryptedEnvelope.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.message.EncryptedEnvelope)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.message.EncryptedEnvelope: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.message.EncryptedEnvelope();
                if (object.version != null)
                    message.version = object.version >>> 0;
                if (object.messageId != null)
                    if (typeof object.messageId === "string")
                        $util.base64.decode(object.messageId, message.messageId = $util.newBuffer($util.base64.length(object.messageId)), 0);
                    else if (object.messageId.length >= 0)
                        message.messageId = object.messageId;
                if (object.recipients) {
                    if (!Array.isArray(object.recipients))
                        throw TypeError(".dmcn.message.EncryptedEnvelope.recipients: array expected");
                    message.recipients = [];
                    for (let i = 0; i < object.recipients.length; ++i) {
                        if (!$util.isObject(object.recipients[i]))
                            throw TypeError(".dmcn.message.EncryptedEnvelope.recipients: object expected");
                        message.recipients[i] = $root.dmcn.message.RecipientRecord.fromObject(object.recipients[i], long + 1);
                    }
                }
                if (object.encryptedPayload != null)
                    if (typeof object.encryptedPayload === "string")
                        $util.base64.decode(object.encryptedPayload, message.encryptedPayload = $util.newBuffer($util.base64.length(object.encryptedPayload)), 0);
                    else if (object.encryptedPayload.length >= 0)
                        message.encryptedPayload = object.encryptedPayload;
                if (object.payloadNonce != null)
                    if (typeof object.payloadNonce === "string")
                        $util.base64.decode(object.payloadNonce, message.payloadNonce = $util.newBuffer($util.base64.length(object.payloadNonce)), 0);
                    else if (object.payloadNonce.length >= 0)
                        message.payloadNonce = object.payloadNonce;
                if (object.payloadTag != null)
                    if (typeof object.payloadTag === "string")
                        $util.base64.decode(object.payloadTag, message.payloadTag = $util.newBuffer($util.base64.length(object.payloadTag)), 0);
                    else if (object.payloadTag.length >= 0)
                        message.payloadTag = object.payloadTag;
                if (object.payloadSizeClass != null)
                    message.payloadSizeClass = object.payloadSizeClass >>> 0;
                if (object.createdAt != null)
                    if ($util.Long)
                        message.createdAt = $util.Long.fromValue(object.createdAt, false);
                    else if (typeof object.createdAt === "string")
                        message.createdAt = parseInt(object.createdAt, 10);
                    else if (typeof object.createdAt === "number")
                        message.createdAt = object.createdAt;
                    else if (typeof object.createdAt === "object")
                        message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
                if (object.ratchetPubKey != null)
                    if (typeof object.ratchetPubKey === "string")
                        $util.base64.decode(object.ratchetPubKey, message.ratchetPubKey = $util.newBuffer($util.base64.length(object.ratchetPubKey)), 0);
                    else if (object.ratchetPubKey.length >= 0)
                        message.ratchetPubKey = object.ratchetPubKey;
                if (object.encryptedHeader != null)
                    if (typeof object.encryptedHeader === "string")
                        $util.base64.decode(object.encryptedHeader, message.encryptedHeader = $util.newBuffer($util.base64.length(object.encryptedHeader)), 0);
                    else if (object.encryptedHeader.length >= 0)
                        message.encryptedHeader = object.encryptedHeader;
                if (object.headerNonce != null)
                    if (typeof object.headerNonce === "string")
                        $util.base64.decode(object.headerNonce, message.headerNonce = $util.newBuffer($util.base64.length(object.headerNonce)), 0);
                    else if (object.headerNonce.length >= 0)
                        message.headerNonce = object.headerNonce;
                if (object.headerTag != null)
                    if (typeof object.headerTag === "string")
                        $util.base64.decode(object.headerTag, message.headerTag = $util.newBuffer($util.base64.length(object.headerTag)), 0);
                    else if (object.headerTag.length >= 0)
                        message.headerTag = object.headerTag;
                if (object.headerSizeClass != null)
                    message.headerSizeClass = object.headerSizeClass >>> 0;
                if (object.encryptedBody != null)
                    if (typeof object.encryptedBody === "string")
                        $util.base64.decode(object.encryptedBody, message.encryptedBody = $util.newBuffer($util.base64.length(object.encryptedBody)), 0);
                    else if (object.encryptedBody.length >= 0)
                        message.encryptedBody = object.encryptedBody;
                if (object.bodyNonce != null)
                    if (typeof object.bodyNonce === "string")
                        $util.base64.decode(object.bodyNonce, message.bodyNonce = $util.newBuffer($util.base64.length(object.bodyNonce)), 0);
                    else if (object.bodyNonce.length >= 0)
                        message.bodyNonce = object.bodyNonce;
                if (object.bodyTag != null)
                    if (typeof object.bodyTag === "string")
                        $util.base64.decode(object.bodyTag, message.bodyTag = $util.newBuffer($util.base64.length(object.bodyTag)), 0);
                    else if (object.bodyTag.length >= 0)
                        message.bodyTag = object.bodyTag;
                if (object.bodySizeClass != null)
                    message.bodySizeClass = object.bodySizeClass >>> 0;
                if (object.bodyContentAddress != null)
                    if (typeof object.bodyContentAddress === "string")
                        $util.base64.decode(object.bodyContentAddress, message.bodyContentAddress = $util.newBuffer($util.base64.length(object.bodyContentAddress)), 0);
                    else if (object.bodyContentAddress.length >= 0)
                        message.bodyContentAddress = object.bodyContentAddress;
                return message;
            };

            /**
             * Creates a plain object from an EncryptedEnvelope message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.message.EncryptedEnvelope
             * @static
             * @param {dmcn.message.EncryptedEnvelope} message EncryptedEnvelope
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            EncryptedEnvelope.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.recipients = [];
                if (options.defaults) {
                    object.version = 0;
                    if (options.bytes === String)
                        object.messageId = "";
                    else {
                        object.messageId = [];
                        if (options.bytes !== Array)
                            object.messageId = $util.newBuffer(object.messageId);
                    }
                    if (options.bytes === String)
                        object.encryptedPayload = "";
                    else {
                        object.encryptedPayload = [];
                        if (options.bytes !== Array)
                            object.encryptedPayload = $util.newBuffer(object.encryptedPayload);
                    }
                    if (options.bytes === String)
                        object.payloadNonce = "";
                    else {
                        object.payloadNonce = [];
                        if (options.bytes !== Array)
                            object.payloadNonce = $util.newBuffer(object.payloadNonce);
                    }
                    if (options.bytes === String)
                        object.payloadTag = "";
                    else {
                        object.payloadTag = [];
                        if (options.bytes !== Array)
                            object.payloadTag = $util.newBuffer(object.payloadTag);
                    }
                    object.payloadSizeClass = 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.createdAt = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.createdAt = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if (options.bytes === String)
                        object.ratchetPubKey = "";
                    else {
                        object.ratchetPubKey = [];
                        if (options.bytes !== Array)
                            object.ratchetPubKey = $util.newBuffer(object.ratchetPubKey);
                    }
                    if (options.bytes === String)
                        object.encryptedHeader = "";
                    else {
                        object.encryptedHeader = [];
                        if (options.bytes !== Array)
                            object.encryptedHeader = $util.newBuffer(object.encryptedHeader);
                    }
                    if (options.bytes === String)
                        object.headerNonce = "";
                    else {
                        object.headerNonce = [];
                        if (options.bytes !== Array)
                            object.headerNonce = $util.newBuffer(object.headerNonce);
                    }
                    if (options.bytes === String)
                        object.headerTag = "";
                    else {
                        object.headerTag = [];
                        if (options.bytes !== Array)
                            object.headerTag = $util.newBuffer(object.headerTag);
                    }
                    object.headerSizeClass = 0;
                    if (options.bytes === String)
                        object.encryptedBody = "";
                    else {
                        object.encryptedBody = [];
                        if (options.bytes !== Array)
                            object.encryptedBody = $util.newBuffer(object.encryptedBody);
                    }
                    if (options.bytes === String)
                        object.bodyNonce = "";
                    else {
                        object.bodyNonce = [];
                        if (options.bytes !== Array)
                            object.bodyNonce = $util.newBuffer(object.bodyNonce);
                    }
                    if (options.bytes === String)
                        object.bodyTag = "";
                    else {
                        object.bodyTag = [];
                        if (options.bytes !== Array)
                            object.bodyTag = $util.newBuffer(object.bodyTag);
                    }
                    object.bodySizeClass = 0;
                    if (options.bytes === String)
                        object.bodyContentAddress = "";
                    else {
                        object.bodyContentAddress = [];
                        if (options.bytes !== Array)
                            object.bodyContentAddress = $util.newBuffer(object.bodyContentAddress);
                    }
                }
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    object.version = message.version;
                if (message.messageId != null && Object.hasOwnProperty.call(message, "messageId"))
                    object.messageId = options.bytes === String ? $util.base64.encode(message.messageId, 0, message.messageId.length) : options.bytes === Array ? Array.prototype.slice.call(message.messageId) : message.messageId;
                if (message.recipients && message.recipients.length) {
                    object.recipients = [];
                    for (let j = 0; j < message.recipients.length; ++j)
                        object.recipients[j] = $root.dmcn.message.RecipientRecord.toObject(message.recipients[j], options, q + 1);
                }
                if (message.encryptedPayload != null && Object.hasOwnProperty.call(message, "encryptedPayload"))
                    object.encryptedPayload = options.bytes === String ? $util.base64.encode(message.encryptedPayload, 0, message.encryptedPayload.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedPayload) : message.encryptedPayload;
                if (message.payloadNonce != null && Object.hasOwnProperty.call(message, "payloadNonce"))
                    object.payloadNonce = options.bytes === String ? $util.base64.encode(message.payloadNonce, 0, message.payloadNonce.length) : options.bytes === Array ? Array.prototype.slice.call(message.payloadNonce) : message.payloadNonce;
                if (message.payloadTag != null && Object.hasOwnProperty.call(message, "payloadTag"))
                    object.payloadTag = options.bytes === String ? $util.base64.encode(message.payloadTag, 0, message.payloadTag.length) : options.bytes === Array ? Array.prototype.slice.call(message.payloadTag) : message.payloadTag;
                if (message.payloadSizeClass != null && Object.hasOwnProperty.call(message, "payloadSizeClass"))
                    object.payloadSizeClass = message.payloadSizeClass;
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.createdAt = typeof message.createdAt === "number" ? BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
                    else if (typeof message.createdAt === "number")
                        object.createdAt = options.longs === String ? String(message.createdAt) : message.createdAt;
                    else
                        object.createdAt = options.longs === String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
                if (message.ratchetPubKey != null && Object.hasOwnProperty.call(message, "ratchetPubKey"))
                    object.ratchetPubKey = options.bytes === String ? $util.base64.encode(message.ratchetPubKey, 0, message.ratchetPubKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.ratchetPubKey) : message.ratchetPubKey;
                if (message.encryptedHeader != null && Object.hasOwnProperty.call(message, "encryptedHeader"))
                    object.encryptedHeader = options.bytes === String ? $util.base64.encode(message.encryptedHeader, 0, message.encryptedHeader.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedHeader) : message.encryptedHeader;
                if (message.headerNonce != null && Object.hasOwnProperty.call(message, "headerNonce"))
                    object.headerNonce = options.bytes === String ? $util.base64.encode(message.headerNonce, 0, message.headerNonce.length) : options.bytes === Array ? Array.prototype.slice.call(message.headerNonce) : message.headerNonce;
                if (message.headerTag != null && Object.hasOwnProperty.call(message, "headerTag"))
                    object.headerTag = options.bytes === String ? $util.base64.encode(message.headerTag, 0, message.headerTag.length) : options.bytes === Array ? Array.prototype.slice.call(message.headerTag) : message.headerTag;
                if (message.headerSizeClass != null && Object.hasOwnProperty.call(message, "headerSizeClass"))
                    object.headerSizeClass = message.headerSizeClass;
                if (message.encryptedBody != null && Object.hasOwnProperty.call(message, "encryptedBody"))
                    object.encryptedBody = options.bytes === String ? $util.base64.encode(message.encryptedBody, 0, message.encryptedBody.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedBody) : message.encryptedBody;
                if (message.bodyNonce != null && Object.hasOwnProperty.call(message, "bodyNonce"))
                    object.bodyNonce = options.bytes === String ? $util.base64.encode(message.bodyNonce, 0, message.bodyNonce.length) : options.bytes === Array ? Array.prototype.slice.call(message.bodyNonce) : message.bodyNonce;
                if (message.bodyTag != null && Object.hasOwnProperty.call(message, "bodyTag"))
                    object.bodyTag = options.bytes === String ? $util.base64.encode(message.bodyTag, 0, message.bodyTag.length) : options.bytes === Array ? Array.prototype.slice.call(message.bodyTag) : message.bodyTag;
                if (message.bodySizeClass != null && Object.hasOwnProperty.call(message, "bodySizeClass"))
                    object.bodySizeClass = message.bodySizeClass;
                if (message.bodyContentAddress != null && Object.hasOwnProperty.call(message, "bodyContentAddress"))
                    object.bodyContentAddress = options.bytes === String ? $util.base64.encode(message.bodyContentAddress, 0, message.bodyContentAddress.length) : options.bytes === Array ? Array.prototype.slice.call(message.bodyContentAddress) : message.bodyContentAddress;
                return object;
            };

            /**
             * Converts this EncryptedEnvelope to JSON.
             * @function toJSON
             * @memberof dmcn.message.EncryptedEnvelope
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            EncryptedEnvelope.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for EncryptedEnvelope
             * @function getTypeUrl
             * @memberof dmcn.message.EncryptedEnvelope
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            EncryptedEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.message.EncryptedEnvelope";
            };

            return EncryptedEnvelope;
        })();

        return message;
    })();

    dmcn.relay = (function() {

        /**
         * Namespace relay.
         * @memberof dmcn
         * @namespace
         */
        const relay = {};

        relay.RelayRequest = (function() {

            /**
             * Properties of a RelayRequest.
             * @memberof dmcn.relay
             * @interface IRelayRequest
             * @property {dmcn.relay.IStoreRequest|null} [store] RelayRequest store
             * @property {dmcn.relay.IFetchInit|null} [fetchInit] RelayRequest fetchInit
             * @property {dmcn.relay.IFetchProof|null} [fetchProof] RelayRequest fetchProof
             * @property {dmcn.relay.IAckRequest|null} [ack] RelayRequest ack
             * @property {dmcn.relay.IPingRequest|null} [ping] RelayRequest ping
             * @property {dmcn.relay.IMailboxOp|null} [mailboxOp] RelayRequest mailboxOp
             * @property {dmcn.relay.IStoreInit|null} [storeInit] RelayRequest storeInit
             * @property {dmcn.relay.IOnionForwardRequest|null} [onionForward] RelayRequest onionForward
             * @property {dmcn.relay.IGetIdentityRequest|null} [getIdentity] RelayRequest getIdentity
             * @property {dmcn.relay.IGetDARRequest|null} [getDar] RelayRequest getDar
             * @property {dmcn.relay.IGetFleetRosterRequest|null} [getFleetRoster] RelayRequest getFleetRoster
             * @property {dmcn.relay.IGetRemovalRequest|null} [getRemoval] RelayRequest getRemoval
             * @property {dmcn.relay.IGetBlocklistRequest|null} [getBlocklist] RelayRequest getBlocklist
             * @property {dmcn.relay.IPutRecordRequest|null} [putRecord] RelayRequest putRecord
             * @property {dmcn.relay.IGetRelayDescriptorRequest|null} [getRelayDescriptor] RelayRequest getRelayDescriptor
             */

            /**
             * Constructs a new RelayRequest.
             * @memberof dmcn.relay
             * @classdesc Represents a RelayRequest.
             * @implements IRelayRequest
             * @constructor
             * @param {dmcn.relay.IRelayRequest=} [properties] Properties to set
             */
            function RelayRequest(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * RelayRequest store.
             * @member {dmcn.relay.IStoreRequest|null|undefined} store
             * @memberof dmcn.relay.RelayRequest
             * @instance
             */
            RelayRequest.prototype.store = null;

            /**
             * RelayRequest fetchInit.
             * @member {dmcn.relay.IFetchInit|null|undefined} fetchInit
             * @memberof dmcn.relay.RelayRequest
             * @instance
             */
            RelayRequest.prototype.fetchInit = null;

            /**
             * RelayRequest fetchProof.
             * @member {dmcn.relay.IFetchProof|null|undefined} fetchProof
             * @memberof dmcn.relay.RelayRequest
             * @instance
             */
            RelayRequest.prototype.fetchProof = null;

            /**
             * RelayRequest ack.
             * @member {dmcn.relay.IAckRequest|null|undefined} ack
             * @memberof dmcn.relay.RelayRequest
             * @instance
             */
            RelayRequest.prototype.ack = null;

            /**
             * RelayRequest ping.
             * @member {dmcn.relay.IPingRequest|null|undefined} ping
             * @memberof dmcn.relay.RelayRequest
             * @instance
             */
            RelayRequest.prototype.ping = null;

            /**
             * RelayRequest mailboxOp.
             * @member {dmcn.relay.IMailboxOp|null|undefined} mailboxOp
             * @memberof dmcn.relay.RelayRequest
             * @instance
             */
            RelayRequest.prototype.mailboxOp = null;

            /**
             * RelayRequest storeInit.
             * @member {dmcn.relay.IStoreInit|null|undefined} storeInit
             * @memberof dmcn.relay.RelayRequest
             * @instance
             */
            RelayRequest.prototype.storeInit = null;

            /**
             * RelayRequest onionForward.
             * @member {dmcn.relay.IOnionForwardRequest|null|undefined} onionForward
             * @memberof dmcn.relay.RelayRequest
             * @instance
             */
            RelayRequest.prototype.onionForward = null;

            /**
             * RelayRequest getIdentity.
             * @member {dmcn.relay.IGetIdentityRequest|null|undefined} getIdentity
             * @memberof dmcn.relay.RelayRequest
             * @instance
             */
            RelayRequest.prototype.getIdentity = null;

            /**
             * RelayRequest getDar.
             * @member {dmcn.relay.IGetDARRequest|null|undefined} getDar
             * @memberof dmcn.relay.RelayRequest
             * @instance
             */
            RelayRequest.prototype.getDar = null;

            /**
             * RelayRequest getFleetRoster.
             * @member {dmcn.relay.IGetFleetRosterRequest|null|undefined} getFleetRoster
             * @memberof dmcn.relay.RelayRequest
             * @instance
             */
            RelayRequest.prototype.getFleetRoster = null;

            /**
             * RelayRequest getRemoval.
             * @member {dmcn.relay.IGetRemovalRequest|null|undefined} getRemoval
             * @memberof dmcn.relay.RelayRequest
             * @instance
             */
            RelayRequest.prototype.getRemoval = null;

            /**
             * RelayRequest getBlocklist.
             * @member {dmcn.relay.IGetBlocklistRequest|null|undefined} getBlocklist
             * @memberof dmcn.relay.RelayRequest
             * @instance
             */
            RelayRequest.prototype.getBlocklist = null;

            /**
             * RelayRequest putRecord.
             * @member {dmcn.relay.IPutRecordRequest|null|undefined} putRecord
             * @memberof dmcn.relay.RelayRequest
             * @instance
             */
            RelayRequest.prototype.putRecord = null;

            /**
             * RelayRequest getRelayDescriptor.
             * @member {dmcn.relay.IGetRelayDescriptorRequest|null|undefined} getRelayDescriptor
             * @memberof dmcn.relay.RelayRequest
             * @instance
             */
            RelayRequest.prototype.getRelayDescriptor = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * RelayRequest request.
             * @member {"store"|"fetchInit"|"fetchProof"|"ack"|"ping"|"mailboxOp"|"storeInit"|"onionForward"|"getIdentity"|"getDar"|"getFleetRoster"|"getRemoval"|"getBlocklist"|"putRecord"|"getRelayDescriptor"|undefined} request
             * @memberof dmcn.relay.RelayRequest
             * @instance
             */
            Object.defineProperty(RelayRequest.prototype, "request", {
                get: $util.oneOfGetter($oneOfFields = ["store", "fetchInit", "fetchProof", "ack", "ping", "mailboxOp", "storeInit", "onionForward", "getIdentity", "getDar", "getFleetRoster", "getRemoval", "getBlocklist", "putRecord", "getRelayDescriptor"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new RelayRequest instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.RelayRequest
             * @static
             * @param {dmcn.relay.IRelayRequest=} [properties] Properties to set
             * @returns {dmcn.relay.RelayRequest} RelayRequest instance
             */
            RelayRequest.create = function create(properties) {
                return new RelayRequest(properties);
            };

            /**
             * Encodes the specified RelayRequest message. Does not implicitly {@link dmcn.relay.RelayRequest.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.RelayRequest
             * @static
             * @param {dmcn.relay.IRelayRequest} message RelayRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RelayRequest.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.store != null && Object.hasOwnProperty.call(message, "store"))
                    $root.dmcn.relay.StoreRequest.encode(message.store, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.fetchInit != null && Object.hasOwnProperty.call(message, "fetchInit"))
                    $root.dmcn.relay.FetchInit.encode(message.fetchInit, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.fetchProof != null && Object.hasOwnProperty.call(message, "fetchProof"))
                    $root.dmcn.relay.FetchProof.encode(message.fetchProof, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.ack != null && Object.hasOwnProperty.call(message, "ack"))
                    $root.dmcn.relay.AckRequest.encode(message.ack, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.ping != null && Object.hasOwnProperty.call(message, "ping"))
                    $root.dmcn.relay.PingRequest.encode(message.ping, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                if (message.mailboxOp != null && Object.hasOwnProperty.call(message, "mailboxOp"))
                    $root.dmcn.relay.MailboxOp.encode(message.mailboxOp, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
                if (message.storeInit != null && Object.hasOwnProperty.call(message, "storeInit"))
                    $root.dmcn.relay.StoreInit.encode(message.storeInit, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
                if (message.onionForward != null && Object.hasOwnProperty.call(message, "onionForward"))
                    $root.dmcn.relay.OnionForwardRequest.encode(message.onionForward, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
                if (message.getIdentity != null && Object.hasOwnProperty.call(message, "getIdentity"))
                    $root.dmcn.relay.GetIdentityRequest.encode(message.getIdentity, writer.uint32(/* id 19, wireType 2 =*/154).fork(), q + 1).ldelim();
                if (message.getDar != null && Object.hasOwnProperty.call(message, "getDar"))
                    $root.dmcn.relay.GetDARRequest.encode(message.getDar, writer.uint32(/* id 20, wireType 2 =*/162).fork(), q + 1).ldelim();
                if (message.getFleetRoster != null && Object.hasOwnProperty.call(message, "getFleetRoster"))
                    $root.dmcn.relay.GetFleetRosterRequest.encode(message.getFleetRoster, writer.uint32(/* id 21, wireType 2 =*/170).fork(), q + 1).ldelim();
                if (message.getRemoval != null && Object.hasOwnProperty.call(message, "getRemoval"))
                    $root.dmcn.relay.GetRemovalRequest.encode(message.getRemoval, writer.uint32(/* id 22, wireType 2 =*/178).fork(), q + 1).ldelim();
                if (message.getBlocklist != null && Object.hasOwnProperty.call(message, "getBlocklist"))
                    $root.dmcn.relay.GetBlocklistRequest.encode(message.getBlocklist, writer.uint32(/* id 23, wireType 2 =*/186).fork(), q + 1).ldelim();
                if (message.putRecord != null && Object.hasOwnProperty.call(message, "putRecord"))
                    $root.dmcn.relay.PutRecordRequest.encode(message.putRecord, writer.uint32(/* id 24, wireType 2 =*/194).fork(), q + 1).ldelim();
                if (message.getRelayDescriptor != null && Object.hasOwnProperty.call(message, "getRelayDescriptor"))
                    $root.dmcn.relay.GetRelayDescriptorRequest.encode(message.getRelayDescriptor, writer.uint32(/* id 25, wireType 2 =*/202).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified RelayRequest message, length delimited. Does not implicitly {@link dmcn.relay.RelayRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.RelayRequest
             * @static
             * @param {dmcn.relay.IRelayRequest} message RelayRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RelayRequest.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a RelayRequest message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.RelayRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.RelayRequest} RelayRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RelayRequest.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.RelayRequest();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.store = $root.dmcn.relay.StoreRequest.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 2: {
                            message.fetchInit = $root.dmcn.relay.FetchInit.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 3: {
                            message.fetchProof = $root.dmcn.relay.FetchProof.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 4: {
                            message.ack = $root.dmcn.relay.AckRequest.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 5: {
                            message.ping = $root.dmcn.relay.PingRequest.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 6: {
                            message.mailboxOp = $root.dmcn.relay.MailboxOp.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 7: {
                            message.storeInit = $root.dmcn.relay.StoreInit.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 8: {
                            message.onionForward = $root.dmcn.relay.OnionForwardRequest.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 19: {
                            message.getIdentity = $root.dmcn.relay.GetIdentityRequest.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 20: {
                            message.getDar = $root.dmcn.relay.GetDARRequest.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 21: {
                            message.getFleetRoster = $root.dmcn.relay.GetFleetRosterRequest.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 22: {
                            message.getRemoval = $root.dmcn.relay.GetRemovalRequest.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 23: {
                            message.getBlocklist = $root.dmcn.relay.GetBlocklistRequest.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 24: {
                            message.putRecord = $root.dmcn.relay.PutRecordRequest.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 25: {
                            message.getRelayDescriptor = $root.dmcn.relay.GetRelayDescriptorRequest.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a RelayRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.RelayRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.RelayRequest} RelayRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RelayRequest.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a RelayRequest message.
             * @function verify
             * @memberof dmcn.relay.RelayRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RelayRequest.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                let properties = {};
                if (message.store != null && Object.hasOwnProperty.call(message, "store")) {
                    properties.request = 1;
                    {
                        let error = $root.dmcn.relay.StoreRequest.verify(message.store, long + 1);
                        if (error)
                            return "store." + error;
                    }
                }
                if (message.fetchInit != null && Object.hasOwnProperty.call(message, "fetchInit")) {
                    if (properties.request === 1)
                        return "request: multiple values";
                    properties.request = 1;
                    {
                        let error = $root.dmcn.relay.FetchInit.verify(message.fetchInit, long + 1);
                        if (error)
                            return "fetchInit." + error;
                    }
                }
                if (message.fetchProof != null && Object.hasOwnProperty.call(message, "fetchProof")) {
                    if (properties.request === 1)
                        return "request: multiple values";
                    properties.request = 1;
                    {
                        let error = $root.dmcn.relay.FetchProof.verify(message.fetchProof, long + 1);
                        if (error)
                            return "fetchProof." + error;
                    }
                }
                if (message.ack != null && Object.hasOwnProperty.call(message, "ack")) {
                    if (properties.request === 1)
                        return "request: multiple values";
                    properties.request = 1;
                    {
                        let error = $root.dmcn.relay.AckRequest.verify(message.ack, long + 1);
                        if (error)
                            return "ack." + error;
                    }
                }
                if (message.ping != null && Object.hasOwnProperty.call(message, "ping")) {
                    if (properties.request === 1)
                        return "request: multiple values";
                    properties.request = 1;
                    {
                        let error = $root.dmcn.relay.PingRequest.verify(message.ping, long + 1);
                        if (error)
                            return "ping." + error;
                    }
                }
                if (message.mailboxOp != null && Object.hasOwnProperty.call(message, "mailboxOp")) {
                    if (properties.request === 1)
                        return "request: multiple values";
                    properties.request = 1;
                    {
                        let error = $root.dmcn.relay.MailboxOp.verify(message.mailboxOp, long + 1);
                        if (error)
                            return "mailboxOp." + error;
                    }
                }
                if (message.storeInit != null && Object.hasOwnProperty.call(message, "storeInit")) {
                    if (properties.request === 1)
                        return "request: multiple values";
                    properties.request = 1;
                    {
                        let error = $root.dmcn.relay.StoreInit.verify(message.storeInit, long + 1);
                        if (error)
                            return "storeInit." + error;
                    }
                }
                if (message.onionForward != null && Object.hasOwnProperty.call(message, "onionForward")) {
                    if (properties.request === 1)
                        return "request: multiple values";
                    properties.request = 1;
                    {
                        let error = $root.dmcn.relay.OnionForwardRequest.verify(message.onionForward, long + 1);
                        if (error)
                            return "onionForward." + error;
                    }
                }
                if (message.getIdentity != null && Object.hasOwnProperty.call(message, "getIdentity")) {
                    if (properties.request === 1)
                        return "request: multiple values";
                    properties.request = 1;
                    {
                        let error = $root.dmcn.relay.GetIdentityRequest.verify(message.getIdentity, long + 1);
                        if (error)
                            return "getIdentity." + error;
                    }
                }
                if (message.getDar != null && Object.hasOwnProperty.call(message, "getDar")) {
                    if (properties.request === 1)
                        return "request: multiple values";
                    properties.request = 1;
                    {
                        let error = $root.dmcn.relay.GetDARRequest.verify(message.getDar, long + 1);
                        if (error)
                            return "getDar." + error;
                    }
                }
                if (message.getFleetRoster != null && Object.hasOwnProperty.call(message, "getFleetRoster")) {
                    if (properties.request === 1)
                        return "request: multiple values";
                    properties.request = 1;
                    {
                        let error = $root.dmcn.relay.GetFleetRosterRequest.verify(message.getFleetRoster, long + 1);
                        if (error)
                            return "getFleetRoster." + error;
                    }
                }
                if (message.getRemoval != null && Object.hasOwnProperty.call(message, "getRemoval")) {
                    if (properties.request === 1)
                        return "request: multiple values";
                    properties.request = 1;
                    {
                        let error = $root.dmcn.relay.GetRemovalRequest.verify(message.getRemoval, long + 1);
                        if (error)
                            return "getRemoval." + error;
                    }
                }
                if (message.getBlocklist != null && Object.hasOwnProperty.call(message, "getBlocklist")) {
                    if (properties.request === 1)
                        return "request: multiple values";
                    properties.request = 1;
                    {
                        let error = $root.dmcn.relay.GetBlocklistRequest.verify(message.getBlocklist, long + 1);
                        if (error)
                            return "getBlocklist." + error;
                    }
                }
                if (message.putRecord != null && Object.hasOwnProperty.call(message, "putRecord")) {
                    if (properties.request === 1)
                        return "request: multiple values";
                    properties.request = 1;
                    {
                        let error = $root.dmcn.relay.PutRecordRequest.verify(message.putRecord, long + 1);
                        if (error)
                            return "putRecord." + error;
                    }
                }
                if (message.getRelayDescriptor != null && Object.hasOwnProperty.call(message, "getRelayDescriptor")) {
                    if (properties.request === 1)
                        return "request: multiple values";
                    properties.request = 1;
                    {
                        let error = $root.dmcn.relay.GetRelayDescriptorRequest.verify(message.getRelayDescriptor, long + 1);
                        if (error)
                            return "getRelayDescriptor." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a RelayRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.RelayRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.RelayRequest} RelayRequest
             */
            RelayRequest.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.RelayRequest)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.RelayRequest: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.RelayRequest();
                if (object.store != null) {
                    if (!$util.isObject(object.store))
                        throw TypeError(".dmcn.relay.RelayRequest.store: object expected");
                    message.store = $root.dmcn.relay.StoreRequest.fromObject(object.store, long + 1);
                }
                if (object.fetchInit != null) {
                    if (!$util.isObject(object.fetchInit))
                        throw TypeError(".dmcn.relay.RelayRequest.fetchInit: object expected");
                    message.fetchInit = $root.dmcn.relay.FetchInit.fromObject(object.fetchInit, long + 1);
                }
                if (object.fetchProof != null) {
                    if (!$util.isObject(object.fetchProof))
                        throw TypeError(".dmcn.relay.RelayRequest.fetchProof: object expected");
                    message.fetchProof = $root.dmcn.relay.FetchProof.fromObject(object.fetchProof, long + 1);
                }
                if (object.ack != null) {
                    if (!$util.isObject(object.ack))
                        throw TypeError(".dmcn.relay.RelayRequest.ack: object expected");
                    message.ack = $root.dmcn.relay.AckRequest.fromObject(object.ack, long + 1);
                }
                if (object.ping != null) {
                    if (!$util.isObject(object.ping))
                        throw TypeError(".dmcn.relay.RelayRequest.ping: object expected");
                    message.ping = $root.dmcn.relay.PingRequest.fromObject(object.ping, long + 1);
                }
                if (object.mailboxOp != null) {
                    if (!$util.isObject(object.mailboxOp))
                        throw TypeError(".dmcn.relay.RelayRequest.mailboxOp: object expected");
                    message.mailboxOp = $root.dmcn.relay.MailboxOp.fromObject(object.mailboxOp, long + 1);
                }
                if (object.storeInit != null) {
                    if (!$util.isObject(object.storeInit))
                        throw TypeError(".dmcn.relay.RelayRequest.storeInit: object expected");
                    message.storeInit = $root.dmcn.relay.StoreInit.fromObject(object.storeInit, long + 1);
                }
                if (object.onionForward != null) {
                    if (!$util.isObject(object.onionForward))
                        throw TypeError(".dmcn.relay.RelayRequest.onionForward: object expected");
                    message.onionForward = $root.dmcn.relay.OnionForwardRequest.fromObject(object.onionForward, long + 1);
                }
                if (object.getIdentity != null) {
                    if (!$util.isObject(object.getIdentity))
                        throw TypeError(".dmcn.relay.RelayRequest.getIdentity: object expected");
                    message.getIdentity = $root.dmcn.relay.GetIdentityRequest.fromObject(object.getIdentity, long + 1);
                }
                if (object.getDar != null) {
                    if (!$util.isObject(object.getDar))
                        throw TypeError(".dmcn.relay.RelayRequest.getDar: object expected");
                    message.getDar = $root.dmcn.relay.GetDARRequest.fromObject(object.getDar, long + 1);
                }
                if (object.getFleetRoster != null) {
                    if (!$util.isObject(object.getFleetRoster))
                        throw TypeError(".dmcn.relay.RelayRequest.getFleetRoster: object expected");
                    message.getFleetRoster = $root.dmcn.relay.GetFleetRosterRequest.fromObject(object.getFleetRoster, long + 1);
                }
                if (object.getRemoval != null) {
                    if (!$util.isObject(object.getRemoval))
                        throw TypeError(".dmcn.relay.RelayRequest.getRemoval: object expected");
                    message.getRemoval = $root.dmcn.relay.GetRemovalRequest.fromObject(object.getRemoval, long + 1);
                }
                if (object.getBlocklist != null) {
                    if (!$util.isObject(object.getBlocklist))
                        throw TypeError(".dmcn.relay.RelayRequest.getBlocklist: object expected");
                    message.getBlocklist = $root.dmcn.relay.GetBlocklistRequest.fromObject(object.getBlocklist, long + 1);
                }
                if (object.putRecord != null) {
                    if (!$util.isObject(object.putRecord))
                        throw TypeError(".dmcn.relay.RelayRequest.putRecord: object expected");
                    message.putRecord = $root.dmcn.relay.PutRecordRequest.fromObject(object.putRecord, long + 1);
                }
                if (object.getRelayDescriptor != null) {
                    if (!$util.isObject(object.getRelayDescriptor))
                        throw TypeError(".dmcn.relay.RelayRequest.getRelayDescriptor: object expected");
                    message.getRelayDescriptor = $root.dmcn.relay.GetRelayDescriptorRequest.fromObject(object.getRelayDescriptor, long + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a RelayRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.RelayRequest
             * @static
             * @param {dmcn.relay.RelayRequest} message RelayRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RelayRequest.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (message.store != null && Object.hasOwnProperty.call(message, "store")) {
                    object.store = $root.dmcn.relay.StoreRequest.toObject(message.store, options, q + 1);
                    if (options.oneofs)
                        object.request = "store";
                }
                if (message.fetchInit != null && Object.hasOwnProperty.call(message, "fetchInit")) {
                    object.fetchInit = $root.dmcn.relay.FetchInit.toObject(message.fetchInit, options, q + 1);
                    if (options.oneofs)
                        object.request = "fetchInit";
                }
                if (message.fetchProof != null && Object.hasOwnProperty.call(message, "fetchProof")) {
                    object.fetchProof = $root.dmcn.relay.FetchProof.toObject(message.fetchProof, options, q + 1);
                    if (options.oneofs)
                        object.request = "fetchProof";
                }
                if (message.ack != null && Object.hasOwnProperty.call(message, "ack")) {
                    object.ack = $root.dmcn.relay.AckRequest.toObject(message.ack, options, q + 1);
                    if (options.oneofs)
                        object.request = "ack";
                }
                if (message.ping != null && Object.hasOwnProperty.call(message, "ping")) {
                    object.ping = $root.dmcn.relay.PingRequest.toObject(message.ping, options, q + 1);
                    if (options.oneofs)
                        object.request = "ping";
                }
                if (message.mailboxOp != null && Object.hasOwnProperty.call(message, "mailboxOp")) {
                    object.mailboxOp = $root.dmcn.relay.MailboxOp.toObject(message.mailboxOp, options, q + 1);
                    if (options.oneofs)
                        object.request = "mailboxOp";
                }
                if (message.storeInit != null && Object.hasOwnProperty.call(message, "storeInit")) {
                    object.storeInit = $root.dmcn.relay.StoreInit.toObject(message.storeInit, options, q + 1);
                    if (options.oneofs)
                        object.request = "storeInit";
                }
                if (message.onionForward != null && Object.hasOwnProperty.call(message, "onionForward")) {
                    object.onionForward = $root.dmcn.relay.OnionForwardRequest.toObject(message.onionForward, options, q + 1);
                    if (options.oneofs)
                        object.request = "onionForward";
                }
                if (message.getIdentity != null && Object.hasOwnProperty.call(message, "getIdentity")) {
                    object.getIdentity = $root.dmcn.relay.GetIdentityRequest.toObject(message.getIdentity, options, q + 1);
                    if (options.oneofs)
                        object.request = "getIdentity";
                }
                if (message.getDar != null && Object.hasOwnProperty.call(message, "getDar")) {
                    object.getDar = $root.dmcn.relay.GetDARRequest.toObject(message.getDar, options, q + 1);
                    if (options.oneofs)
                        object.request = "getDar";
                }
                if (message.getFleetRoster != null && Object.hasOwnProperty.call(message, "getFleetRoster")) {
                    object.getFleetRoster = $root.dmcn.relay.GetFleetRosterRequest.toObject(message.getFleetRoster, options, q + 1);
                    if (options.oneofs)
                        object.request = "getFleetRoster";
                }
                if (message.getRemoval != null && Object.hasOwnProperty.call(message, "getRemoval")) {
                    object.getRemoval = $root.dmcn.relay.GetRemovalRequest.toObject(message.getRemoval, options, q + 1);
                    if (options.oneofs)
                        object.request = "getRemoval";
                }
                if (message.getBlocklist != null && Object.hasOwnProperty.call(message, "getBlocklist")) {
                    object.getBlocklist = $root.dmcn.relay.GetBlocklistRequest.toObject(message.getBlocklist, options, q + 1);
                    if (options.oneofs)
                        object.request = "getBlocklist";
                }
                if (message.putRecord != null && Object.hasOwnProperty.call(message, "putRecord")) {
                    object.putRecord = $root.dmcn.relay.PutRecordRequest.toObject(message.putRecord, options, q + 1);
                    if (options.oneofs)
                        object.request = "putRecord";
                }
                if (message.getRelayDescriptor != null && Object.hasOwnProperty.call(message, "getRelayDescriptor")) {
                    object.getRelayDescriptor = $root.dmcn.relay.GetRelayDescriptorRequest.toObject(message.getRelayDescriptor, options, q + 1);
                    if (options.oneofs)
                        object.request = "getRelayDescriptor";
                }
                return object;
            };

            /**
             * Converts this RelayRequest to JSON.
             * @function toJSON
             * @memberof dmcn.relay.RelayRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RelayRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for RelayRequest
             * @function getTypeUrl
             * @memberof dmcn.relay.RelayRequest
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            RelayRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.RelayRequest";
            };

            return RelayRequest;
        })();

        relay.RelayResponse = (function() {

            /**
             * Properties of a RelayResponse.
             * @memberof dmcn.relay
             * @interface IRelayResponse
             * @property {dmcn.relay.IStoreResponse|null} [store] RelayResponse store
             * @property {dmcn.relay.IFetchChallenge|null} [fetchChallenge] RelayResponse fetchChallenge
             * @property {dmcn.relay.IFetchResponse|null} [fetch] RelayResponse fetch
             * @property {dmcn.relay.IAckResponse|null} [ack] RelayResponse ack
             * @property {dmcn.relay.IPingResponse|null} [ping] RelayResponse ping
             * @property {dmcn.relay.IErrorResponse|null} [error] RelayResponse error
             * @property {dmcn.relay.IMailboxListResponse|null} [mailboxList] RelayResponse mailboxList
             * @property {dmcn.relay.IMailboxBodyHeader|null} [mailboxBodyHeader] RelayResponse mailboxBodyHeader
             * @property {dmcn.relay.IMailboxDeleteResponse|null} [mailboxDelete] RelayResponse mailboxDelete
             * @property {dmcn.relay.IOnionForwardResponse|null} [onionForward] RelayResponse onionForward
             * @property {dmcn.relay.IGetIdentityResponse|null} [getIdentity] RelayResponse getIdentity
             * @property {dmcn.relay.IGetDARResponse|null} [getDar] RelayResponse getDar
             * @property {dmcn.relay.IGetFleetRosterResponse|null} [getFleetRoster] RelayResponse getFleetRoster
             * @property {dmcn.relay.IGetRemovalResponse|null} [getRemoval] RelayResponse getRemoval
             * @property {dmcn.relay.IGetBlocklistResponse|null} [getBlocklist] RelayResponse getBlocklist
             * @property {dmcn.relay.IPutRecordResponse|null} [putRecord] RelayResponse putRecord
             * @property {dmcn.relay.IGetRelayDescriptorResponse|null} [getRelayDescriptor] RelayResponse getRelayDescriptor
             */

            /**
             * Constructs a new RelayResponse.
             * @memberof dmcn.relay
             * @classdesc Represents a RelayResponse.
             * @implements IRelayResponse
             * @constructor
             * @param {dmcn.relay.IRelayResponse=} [properties] Properties to set
             */
            function RelayResponse(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * RelayResponse store.
             * @member {dmcn.relay.IStoreResponse|null|undefined} store
             * @memberof dmcn.relay.RelayResponse
             * @instance
             */
            RelayResponse.prototype.store = null;

            /**
             * RelayResponse fetchChallenge.
             * @member {dmcn.relay.IFetchChallenge|null|undefined} fetchChallenge
             * @memberof dmcn.relay.RelayResponse
             * @instance
             */
            RelayResponse.prototype.fetchChallenge = null;

            /**
             * RelayResponse fetch.
             * @member {dmcn.relay.IFetchResponse|null|undefined} fetch
             * @memberof dmcn.relay.RelayResponse
             * @instance
             */
            RelayResponse.prototype.fetch = null;

            /**
             * RelayResponse ack.
             * @member {dmcn.relay.IAckResponse|null|undefined} ack
             * @memberof dmcn.relay.RelayResponse
             * @instance
             */
            RelayResponse.prototype.ack = null;

            /**
             * RelayResponse ping.
             * @member {dmcn.relay.IPingResponse|null|undefined} ping
             * @memberof dmcn.relay.RelayResponse
             * @instance
             */
            RelayResponse.prototype.ping = null;

            /**
             * RelayResponse error.
             * @member {dmcn.relay.IErrorResponse|null|undefined} error
             * @memberof dmcn.relay.RelayResponse
             * @instance
             */
            RelayResponse.prototype.error = null;

            /**
             * RelayResponse mailboxList.
             * @member {dmcn.relay.IMailboxListResponse|null|undefined} mailboxList
             * @memberof dmcn.relay.RelayResponse
             * @instance
             */
            RelayResponse.prototype.mailboxList = null;

            /**
             * RelayResponse mailboxBodyHeader.
             * @member {dmcn.relay.IMailboxBodyHeader|null|undefined} mailboxBodyHeader
             * @memberof dmcn.relay.RelayResponse
             * @instance
             */
            RelayResponse.prototype.mailboxBodyHeader = null;

            /**
             * RelayResponse mailboxDelete.
             * @member {dmcn.relay.IMailboxDeleteResponse|null|undefined} mailboxDelete
             * @memberof dmcn.relay.RelayResponse
             * @instance
             */
            RelayResponse.prototype.mailboxDelete = null;

            /**
             * RelayResponse onionForward.
             * @member {dmcn.relay.IOnionForwardResponse|null|undefined} onionForward
             * @memberof dmcn.relay.RelayResponse
             * @instance
             */
            RelayResponse.prototype.onionForward = null;

            /**
             * RelayResponse getIdentity.
             * @member {dmcn.relay.IGetIdentityResponse|null|undefined} getIdentity
             * @memberof dmcn.relay.RelayResponse
             * @instance
             */
            RelayResponse.prototype.getIdentity = null;

            /**
             * RelayResponse getDar.
             * @member {dmcn.relay.IGetDARResponse|null|undefined} getDar
             * @memberof dmcn.relay.RelayResponse
             * @instance
             */
            RelayResponse.prototype.getDar = null;

            /**
             * RelayResponse getFleetRoster.
             * @member {dmcn.relay.IGetFleetRosterResponse|null|undefined} getFleetRoster
             * @memberof dmcn.relay.RelayResponse
             * @instance
             */
            RelayResponse.prototype.getFleetRoster = null;

            /**
             * RelayResponse getRemoval.
             * @member {dmcn.relay.IGetRemovalResponse|null|undefined} getRemoval
             * @memberof dmcn.relay.RelayResponse
             * @instance
             */
            RelayResponse.prototype.getRemoval = null;

            /**
             * RelayResponse getBlocklist.
             * @member {dmcn.relay.IGetBlocklistResponse|null|undefined} getBlocklist
             * @memberof dmcn.relay.RelayResponse
             * @instance
             */
            RelayResponse.prototype.getBlocklist = null;

            /**
             * RelayResponse putRecord.
             * @member {dmcn.relay.IPutRecordResponse|null|undefined} putRecord
             * @memberof dmcn.relay.RelayResponse
             * @instance
             */
            RelayResponse.prototype.putRecord = null;

            /**
             * RelayResponse getRelayDescriptor.
             * @member {dmcn.relay.IGetRelayDescriptorResponse|null|undefined} getRelayDescriptor
             * @memberof dmcn.relay.RelayResponse
             * @instance
             */
            RelayResponse.prototype.getRelayDescriptor = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * RelayResponse response.
             * @member {"store"|"fetchChallenge"|"fetch"|"ack"|"ping"|"error"|"mailboxList"|"mailboxBodyHeader"|"mailboxDelete"|"onionForward"|"getIdentity"|"getDar"|"getFleetRoster"|"getRemoval"|"getBlocklist"|"putRecord"|"getRelayDescriptor"|undefined} response
             * @memberof dmcn.relay.RelayResponse
             * @instance
             */
            Object.defineProperty(RelayResponse.prototype, "response", {
                get: $util.oneOfGetter($oneOfFields = ["store", "fetchChallenge", "fetch", "ack", "ping", "error", "mailboxList", "mailboxBodyHeader", "mailboxDelete", "onionForward", "getIdentity", "getDar", "getFleetRoster", "getRemoval", "getBlocklist", "putRecord", "getRelayDescriptor"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new RelayResponse instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.RelayResponse
             * @static
             * @param {dmcn.relay.IRelayResponse=} [properties] Properties to set
             * @returns {dmcn.relay.RelayResponse} RelayResponse instance
             */
            RelayResponse.create = function create(properties) {
                return new RelayResponse(properties);
            };

            /**
             * Encodes the specified RelayResponse message. Does not implicitly {@link dmcn.relay.RelayResponse.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.RelayResponse
             * @static
             * @param {dmcn.relay.IRelayResponse} message RelayResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RelayResponse.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.store != null && Object.hasOwnProperty.call(message, "store"))
                    $root.dmcn.relay.StoreResponse.encode(message.store, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.fetchChallenge != null && Object.hasOwnProperty.call(message, "fetchChallenge"))
                    $root.dmcn.relay.FetchChallenge.encode(message.fetchChallenge, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.fetch != null && Object.hasOwnProperty.call(message, "fetch"))
                    $root.dmcn.relay.FetchResponse.encode(message.fetch, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.ack != null && Object.hasOwnProperty.call(message, "ack"))
                    $root.dmcn.relay.AckResponse.encode(message.ack, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.ping != null && Object.hasOwnProperty.call(message, "ping"))
                    $root.dmcn.relay.PingResponse.encode(message.ping, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                    $root.dmcn.relay.ErrorResponse.encode(message.error, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
                if (message.mailboxList != null && Object.hasOwnProperty.call(message, "mailboxList"))
                    $root.dmcn.relay.MailboxListResponse.encode(message.mailboxList, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
                if (message.mailboxBodyHeader != null && Object.hasOwnProperty.call(message, "mailboxBodyHeader"))
                    $root.dmcn.relay.MailboxBodyHeader.encode(message.mailboxBodyHeader, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
                if (message.mailboxDelete != null && Object.hasOwnProperty.call(message, "mailboxDelete"))
                    $root.dmcn.relay.MailboxDeleteResponse.encode(message.mailboxDelete, writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
                if (message.onionForward != null && Object.hasOwnProperty.call(message, "onionForward"))
                    $root.dmcn.relay.OnionForwardResponse.encode(message.onionForward, writer.uint32(/* id 10, wireType 2 =*/82).fork(), q + 1).ldelim();
                if (message.getIdentity != null && Object.hasOwnProperty.call(message, "getIdentity"))
                    $root.dmcn.relay.GetIdentityResponse.encode(message.getIdentity, writer.uint32(/* id 28, wireType 2 =*/226).fork(), q + 1).ldelim();
                if (message.getDar != null && Object.hasOwnProperty.call(message, "getDar"))
                    $root.dmcn.relay.GetDARResponse.encode(message.getDar, writer.uint32(/* id 29, wireType 2 =*/234).fork(), q + 1).ldelim();
                if (message.getFleetRoster != null && Object.hasOwnProperty.call(message, "getFleetRoster"))
                    $root.dmcn.relay.GetFleetRosterResponse.encode(message.getFleetRoster, writer.uint32(/* id 30, wireType 2 =*/242).fork(), q + 1).ldelim();
                if (message.getRemoval != null && Object.hasOwnProperty.call(message, "getRemoval"))
                    $root.dmcn.relay.GetRemovalResponse.encode(message.getRemoval, writer.uint32(/* id 31, wireType 2 =*/250).fork(), q + 1).ldelim();
                if (message.getBlocklist != null && Object.hasOwnProperty.call(message, "getBlocklist"))
                    $root.dmcn.relay.GetBlocklistResponse.encode(message.getBlocklist, writer.uint32(/* id 32, wireType 2 =*/258).fork(), q + 1).ldelim();
                if (message.putRecord != null && Object.hasOwnProperty.call(message, "putRecord"))
                    $root.dmcn.relay.PutRecordResponse.encode(message.putRecord, writer.uint32(/* id 33, wireType 2 =*/266).fork(), q + 1).ldelim();
                if (message.getRelayDescriptor != null && Object.hasOwnProperty.call(message, "getRelayDescriptor"))
                    $root.dmcn.relay.GetRelayDescriptorResponse.encode(message.getRelayDescriptor, writer.uint32(/* id 34, wireType 2 =*/274).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified RelayResponse message, length delimited. Does not implicitly {@link dmcn.relay.RelayResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.RelayResponse
             * @static
             * @param {dmcn.relay.IRelayResponse} message RelayResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RelayResponse.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a RelayResponse message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.RelayResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.RelayResponse} RelayResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RelayResponse.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.RelayResponse();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.store = $root.dmcn.relay.StoreResponse.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 2: {
                            message.fetchChallenge = $root.dmcn.relay.FetchChallenge.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 3: {
                            message.fetch = $root.dmcn.relay.FetchResponse.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 4: {
                            message.ack = $root.dmcn.relay.AckResponse.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 5: {
                            message.ping = $root.dmcn.relay.PingResponse.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 6: {
                            message.error = $root.dmcn.relay.ErrorResponse.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 7: {
                            message.mailboxList = $root.dmcn.relay.MailboxListResponse.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 8: {
                            message.mailboxBodyHeader = $root.dmcn.relay.MailboxBodyHeader.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 9: {
                            message.mailboxDelete = $root.dmcn.relay.MailboxDeleteResponse.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 10: {
                            message.onionForward = $root.dmcn.relay.OnionForwardResponse.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 28: {
                            message.getIdentity = $root.dmcn.relay.GetIdentityResponse.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 29: {
                            message.getDar = $root.dmcn.relay.GetDARResponse.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 30: {
                            message.getFleetRoster = $root.dmcn.relay.GetFleetRosterResponse.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 31: {
                            message.getRemoval = $root.dmcn.relay.GetRemovalResponse.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 32: {
                            message.getBlocklist = $root.dmcn.relay.GetBlocklistResponse.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 33: {
                            message.putRecord = $root.dmcn.relay.PutRecordResponse.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 34: {
                            message.getRelayDescriptor = $root.dmcn.relay.GetRelayDescriptorResponse.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a RelayResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.RelayResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.RelayResponse} RelayResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RelayResponse.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a RelayResponse message.
             * @function verify
             * @memberof dmcn.relay.RelayResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RelayResponse.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                let properties = {};
                if (message.store != null && Object.hasOwnProperty.call(message, "store")) {
                    properties.response = 1;
                    {
                        let error = $root.dmcn.relay.StoreResponse.verify(message.store, long + 1);
                        if (error)
                            return "store." + error;
                    }
                }
                if (message.fetchChallenge != null && Object.hasOwnProperty.call(message, "fetchChallenge")) {
                    if (properties.response === 1)
                        return "response: multiple values";
                    properties.response = 1;
                    {
                        let error = $root.dmcn.relay.FetchChallenge.verify(message.fetchChallenge, long + 1);
                        if (error)
                            return "fetchChallenge." + error;
                    }
                }
                if (message.fetch != null && Object.hasOwnProperty.call(message, "fetch")) {
                    if (properties.response === 1)
                        return "response: multiple values";
                    properties.response = 1;
                    {
                        let error = $root.dmcn.relay.FetchResponse.verify(message.fetch, long + 1);
                        if (error)
                            return "fetch." + error;
                    }
                }
                if (message.ack != null && Object.hasOwnProperty.call(message, "ack")) {
                    if (properties.response === 1)
                        return "response: multiple values";
                    properties.response = 1;
                    {
                        let error = $root.dmcn.relay.AckResponse.verify(message.ack, long + 1);
                        if (error)
                            return "ack." + error;
                    }
                }
                if (message.ping != null && Object.hasOwnProperty.call(message, "ping")) {
                    if (properties.response === 1)
                        return "response: multiple values";
                    properties.response = 1;
                    {
                        let error = $root.dmcn.relay.PingResponse.verify(message.ping, long + 1);
                        if (error)
                            return "ping." + error;
                    }
                }
                if (message.error != null && Object.hasOwnProperty.call(message, "error")) {
                    if (properties.response === 1)
                        return "response: multiple values";
                    properties.response = 1;
                    {
                        let error = $root.dmcn.relay.ErrorResponse.verify(message.error, long + 1);
                        if (error)
                            return "error." + error;
                    }
                }
                if (message.mailboxList != null && Object.hasOwnProperty.call(message, "mailboxList")) {
                    if (properties.response === 1)
                        return "response: multiple values";
                    properties.response = 1;
                    {
                        let error = $root.dmcn.relay.MailboxListResponse.verify(message.mailboxList, long + 1);
                        if (error)
                            return "mailboxList." + error;
                    }
                }
                if (message.mailboxBodyHeader != null && Object.hasOwnProperty.call(message, "mailboxBodyHeader")) {
                    if (properties.response === 1)
                        return "response: multiple values";
                    properties.response = 1;
                    {
                        let error = $root.dmcn.relay.MailboxBodyHeader.verify(message.mailboxBodyHeader, long + 1);
                        if (error)
                            return "mailboxBodyHeader." + error;
                    }
                }
                if (message.mailboxDelete != null && Object.hasOwnProperty.call(message, "mailboxDelete")) {
                    if (properties.response === 1)
                        return "response: multiple values";
                    properties.response = 1;
                    {
                        let error = $root.dmcn.relay.MailboxDeleteResponse.verify(message.mailboxDelete, long + 1);
                        if (error)
                            return "mailboxDelete." + error;
                    }
                }
                if (message.onionForward != null && Object.hasOwnProperty.call(message, "onionForward")) {
                    if (properties.response === 1)
                        return "response: multiple values";
                    properties.response = 1;
                    {
                        let error = $root.dmcn.relay.OnionForwardResponse.verify(message.onionForward, long + 1);
                        if (error)
                            return "onionForward." + error;
                    }
                }
                if (message.getIdentity != null && Object.hasOwnProperty.call(message, "getIdentity")) {
                    if (properties.response === 1)
                        return "response: multiple values";
                    properties.response = 1;
                    {
                        let error = $root.dmcn.relay.GetIdentityResponse.verify(message.getIdentity, long + 1);
                        if (error)
                            return "getIdentity." + error;
                    }
                }
                if (message.getDar != null && Object.hasOwnProperty.call(message, "getDar")) {
                    if (properties.response === 1)
                        return "response: multiple values";
                    properties.response = 1;
                    {
                        let error = $root.dmcn.relay.GetDARResponse.verify(message.getDar, long + 1);
                        if (error)
                            return "getDar." + error;
                    }
                }
                if (message.getFleetRoster != null && Object.hasOwnProperty.call(message, "getFleetRoster")) {
                    if (properties.response === 1)
                        return "response: multiple values";
                    properties.response = 1;
                    {
                        let error = $root.dmcn.relay.GetFleetRosterResponse.verify(message.getFleetRoster, long + 1);
                        if (error)
                            return "getFleetRoster." + error;
                    }
                }
                if (message.getRemoval != null && Object.hasOwnProperty.call(message, "getRemoval")) {
                    if (properties.response === 1)
                        return "response: multiple values";
                    properties.response = 1;
                    {
                        let error = $root.dmcn.relay.GetRemovalResponse.verify(message.getRemoval, long + 1);
                        if (error)
                            return "getRemoval." + error;
                    }
                }
                if (message.getBlocklist != null && Object.hasOwnProperty.call(message, "getBlocklist")) {
                    if (properties.response === 1)
                        return "response: multiple values";
                    properties.response = 1;
                    {
                        let error = $root.dmcn.relay.GetBlocklistResponse.verify(message.getBlocklist, long + 1);
                        if (error)
                            return "getBlocklist." + error;
                    }
                }
                if (message.putRecord != null && Object.hasOwnProperty.call(message, "putRecord")) {
                    if (properties.response === 1)
                        return "response: multiple values";
                    properties.response = 1;
                    {
                        let error = $root.dmcn.relay.PutRecordResponse.verify(message.putRecord, long + 1);
                        if (error)
                            return "putRecord." + error;
                    }
                }
                if (message.getRelayDescriptor != null && Object.hasOwnProperty.call(message, "getRelayDescriptor")) {
                    if (properties.response === 1)
                        return "response: multiple values";
                    properties.response = 1;
                    {
                        let error = $root.dmcn.relay.GetRelayDescriptorResponse.verify(message.getRelayDescriptor, long + 1);
                        if (error)
                            return "getRelayDescriptor." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a RelayResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.RelayResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.RelayResponse} RelayResponse
             */
            RelayResponse.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.RelayResponse)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.RelayResponse: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.RelayResponse();
                if (object.store != null) {
                    if (!$util.isObject(object.store))
                        throw TypeError(".dmcn.relay.RelayResponse.store: object expected");
                    message.store = $root.dmcn.relay.StoreResponse.fromObject(object.store, long + 1);
                }
                if (object.fetchChallenge != null) {
                    if (!$util.isObject(object.fetchChallenge))
                        throw TypeError(".dmcn.relay.RelayResponse.fetchChallenge: object expected");
                    message.fetchChallenge = $root.dmcn.relay.FetchChallenge.fromObject(object.fetchChallenge, long + 1);
                }
                if (object.fetch != null) {
                    if (!$util.isObject(object.fetch))
                        throw TypeError(".dmcn.relay.RelayResponse.fetch: object expected");
                    message.fetch = $root.dmcn.relay.FetchResponse.fromObject(object.fetch, long + 1);
                }
                if (object.ack != null) {
                    if (!$util.isObject(object.ack))
                        throw TypeError(".dmcn.relay.RelayResponse.ack: object expected");
                    message.ack = $root.dmcn.relay.AckResponse.fromObject(object.ack, long + 1);
                }
                if (object.ping != null) {
                    if (!$util.isObject(object.ping))
                        throw TypeError(".dmcn.relay.RelayResponse.ping: object expected");
                    message.ping = $root.dmcn.relay.PingResponse.fromObject(object.ping, long + 1);
                }
                if (object.error != null) {
                    if (!$util.isObject(object.error))
                        throw TypeError(".dmcn.relay.RelayResponse.error: object expected");
                    message.error = $root.dmcn.relay.ErrorResponse.fromObject(object.error, long + 1);
                }
                if (object.mailboxList != null) {
                    if (!$util.isObject(object.mailboxList))
                        throw TypeError(".dmcn.relay.RelayResponse.mailboxList: object expected");
                    message.mailboxList = $root.dmcn.relay.MailboxListResponse.fromObject(object.mailboxList, long + 1);
                }
                if (object.mailboxBodyHeader != null) {
                    if (!$util.isObject(object.mailboxBodyHeader))
                        throw TypeError(".dmcn.relay.RelayResponse.mailboxBodyHeader: object expected");
                    message.mailboxBodyHeader = $root.dmcn.relay.MailboxBodyHeader.fromObject(object.mailboxBodyHeader, long + 1);
                }
                if (object.mailboxDelete != null) {
                    if (!$util.isObject(object.mailboxDelete))
                        throw TypeError(".dmcn.relay.RelayResponse.mailboxDelete: object expected");
                    message.mailboxDelete = $root.dmcn.relay.MailboxDeleteResponse.fromObject(object.mailboxDelete, long + 1);
                }
                if (object.onionForward != null) {
                    if (!$util.isObject(object.onionForward))
                        throw TypeError(".dmcn.relay.RelayResponse.onionForward: object expected");
                    message.onionForward = $root.dmcn.relay.OnionForwardResponse.fromObject(object.onionForward, long + 1);
                }
                if (object.getIdentity != null) {
                    if (!$util.isObject(object.getIdentity))
                        throw TypeError(".dmcn.relay.RelayResponse.getIdentity: object expected");
                    message.getIdentity = $root.dmcn.relay.GetIdentityResponse.fromObject(object.getIdentity, long + 1);
                }
                if (object.getDar != null) {
                    if (!$util.isObject(object.getDar))
                        throw TypeError(".dmcn.relay.RelayResponse.getDar: object expected");
                    message.getDar = $root.dmcn.relay.GetDARResponse.fromObject(object.getDar, long + 1);
                }
                if (object.getFleetRoster != null) {
                    if (!$util.isObject(object.getFleetRoster))
                        throw TypeError(".dmcn.relay.RelayResponse.getFleetRoster: object expected");
                    message.getFleetRoster = $root.dmcn.relay.GetFleetRosterResponse.fromObject(object.getFleetRoster, long + 1);
                }
                if (object.getRemoval != null) {
                    if (!$util.isObject(object.getRemoval))
                        throw TypeError(".dmcn.relay.RelayResponse.getRemoval: object expected");
                    message.getRemoval = $root.dmcn.relay.GetRemovalResponse.fromObject(object.getRemoval, long + 1);
                }
                if (object.getBlocklist != null) {
                    if (!$util.isObject(object.getBlocklist))
                        throw TypeError(".dmcn.relay.RelayResponse.getBlocklist: object expected");
                    message.getBlocklist = $root.dmcn.relay.GetBlocklistResponse.fromObject(object.getBlocklist, long + 1);
                }
                if (object.putRecord != null) {
                    if (!$util.isObject(object.putRecord))
                        throw TypeError(".dmcn.relay.RelayResponse.putRecord: object expected");
                    message.putRecord = $root.dmcn.relay.PutRecordResponse.fromObject(object.putRecord, long + 1);
                }
                if (object.getRelayDescriptor != null) {
                    if (!$util.isObject(object.getRelayDescriptor))
                        throw TypeError(".dmcn.relay.RelayResponse.getRelayDescriptor: object expected");
                    message.getRelayDescriptor = $root.dmcn.relay.GetRelayDescriptorResponse.fromObject(object.getRelayDescriptor, long + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a RelayResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.RelayResponse
             * @static
             * @param {dmcn.relay.RelayResponse} message RelayResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RelayResponse.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (message.store != null && Object.hasOwnProperty.call(message, "store")) {
                    object.store = $root.dmcn.relay.StoreResponse.toObject(message.store, options, q + 1);
                    if (options.oneofs)
                        object.response = "store";
                }
                if (message.fetchChallenge != null && Object.hasOwnProperty.call(message, "fetchChallenge")) {
                    object.fetchChallenge = $root.dmcn.relay.FetchChallenge.toObject(message.fetchChallenge, options, q + 1);
                    if (options.oneofs)
                        object.response = "fetchChallenge";
                }
                if (message.fetch != null && Object.hasOwnProperty.call(message, "fetch")) {
                    object.fetch = $root.dmcn.relay.FetchResponse.toObject(message.fetch, options, q + 1);
                    if (options.oneofs)
                        object.response = "fetch";
                }
                if (message.ack != null && Object.hasOwnProperty.call(message, "ack")) {
                    object.ack = $root.dmcn.relay.AckResponse.toObject(message.ack, options, q + 1);
                    if (options.oneofs)
                        object.response = "ack";
                }
                if (message.ping != null && Object.hasOwnProperty.call(message, "ping")) {
                    object.ping = $root.dmcn.relay.PingResponse.toObject(message.ping, options, q + 1);
                    if (options.oneofs)
                        object.response = "ping";
                }
                if (message.error != null && Object.hasOwnProperty.call(message, "error")) {
                    object.error = $root.dmcn.relay.ErrorResponse.toObject(message.error, options, q + 1);
                    if (options.oneofs)
                        object.response = "error";
                }
                if (message.mailboxList != null && Object.hasOwnProperty.call(message, "mailboxList")) {
                    object.mailboxList = $root.dmcn.relay.MailboxListResponse.toObject(message.mailboxList, options, q + 1);
                    if (options.oneofs)
                        object.response = "mailboxList";
                }
                if (message.mailboxBodyHeader != null && Object.hasOwnProperty.call(message, "mailboxBodyHeader")) {
                    object.mailboxBodyHeader = $root.dmcn.relay.MailboxBodyHeader.toObject(message.mailboxBodyHeader, options, q + 1);
                    if (options.oneofs)
                        object.response = "mailboxBodyHeader";
                }
                if (message.mailboxDelete != null && Object.hasOwnProperty.call(message, "mailboxDelete")) {
                    object.mailboxDelete = $root.dmcn.relay.MailboxDeleteResponse.toObject(message.mailboxDelete, options, q + 1);
                    if (options.oneofs)
                        object.response = "mailboxDelete";
                }
                if (message.onionForward != null && Object.hasOwnProperty.call(message, "onionForward")) {
                    object.onionForward = $root.dmcn.relay.OnionForwardResponse.toObject(message.onionForward, options, q + 1);
                    if (options.oneofs)
                        object.response = "onionForward";
                }
                if (message.getIdentity != null && Object.hasOwnProperty.call(message, "getIdentity")) {
                    object.getIdentity = $root.dmcn.relay.GetIdentityResponse.toObject(message.getIdentity, options, q + 1);
                    if (options.oneofs)
                        object.response = "getIdentity";
                }
                if (message.getDar != null && Object.hasOwnProperty.call(message, "getDar")) {
                    object.getDar = $root.dmcn.relay.GetDARResponse.toObject(message.getDar, options, q + 1);
                    if (options.oneofs)
                        object.response = "getDar";
                }
                if (message.getFleetRoster != null && Object.hasOwnProperty.call(message, "getFleetRoster")) {
                    object.getFleetRoster = $root.dmcn.relay.GetFleetRosterResponse.toObject(message.getFleetRoster, options, q + 1);
                    if (options.oneofs)
                        object.response = "getFleetRoster";
                }
                if (message.getRemoval != null && Object.hasOwnProperty.call(message, "getRemoval")) {
                    object.getRemoval = $root.dmcn.relay.GetRemovalResponse.toObject(message.getRemoval, options, q + 1);
                    if (options.oneofs)
                        object.response = "getRemoval";
                }
                if (message.getBlocklist != null && Object.hasOwnProperty.call(message, "getBlocklist")) {
                    object.getBlocklist = $root.dmcn.relay.GetBlocklistResponse.toObject(message.getBlocklist, options, q + 1);
                    if (options.oneofs)
                        object.response = "getBlocklist";
                }
                if (message.putRecord != null && Object.hasOwnProperty.call(message, "putRecord")) {
                    object.putRecord = $root.dmcn.relay.PutRecordResponse.toObject(message.putRecord, options, q + 1);
                    if (options.oneofs)
                        object.response = "putRecord";
                }
                if (message.getRelayDescriptor != null && Object.hasOwnProperty.call(message, "getRelayDescriptor")) {
                    object.getRelayDescriptor = $root.dmcn.relay.GetRelayDescriptorResponse.toObject(message.getRelayDescriptor, options, q + 1);
                    if (options.oneofs)
                        object.response = "getRelayDescriptor";
                }
                return object;
            };

            /**
             * Converts this RelayResponse to JSON.
             * @function toJSON
             * @memberof dmcn.relay.RelayResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RelayResponse.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for RelayResponse
             * @function getTypeUrl
             * @memberof dmcn.relay.RelayResponse
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            RelayResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.RelayResponse";
            };

            return RelayResponse;
        })();

        relay.OnionPacket = (function() {

            /**
             * Properties of an OnionPacket.
             * @memberof dmcn.relay
             * @interface IOnionPacket
             * @property {number|null} [version] OnionPacket version
             * @property {Uint8Array|null} [ephemeralPub] OnionPacket ephemeralPub
             * @property {Uint8Array|null} [nonce] OnionPacket nonce
             * @property {Uint8Array|null} [tag] OnionPacket tag
             * @property {Uint8Array|null} [encryptedLayer] OnionPacket encryptedLayer
             */

            /**
             * Constructs a new OnionPacket.
             * @memberof dmcn.relay
             * @classdesc Represents an OnionPacket.
             * @implements IOnionPacket
             * @constructor
             * @param {dmcn.relay.IOnionPacket=} [properties] Properties to set
             */
            function OnionPacket(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * OnionPacket version.
             * @member {number} version
             * @memberof dmcn.relay.OnionPacket
             * @instance
             */
            OnionPacket.prototype.version = 0;

            /**
             * OnionPacket ephemeralPub.
             * @member {Uint8Array} ephemeralPub
             * @memberof dmcn.relay.OnionPacket
             * @instance
             */
            OnionPacket.prototype.ephemeralPub = $util.newBuffer([]);

            /**
             * OnionPacket nonce.
             * @member {Uint8Array} nonce
             * @memberof dmcn.relay.OnionPacket
             * @instance
             */
            OnionPacket.prototype.nonce = $util.newBuffer([]);

            /**
             * OnionPacket tag.
             * @member {Uint8Array} tag
             * @memberof dmcn.relay.OnionPacket
             * @instance
             */
            OnionPacket.prototype.tag = $util.newBuffer([]);

            /**
             * OnionPacket encryptedLayer.
             * @member {Uint8Array} encryptedLayer
             * @memberof dmcn.relay.OnionPacket
             * @instance
             */
            OnionPacket.prototype.encryptedLayer = $util.newBuffer([]);

            /**
             * Creates a new OnionPacket instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.OnionPacket
             * @static
             * @param {dmcn.relay.IOnionPacket=} [properties] Properties to set
             * @returns {dmcn.relay.OnionPacket} OnionPacket instance
             */
            OnionPacket.create = function create(properties) {
                return new OnionPacket(properties);
            };

            /**
             * Encodes the specified OnionPacket message. Does not implicitly {@link dmcn.relay.OnionPacket.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.OnionPacket
             * @static
             * @param {dmcn.relay.IOnionPacket} message OnionPacket message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OnionPacket.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.version);
                if (message.ephemeralPub != null && Object.hasOwnProperty.call(message, "ephemeralPub"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.ephemeralPub);
                if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.nonce);
                if (message.tag != null && Object.hasOwnProperty.call(message, "tag"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.tag);
                if (message.encryptedLayer != null && Object.hasOwnProperty.call(message, "encryptedLayer"))
                    writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.encryptedLayer);
                return writer;
            };

            /**
             * Encodes the specified OnionPacket message, length delimited. Does not implicitly {@link dmcn.relay.OnionPacket.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.OnionPacket
             * @static
             * @param {dmcn.relay.IOnionPacket} message OnionPacket message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OnionPacket.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes an OnionPacket message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.OnionPacket
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.OnionPacket} OnionPacket
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OnionPacket.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.OnionPacket();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.version = reader.uint32();
                            break;
                        }
                    case 2: {
                            message.ephemeralPub = reader.bytes();
                            break;
                        }
                    case 3: {
                            message.nonce = reader.bytes();
                            break;
                        }
                    case 4: {
                            message.tag = reader.bytes();
                            break;
                        }
                    case 5: {
                            message.encryptedLayer = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an OnionPacket message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.OnionPacket
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.OnionPacket} OnionPacket
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OnionPacket.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an OnionPacket message.
             * @function verify
             * @memberof dmcn.relay.OnionPacket
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            OnionPacket.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    if (!$util.isInteger(message.version))
                        return "version: integer expected";
                if (message.ephemeralPub != null && Object.hasOwnProperty.call(message, "ephemeralPub"))
                    if (!(message.ephemeralPub && typeof message.ephemeralPub.length === "number" || $util.isString(message.ephemeralPub)))
                        return "ephemeralPub: buffer expected";
                if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                    if (!(message.nonce && typeof message.nonce.length === "number" || $util.isString(message.nonce)))
                        return "nonce: buffer expected";
                if (message.tag != null && Object.hasOwnProperty.call(message, "tag"))
                    if (!(message.tag && typeof message.tag.length === "number" || $util.isString(message.tag)))
                        return "tag: buffer expected";
                if (message.encryptedLayer != null && Object.hasOwnProperty.call(message, "encryptedLayer"))
                    if (!(message.encryptedLayer && typeof message.encryptedLayer.length === "number" || $util.isString(message.encryptedLayer)))
                        return "encryptedLayer: buffer expected";
                return null;
            };

            /**
             * Creates an OnionPacket message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.OnionPacket
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.OnionPacket} OnionPacket
             */
            OnionPacket.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.OnionPacket)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.OnionPacket: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.OnionPacket();
                if (object.version != null)
                    message.version = object.version >>> 0;
                if (object.ephemeralPub != null)
                    if (typeof object.ephemeralPub === "string")
                        $util.base64.decode(object.ephemeralPub, message.ephemeralPub = $util.newBuffer($util.base64.length(object.ephemeralPub)), 0);
                    else if (object.ephemeralPub.length >= 0)
                        message.ephemeralPub = object.ephemeralPub;
                if (object.nonce != null)
                    if (typeof object.nonce === "string")
                        $util.base64.decode(object.nonce, message.nonce = $util.newBuffer($util.base64.length(object.nonce)), 0);
                    else if (object.nonce.length >= 0)
                        message.nonce = object.nonce;
                if (object.tag != null)
                    if (typeof object.tag === "string")
                        $util.base64.decode(object.tag, message.tag = $util.newBuffer($util.base64.length(object.tag)), 0);
                    else if (object.tag.length >= 0)
                        message.tag = object.tag;
                if (object.encryptedLayer != null)
                    if (typeof object.encryptedLayer === "string")
                        $util.base64.decode(object.encryptedLayer, message.encryptedLayer = $util.newBuffer($util.base64.length(object.encryptedLayer)), 0);
                    else if (object.encryptedLayer.length >= 0)
                        message.encryptedLayer = object.encryptedLayer;
                return message;
            };

            /**
             * Creates a plain object from an OnionPacket message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.OnionPacket
             * @static
             * @param {dmcn.relay.OnionPacket} message OnionPacket
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            OnionPacket.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.version = 0;
                    if (options.bytes === String)
                        object.ephemeralPub = "";
                    else {
                        object.ephemeralPub = [];
                        if (options.bytes !== Array)
                            object.ephemeralPub = $util.newBuffer(object.ephemeralPub);
                    }
                    if (options.bytes === String)
                        object.nonce = "";
                    else {
                        object.nonce = [];
                        if (options.bytes !== Array)
                            object.nonce = $util.newBuffer(object.nonce);
                    }
                    if (options.bytes === String)
                        object.tag = "";
                    else {
                        object.tag = [];
                        if (options.bytes !== Array)
                            object.tag = $util.newBuffer(object.tag);
                    }
                    if (options.bytes === String)
                        object.encryptedLayer = "";
                    else {
                        object.encryptedLayer = [];
                        if (options.bytes !== Array)
                            object.encryptedLayer = $util.newBuffer(object.encryptedLayer);
                    }
                }
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    object.version = message.version;
                if (message.ephemeralPub != null && Object.hasOwnProperty.call(message, "ephemeralPub"))
                    object.ephemeralPub = options.bytes === String ? $util.base64.encode(message.ephemeralPub, 0, message.ephemeralPub.length) : options.bytes === Array ? Array.prototype.slice.call(message.ephemeralPub) : message.ephemeralPub;
                if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                    object.nonce = options.bytes === String ? $util.base64.encode(message.nonce, 0, message.nonce.length) : options.bytes === Array ? Array.prototype.slice.call(message.nonce) : message.nonce;
                if (message.tag != null && Object.hasOwnProperty.call(message, "tag"))
                    object.tag = options.bytes === String ? $util.base64.encode(message.tag, 0, message.tag.length) : options.bytes === Array ? Array.prototype.slice.call(message.tag) : message.tag;
                if (message.encryptedLayer != null && Object.hasOwnProperty.call(message, "encryptedLayer"))
                    object.encryptedLayer = options.bytes === String ? $util.base64.encode(message.encryptedLayer, 0, message.encryptedLayer.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedLayer) : message.encryptedLayer;
                return object;
            };

            /**
             * Converts this OnionPacket to JSON.
             * @function toJSON
             * @memberof dmcn.relay.OnionPacket
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            OnionPacket.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for OnionPacket
             * @function getTypeUrl
             * @memberof dmcn.relay.OnionPacket
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            OnionPacket.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.OnionPacket";
            };

            return OnionPacket;
        })();

        relay.OnionLayer = (function() {

            /**
             * Properties of an OnionLayer.
             * @memberof dmcn.relay
             * @interface IOnionLayer
             * @property {string|null} [nextHop] OnionLayer nextHop
             * @property {number|Long|null} [ttlUnix] OnionLayer ttlUnix
             * @property {dmcn.relay.IOnionPacket|null} [inner] OnionLayer inner
             * @property {Uint8Array|null} [delivery] OnionLayer delivery
             */

            /**
             * Constructs a new OnionLayer.
             * @memberof dmcn.relay
             * @classdesc Represents an OnionLayer.
             * @implements IOnionLayer
             * @constructor
             * @param {dmcn.relay.IOnionLayer=} [properties] Properties to set
             */
            function OnionLayer(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * OnionLayer nextHop.
             * @member {string} nextHop
             * @memberof dmcn.relay.OnionLayer
             * @instance
             */
            OnionLayer.prototype.nextHop = "";

            /**
             * OnionLayer ttlUnix.
             * @member {number|Long} ttlUnix
             * @memberof dmcn.relay.OnionLayer
             * @instance
             */
            OnionLayer.prototype.ttlUnix = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * OnionLayer inner.
             * @member {dmcn.relay.IOnionPacket|null|undefined} inner
             * @memberof dmcn.relay.OnionLayer
             * @instance
             */
            OnionLayer.prototype.inner = null;

            /**
             * OnionLayer delivery.
             * @member {Uint8Array} delivery
             * @memberof dmcn.relay.OnionLayer
             * @instance
             */
            OnionLayer.prototype.delivery = $util.newBuffer([]);

            /**
             * Creates a new OnionLayer instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.OnionLayer
             * @static
             * @param {dmcn.relay.IOnionLayer=} [properties] Properties to set
             * @returns {dmcn.relay.OnionLayer} OnionLayer instance
             */
            OnionLayer.create = function create(properties) {
                return new OnionLayer(properties);
            };

            /**
             * Encodes the specified OnionLayer message. Does not implicitly {@link dmcn.relay.OnionLayer.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.OnionLayer
             * @static
             * @param {dmcn.relay.IOnionLayer} message OnionLayer message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OnionLayer.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.nextHop != null && Object.hasOwnProperty.call(message, "nextHop"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.nextHop);
                if (message.ttlUnix != null && Object.hasOwnProperty.call(message, "ttlUnix"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.ttlUnix);
                if (message.inner != null && Object.hasOwnProperty.call(message, "inner"))
                    $root.dmcn.relay.OnionPacket.encode(message.inner, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.delivery != null && Object.hasOwnProperty.call(message, "delivery"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.delivery);
                return writer;
            };

            /**
             * Encodes the specified OnionLayer message, length delimited. Does not implicitly {@link dmcn.relay.OnionLayer.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.OnionLayer
             * @static
             * @param {dmcn.relay.IOnionLayer} message OnionLayer message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OnionLayer.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes an OnionLayer message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.OnionLayer
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.OnionLayer} OnionLayer
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OnionLayer.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.OnionLayer();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.nextHop = reader.string();
                            break;
                        }
                    case 2: {
                            message.ttlUnix = reader.int64();
                            break;
                        }
                    case 3: {
                            message.inner = $root.dmcn.relay.OnionPacket.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 4: {
                            message.delivery = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an OnionLayer message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.OnionLayer
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.OnionLayer} OnionLayer
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OnionLayer.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an OnionLayer message.
             * @function verify
             * @memberof dmcn.relay.OnionLayer
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            OnionLayer.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.nextHop != null && Object.hasOwnProperty.call(message, "nextHop"))
                    if (!$util.isString(message.nextHop))
                        return "nextHop: string expected";
                if (message.ttlUnix != null && Object.hasOwnProperty.call(message, "ttlUnix"))
                    if (!$util.isInteger(message.ttlUnix) && !(message.ttlUnix && $util.isInteger(message.ttlUnix.low) && $util.isInteger(message.ttlUnix.high)))
                        return "ttlUnix: integer|Long expected";
                if (message.inner != null && Object.hasOwnProperty.call(message, "inner")) {
                    let error = $root.dmcn.relay.OnionPacket.verify(message.inner, long + 1);
                    if (error)
                        return "inner." + error;
                }
                if (message.delivery != null && Object.hasOwnProperty.call(message, "delivery"))
                    if (!(message.delivery && typeof message.delivery.length === "number" || $util.isString(message.delivery)))
                        return "delivery: buffer expected";
                return null;
            };

            /**
             * Creates an OnionLayer message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.OnionLayer
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.OnionLayer} OnionLayer
             */
            OnionLayer.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.OnionLayer)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.OnionLayer: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.OnionLayer();
                if (object.nextHop != null)
                    message.nextHop = String(object.nextHop);
                if (object.ttlUnix != null)
                    if ($util.Long)
                        message.ttlUnix = $util.Long.fromValue(object.ttlUnix, false);
                    else if (typeof object.ttlUnix === "string")
                        message.ttlUnix = parseInt(object.ttlUnix, 10);
                    else if (typeof object.ttlUnix === "number")
                        message.ttlUnix = object.ttlUnix;
                    else if (typeof object.ttlUnix === "object")
                        message.ttlUnix = new $util.LongBits(object.ttlUnix.low >>> 0, object.ttlUnix.high >>> 0).toNumber();
                if (object.inner != null) {
                    if (!$util.isObject(object.inner))
                        throw TypeError(".dmcn.relay.OnionLayer.inner: object expected");
                    message.inner = $root.dmcn.relay.OnionPacket.fromObject(object.inner, long + 1);
                }
                if (object.delivery != null)
                    if (typeof object.delivery === "string")
                        $util.base64.decode(object.delivery, message.delivery = $util.newBuffer($util.base64.length(object.delivery)), 0);
                    else if (object.delivery.length >= 0)
                        message.delivery = object.delivery;
                return message;
            };

            /**
             * Creates a plain object from an OnionLayer message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.OnionLayer
             * @static
             * @param {dmcn.relay.OnionLayer} message OnionLayer
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            OnionLayer.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.nextHop = "";
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.ttlUnix = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.ttlUnix = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    object.inner = null;
                    if (options.bytes === String)
                        object.delivery = "";
                    else {
                        object.delivery = [];
                        if (options.bytes !== Array)
                            object.delivery = $util.newBuffer(object.delivery);
                    }
                }
                if (message.nextHop != null && Object.hasOwnProperty.call(message, "nextHop"))
                    object.nextHop = message.nextHop;
                if (message.ttlUnix != null && Object.hasOwnProperty.call(message, "ttlUnix"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.ttlUnix = typeof message.ttlUnix === "number" ? BigInt(message.ttlUnix) : $util.Long.fromBits(message.ttlUnix.low >>> 0, message.ttlUnix.high >>> 0, false).toBigInt();
                    else if (typeof message.ttlUnix === "number")
                        object.ttlUnix = options.longs === String ? String(message.ttlUnix) : message.ttlUnix;
                    else
                        object.ttlUnix = options.longs === String ? $util.Long.prototype.toString.call(message.ttlUnix) : options.longs === Number ? new $util.LongBits(message.ttlUnix.low >>> 0, message.ttlUnix.high >>> 0).toNumber() : message.ttlUnix;
                if (message.inner != null && Object.hasOwnProperty.call(message, "inner"))
                    object.inner = $root.dmcn.relay.OnionPacket.toObject(message.inner, options, q + 1);
                if (message.delivery != null && Object.hasOwnProperty.call(message, "delivery"))
                    object.delivery = options.bytes === String ? $util.base64.encode(message.delivery, 0, message.delivery.length) : options.bytes === Array ? Array.prototype.slice.call(message.delivery) : message.delivery;
                return object;
            };

            /**
             * Converts this OnionLayer to JSON.
             * @function toJSON
             * @memberof dmcn.relay.OnionLayer
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            OnionLayer.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for OnionLayer
             * @function getTypeUrl
             * @memberof dmcn.relay.OnionLayer
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            OnionLayer.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.OnionLayer";
            };

            return OnionLayer;
        })();

        relay.OnionForwardRequest = (function() {

            /**
             * Properties of an OnionForwardRequest.
             * @memberof dmcn.relay
             * @interface IOnionForwardRequest
             * @property {dmcn.relay.IOnionPacket|null} [packet] OnionForwardRequest packet
             */

            /**
             * Constructs a new OnionForwardRequest.
             * @memberof dmcn.relay
             * @classdesc Represents an OnionForwardRequest.
             * @implements IOnionForwardRequest
             * @constructor
             * @param {dmcn.relay.IOnionForwardRequest=} [properties] Properties to set
             */
            function OnionForwardRequest(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * OnionForwardRequest packet.
             * @member {dmcn.relay.IOnionPacket|null|undefined} packet
             * @memberof dmcn.relay.OnionForwardRequest
             * @instance
             */
            OnionForwardRequest.prototype.packet = null;

            /**
             * Creates a new OnionForwardRequest instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.OnionForwardRequest
             * @static
             * @param {dmcn.relay.IOnionForwardRequest=} [properties] Properties to set
             * @returns {dmcn.relay.OnionForwardRequest} OnionForwardRequest instance
             */
            OnionForwardRequest.create = function create(properties) {
                return new OnionForwardRequest(properties);
            };

            /**
             * Encodes the specified OnionForwardRequest message. Does not implicitly {@link dmcn.relay.OnionForwardRequest.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.OnionForwardRequest
             * @static
             * @param {dmcn.relay.IOnionForwardRequest} message OnionForwardRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OnionForwardRequest.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.packet != null && Object.hasOwnProperty.call(message, "packet"))
                    $root.dmcn.relay.OnionPacket.encode(message.packet, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified OnionForwardRequest message, length delimited. Does not implicitly {@link dmcn.relay.OnionForwardRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.OnionForwardRequest
             * @static
             * @param {dmcn.relay.IOnionForwardRequest} message OnionForwardRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OnionForwardRequest.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes an OnionForwardRequest message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.OnionForwardRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.OnionForwardRequest} OnionForwardRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OnionForwardRequest.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.OnionForwardRequest();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.packet = $root.dmcn.relay.OnionPacket.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an OnionForwardRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.OnionForwardRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.OnionForwardRequest} OnionForwardRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OnionForwardRequest.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an OnionForwardRequest message.
             * @function verify
             * @memberof dmcn.relay.OnionForwardRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            OnionForwardRequest.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.packet != null && Object.hasOwnProperty.call(message, "packet")) {
                    let error = $root.dmcn.relay.OnionPacket.verify(message.packet, long + 1);
                    if (error)
                        return "packet." + error;
                }
                return null;
            };

            /**
             * Creates an OnionForwardRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.OnionForwardRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.OnionForwardRequest} OnionForwardRequest
             */
            OnionForwardRequest.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.OnionForwardRequest)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.OnionForwardRequest: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.OnionForwardRequest();
                if (object.packet != null) {
                    if (!$util.isObject(object.packet))
                        throw TypeError(".dmcn.relay.OnionForwardRequest.packet: object expected");
                    message.packet = $root.dmcn.relay.OnionPacket.fromObject(object.packet, long + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from an OnionForwardRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.OnionForwardRequest
             * @static
             * @param {dmcn.relay.OnionForwardRequest} message OnionForwardRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            OnionForwardRequest.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    object.packet = null;
                if (message.packet != null && Object.hasOwnProperty.call(message, "packet"))
                    object.packet = $root.dmcn.relay.OnionPacket.toObject(message.packet, options, q + 1);
                return object;
            };

            /**
             * Converts this OnionForwardRequest to JSON.
             * @function toJSON
             * @memberof dmcn.relay.OnionForwardRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            OnionForwardRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for OnionForwardRequest
             * @function getTypeUrl
             * @memberof dmcn.relay.OnionForwardRequest
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            OnionForwardRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.OnionForwardRequest";
            };

            return OnionForwardRequest;
        })();

        relay.OnionForwardResponse = (function() {

            /**
             * Properties of an OnionForwardResponse.
             * @memberof dmcn.relay
             * @interface IOnionForwardResponse
             * @property {boolean|null} [accepted] OnionForwardResponse accepted
             */

            /**
             * Constructs a new OnionForwardResponse.
             * @memberof dmcn.relay
             * @classdesc Represents an OnionForwardResponse.
             * @implements IOnionForwardResponse
             * @constructor
             * @param {dmcn.relay.IOnionForwardResponse=} [properties] Properties to set
             */
            function OnionForwardResponse(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * OnionForwardResponse accepted.
             * @member {boolean} accepted
             * @memberof dmcn.relay.OnionForwardResponse
             * @instance
             */
            OnionForwardResponse.prototype.accepted = false;

            /**
             * Creates a new OnionForwardResponse instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.OnionForwardResponse
             * @static
             * @param {dmcn.relay.IOnionForwardResponse=} [properties] Properties to set
             * @returns {dmcn.relay.OnionForwardResponse} OnionForwardResponse instance
             */
            OnionForwardResponse.create = function create(properties) {
                return new OnionForwardResponse(properties);
            };

            /**
             * Encodes the specified OnionForwardResponse message. Does not implicitly {@link dmcn.relay.OnionForwardResponse.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.OnionForwardResponse
             * @static
             * @param {dmcn.relay.IOnionForwardResponse} message OnionForwardResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OnionForwardResponse.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.accepted != null && Object.hasOwnProperty.call(message, "accepted"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.accepted);
                return writer;
            };

            /**
             * Encodes the specified OnionForwardResponse message, length delimited. Does not implicitly {@link dmcn.relay.OnionForwardResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.OnionForwardResponse
             * @static
             * @param {dmcn.relay.IOnionForwardResponse} message OnionForwardResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OnionForwardResponse.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes an OnionForwardResponse message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.OnionForwardResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.OnionForwardResponse} OnionForwardResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OnionForwardResponse.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.OnionForwardResponse();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.accepted = reader.bool();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an OnionForwardResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.OnionForwardResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.OnionForwardResponse} OnionForwardResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OnionForwardResponse.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an OnionForwardResponse message.
             * @function verify
             * @memberof dmcn.relay.OnionForwardResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            OnionForwardResponse.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.accepted != null && Object.hasOwnProperty.call(message, "accepted"))
                    if (typeof message.accepted !== "boolean")
                        return "accepted: boolean expected";
                return null;
            };

            /**
             * Creates an OnionForwardResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.OnionForwardResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.OnionForwardResponse} OnionForwardResponse
             */
            OnionForwardResponse.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.OnionForwardResponse)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.OnionForwardResponse: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.OnionForwardResponse();
                if (object.accepted != null)
                    message.accepted = Boolean(object.accepted);
                return message;
            };

            /**
             * Creates a plain object from an OnionForwardResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.OnionForwardResponse
             * @static
             * @param {dmcn.relay.OnionForwardResponse} message OnionForwardResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            OnionForwardResponse.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    object.accepted = false;
                if (message.accepted != null && Object.hasOwnProperty.call(message, "accepted"))
                    object.accepted = message.accepted;
                return object;
            };

            /**
             * Converts this OnionForwardResponse to JSON.
             * @function toJSON
             * @memberof dmcn.relay.OnionForwardResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            OnionForwardResponse.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for OnionForwardResponse
             * @function getTypeUrl
             * @memberof dmcn.relay.OnionForwardResponse
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            OnionForwardResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.OnionForwardResponse";
            };

            return OnionForwardResponse;
        })();

        relay.StoreRequest = (function() {

            /**
             * Properties of a StoreRequest.
             * @memberof dmcn.relay
             * @interface IStoreRequest
             * @property {string|null} [senderAddress] StoreRequest senderAddress
             * @property {Uint8Array|null} [senderSignature] StoreRequest senderSignature
             * @property {dmcn.message.IEncryptedEnvelope|null} [envelope] StoreRequest envelope
             */

            /**
             * Constructs a new StoreRequest.
             * @memberof dmcn.relay
             * @classdesc Represents a StoreRequest.
             * @implements IStoreRequest
             * @constructor
             * @param {dmcn.relay.IStoreRequest=} [properties] Properties to set
             */
            function StoreRequest(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * StoreRequest senderAddress.
             * @member {string} senderAddress
             * @memberof dmcn.relay.StoreRequest
             * @instance
             */
            StoreRequest.prototype.senderAddress = "";

            /**
             * StoreRequest senderSignature.
             * @member {Uint8Array} senderSignature
             * @memberof dmcn.relay.StoreRequest
             * @instance
             */
            StoreRequest.prototype.senderSignature = $util.newBuffer([]);

            /**
             * StoreRequest envelope.
             * @member {dmcn.message.IEncryptedEnvelope|null|undefined} envelope
             * @memberof dmcn.relay.StoreRequest
             * @instance
             */
            StoreRequest.prototype.envelope = null;

            /**
             * Creates a new StoreRequest instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.StoreRequest
             * @static
             * @param {dmcn.relay.IStoreRequest=} [properties] Properties to set
             * @returns {dmcn.relay.StoreRequest} StoreRequest instance
             */
            StoreRequest.create = function create(properties) {
                return new StoreRequest(properties);
            };

            /**
             * Encodes the specified StoreRequest message. Does not implicitly {@link dmcn.relay.StoreRequest.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.StoreRequest
             * @static
             * @param {dmcn.relay.IStoreRequest} message StoreRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StoreRequest.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.senderAddress != null && Object.hasOwnProperty.call(message, "senderAddress"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.senderAddress);
                if (message.senderSignature != null && Object.hasOwnProperty.call(message, "senderSignature"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.senderSignature);
                if (message.envelope != null && Object.hasOwnProperty.call(message, "envelope"))
                    $root.dmcn.message.EncryptedEnvelope.encode(message.envelope, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified StoreRequest message, length delimited. Does not implicitly {@link dmcn.relay.StoreRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.StoreRequest
             * @static
             * @param {dmcn.relay.IStoreRequest} message StoreRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StoreRequest.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a StoreRequest message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.StoreRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.StoreRequest} StoreRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StoreRequest.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.StoreRequest();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.senderAddress = reader.string();
                            break;
                        }
                    case 2: {
                            message.senderSignature = reader.bytes();
                            break;
                        }
                    case 3: {
                            message.envelope = $root.dmcn.message.EncryptedEnvelope.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a StoreRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.StoreRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.StoreRequest} StoreRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StoreRequest.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a StoreRequest message.
             * @function verify
             * @memberof dmcn.relay.StoreRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StoreRequest.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.senderAddress != null && Object.hasOwnProperty.call(message, "senderAddress"))
                    if (!$util.isString(message.senderAddress))
                        return "senderAddress: string expected";
                if (message.senderSignature != null && Object.hasOwnProperty.call(message, "senderSignature"))
                    if (!(message.senderSignature && typeof message.senderSignature.length === "number" || $util.isString(message.senderSignature)))
                        return "senderSignature: buffer expected";
                if (message.envelope != null && Object.hasOwnProperty.call(message, "envelope")) {
                    let error = $root.dmcn.message.EncryptedEnvelope.verify(message.envelope, long + 1);
                    if (error)
                        return "envelope." + error;
                }
                return null;
            };

            /**
             * Creates a StoreRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.StoreRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.StoreRequest} StoreRequest
             */
            StoreRequest.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.StoreRequest)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.StoreRequest: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.StoreRequest();
                if (object.senderAddress != null)
                    message.senderAddress = String(object.senderAddress);
                if (object.senderSignature != null)
                    if (typeof object.senderSignature === "string")
                        $util.base64.decode(object.senderSignature, message.senderSignature = $util.newBuffer($util.base64.length(object.senderSignature)), 0);
                    else if (object.senderSignature.length >= 0)
                        message.senderSignature = object.senderSignature;
                if (object.envelope != null) {
                    if (!$util.isObject(object.envelope))
                        throw TypeError(".dmcn.relay.StoreRequest.envelope: object expected");
                    message.envelope = $root.dmcn.message.EncryptedEnvelope.fromObject(object.envelope, long + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a StoreRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.StoreRequest
             * @static
             * @param {dmcn.relay.StoreRequest} message StoreRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StoreRequest.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.senderAddress = "";
                    if (options.bytes === String)
                        object.senderSignature = "";
                    else {
                        object.senderSignature = [];
                        if (options.bytes !== Array)
                            object.senderSignature = $util.newBuffer(object.senderSignature);
                    }
                    object.envelope = null;
                }
                if (message.senderAddress != null && Object.hasOwnProperty.call(message, "senderAddress"))
                    object.senderAddress = message.senderAddress;
                if (message.senderSignature != null && Object.hasOwnProperty.call(message, "senderSignature"))
                    object.senderSignature = options.bytes === String ? $util.base64.encode(message.senderSignature, 0, message.senderSignature.length) : options.bytes === Array ? Array.prototype.slice.call(message.senderSignature) : message.senderSignature;
                if (message.envelope != null && Object.hasOwnProperty.call(message, "envelope"))
                    object.envelope = $root.dmcn.message.EncryptedEnvelope.toObject(message.envelope, options, q + 1);
                return object;
            };

            /**
             * Converts this StoreRequest to JSON.
             * @function toJSON
             * @memberof dmcn.relay.StoreRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StoreRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for StoreRequest
             * @function getTypeUrl
             * @memberof dmcn.relay.StoreRequest
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            StoreRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.StoreRequest";
            };

            return StoreRequest;
        })();

        relay.StoreResponse = (function() {

            /**
             * Properties of a StoreResponse.
             * @memberof dmcn.relay
             * @interface IStoreResponse
             * @property {Uint8Array|null} [envelopeHash] StoreResponse envelopeHash
             */

            /**
             * Constructs a new StoreResponse.
             * @memberof dmcn.relay
             * @classdesc Represents a StoreResponse.
             * @implements IStoreResponse
             * @constructor
             * @param {dmcn.relay.IStoreResponse=} [properties] Properties to set
             */
            function StoreResponse(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * StoreResponse envelopeHash.
             * @member {Uint8Array} envelopeHash
             * @memberof dmcn.relay.StoreResponse
             * @instance
             */
            StoreResponse.prototype.envelopeHash = $util.newBuffer([]);

            /**
             * Creates a new StoreResponse instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.StoreResponse
             * @static
             * @param {dmcn.relay.IStoreResponse=} [properties] Properties to set
             * @returns {dmcn.relay.StoreResponse} StoreResponse instance
             */
            StoreResponse.create = function create(properties) {
                return new StoreResponse(properties);
            };

            /**
             * Encodes the specified StoreResponse message. Does not implicitly {@link dmcn.relay.StoreResponse.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.StoreResponse
             * @static
             * @param {dmcn.relay.IStoreResponse} message StoreResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StoreResponse.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.envelopeHash != null && Object.hasOwnProperty.call(message, "envelopeHash"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.envelopeHash);
                return writer;
            };

            /**
             * Encodes the specified StoreResponse message, length delimited. Does not implicitly {@link dmcn.relay.StoreResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.StoreResponse
             * @static
             * @param {dmcn.relay.IStoreResponse} message StoreResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StoreResponse.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a StoreResponse message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.StoreResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.StoreResponse} StoreResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StoreResponse.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.StoreResponse();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.envelopeHash = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a StoreResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.StoreResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.StoreResponse} StoreResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StoreResponse.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a StoreResponse message.
             * @function verify
             * @memberof dmcn.relay.StoreResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StoreResponse.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.envelopeHash != null && Object.hasOwnProperty.call(message, "envelopeHash"))
                    if (!(message.envelopeHash && typeof message.envelopeHash.length === "number" || $util.isString(message.envelopeHash)))
                        return "envelopeHash: buffer expected";
                return null;
            };

            /**
             * Creates a StoreResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.StoreResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.StoreResponse} StoreResponse
             */
            StoreResponse.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.StoreResponse)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.StoreResponse: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.StoreResponse();
                if (object.envelopeHash != null)
                    if (typeof object.envelopeHash === "string")
                        $util.base64.decode(object.envelopeHash, message.envelopeHash = $util.newBuffer($util.base64.length(object.envelopeHash)), 0);
                    else if (object.envelopeHash.length >= 0)
                        message.envelopeHash = object.envelopeHash;
                return message;
            };

            /**
             * Creates a plain object from a StoreResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.StoreResponse
             * @static
             * @param {dmcn.relay.StoreResponse} message StoreResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StoreResponse.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    if (options.bytes === String)
                        object.envelopeHash = "";
                    else {
                        object.envelopeHash = [];
                        if (options.bytes !== Array)
                            object.envelopeHash = $util.newBuffer(object.envelopeHash);
                    }
                if (message.envelopeHash != null && Object.hasOwnProperty.call(message, "envelopeHash"))
                    object.envelopeHash = options.bytes === String ? $util.base64.encode(message.envelopeHash, 0, message.envelopeHash.length) : options.bytes === Array ? Array.prototype.slice.call(message.envelopeHash) : message.envelopeHash;
                return object;
            };

            /**
             * Converts this StoreResponse to JSON.
             * @function toJSON
             * @memberof dmcn.relay.StoreResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StoreResponse.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for StoreResponse
             * @function getTypeUrl
             * @memberof dmcn.relay.StoreResponse
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            StoreResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.StoreResponse";
            };

            return StoreResponse;
        })();

        relay.FetchInit = (function() {

            /**
             * Properties of a FetchInit.
             * @memberof dmcn.relay
             * @interface IFetchInit
             * @property {string|null} [address] FetchInit address
             */

            /**
             * Constructs a new FetchInit.
             * @memberof dmcn.relay
             * @classdesc Represents a FetchInit.
             * @implements IFetchInit
             * @constructor
             * @param {dmcn.relay.IFetchInit=} [properties] Properties to set
             */
            function FetchInit(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * FetchInit address.
             * @member {string} address
             * @memberof dmcn.relay.FetchInit
             * @instance
             */
            FetchInit.prototype.address = "";

            /**
             * Creates a new FetchInit instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.FetchInit
             * @static
             * @param {dmcn.relay.IFetchInit=} [properties] Properties to set
             * @returns {dmcn.relay.FetchInit} FetchInit instance
             */
            FetchInit.create = function create(properties) {
                return new FetchInit(properties);
            };

            /**
             * Encodes the specified FetchInit message. Does not implicitly {@link dmcn.relay.FetchInit.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.FetchInit
             * @static
             * @param {dmcn.relay.IFetchInit} message FetchInit message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FetchInit.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.address);
                return writer;
            };

            /**
             * Encodes the specified FetchInit message, length delimited. Does not implicitly {@link dmcn.relay.FetchInit.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.FetchInit
             * @static
             * @param {dmcn.relay.IFetchInit} message FetchInit message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FetchInit.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a FetchInit message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.FetchInit
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.FetchInit} FetchInit
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FetchInit.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.FetchInit();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.address = reader.string();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a FetchInit message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.FetchInit
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.FetchInit} FetchInit
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FetchInit.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FetchInit message.
             * @function verify
             * @memberof dmcn.relay.FetchInit
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FetchInit.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    if (!$util.isString(message.address))
                        return "address: string expected";
                return null;
            };

            /**
             * Creates a FetchInit message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.FetchInit
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.FetchInit} FetchInit
             */
            FetchInit.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.FetchInit)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.FetchInit: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.FetchInit();
                if (object.address != null)
                    message.address = String(object.address);
                return message;
            };

            /**
             * Creates a plain object from a FetchInit message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.FetchInit
             * @static
             * @param {dmcn.relay.FetchInit} message FetchInit
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FetchInit.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    object.address = "";
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    object.address = message.address;
                return object;
            };

            /**
             * Converts this FetchInit to JSON.
             * @function toJSON
             * @memberof dmcn.relay.FetchInit
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FetchInit.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for FetchInit
             * @function getTypeUrl
             * @memberof dmcn.relay.FetchInit
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            FetchInit.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.FetchInit";
            };

            return FetchInit;
        })();

        relay.FetchChallenge = (function() {

            /**
             * Properties of a FetchChallenge.
             * @memberof dmcn.relay
             * @interface IFetchChallenge
             * @property {Uint8Array|null} [nonce] FetchChallenge nonce
             */

            /**
             * Constructs a new FetchChallenge.
             * @memberof dmcn.relay
             * @classdesc Represents a FetchChallenge.
             * @implements IFetchChallenge
             * @constructor
             * @param {dmcn.relay.IFetchChallenge=} [properties] Properties to set
             */
            function FetchChallenge(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * FetchChallenge nonce.
             * @member {Uint8Array} nonce
             * @memberof dmcn.relay.FetchChallenge
             * @instance
             */
            FetchChallenge.prototype.nonce = $util.newBuffer([]);

            /**
             * Creates a new FetchChallenge instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.FetchChallenge
             * @static
             * @param {dmcn.relay.IFetchChallenge=} [properties] Properties to set
             * @returns {dmcn.relay.FetchChallenge} FetchChallenge instance
             */
            FetchChallenge.create = function create(properties) {
                return new FetchChallenge(properties);
            };

            /**
             * Encodes the specified FetchChallenge message. Does not implicitly {@link dmcn.relay.FetchChallenge.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.FetchChallenge
             * @static
             * @param {dmcn.relay.IFetchChallenge} message FetchChallenge message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FetchChallenge.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.nonce);
                return writer;
            };

            /**
             * Encodes the specified FetchChallenge message, length delimited. Does not implicitly {@link dmcn.relay.FetchChallenge.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.FetchChallenge
             * @static
             * @param {dmcn.relay.IFetchChallenge} message FetchChallenge message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FetchChallenge.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a FetchChallenge message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.FetchChallenge
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.FetchChallenge} FetchChallenge
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FetchChallenge.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.FetchChallenge();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.nonce = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a FetchChallenge message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.FetchChallenge
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.FetchChallenge} FetchChallenge
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FetchChallenge.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FetchChallenge message.
             * @function verify
             * @memberof dmcn.relay.FetchChallenge
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FetchChallenge.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                    if (!(message.nonce && typeof message.nonce.length === "number" || $util.isString(message.nonce)))
                        return "nonce: buffer expected";
                return null;
            };

            /**
             * Creates a FetchChallenge message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.FetchChallenge
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.FetchChallenge} FetchChallenge
             */
            FetchChallenge.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.FetchChallenge)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.FetchChallenge: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.FetchChallenge();
                if (object.nonce != null)
                    if (typeof object.nonce === "string")
                        $util.base64.decode(object.nonce, message.nonce = $util.newBuffer($util.base64.length(object.nonce)), 0);
                    else if (object.nonce.length >= 0)
                        message.nonce = object.nonce;
                return message;
            };

            /**
             * Creates a plain object from a FetchChallenge message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.FetchChallenge
             * @static
             * @param {dmcn.relay.FetchChallenge} message FetchChallenge
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FetchChallenge.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    if (options.bytes === String)
                        object.nonce = "";
                    else {
                        object.nonce = [];
                        if (options.bytes !== Array)
                            object.nonce = $util.newBuffer(object.nonce);
                    }
                if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                    object.nonce = options.bytes === String ? $util.base64.encode(message.nonce, 0, message.nonce.length) : options.bytes === Array ? Array.prototype.slice.call(message.nonce) : message.nonce;
                return object;
            };

            /**
             * Converts this FetchChallenge to JSON.
             * @function toJSON
             * @memberof dmcn.relay.FetchChallenge
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FetchChallenge.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for FetchChallenge
             * @function getTypeUrl
             * @memberof dmcn.relay.FetchChallenge
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            FetchChallenge.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.FetchChallenge";
            };

            return FetchChallenge;
        })();

        relay.FetchProof = (function() {

            /**
             * Properties of a FetchProof.
             * @memberof dmcn.relay
             * @interface IFetchProof
             * @property {string|null} [address] FetchProof address
             * @property {Uint8Array|null} [nonce] FetchProof nonce
             * @property {Uint8Array|null} [signature] FetchProof signature
             */

            /**
             * Constructs a new FetchProof.
             * @memberof dmcn.relay
             * @classdesc Represents a FetchProof.
             * @implements IFetchProof
             * @constructor
             * @param {dmcn.relay.IFetchProof=} [properties] Properties to set
             */
            function FetchProof(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * FetchProof address.
             * @member {string} address
             * @memberof dmcn.relay.FetchProof
             * @instance
             */
            FetchProof.prototype.address = "";

            /**
             * FetchProof nonce.
             * @member {Uint8Array} nonce
             * @memberof dmcn.relay.FetchProof
             * @instance
             */
            FetchProof.prototype.nonce = $util.newBuffer([]);

            /**
             * FetchProof signature.
             * @member {Uint8Array} signature
             * @memberof dmcn.relay.FetchProof
             * @instance
             */
            FetchProof.prototype.signature = $util.newBuffer([]);

            /**
             * Creates a new FetchProof instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.FetchProof
             * @static
             * @param {dmcn.relay.IFetchProof=} [properties] Properties to set
             * @returns {dmcn.relay.FetchProof} FetchProof instance
             */
            FetchProof.create = function create(properties) {
                return new FetchProof(properties);
            };

            /**
             * Encodes the specified FetchProof message. Does not implicitly {@link dmcn.relay.FetchProof.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.FetchProof
             * @static
             * @param {dmcn.relay.IFetchProof} message FetchProof message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FetchProof.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.address);
                if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.nonce);
                if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.signature);
                return writer;
            };

            /**
             * Encodes the specified FetchProof message, length delimited. Does not implicitly {@link dmcn.relay.FetchProof.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.FetchProof
             * @static
             * @param {dmcn.relay.IFetchProof} message FetchProof message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FetchProof.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a FetchProof message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.FetchProof
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.FetchProof} FetchProof
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FetchProof.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.FetchProof();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.address = reader.string();
                            break;
                        }
                    case 2: {
                            message.nonce = reader.bytes();
                            break;
                        }
                    case 3: {
                            message.signature = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a FetchProof message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.FetchProof
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.FetchProof} FetchProof
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FetchProof.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FetchProof message.
             * @function verify
             * @memberof dmcn.relay.FetchProof
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FetchProof.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    if (!$util.isString(message.address))
                        return "address: string expected";
                if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                    if (!(message.nonce && typeof message.nonce.length === "number" || $util.isString(message.nonce)))
                        return "nonce: buffer expected";
                if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                    if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                        return "signature: buffer expected";
                return null;
            };

            /**
             * Creates a FetchProof message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.FetchProof
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.FetchProof} FetchProof
             */
            FetchProof.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.FetchProof)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.FetchProof: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.FetchProof();
                if (object.address != null)
                    message.address = String(object.address);
                if (object.nonce != null)
                    if (typeof object.nonce === "string")
                        $util.base64.decode(object.nonce, message.nonce = $util.newBuffer($util.base64.length(object.nonce)), 0);
                    else if (object.nonce.length >= 0)
                        message.nonce = object.nonce;
                if (object.signature != null)
                    if (typeof object.signature === "string")
                        $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                    else if (object.signature.length >= 0)
                        message.signature = object.signature;
                return message;
            };

            /**
             * Creates a plain object from a FetchProof message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.FetchProof
             * @static
             * @param {dmcn.relay.FetchProof} message FetchProof
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FetchProof.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.address = "";
                    if (options.bytes === String)
                        object.nonce = "";
                    else {
                        object.nonce = [];
                        if (options.bytes !== Array)
                            object.nonce = $util.newBuffer(object.nonce);
                    }
                    if (options.bytes === String)
                        object.signature = "";
                    else {
                        object.signature = [];
                        if (options.bytes !== Array)
                            object.signature = $util.newBuffer(object.signature);
                    }
                }
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    object.address = message.address;
                if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                    object.nonce = options.bytes === String ? $util.base64.encode(message.nonce, 0, message.nonce.length) : options.bytes === Array ? Array.prototype.slice.call(message.nonce) : message.nonce;
                if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                    object.signature = options.bytes === String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === Array ? Array.prototype.slice.call(message.signature) : message.signature;
                return object;
            };

            /**
             * Converts this FetchProof to JSON.
             * @function toJSON
             * @memberof dmcn.relay.FetchProof
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FetchProof.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for FetchProof
             * @function getTypeUrl
             * @memberof dmcn.relay.FetchProof
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            FetchProof.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.FetchProof";
            };

            return FetchProof;
        })();

        relay.FetchResponse = (function() {

            /**
             * Properties of a FetchResponse.
             * @memberof dmcn.relay
             * @interface IFetchResponse
             * @property {Array.<dmcn.message.IEncryptedEnvelope>|null} [envelopes] FetchResponse envelopes
             * @property {Array.<Uint8Array>|null} [envelopeHashes] FetchResponse envelopeHashes
             */

            /**
             * Constructs a new FetchResponse.
             * @memberof dmcn.relay
             * @classdesc Represents a FetchResponse.
             * @implements IFetchResponse
             * @constructor
             * @param {dmcn.relay.IFetchResponse=} [properties] Properties to set
             */
            function FetchResponse(properties) {
                this.envelopes = [];
                this.envelopeHashes = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * FetchResponse envelopes.
             * @member {Array.<dmcn.message.IEncryptedEnvelope>} envelopes
             * @memberof dmcn.relay.FetchResponse
             * @instance
             */
            FetchResponse.prototype.envelopes = $util.emptyArray;

            /**
             * FetchResponse envelopeHashes.
             * @member {Array.<Uint8Array>} envelopeHashes
             * @memberof dmcn.relay.FetchResponse
             * @instance
             */
            FetchResponse.prototype.envelopeHashes = $util.emptyArray;

            /**
             * Creates a new FetchResponse instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.FetchResponse
             * @static
             * @param {dmcn.relay.IFetchResponse=} [properties] Properties to set
             * @returns {dmcn.relay.FetchResponse} FetchResponse instance
             */
            FetchResponse.create = function create(properties) {
                return new FetchResponse(properties);
            };

            /**
             * Encodes the specified FetchResponse message. Does not implicitly {@link dmcn.relay.FetchResponse.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.FetchResponse
             * @static
             * @param {dmcn.relay.IFetchResponse} message FetchResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FetchResponse.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.envelopes != null && message.envelopes.length)
                    for (let i = 0; i < message.envelopes.length; ++i)
                        $root.dmcn.message.EncryptedEnvelope.encode(message.envelopes[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.envelopeHashes != null && message.envelopeHashes.length)
                    for (let i = 0; i < message.envelopeHashes.length; ++i)
                        writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.envelopeHashes[i]);
                return writer;
            };

            /**
             * Encodes the specified FetchResponse message, length delimited. Does not implicitly {@link dmcn.relay.FetchResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.FetchResponse
             * @static
             * @param {dmcn.relay.IFetchResponse} message FetchResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FetchResponse.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a FetchResponse message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.FetchResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.FetchResponse} FetchResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FetchResponse.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.FetchResponse();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            if (!(message.envelopes && message.envelopes.length))
                                message.envelopes = [];
                            message.envelopes.push($root.dmcn.message.EncryptedEnvelope.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 2: {
                            if (!(message.envelopeHashes && message.envelopeHashes.length))
                                message.envelopeHashes = [];
                            message.envelopeHashes.push(reader.bytes());
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a FetchResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.FetchResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.FetchResponse} FetchResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FetchResponse.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FetchResponse message.
             * @function verify
             * @memberof dmcn.relay.FetchResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FetchResponse.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.envelopes != null && Object.hasOwnProperty.call(message, "envelopes")) {
                    if (!Array.isArray(message.envelopes))
                        return "envelopes: array expected";
                    for (let i = 0; i < message.envelopes.length; ++i) {
                        let error = $root.dmcn.message.EncryptedEnvelope.verify(message.envelopes[i], long + 1);
                        if (error)
                            return "envelopes." + error;
                    }
                }
                if (message.envelopeHashes != null && Object.hasOwnProperty.call(message, "envelopeHashes")) {
                    if (!Array.isArray(message.envelopeHashes))
                        return "envelopeHashes: array expected";
                    for (let i = 0; i < message.envelopeHashes.length; ++i)
                        if (!(message.envelopeHashes[i] && typeof message.envelopeHashes[i].length === "number" || $util.isString(message.envelopeHashes[i])))
                            return "envelopeHashes: buffer[] expected";
                }
                return null;
            };

            /**
             * Creates a FetchResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.FetchResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.FetchResponse} FetchResponse
             */
            FetchResponse.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.FetchResponse)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.FetchResponse: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.FetchResponse();
                if (object.envelopes) {
                    if (!Array.isArray(object.envelopes))
                        throw TypeError(".dmcn.relay.FetchResponse.envelopes: array expected");
                    message.envelopes = [];
                    for (let i = 0; i < object.envelopes.length; ++i) {
                        if (!$util.isObject(object.envelopes[i]))
                            throw TypeError(".dmcn.relay.FetchResponse.envelopes: object expected");
                        message.envelopes[i] = $root.dmcn.message.EncryptedEnvelope.fromObject(object.envelopes[i], long + 1);
                    }
                }
                if (object.envelopeHashes) {
                    if (!Array.isArray(object.envelopeHashes))
                        throw TypeError(".dmcn.relay.FetchResponse.envelopeHashes: array expected");
                    message.envelopeHashes = [];
                    for (let i = 0; i < object.envelopeHashes.length; ++i)
                        if (typeof object.envelopeHashes[i] === "string")
                            $util.base64.decode(object.envelopeHashes[i], message.envelopeHashes[i] = $util.newBuffer($util.base64.length(object.envelopeHashes[i])), 0);
                        else if (object.envelopeHashes[i].length >= 0)
                            message.envelopeHashes[i] = object.envelopeHashes[i];
                }
                return message;
            };

            /**
             * Creates a plain object from a FetchResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.FetchResponse
             * @static
             * @param {dmcn.relay.FetchResponse} message FetchResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FetchResponse.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.envelopes = [];
                    object.envelopeHashes = [];
                }
                if (message.envelopes && message.envelopes.length) {
                    object.envelopes = [];
                    for (let j = 0; j < message.envelopes.length; ++j)
                        object.envelopes[j] = $root.dmcn.message.EncryptedEnvelope.toObject(message.envelopes[j], options, q + 1);
                }
                if (message.envelopeHashes && message.envelopeHashes.length) {
                    object.envelopeHashes = [];
                    for (let j = 0; j < message.envelopeHashes.length; ++j)
                        object.envelopeHashes[j] = options.bytes === String ? $util.base64.encode(message.envelopeHashes[j], 0, message.envelopeHashes[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.envelopeHashes[j]) : message.envelopeHashes[j];
                }
                return object;
            };

            /**
             * Converts this FetchResponse to JSON.
             * @function toJSON
             * @memberof dmcn.relay.FetchResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FetchResponse.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for FetchResponse
             * @function getTypeUrl
             * @memberof dmcn.relay.FetchResponse
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            FetchResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.FetchResponse";
            };

            return FetchResponse;
        })();

        relay.AckRequest = (function() {

            /**
             * Properties of an AckRequest.
             * @memberof dmcn.relay
             * @interface IAckRequest
             * @property {Uint8Array|null} [envelopeHash] AckRequest envelopeHash
             */

            /**
             * Constructs a new AckRequest.
             * @memberof dmcn.relay
             * @classdesc Represents an AckRequest.
             * @implements IAckRequest
             * @constructor
             * @param {dmcn.relay.IAckRequest=} [properties] Properties to set
             */
            function AckRequest(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AckRequest envelopeHash.
             * @member {Uint8Array} envelopeHash
             * @memberof dmcn.relay.AckRequest
             * @instance
             */
            AckRequest.prototype.envelopeHash = $util.newBuffer([]);

            /**
             * Creates a new AckRequest instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.AckRequest
             * @static
             * @param {dmcn.relay.IAckRequest=} [properties] Properties to set
             * @returns {dmcn.relay.AckRequest} AckRequest instance
             */
            AckRequest.create = function create(properties) {
                return new AckRequest(properties);
            };

            /**
             * Encodes the specified AckRequest message. Does not implicitly {@link dmcn.relay.AckRequest.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.AckRequest
             * @static
             * @param {dmcn.relay.IAckRequest} message AckRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AckRequest.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.envelopeHash != null && Object.hasOwnProperty.call(message, "envelopeHash"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.envelopeHash);
                return writer;
            };

            /**
             * Encodes the specified AckRequest message, length delimited. Does not implicitly {@link dmcn.relay.AckRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.AckRequest
             * @static
             * @param {dmcn.relay.IAckRequest} message AckRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AckRequest.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes an AckRequest message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.AckRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.AckRequest} AckRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AckRequest.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.AckRequest();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.envelopeHash = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an AckRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.AckRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.AckRequest} AckRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AckRequest.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AckRequest message.
             * @function verify
             * @memberof dmcn.relay.AckRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AckRequest.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.envelopeHash != null && Object.hasOwnProperty.call(message, "envelopeHash"))
                    if (!(message.envelopeHash && typeof message.envelopeHash.length === "number" || $util.isString(message.envelopeHash)))
                        return "envelopeHash: buffer expected";
                return null;
            };

            /**
             * Creates an AckRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.AckRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.AckRequest} AckRequest
             */
            AckRequest.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.AckRequest)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.AckRequest: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.AckRequest();
                if (object.envelopeHash != null)
                    if (typeof object.envelopeHash === "string")
                        $util.base64.decode(object.envelopeHash, message.envelopeHash = $util.newBuffer($util.base64.length(object.envelopeHash)), 0);
                    else if (object.envelopeHash.length >= 0)
                        message.envelopeHash = object.envelopeHash;
                return message;
            };

            /**
             * Creates a plain object from an AckRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.AckRequest
             * @static
             * @param {dmcn.relay.AckRequest} message AckRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AckRequest.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    if (options.bytes === String)
                        object.envelopeHash = "";
                    else {
                        object.envelopeHash = [];
                        if (options.bytes !== Array)
                            object.envelopeHash = $util.newBuffer(object.envelopeHash);
                    }
                if (message.envelopeHash != null && Object.hasOwnProperty.call(message, "envelopeHash"))
                    object.envelopeHash = options.bytes === String ? $util.base64.encode(message.envelopeHash, 0, message.envelopeHash.length) : options.bytes === Array ? Array.prototype.slice.call(message.envelopeHash) : message.envelopeHash;
                return object;
            };

            /**
             * Converts this AckRequest to JSON.
             * @function toJSON
             * @memberof dmcn.relay.AckRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AckRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AckRequest
             * @function getTypeUrl
             * @memberof dmcn.relay.AckRequest
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AckRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.AckRequest";
            };

            return AckRequest;
        })();

        relay.AckResponse = (function() {

            /**
             * Properties of an AckResponse.
             * @memberof dmcn.relay
             * @interface IAckResponse
             * @property {boolean|null} [success] AckResponse success
             */

            /**
             * Constructs a new AckResponse.
             * @memberof dmcn.relay
             * @classdesc Represents an AckResponse.
             * @implements IAckResponse
             * @constructor
             * @param {dmcn.relay.IAckResponse=} [properties] Properties to set
             */
            function AckResponse(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AckResponse success.
             * @member {boolean} success
             * @memberof dmcn.relay.AckResponse
             * @instance
             */
            AckResponse.prototype.success = false;

            /**
             * Creates a new AckResponse instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.AckResponse
             * @static
             * @param {dmcn.relay.IAckResponse=} [properties] Properties to set
             * @returns {dmcn.relay.AckResponse} AckResponse instance
             */
            AckResponse.create = function create(properties) {
                return new AckResponse(properties);
            };

            /**
             * Encodes the specified AckResponse message. Does not implicitly {@link dmcn.relay.AckResponse.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.AckResponse
             * @static
             * @param {dmcn.relay.IAckResponse} message AckResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AckResponse.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.success != null && Object.hasOwnProperty.call(message, "success"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.success);
                return writer;
            };

            /**
             * Encodes the specified AckResponse message, length delimited. Does not implicitly {@link dmcn.relay.AckResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.AckResponse
             * @static
             * @param {dmcn.relay.IAckResponse} message AckResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AckResponse.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes an AckResponse message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.AckResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.AckResponse} AckResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AckResponse.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.AckResponse();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.success = reader.bool();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an AckResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.AckResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.AckResponse} AckResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AckResponse.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AckResponse message.
             * @function verify
             * @memberof dmcn.relay.AckResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AckResponse.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.success != null && Object.hasOwnProperty.call(message, "success"))
                    if (typeof message.success !== "boolean")
                        return "success: boolean expected";
                return null;
            };

            /**
             * Creates an AckResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.AckResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.AckResponse} AckResponse
             */
            AckResponse.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.AckResponse)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.AckResponse: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.AckResponse();
                if (object.success != null)
                    message.success = Boolean(object.success);
                return message;
            };

            /**
             * Creates a plain object from an AckResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.AckResponse
             * @static
             * @param {dmcn.relay.AckResponse} message AckResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AckResponse.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    object.success = false;
                if (message.success != null && Object.hasOwnProperty.call(message, "success"))
                    object.success = message.success;
                return object;
            };

            /**
             * Converts this AckResponse to JSON.
             * @function toJSON
             * @memberof dmcn.relay.AckResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AckResponse.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AckResponse
             * @function getTypeUrl
             * @memberof dmcn.relay.AckResponse
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AckResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.AckResponse";
            };

            return AckResponse;
        })();

        relay.PingRequest = (function() {

            /**
             * Properties of a PingRequest.
             * @memberof dmcn.relay
             * @interface IPingRequest
             */

            /**
             * Constructs a new PingRequest.
             * @memberof dmcn.relay
             * @classdesc Represents a PingRequest.
             * @implements IPingRequest
             * @constructor
             * @param {dmcn.relay.IPingRequest=} [properties] Properties to set
             */
            function PingRequest(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Creates a new PingRequest instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.PingRequest
             * @static
             * @param {dmcn.relay.IPingRequest=} [properties] Properties to set
             * @returns {dmcn.relay.PingRequest} PingRequest instance
             */
            PingRequest.create = function create(properties) {
                return new PingRequest(properties);
            };

            /**
             * Encodes the specified PingRequest message. Does not implicitly {@link dmcn.relay.PingRequest.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.PingRequest
             * @static
             * @param {dmcn.relay.IPingRequest} message PingRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PingRequest.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                return writer;
            };

            /**
             * Encodes the specified PingRequest message, length delimited. Does not implicitly {@link dmcn.relay.PingRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.PingRequest
             * @static
             * @param {dmcn.relay.IPingRequest} message PingRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PingRequest.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a PingRequest message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.PingRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.PingRequest} PingRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PingRequest.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.PingRequest();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a PingRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.PingRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.PingRequest} PingRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PingRequest.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PingRequest message.
             * @function verify
             * @memberof dmcn.relay.PingRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PingRequest.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                return null;
            };

            /**
             * Creates a PingRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.PingRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.PingRequest} PingRequest
             */
            PingRequest.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.PingRequest)
                    return object;
                return new $root.dmcn.relay.PingRequest();
            };

            /**
             * Creates a plain object from a PingRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.PingRequest
             * @static
             * @param {dmcn.relay.PingRequest} message PingRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PingRequest.toObject = function toObject() {
                return {};
            };

            /**
             * Converts this PingRequest to JSON.
             * @function toJSON
             * @memberof dmcn.relay.PingRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PingRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PingRequest
             * @function getTypeUrl
             * @memberof dmcn.relay.PingRequest
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PingRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.PingRequest";
            };

            return PingRequest;
        })();

        relay.PingResponse = (function() {

            /**
             * Properties of a PingResponse.
             * @memberof dmcn.relay
             * @interface IPingResponse
             * @property {string|null} [version] PingResponse version
             * @property {number|Long|null} [uptimeSeconds] PingResponse uptimeSeconds
             * @property {number|null} [storedEnvelopes] PingResponse storedEnvelopes
             */

            /**
             * Constructs a new PingResponse.
             * @memberof dmcn.relay
             * @classdesc Represents a PingResponse.
             * @implements IPingResponse
             * @constructor
             * @param {dmcn.relay.IPingResponse=} [properties] Properties to set
             */
            function PingResponse(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PingResponse version.
             * @member {string} version
             * @memberof dmcn.relay.PingResponse
             * @instance
             */
            PingResponse.prototype.version = "";

            /**
             * PingResponse uptimeSeconds.
             * @member {number|Long} uptimeSeconds
             * @memberof dmcn.relay.PingResponse
             * @instance
             */
            PingResponse.prototype.uptimeSeconds = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * PingResponse storedEnvelopes.
             * @member {number} storedEnvelopes
             * @memberof dmcn.relay.PingResponse
             * @instance
             */
            PingResponse.prototype.storedEnvelopes = 0;

            /**
             * Creates a new PingResponse instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.PingResponse
             * @static
             * @param {dmcn.relay.IPingResponse=} [properties] Properties to set
             * @returns {dmcn.relay.PingResponse} PingResponse instance
             */
            PingResponse.create = function create(properties) {
                return new PingResponse(properties);
            };

            /**
             * Encodes the specified PingResponse message. Does not implicitly {@link dmcn.relay.PingResponse.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.PingResponse
             * @static
             * @param {dmcn.relay.IPingResponse} message PingResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PingResponse.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.version);
                if (message.uptimeSeconds != null && Object.hasOwnProperty.call(message, "uptimeSeconds"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.uptimeSeconds);
                if (message.storedEnvelopes != null && Object.hasOwnProperty.call(message, "storedEnvelopes"))
                    writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.storedEnvelopes);
                return writer;
            };

            /**
             * Encodes the specified PingResponse message, length delimited. Does not implicitly {@link dmcn.relay.PingResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.PingResponse
             * @static
             * @param {dmcn.relay.IPingResponse} message PingResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PingResponse.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a PingResponse message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.PingResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.PingResponse} PingResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PingResponse.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.PingResponse();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.version = reader.string();
                            break;
                        }
                    case 2: {
                            message.uptimeSeconds = reader.int64();
                            break;
                        }
                    case 3: {
                            message.storedEnvelopes = reader.uint32();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a PingResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.PingResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.PingResponse} PingResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PingResponse.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PingResponse message.
             * @function verify
             * @memberof dmcn.relay.PingResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PingResponse.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    if (!$util.isString(message.version))
                        return "version: string expected";
                if (message.uptimeSeconds != null && Object.hasOwnProperty.call(message, "uptimeSeconds"))
                    if (!$util.isInteger(message.uptimeSeconds) && !(message.uptimeSeconds && $util.isInteger(message.uptimeSeconds.low) && $util.isInteger(message.uptimeSeconds.high)))
                        return "uptimeSeconds: integer|Long expected";
                if (message.storedEnvelopes != null && Object.hasOwnProperty.call(message, "storedEnvelopes"))
                    if (!$util.isInteger(message.storedEnvelopes))
                        return "storedEnvelopes: integer expected";
                return null;
            };

            /**
             * Creates a PingResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.PingResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.PingResponse} PingResponse
             */
            PingResponse.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.PingResponse)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.PingResponse: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.PingResponse();
                if (object.version != null)
                    message.version = String(object.version);
                if (object.uptimeSeconds != null)
                    if ($util.Long)
                        message.uptimeSeconds = $util.Long.fromValue(object.uptimeSeconds, false);
                    else if (typeof object.uptimeSeconds === "string")
                        message.uptimeSeconds = parseInt(object.uptimeSeconds, 10);
                    else if (typeof object.uptimeSeconds === "number")
                        message.uptimeSeconds = object.uptimeSeconds;
                    else if (typeof object.uptimeSeconds === "object")
                        message.uptimeSeconds = new $util.LongBits(object.uptimeSeconds.low >>> 0, object.uptimeSeconds.high >>> 0).toNumber();
                if (object.storedEnvelopes != null)
                    message.storedEnvelopes = object.storedEnvelopes >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a PingResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.PingResponse
             * @static
             * @param {dmcn.relay.PingResponse} message PingResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PingResponse.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.version = "";
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.uptimeSeconds = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.uptimeSeconds = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    object.storedEnvelopes = 0;
                }
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    object.version = message.version;
                if (message.uptimeSeconds != null && Object.hasOwnProperty.call(message, "uptimeSeconds"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.uptimeSeconds = typeof message.uptimeSeconds === "number" ? BigInt(message.uptimeSeconds) : $util.Long.fromBits(message.uptimeSeconds.low >>> 0, message.uptimeSeconds.high >>> 0, false).toBigInt();
                    else if (typeof message.uptimeSeconds === "number")
                        object.uptimeSeconds = options.longs === String ? String(message.uptimeSeconds) : message.uptimeSeconds;
                    else
                        object.uptimeSeconds = options.longs === String ? $util.Long.prototype.toString.call(message.uptimeSeconds) : options.longs === Number ? new $util.LongBits(message.uptimeSeconds.low >>> 0, message.uptimeSeconds.high >>> 0).toNumber() : message.uptimeSeconds;
                if (message.storedEnvelopes != null && Object.hasOwnProperty.call(message, "storedEnvelopes"))
                    object.storedEnvelopes = message.storedEnvelopes;
                return object;
            };

            /**
             * Converts this PingResponse to JSON.
             * @function toJSON
             * @memberof dmcn.relay.PingResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PingResponse.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PingResponse
             * @function getTypeUrl
             * @memberof dmcn.relay.PingResponse
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PingResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.PingResponse";
            };

            return PingResponse;
        })();

        relay.GetIdentityRequest = (function() {

            /**
             * Properties of a GetIdentityRequest.
             * @memberof dmcn.relay
             * @interface IGetIdentityRequest
             * @property {string|null} [address] GetIdentityRequest address
             */

            /**
             * Constructs a new GetIdentityRequest.
             * @memberof dmcn.relay
             * @classdesc Represents a GetIdentityRequest.
             * @implements IGetIdentityRequest
             * @constructor
             * @param {dmcn.relay.IGetIdentityRequest=} [properties] Properties to set
             */
            function GetIdentityRequest(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * GetIdentityRequest address.
             * @member {string} address
             * @memberof dmcn.relay.GetIdentityRequest
             * @instance
             */
            GetIdentityRequest.prototype.address = "";

            /**
             * Creates a new GetIdentityRequest instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.GetIdentityRequest
             * @static
             * @param {dmcn.relay.IGetIdentityRequest=} [properties] Properties to set
             * @returns {dmcn.relay.GetIdentityRequest} GetIdentityRequest instance
             */
            GetIdentityRequest.create = function create(properties) {
                return new GetIdentityRequest(properties);
            };

            /**
             * Encodes the specified GetIdentityRequest message. Does not implicitly {@link dmcn.relay.GetIdentityRequest.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.GetIdentityRequest
             * @static
             * @param {dmcn.relay.IGetIdentityRequest} message GetIdentityRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetIdentityRequest.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.address);
                return writer;
            };

            /**
             * Encodes the specified GetIdentityRequest message, length delimited. Does not implicitly {@link dmcn.relay.GetIdentityRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.GetIdentityRequest
             * @static
             * @param {dmcn.relay.IGetIdentityRequest} message GetIdentityRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetIdentityRequest.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a GetIdentityRequest message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.GetIdentityRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.GetIdentityRequest} GetIdentityRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetIdentityRequest.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.GetIdentityRequest();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.address = reader.string();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a GetIdentityRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.GetIdentityRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.GetIdentityRequest} GetIdentityRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetIdentityRequest.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetIdentityRequest message.
             * @function verify
             * @memberof dmcn.relay.GetIdentityRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetIdentityRequest.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    if (!$util.isString(message.address))
                        return "address: string expected";
                return null;
            };

            /**
             * Creates a GetIdentityRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.GetIdentityRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.GetIdentityRequest} GetIdentityRequest
             */
            GetIdentityRequest.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.GetIdentityRequest)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.GetIdentityRequest: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.GetIdentityRequest();
                if (object.address != null)
                    message.address = String(object.address);
                return message;
            };

            /**
             * Creates a plain object from a GetIdentityRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.GetIdentityRequest
             * @static
             * @param {dmcn.relay.GetIdentityRequest} message GetIdentityRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetIdentityRequest.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    object.address = "";
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    object.address = message.address;
                return object;
            };

            /**
             * Converts this GetIdentityRequest to JSON.
             * @function toJSON
             * @memberof dmcn.relay.GetIdentityRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetIdentityRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for GetIdentityRequest
             * @function getTypeUrl
             * @memberof dmcn.relay.GetIdentityRequest
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            GetIdentityRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.GetIdentityRequest";
            };

            return GetIdentityRequest;
        })();

        relay.GetIdentityResponse = (function() {

            /**
             * Properties of a GetIdentityResponse.
             * @memberof dmcn.relay
             * @interface IGetIdentityResponse
             * @property {boolean|null} [found] GetIdentityResponse found
             * @property {Uint8Array|null} [record] GetIdentityResponse record
             */

            /**
             * Constructs a new GetIdentityResponse.
             * @memberof dmcn.relay
             * @classdesc Represents a GetIdentityResponse.
             * @implements IGetIdentityResponse
             * @constructor
             * @param {dmcn.relay.IGetIdentityResponse=} [properties] Properties to set
             */
            function GetIdentityResponse(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * GetIdentityResponse found.
             * @member {boolean} found
             * @memberof dmcn.relay.GetIdentityResponse
             * @instance
             */
            GetIdentityResponse.prototype.found = false;

            /**
             * GetIdentityResponse record.
             * @member {Uint8Array} record
             * @memberof dmcn.relay.GetIdentityResponse
             * @instance
             */
            GetIdentityResponse.prototype.record = $util.newBuffer([]);

            /**
             * Creates a new GetIdentityResponse instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.GetIdentityResponse
             * @static
             * @param {dmcn.relay.IGetIdentityResponse=} [properties] Properties to set
             * @returns {dmcn.relay.GetIdentityResponse} GetIdentityResponse instance
             */
            GetIdentityResponse.create = function create(properties) {
                return new GetIdentityResponse(properties);
            };

            /**
             * Encodes the specified GetIdentityResponse message. Does not implicitly {@link dmcn.relay.GetIdentityResponse.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.GetIdentityResponse
             * @static
             * @param {dmcn.relay.IGetIdentityResponse} message GetIdentityResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetIdentityResponse.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.found != null && Object.hasOwnProperty.call(message, "found"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.found);
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.record);
                return writer;
            };

            /**
             * Encodes the specified GetIdentityResponse message, length delimited. Does not implicitly {@link dmcn.relay.GetIdentityResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.GetIdentityResponse
             * @static
             * @param {dmcn.relay.IGetIdentityResponse} message GetIdentityResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetIdentityResponse.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a GetIdentityResponse message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.GetIdentityResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.GetIdentityResponse} GetIdentityResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetIdentityResponse.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.GetIdentityResponse();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.found = reader.bool();
                            break;
                        }
                    case 2: {
                            message.record = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a GetIdentityResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.GetIdentityResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.GetIdentityResponse} GetIdentityResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetIdentityResponse.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetIdentityResponse message.
             * @function verify
             * @memberof dmcn.relay.GetIdentityResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetIdentityResponse.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.found != null && Object.hasOwnProperty.call(message, "found"))
                    if (typeof message.found !== "boolean")
                        return "found: boolean expected";
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    if (!(message.record && typeof message.record.length === "number" || $util.isString(message.record)))
                        return "record: buffer expected";
                return null;
            };

            /**
             * Creates a GetIdentityResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.GetIdentityResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.GetIdentityResponse} GetIdentityResponse
             */
            GetIdentityResponse.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.GetIdentityResponse)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.GetIdentityResponse: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.GetIdentityResponse();
                if (object.found != null)
                    message.found = Boolean(object.found);
                if (object.record != null)
                    if (typeof object.record === "string")
                        $util.base64.decode(object.record, message.record = $util.newBuffer($util.base64.length(object.record)), 0);
                    else if (object.record.length >= 0)
                        message.record = object.record;
                return message;
            };

            /**
             * Creates a plain object from a GetIdentityResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.GetIdentityResponse
             * @static
             * @param {dmcn.relay.GetIdentityResponse} message GetIdentityResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetIdentityResponse.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.found = false;
                    if (options.bytes === String)
                        object.record = "";
                    else {
                        object.record = [];
                        if (options.bytes !== Array)
                            object.record = $util.newBuffer(object.record);
                    }
                }
                if (message.found != null && Object.hasOwnProperty.call(message, "found"))
                    object.found = message.found;
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    object.record = options.bytes === String ? $util.base64.encode(message.record, 0, message.record.length) : options.bytes === Array ? Array.prototype.slice.call(message.record) : message.record;
                return object;
            };

            /**
             * Converts this GetIdentityResponse to JSON.
             * @function toJSON
             * @memberof dmcn.relay.GetIdentityResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetIdentityResponse.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for GetIdentityResponse
             * @function getTypeUrl
             * @memberof dmcn.relay.GetIdentityResponse
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            GetIdentityResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.GetIdentityResponse";
            };

            return GetIdentityResponse;
        })();

        relay.GetDARRequest = (function() {

            /**
             * Properties of a GetDARRequest.
             * @memberof dmcn.relay
             * @interface IGetDARRequest
             * @property {string|null} [domain] GetDARRequest domain
             */

            /**
             * Constructs a new GetDARRequest.
             * @memberof dmcn.relay
             * @classdesc Represents a GetDARRequest.
             * @implements IGetDARRequest
             * @constructor
             * @param {dmcn.relay.IGetDARRequest=} [properties] Properties to set
             */
            function GetDARRequest(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * GetDARRequest domain.
             * @member {string} domain
             * @memberof dmcn.relay.GetDARRequest
             * @instance
             */
            GetDARRequest.prototype.domain = "";

            /**
             * Creates a new GetDARRequest instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.GetDARRequest
             * @static
             * @param {dmcn.relay.IGetDARRequest=} [properties] Properties to set
             * @returns {dmcn.relay.GetDARRequest} GetDARRequest instance
             */
            GetDARRequest.create = function create(properties) {
                return new GetDARRequest(properties);
            };

            /**
             * Encodes the specified GetDARRequest message. Does not implicitly {@link dmcn.relay.GetDARRequest.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.GetDARRequest
             * @static
             * @param {dmcn.relay.IGetDARRequest} message GetDARRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetDARRequest.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.domain);
                return writer;
            };

            /**
             * Encodes the specified GetDARRequest message, length delimited. Does not implicitly {@link dmcn.relay.GetDARRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.GetDARRequest
             * @static
             * @param {dmcn.relay.IGetDARRequest} message GetDARRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetDARRequest.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a GetDARRequest message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.GetDARRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.GetDARRequest} GetDARRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetDARRequest.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.GetDARRequest();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.domain = reader.string();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a GetDARRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.GetDARRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.GetDARRequest} GetDARRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetDARRequest.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetDARRequest message.
             * @function verify
             * @memberof dmcn.relay.GetDARRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetDARRequest.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    if (!$util.isString(message.domain))
                        return "domain: string expected";
                return null;
            };

            /**
             * Creates a GetDARRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.GetDARRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.GetDARRequest} GetDARRequest
             */
            GetDARRequest.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.GetDARRequest)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.GetDARRequest: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.GetDARRequest();
                if (object.domain != null)
                    message.domain = String(object.domain);
                return message;
            };

            /**
             * Creates a plain object from a GetDARRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.GetDARRequest
             * @static
             * @param {dmcn.relay.GetDARRequest} message GetDARRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetDARRequest.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    object.domain = "";
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    object.domain = message.domain;
                return object;
            };

            /**
             * Converts this GetDARRequest to JSON.
             * @function toJSON
             * @memberof dmcn.relay.GetDARRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetDARRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for GetDARRequest
             * @function getTypeUrl
             * @memberof dmcn.relay.GetDARRequest
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            GetDARRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.GetDARRequest";
            };

            return GetDARRequest;
        })();

        relay.GetDARResponse = (function() {

            /**
             * Properties of a GetDARResponse.
             * @memberof dmcn.relay
             * @interface IGetDARResponse
             * @property {boolean|null} [found] GetDARResponse found
             * @property {Uint8Array|null} [record] GetDARResponse record
             */

            /**
             * Constructs a new GetDARResponse.
             * @memberof dmcn.relay
             * @classdesc Represents a GetDARResponse.
             * @implements IGetDARResponse
             * @constructor
             * @param {dmcn.relay.IGetDARResponse=} [properties] Properties to set
             */
            function GetDARResponse(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * GetDARResponse found.
             * @member {boolean} found
             * @memberof dmcn.relay.GetDARResponse
             * @instance
             */
            GetDARResponse.prototype.found = false;

            /**
             * GetDARResponse record.
             * @member {Uint8Array} record
             * @memberof dmcn.relay.GetDARResponse
             * @instance
             */
            GetDARResponse.prototype.record = $util.newBuffer([]);

            /**
             * Creates a new GetDARResponse instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.GetDARResponse
             * @static
             * @param {dmcn.relay.IGetDARResponse=} [properties] Properties to set
             * @returns {dmcn.relay.GetDARResponse} GetDARResponse instance
             */
            GetDARResponse.create = function create(properties) {
                return new GetDARResponse(properties);
            };

            /**
             * Encodes the specified GetDARResponse message. Does not implicitly {@link dmcn.relay.GetDARResponse.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.GetDARResponse
             * @static
             * @param {dmcn.relay.IGetDARResponse} message GetDARResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetDARResponse.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.found != null && Object.hasOwnProperty.call(message, "found"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.found);
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.record);
                return writer;
            };

            /**
             * Encodes the specified GetDARResponse message, length delimited. Does not implicitly {@link dmcn.relay.GetDARResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.GetDARResponse
             * @static
             * @param {dmcn.relay.IGetDARResponse} message GetDARResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetDARResponse.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a GetDARResponse message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.GetDARResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.GetDARResponse} GetDARResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetDARResponse.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.GetDARResponse();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.found = reader.bool();
                            break;
                        }
                    case 2: {
                            message.record = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a GetDARResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.GetDARResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.GetDARResponse} GetDARResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetDARResponse.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetDARResponse message.
             * @function verify
             * @memberof dmcn.relay.GetDARResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetDARResponse.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.found != null && Object.hasOwnProperty.call(message, "found"))
                    if (typeof message.found !== "boolean")
                        return "found: boolean expected";
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    if (!(message.record && typeof message.record.length === "number" || $util.isString(message.record)))
                        return "record: buffer expected";
                return null;
            };

            /**
             * Creates a GetDARResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.GetDARResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.GetDARResponse} GetDARResponse
             */
            GetDARResponse.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.GetDARResponse)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.GetDARResponse: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.GetDARResponse();
                if (object.found != null)
                    message.found = Boolean(object.found);
                if (object.record != null)
                    if (typeof object.record === "string")
                        $util.base64.decode(object.record, message.record = $util.newBuffer($util.base64.length(object.record)), 0);
                    else if (object.record.length >= 0)
                        message.record = object.record;
                return message;
            };

            /**
             * Creates a plain object from a GetDARResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.GetDARResponse
             * @static
             * @param {dmcn.relay.GetDARResponse} message GetDARResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetDARResponse.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.found = false;
                    if (options.bytes === String)
                        object.record = "";
                    else {
                        object.record = [];
                        if (options.bytes !== Array)
                            object.record = $util.newBuffer(object.record);
                    }
                }
                if (message.found != null && Object.hasOwnProperty.call(message, "found"))
                    object.found = message.found;
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    object.record = options.bytes === String ? $util.base64.encode(message.record, 0, message.record.length) : options.bytes === Array ? Array.prototype.slice.call(message.record) : message.record;
                return object;
            };

            /**
             * Converts this GetDARResponse to JSON.
             * @function toJSON
             * @memberof dmcn.relay.GetDARResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetDARResponse.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for GetDARResponse
             * @function getTypeUrl
             * @memberof dmcn.relay.GetDARResponse
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            GetDARResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.GetDARResponse";
            };

            return GetDARResponse;
        })();

        relay.GetFleetRosterRequest = (function() {

            /**
             * Properties of a GetFleetRosterRequest.
             * @memberof dmcn.relay
             * @interface IGetFleetRosterRequest
             * @property {string|null} [fleetDomain] GetFleetRosterRequest fleetDomain
             */

            /**
             * Constructs a new GetFleetRosterRequest.
             * @memberof dmcn.relay
             * @classdesc Represents a GetFleetRosterRequest.
             * @implements IGetFleetRosterRequest
             * @constructor
             * @param {dmcn.relay.IGetFleetRosterRequest=} [properties] Properties to set
             */
            function GetFleetRosterRequest(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * GetFleetRosterRequest fleetDomain.
             * @member {string} fleetDomain
             * @memberof dmcn.relay.GetFleetRosterRequest
             * @instance
             */
            GetFleetRosterRequest.prototype.fleetDomain = "";

            /**
             * Creates a new GetFleetRosterRequest instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.GetFleetRosterRequest
             * @static
             * @param {dmcn.relay.IGetFleetRosterRequest=} [properties] Properties to set
             * @returns {dmcn.relay.GetFleetRosterRequest} GetFleetRosterRequest instance
             */
            GetFleetRosterRequest.create = function create(properties) {
                return new GetFleetRosterRequest(properties);
            };

            /**
             * Encodes the specified GetFleetRosterRequest message. Does not implicitly {@link dmcn.relay.GetFleetRosterRequest.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.GetFleetRosterRequest
             * @static
             * @param {dmcn.relay.IGetFleetRosterRequest} message GetFleetRosterRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetFleetRosterRequest.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.fleetDomain != null && Object.hasOwnProperty.call(message, "fleetDomain"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.fleetDomain);
                return writer;
            };

            /**
             * Encodes the specified GetFleetRosterRequest message, length delimited. Does not implicitly {@link dmcn.relay.GetFleetRosterRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.GetFleetRosterRequest
             * @static
             * @param {dmcn.relay.IGetFleetRosterRequest} message GetFleetRosterRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetFleetRosterRequest.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a GetFleetRosterRequest message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.GetFleetRosterRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.GetFleetRosterRequest} GetFleetRosterRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetFleetRosterRequest.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.GetFleetRosterRequest();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.fleetDomain = reader.string();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a GetFleetRosterRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.GetFleetRosterRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.GetFleetRosterRequest} GetFleetRosterRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetFleetRosterRequest.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetFleetRosterRequest message.
             * @function verify
             * @memberof dmcn.relay.GetFleetRosterRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetFleetRosterRequest.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.fleetDomain != null && Object.hasOwnProperty.call(message, "fleetDomain"))
                    if (!$util.isString(message.fleetDomain))
                        return "fleetDomain: string expected";
                return null;
            };

            /**
             * Creates a GetFleetRosterRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.GetFleetRosterRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.GetFleetRosterRequest} GetFleetRosterRequest
             */
            GetFleetRosterRequest.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.GetFleetRosterRequest)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.GetFleetRosterRequest: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.GetFleetRosterRequest();
                if (object.fleetDomain != null)
                    message.fleetDomain = String(object.fleetDomain);
                return message;
            };

            /**
             * Creates a plain object from a GetFleetRosterRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.GetFleetRosterRequest
             * @static
             * @param {dmcn.relay.GetFleetRosterRequest} message GetFleetRosterRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetFleetRosterRequest.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    object.fleetDomain = "";
                if (message.fleetDomain != null && Object.hasOwnProperty.call(message, "fleetDomain"))
                    object.fleetDomain = message.fleetDomain;
                return object;
            };

            /**
             * Converts this GetFleetRosterRequest to JSON.
             * @function toJSON
             * @memberof dmcn.relay.GetFleetRosterRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetFleetRosterRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for GetFleetRosterRequest
             * @function getTypeUrl
             * @memberof dmcn.relay.GetFleetRosterRequest
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            GetFleetRosterRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.GetFleetRosterRequest";
            };

            return GetFleetRosterRequest;
        })();

        relay.GetFleetRosterResponse = (function() {

            /**
             * Properties of a GetFleetRosterResponse.
             * @memberof dmcn.relay
             * @interface IGetFleetRosterResponse
             * @property {boolean|null} [found] GetFleetRosterResponse found
             * @property {Uint8Array|null} [record] GetFleetRosterResponse record
             */

            /**
             * Constructs a new GetFleetRosterResponse.
             * @memberof dmcn.relay
             * @classdesc Represents a GetFleetRosterResponse.
             * @implements IGetFleetRosterResponse
             * @constructor
             * @param {dmcn.relay.IGetFleetRosterResponse=} [properties] Properties to set
             */
            function GetFleetRosterResponse(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * GetFleetRosterResponse found.
             * @member {boolean} found
             * @memberof dmcn.relay.GetFleetRosterResponse
             * @instance
             */
            GetFleetRosterResponse.prototype.found = false;

            /**
             * GetFleetRosterResponse record.
             * @member {Uint8Array} record
             * @memberof dmcn.relay.GetFleetRosterResponse
             * @instance
             */
            GetFleetRosterResponse.prototype.record = $util.newBuffer([]);

            /**
             * Creates a new GetFleetRosterResponse instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.GetFleetRosterResponse
             * @static
             * @param {dmcn.relay.IGetFleetRosterResponse=} [properties] Properties to set
             * @returns {dmcn.relay.GetFleetRosterResponse} GetFleetRosterResponse instance
             */
            GetFleetRosterResponse.create = function create(properties) {
                return new GetFleetRosterResponse(properties);
            };

            /**
             * Encodes the specified GetFleetRosterResponse message. Does not implicitly {@link dmcn.relay.GetFleetRosterResponse.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.GetFleetRosterResponse
             * @static
             * @param {dmcn.relay.IGetFleetRosterResponse} message GetFleetRosterResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetFleetRosterResponse.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.found != null && Object.hasOwnProperty.call(message, "found"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.found);
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.record);
                return writer;
            };

            /**
             * Encodes the specified GetFleetRosterResponse message, length delimited. Does not implicitly {@link dmcn.relay.GetFleetRosterResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.GetFleetRosterResponse
             * @static
             * @param {dmcn.relay.IGetFleetRosterResponse} message GetFleetRosterResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetFleetRosterResponse.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a GetFleetRosterResponse message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.GetFleetRosterResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.GetFleetRosterResponse} GetFleetRosterResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetFleetRosterResponse.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.GetFleetRosterResponse();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.found = reader.bool();
                            break;
                        }
                    case 2: {
                            message.record = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a GetFleetRosterResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.GetFleetRosterResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.GetFleetRosterResponse} GetFleetRosterResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetFleetRosterResponse.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetFleetRosterResponse message.
             * @function verify
             * @memberof dmcn.relay.GetFleetRosterResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetFleetRosterResponse.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.found != null && Object.hasOwnProperty.call(message, "found"))
                    if (typeof message.found !== "boolean")
                        return "found: boolean expected";
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    if (!(message.record && typeof message.record.length === "number" || $util.isString(message.record)))
                        return "record: buffer expected";
                return null;
            };

            /**
             * Creates a GetFleetRosterResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.GetFleetRosterResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.GetFleetRosterResponse} GetFleetRosterResponse
             */
            GetFleetRosterResponse.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.GetFleetRosterResponse)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.GetFleetRosterResponse: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.GetFleetRosterResponse();
                if (object.found != null)
                    message.found = Boolean(object.found);
                if (object.record != null)
                    if (typeof object.record === "string")
                        $util.base64.decode(object.record, message.record = $util.newBuffer($util.base64.length(object.record)), 0);
                    else if (object.record.length >= 0)
                        message.record = object.record;
                return message;
            };

            /**
             * Creates a plain object from a GetFleetRosterResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.GetFleetRosterResponse
             * @static
             * @param {dmcn.relay.GetFleetRosterResponse} message GetFleetRosterResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetFleetRosterResponse.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.found = false;
                    if (options.bytes === String)
                        object.record = "";
                    else {
                        object.record = [];
                        if (options.bytes !== Array)
                            object.record = $util.newBuffer(object.record);
                    }
                }
                if (message.found != null && Object.hasOwnProperty.call(message, "found"))
                    object.found = message.found;
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    object.record = options.bytes === String ? $util.base64.encode(message.record, 0, message.record.length) : options.bytes === Array ? Array.prototype.slice.call(message.record) : message.record;
                return object;
            };

            /**
             * Converts this GetFleetRosterResponse to JSON.
             * @function toJSON
             * @memberof dmcn.relay.GetFleetRosterResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetFleetRosterResponse.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for GetFleetRosterResponse
             * @function getTypeUrl
             * @memberof dmcn.relay.GetFleetRosterResponse
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            GetFleetRosterResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.GetFleetRosterResponse";
            };

            return GetFleetRosterResponse;
        })();

        relay.GetRemovalRequest = (function() {

            /**
             * Properties of a GetRemovalRequest.
             * @memberof dmcn.relay
             * @interface IGetRemovalRequest
             * @property {string|null} [address] GetRemovalRequest address
             */

            /**
             * Constructs a new GetRemovalRequest.
             * @memberof dmcn.relay
             * @classdesc Represents a GetRemovalRequest.
             * @implements IGetRemovalRequest
             * @constructor
             * @param {dmcn.relay.IGetRemovalRequest=} [properties] Properties to set
             */
            function GetRemovalRequest(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * GetRemovalRequest address.
             * @member {string} address
             * @memberof dmcn.relay.GetRemovalRequest
             * @instance
             */
            GetRemovalRequest.prototype.address = "";

            /**
             * Creates a new GetRemovalRequest instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.GetRemovalRequest
             * @static
             * @param {dmcn.relay.IGetRemovalRequest=} [properties] Properties to set
             * @returns {dmcn.relay.GetRemovalRequest} GetRemovalRequest instance
             */
            GetRemovalRequest.create = function create(properties) {
                return new GetRemovalRequest(properties);
            };

            /**
             * Encodes the specified GetRemovalRequest message. Does not implicitly {@link dmcn.relay.GetRemovalRequest.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.GetRemovalRequest
             * @static
             * @param {dmcn.relay.IGetRemovalRequest} message GetRemovalRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetRemovalRequest.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.address);
                return writer;
            };

            /**
             * Encodes the specified GetRemovalRequest message, length delimited. Does not implicitly {@link dmcn.relay.GetRemovalRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.GetRemovalRequest
             * @static
             * @param {dmcn.relay.IGetRemovalRequest} message GetRemovalRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetRemovalRequest.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a GetRemovalRequest message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.GetRemovalRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.GetRemovalRequest} GetRemovalRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetRemovalRequest.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.GetRemovalRequest();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.address = reader.string();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a GetRemovalRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.GetRemovalRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.GetRemovalRequest} GetRemovalRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetRemovalRequest.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetRemovalRequest message.
             * @function verify
             * @memberof dmcn.relay.GetRemovalRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetRemovalRequest.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    if (!$util.isString(message.address))
                        return "address: string expected";
                return null;
            };

            /**
             * Creates a GetRemovalRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.GetRemovalRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.GetRemovalRequest} GetRemovalRequest
             */
            GetRemovalRequest.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.GetRemovalRequest)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.GetRemovalRequest: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.GetRemovalRequest();
                if (object.address != null)
                    message.address = String(object.address);
                return message;
            };

            /**
             * Creates a plain object from a GetRemovalRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.GetRemovalRequest
             * @static
             * @param {dmcn.relay.GetRemovalRequest} message GetRemovalRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetRemovalRequest.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    object.address = "";
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    object.address = message.address;
                return object;
            };

            /**
             * Converts this GetRemovalRequest to JSON.
             * @function toJSON
             * @memberof dmcn.relay.GetRemovalRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetRemovalRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for GetRemovalRequest
             * @function getTypeUrl
             * @memberof dmcn.relay.GetRemovalRequest
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            GetRemovalRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.GetRemovalRequest";
            };

            return GetRemovalRequest;
        })();

        relay.GetRemovalResponse = (function() {

            /**
             * Properties of a GetRemovalResponse.
             * @memberof dmcn.relay
             * @interface IGetRemovalResponse
             * @property {boolean|null} [found] GetRemovalResponse found
             * @property {Uint8Array|null} [record] GetRemovalResponse record
             */

            /**
             * Constructs a new GetRemovalResponse.
             * @memberof dmcn.relay
             * @classdesc Represents a GetRemovalResponse.
             * @implements IGetRemovalResponse
             * @constructor
             * @param {dmcn.relay.IGetRemovalResponse=} [properties] Properties to set
             */
            function GetRemovalResponse(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * GetRemovalResponse found.
             * @member {boolean} found
             * @memberof dmcn.relay.GetRemovalResponse
             * @instance
             */
            GetRemovalResponse.prototype.found = false;

            /**
             * GetRemovalResponse record.
             * @member {Uint8Array} record
             * @memberof dmcn.relay.GetRemovalResponse
             * @instance
             */
            GetRemovalResponse.prototype.record = $util.newBuffer([]);

            /**
             * Creates a new GetRemovalResponse instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.GetRemovalResponse
             * @static
             * @param {dmcn.relay.IGetRemovalResponse=} [properties] Properties to set
             * @returns {dmcn.relay.GetRemovalResponse} GetRemovalResponse instance
             */
            GetRemovalResponse.create = function create(properties) {
                return new GetRemovalResponse(properties);
            };

            /**
             * Encodes the specified GetRemovalResponse message. Does not implicitly {@link dmcn.relay.GetRemovalResponse.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.GetRemovalResponse
             * @static
             * @param {dmcn.relay.IGetRemovalResponse} message GetRemovalResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetRemovalResponse.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.found != null && Object.hasOwnProperty.call(message, "found"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.found);
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.record);
                return writer;
            };

            /**
             * Encodes the specified GetRemovalResponse message, length delimited. Does not implicitly {@link dmcn.relay.GetRemovalResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.GetRemovalResponse
             * @static
             * @param {dmcn.relay.IGetRemovalResponse} message GetRemovalResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetRemovalResponse.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a GetRemovalResponse message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.GetRemovalResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.GetRemovalResponse} GetRemovalResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetRemovalResponse.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.GetRemovalResponse();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.found = reader.bool();
                            break;
                        }
                    case 2: {
                            message.record = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a GetRemovalResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.GetRemovalResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.GetRemovalResponse} GetRemovalResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetRemovalResponse.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetRemovalResponse message.
             * @function verify
             * @memberof dmcn.relay.GetRemovalResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetRemovalResponse.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.found != null && Object.hasOwnProperty.call(message, "found"))
                    if (typeof message.found !== "boolean")
                        return "found: boolean expected";
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    if (!(message.record && typeof message.record.length === "number" || $util.isString(message.record)))
                        return "record: buffer expected";
                return null;
            };

            /**
             * Creates a GetRemovalResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.GetRemovalResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.GetRemovalResponse} GetRemovalResponse
             */
            GetRemovalResponse.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.GetRemovalResponse)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.GetRemovalResponse: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.GetRemovalResponse();
                if (object.found != null)
                    message.found = Boolean(object.found);
                if (object.record != null)
                    if (typeof object.record === "string")
                        $util.base64.decode(object.record, message.record = $util.newBuffer($util.base64.length(object.record)), 0);
                    else if (object.record.length >= 0)
                        message.record = object.record;
                return message;
            };

            /**
             * Creates a plain object from a GetRemovalResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.GetRemovalResponse
             * @static
             * @param {dmcn.relay.GetRemovalResponse} message GetRemovalResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetRemovalResponse.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.found = false;
                    if (options.bytes === String)
                        object.record = "";
                    else {
                        object.record = [];
                        if (options.bytes !== Array)
                            object.record = $util.newBuffer(object.record);
                    }
                }
                if (message.found != null && Object.hasOwnProperty.call(message, "found"))
                    object.found = message.found;
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    object.record = options.bytes === String ? $util.base64.encode(message.record, 0, message.record.length) : options.bytes === Array ? Array.prototype.slice.call(message.record) : message.record;
                return object;
            };

            /**
             * Converts this GetRemovalResponse to JSON.
             * @function toJSON
             * @memberof dmcn.relay.GetRemovalResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetRemovalResponse.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for GetRemovalResponse
             * @function getTypeUrl
             * @memberof dmcn.relay.GetRemovalResponse
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            GetRemovalResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.GetRemovalResponse";
            };

            return GetRemovalResponse;
        })();

        relay.GetBlocklistRequest = (function() {

            /**
             * Properties of a GetBlocklistRequest.
             * @memberof dmcn.relay
             * @interface IGetBlocklistRequest
             * @property {string|null} [domain] GetBlocklistRequest domain
             */

            /**
             * Constructs a new GetBlocklistRequest.
             * @memberof dmcn.relay
             * @classdesc Represents a GetBlocklistRequest.
             * @implements IGetBlocklistRequest
             * @constructor
             * @param {dmcn.relay.IGetBlocklistRequest=} [properties] Properties to set
             */
            function GetBlocklistRequest(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * GetBlocklistRequest domain.
             * @member {string} domain
             * @memberof dmcn.relay.GetBlocklistRequest
             * @instance
             */
            GetBlocklistRequest.prototype.domain = "";

            /**
             * Creates a new GetBlocklistRequest instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.GetBlocklistRequest
             * @static
             * @param {dmcn.relay.IGetBlocklistRequest=} [properties] Properties to set
             * @returns {dmcn.relay.GetBlocklistRequest} GetBlocklistRequest instance
             */
            GetBlocklistRequest.create = function create(properties) {
                return new GetBlocklistRequest(properties);
            };

            /**
             * Encodes the specified GetBlocklistRequest message. Does not implicitly {@link dmcn.relay.GetBlocklistRequest.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.GetBlocklistRequest
             * @static
             * @param {dmcn.relay.IGetBlocklistRequest} message GetBlocklistRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetBlocklistRequest.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.domain);
                return writer;
            };

            /**
             * Encodes the specified GetBlocklistRequest message, length delimited. Does not implicitly {@link dmcn.relay.GetBlocklistRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.GetBlocklistRequest
             * @static
             * @param {dmcn.relay.IGetBlocklistRequest} message GetBlocklistRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetBlocklistRequest.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a GetBlocklistRequest message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.GetBlocklistRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.GetBlocklistRequest} GetBlocklistRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetBlocklistRequest.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.GetBlocklistRequest();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.domain = reader.string();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a GetBlocklistRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.GetBlocklistRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.GetBlocklistRequest} GetBlocklistRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetBlocklistRequest.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetBlocklistRequest message.
             * @function verify
             * @memberof dmcn.relay.GetBlocklistRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetBlocklistRequest.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    if (!$util.isString(message.domain))
                        return "domain: string expected";
                return null;
            };

            /**
             * Creates a GetBlocklistRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.GetBlocklistRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.GetBlocklistRequest} GetBlocklistRequest
             */
            GetBlocklistRequest.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.GetBlocklistRequest)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.GetBlocklistRequest: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.GetBlocklistRequest();
                if (object.domain != null)
                    message.domain = String(object.domain);
                return message;
            };

            /**
             * Creates a plain object from a GetBlocklistRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.GetBlocklistRequest
             * @static
             * @param {dmcn.relay.GetBlocklistRequest} message GetBlocklistRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetBlocklistRequest.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    object.domain = "";
                if (message.domain != null && Object.hasOwnProperty.call(message, "domain"))
                    object.domain = message.domain;
                return object;
            };

            /**
             * Converts this GetBlocklistRequest to JSON.
             * @function toJSON
             * @memberof dmcn.relay.GetBlocklistRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetBlocklistRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for GetBlocklistRequest
             * @function getTypeUrl
             * @memberof dmcn.relay.GetBlocklistRequest
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            GetBlocklistRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.GetBlocklistRequest";
            };

            return GetBlocklistRequest;
        })();

        relay.GetBlocklistResponse = (function() {

            /**
             * Properties of a GetBlocklistResponse.
             * @memberof dmcn.relay
             * @interface IGetBlocklistResponse
             * @property {boolean|null} [found] GetBlocklistResponse found
             * @property {Uint8Array|null} [record] GetBlocklistResponse record
             */

            /**
             * Constructs a new GetBlocklistResponse.
             * @memberof dmcn.relay
             * @classdesc Represents a GetBlocklistResponse.
             * @implements IGetBlocklistResponse
             * @constructor
             * @param {dmcn.relay.IGetBlocklistResponse=} [properties] Properties to set
             */
            function GetBlocklistResponse(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * GetBlocklistResponse found.
             * @member {boolean} found
             * @memberof dmcn.relay.GetBlocklistResponse
             * @instance
             */
            GetBlocklistResponse.prototype.found = false;

            /**
             * GetBlocklistResponse record.
             * @member {Uint8Array} record
             * @memberof dmcn.relay.GetBlocklistResponse
             * @instance
             */
            GetBlocklistResponse.prototype.record = $util.newBuffer([]);

            /**
             * Creates a new GetBlocklistResponse instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.GetBlocklistResponse
             * @static
             * @param {dmcn.relay.IGetBlocklistResponse=} [properties] Properties to set
             * @returns {dmcn.relay.GetBlocklistResponse} GetBlocklistResponse instance
             */
            GetBlocklistResponse.create = function create(properties) {
                return new GetBlocklistResponse(properties);
            };

            /**
             * Encodes the specified GetBlocklistResponse message. Does not implicitly {@link dmcn.relay.GetBlocklistResponse.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.GetBlocklistResponse
             * @static
             * @param {dmcn.relay.IGetBlocklistResponse} message GetBlocklistResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetBlocklistResponse.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.found != null && Object.hasOwnProperty.call(message, "found"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.found);
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.record);
                return writer;
            };

            /**
             * Encodes the specified GetBlocklistResponse message, length delimited. Does not implicitly {@link dmcn.relay.GetBlocklistResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.GetBlocklistResponse
             * @static
             * @param {dmcn.relay.IGetBlocklistResponse} message GetBlocklistResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetBlocklistResponse.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a GetBlocklistResponse message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.GetBlocklistResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.GetBlocklistResponse} GetBlocklistResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetBlocklistResponse.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.GetBlocklistResponse();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.found = reader.bool();
                            break;
                        }
                    case 2: {
                            message.record = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a GetBlocklistResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.GetBlocklistResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.GetBlocklistResponse} GetBlocklistResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetBlocklistResponse.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetBlocklistResponse message.
             * @function verify
             * @memberof dmcn.relay.GetBlocklistResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetBlocklistResponse.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.found != null && Object.hasOwnProperty.call(message, "found"))
                    if (typeof message.found !== "boolean")
                        return "found: boolean expected";
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    if (!(message.record && typeof message.record.length === "number" || $util.isString(message.record)))
                        return "record: buffer expected";
                return null;
            };

            /**
             * Creates a GetBlocklistResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.GetBlocklistResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.GetBlocklistResponse} GetBlocklistResponse
             */
            GetBlocklistResponse.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.GetBlocklistResponse)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.GetBlocklistResponse: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.GetBlocklistResponse();
                if (object.found != null)
                    message.found = Boolean(object.found);
                if (object.record != null)
                    if (typeof object.record === "string")
                        $util.base64.decode(object.record, message.record = $util.newBuffer($util.base64.length(object.record)), 0);
                    else if (object.record.length >= 0)
                        message.record = object.record;
                return message;
            };

            /**
             * Creates a plain object from a GetBlocklistResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.GetBlocklistResponse
             * @static
             * @param {dmcn.relay.GetBlocklistResponse} message GetBlocklistResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetBlocklistResponse.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.found = false;
                    if (options.bytes === String)
                        object.record = "";
                    else {
                        object.record = [];
                        if (options.bytes !== Array)
                            object.record = $util.newBuffer(object.record);
                    }
                }
                if (message.found != null && Object.hasOwnProperty.call(message, "found"))
                    object.found = message.found;
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    object.record = options.bytes === String ? $util.base64.encode(message.record, 0, message.record.length) : options.bytes === Array ? Array.prototype.slice.call(message.record) : message.record;
                return object;
            };

            /**
             * Converts this GetBlocklistResponse to JSON.
             * @function toJSON
             * @memberof dmcn.relay.GetBlocklistResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetBlocklistResponse.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for GetBlocklistResponse
             * @function getTypeUrl
             * @memberof dmcn.relay.GetBlocklistResponse
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            GetBlocklistResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.GetBlocklistResponse";
            };

            return GetBlocklistResponse;
        })();

        /**
         * RecordKind enum.
         * @name dmcn.relay.RecordKind
         * @enum {number}
         * @property {number} RECORD_KIND_UNSPECIFIED=0 RECORD_KIND_UNSPECIFIED value
         * @property {number} RECORD_KIND_IDENTITY=1 RECORD_KIND_IDENTITY value
         * @property {number} RECORD_KIND_DAR=2 RECORD_KIND_DAR value
         * @property {number} RECORD_KIND_ROSTER=3 RECORD_KIND_ROSTER value
         * @property {number} RECORD_KIND_REMOVAL=4 RECORD_KIND_REMOVAL value
         * @property {number} RECORD_KIND_BLOCKLIST=5 RECORD_KIND_BLOCKLIST value
         */
        relay.RecordKind = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "RECORD_KIND_UNSPECIFIED"] = 0;
            values[valuesById[1] = "RECORD_KIND_IDENTITY"] = 1;
            values[valuesById[2] = "RECORD_KIND_DAR"] = 2;
            values[valuesById[3] = "RECORD_KIND_ROSTER"] = 3;
            values[valuesById[4] = "RECORD_KIND_REMOVAL"] = 4;
            values[valuesById[5] = "RECORD_KIND_BLOCKLIST"] = 5;
            return values;
        })();

        relay.PutRecordRequest = (function() {

            /**
             * Properties of a PutRecordRequest.
             * @memberof dmcn.relay
             * @interface IPutRecordRequest
             * @property {dmcn.relay.RecordKind|null} [kind] PutRecordRequest kind
             * @property {Uint8Array|null} [record] PutRecordRequest record
             */

            /**
             * Constructs a new PutRecordRequest.
             * @memberof dmcn.relay
             * @classdesc Represents a PutRecordRequest.
             * @implements IPutRecordRequest
             * @constructor
             * @param {dmcn.relay.IPutRecordRequest=} [properties] Properties to set
             */
            function PutRecordRequest(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PutRecordRequest kind.
             * @member {dmcn.relay.RecordKind} kind
             * @memberof dmcn.relay.PutRecordRequest
             * @instance
             */
            PutRecordRequest.prototype.kind = 0;

            /**
             * PutRecordRequest record.
             * @member {Uint8Array} record
             * @memberof dmcn.relay.PutRecordRequest
             * @instance
             */
            PutRecordRequest.prototype.record = $util.newBuffer([]);

            /**
             * Creates a new PutRecordRequest instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.PutRecordRequest
             * @static
             * @param {dmcn.relay.IPutRecordRequest=} [properties] Properties to set
             * @returns {dmcn.relay.PutRecordRequest} PutRecordRequest instance
             */
            PutRecordRequest.create = function create(properties) {
                return new PutRecordRequest(properties);
            };

            /**
             * Encodes the specified PutRecordRequest message. Does not implicitly {@link dmcn.relay.PutRecordRequest.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.PutRecordRequest
             * @static
             * @param {dmcn.relay.IPutRecordRequest} message PutRecordRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PutRecordRequest.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.kind != null && Object.hasOwnProperty.call(message, "kind"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.kind);
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.record);
                return writer;
            };

            /**
             * Encodes the specified PutRecordRequest message, length delimited. Does not implicitly {@link dmcn.relay.PutRecordRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.PutRecordRequest
             * @static
             * @param {dmcn.relay.IPutRecordRequest} message PutRecordRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PutRecordRequest.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a PutRecordRequest message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.PutRecordRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.PutRecordRequest} PutRecordRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PutRecordRequest.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.PutRecordRequest();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.kind = reader.int32();
                            break;
                        }
                    case 2: {
                            message.record = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a PutRecordRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.PutRecordRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.PutRecordRequest} PutRecordRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PutRecordRequest.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PutRecordRequest message.
             * @function verify
             * @memberof dmcn.relay.PutRecordRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PutRecordRequest.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.kind != null && Object.hasOwnProperty.call(message, "kind"))
                    switch (message.kind) {
                    default:
                        return "kind: enum value expected";
                    case 0:
                    case 1:
                    case 2:
                    case 3:
                    case 4:
                    case 5:
                        break;
                    }
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    if (!(message.record && typeof message.record.length === "number" || $util.isString(message.record)))
                        return "record: buffer expected";
                return null;
            };

            /**
             * Creates a PutRecordRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.PutRecordRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.PutRecordRequest} PutRecordRequest
             */
            PutRecordRequest.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.PutRecordRequest)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.PutRecordRequest: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.PutRecordRequest();
                switch (object.kind) {
                default:
                    if (typeof object.kind === "number") {
                        message.kind = object.kind;
                        break;
                    }
                    break;
                case "RECORD_KIND_UNSPECIFIED":
                case 0:
                    message.kind = 0;
                    break;
                case "RECORD_KIND_IDENTITY":
                case 1:
                    message.kind = 1;
                    break;
                case "RECORD_KIND_DAR":
                case 2:
                    message.kind = 2;
                    break;
                case "RECORD_KIND_ROSTER":
                case 3:
                    message.kind = 3;
                    break;
                case "RECORD_KIND_REMOVAL":
                case 4:
                    message.kind = 4;
                    break;
                case "RECORD_KIND_BLOCKLIST":
                case 5:
                    message.kind = 5;
                    break;
                }
                if (object.record != null)
                    if (typeof object.record === "string")
                        $util.base64.decode(object.record, message.record = $util.newBuffer($util.base64.length(object.record)), 0);
                    else if (object.record.length >= 0)
                        message.record = object.record;
                return message;
            };

            /**
             * Creates a plain object from a PutRecordRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.PutRecordRequest
             * @static
             * @param {dmcn.relay.PutRecordRequest} message PutRecordRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PutRecordRequest.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.kind = options.enums === String ? "RECORD_KIND_UNSPECIFIED" : 0;
                    if (options.bytes === String)
                        object.record = "";
                    else {
                        object.record = [];
                        if (options.bytes !== Array)
                            object.record = $util.newBuffer(object.record);
                    }
                }
                if (message.kind != null && Object.hasOwnProperty.call(message, "kind"))
                    object.kind = options.enums === String ? $root.dmcn.relay.RecordKind[message.kind] === undefined ? message.kind : $root.dmcn.relay.RecordKind[message.kind] : message.kind;
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    object.record = options.bytes === String ? $util.base64.encode(message.record, 0, message.record.length) : options.bytes === Array ? Array.prototype.slice.call(message.record) : message.record;
                return object;
            };

            /**
             * Converts this PutRecordRequest to JSON.
             * @function toJSON
             * @memberof dmcn.relay.PutRecordRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PutRecordRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PutRecordRequest
             * @function getTypeUrl
             * @memberof dmcn.relay.PutRecordRequest
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PutRecordRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.PutRecordRequest";
            };

            return PutRecordRequest;
        })();

        relay.PutRecordResponse = (function() {

            /**
             * Properties of a PutRecordResponse.
             * @memberof dmcn.relay
             * @interface IPutRecordResponse
             * @property {boolean|null} [accepted] PutRecordResponse accepted
             * @property {string|null} [reason] PutRecordResponse reason
             */

            /**
             * Constructs a new PutRecordResponse.
             * @memberof dmcn.relay
             * @classdesc Represents a PutRecordResponse.
             * @implements IPutRecordResponse
             * @constructor
             * @param {dmcn.relay.IPutRecordResponse=} [properties] Properties to set
             */
            function PutRecordResponse(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PutRecordResponse accepted.
             * @member {boolean} accepted
             * @memberof dmcn.relay.PutRecordResponse
             * @instance
             */
            PutRecordResponse.prototype.accepted = false;

            /**
             * PutRecordResponse reason.
             * @member {string} reason
             * @memberof dmcn.relay.PutRecordResponse
             * @instance
             */
            PutRecordResponse.prototype.reason = "";

            /**
             * Creates a new PutRecordResponse instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.PutRecordResponse
             * @static
             * @param {dmcn.relay.IPutRecordResponse=} [properties] Properties to set
             * @returns {dmcn.relay.PutRecordResponse} PutRecordResponse instance
             */
            PutRecordResponse.create = function create(properties) {
                return new PutRecordResponse(properties);
            };

            /**
             * Encodes the specified PutRecordResponse message. Does not implicitly {@link dmcn.relay.PutRecordResponse.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.PutRecordResponse
             * @static
             * @param {dmcn.relay.IPutRecordResponse} message PutRecordResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PutRecordResponse.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.accepted != null && Object.hasOwnProperty.call(message, "accepted"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.accepted);
                if (message.reason != null && Object.hasOwnProperty.call(message, "reason"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.reason);
                return writer;
            };

            /**
             * Encodes the specified PutRecordResponse message, length delimited. Does not implicitly {@link dmcn.relay.PutRecordResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.PutRecordResponse
             * @static
             * @param {dmcn.relay.IPutRecordResponse} message PutRecordResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PutRecordResponse.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a PutRecordResponse message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.PutRecordResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.PutRecordResponse} PutRecordResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PutRecordResponse.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.PutRecordResponse();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.accepted = reader.bool();
                            break;
                        }
                    case 2: {
                            message.reason = reader.string();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a PutRecordResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.PutRecordResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.PutRecordResponse} PutRecordResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PutRecordResponse.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PutRecordResponse message.
             * @function verify
             * @memberof dmcn.relay.PutRecordResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PutRecordResponse.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.accepted != null && Object.hasOwnProperty.call(message, "accepted"))
                    if (typeof message.accepted !== "boolean")
                        return "accepted: boolean expected";
                if (message.reason != null && Object.hasOwnProperty.call(message, "reason"))
                    if (!$util.isString(message.reason))
                        return "reason: string expected";
                return null;
            };

            /**
             * Creates a PutRecordResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.PutRecordResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.PutRecordResponse} PutRecordResponse
             */
            PutRecordResponse.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.PutRecordResponse)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.PutRecordResponse: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.PutRecordResponse();
                if (object.accepted != null)
                    message.accepted = Boolean(object.accepted);
                if (object.reason != null)
                    message.reason = String(object.reason);
                return message;
            };

            /**
             * Creates a plain object from a PutRecordResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.PutRecordResponse
             * @static
             * @param {dmcn.relay.PutRecordResponse} message PutRecordResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PutRecordResponse.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.accepted = false;
                    object.reason = "";
                }
                if (message.accepted != null && Object.hasOwnProperty.call(message, "accepted"))
                    object.accepted = message.accepted;
                if (message.reason != null && Object.hasOwnProperty.call(message, "reason"))
                    object.reason = message.reason;
                return object;
            };

            /**
             * Converts this PutRecordResponse to JSON.
             * @function toJSON
             * @memberof dmcn.relay.PutRecordResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PutRecordResponse.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PutRecordResponse
             * @function getTypeUrl
             * @memberof dmcn.relay.PutRecordResponse
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PutRecordResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.PutRecordResponse";
            };

            return PutRecordResponse;
        })();

        relay.GetRelayDescriptorRequest = (function() {

            /**
             * Properties of a GetRelayDescriptorRequest.
             * @memberof dmcn.relay
             * @interface IGetRelayDescriptorRequest
             * @property {string|null} [peerId] GetRelayDescriptorRequest peerId
             */

            /**
             * Constructs a new GetRelayDescriptorRequest.
             * @memberof dmcn.relay
             * @classdesc Represents a GetRelayDescriptorRequest.
             * @implements IGetRelayDescriptorRequest
             * @constructor
             * @param {dmcn.relay.IGetRelayDescriptorRequest=} [properties] Properties to set
             */
            function GetRelayDescriptorRequest(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * GetRelayDescriptorRequest peerId.
             * @member {string} peerId
             * @memberof dmcn.relay.GetRelayDescriptorRequest
             * @instance
             */
            GetRelayDescriptorRequest.prototype.peerId = "";

            /**
             * Creates a new GetRelayDescriptorRequest instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.GetRelayDescriptorRequest
             * @static
             * @param {dmcn.relay.IGetRelayDescriptorRequest=} [properties] Properties to set
             * @returns {dmcn.relay.GetRelayDescriptorRequest} GetRelayDescriptorRequest instance
             */
            GetRelayDescriptorRequest.create = function create(properties) {
                return new GetRelayDescriptorRequest(properties);
            };

            /**
             * Encodes the specified GetRelayDescriptorRequest message. Does not implicitly {@link dmcn.relay.GetRelayDescriptorRequest.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.GetRelayDescriptorRequest
             * @static
             * @param {dmcn.relay.IGetRelayDescriptorRequest} message GetRelayDescriptorRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetRelayDescriptorRequest.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.peerId != null && Object.hasOwnProperty.call(message, "peerId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.peerId);
                return writer;
            };

            /**
             * Encodes the specified GetRelayDescriptorRequest message, length delimited. Does not implicitly {@link dmcn.relay.GetRelayDescriptorRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.GetRelayDescriptorRequest
             * @static
             * @param {dmcn.relay.IGetRelayDescriptorRequest} message GetRelayDescriptorRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetRelayDescriptorRequest.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a GetRelayDescriptorRequest message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.GetRelayDescriptorRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.GetRelayDescriptorRequest} GetRelayDescriptorRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetRelayDescriptorRequest.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.GetRelayDescriptorRequest();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.peerId = reader.string();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a GetRelayDescriptorRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.GetRelayDescriptorRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.GetRelayDescriptorRequest} GetRelayDescriptorRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetRelayDescriptorRequest.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetRelayDescriptorRequest message.
             * @function verify
             * @memberof dmcn.relay.GetRelayDescriptorRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetRelayDescriptorRequest.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.peerId != null && Object.hasOwnProperty.call(message, "peerId"))
                    if (!$util.isString(message.peerId))
                        return "peerId: string expected";
                return null;
            };

            /**
             * Creates a GetRelayDescriptorRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.GetRelayDescriptorRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.GetRelayDescriptorRequest} GetRelayDescriptorRequest
             */
            GetRelayDescriptorRequest.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.GetRelayDescriptorRequest)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.GetRelayDescriptorRequest: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.GetRelayDescriptorRequest();
                if (object.peerId != null)
                    message.peerId = String(object.peerId);
                return message;
            };

            /**
             * Creates a plain object from a GetRelayDescriptorRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.GetRelayDescriptorRequest
             * @static
             * @param {dmcn.relay.GetRelayDescriptorRequest} message GetRelayDescriptorRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetRelayDescriptorRequest.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    object.peerId = "";
                if (message.peerId != null && Object.hasOwnProperty.call(message, "peerId"))
                    object.peerId = message.peerId;
                return object;
            };

            /**
             * Converts this GetRelayDescriptorRequest to JSON.
             * @function toJSON
             * @memberof dmcn.relay.GetRelayDescriptorRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetRelayDescriptorRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for GetRelayDescriptorRequest
             * @function getTypeUrl
             * @memberof dmcn.relay.GetRelayDescriptorRequest
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            GetRelayDescriptorRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.GetRelayDescriptorRequest";
            };

            return GetRelayDescriptorRequest;
        })();

        relay.GetRelayDescriptorResponse = (function() {

            /**
             * Properties of a GetRelayDescriptorResponse.
             * @memberof dmcn.relay
             * @interface IGetRelayDescriptorResponse
             * @property {boolean|null} [found] GetRelayDescriptorResponse found
             * @property {Uint8Array|null} [record] GetRelayDescriptorResponse record
             */

            /**
             * Constructs a new GetRelayDescriptorResponse.
             * @memberof dmcn.relay
             * @classdesc Represents a GetRelayDescriptorResponse.
             * @implements IGetRelayDescriptorResponse
             * @constructor
             * @param {dmcn.relay.IGetRelayDescriptorResponse=} [properties] Properties to set
             */
            function GetRelayDescriptorResponse(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * GetRelayDescriptorResponse found.
             * @member {boolean} found
             * @memberof dmcn.relay.GetRelayDescriptorResponse
             * @instance
             */
            GetRelayDescriptorResponse.prototype.found = false;

            /**
             * GetRelayDescriptorResponse record.
             * @member {Uint8Array} record
             * @memberof dmcn.relay.GetRelayDescriptorResponse
             * @instance
             */
            GetRelayDescriptorResponse.prototype.record = $util.newBuffer([]);

            /**
             * Creates a new GetRelayDescriptorResponse instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.GetRelayDescriptorResponse
             * @static
             * @param {dmcn.relay.IGetRelayDescriptorResponse=} [properties] Properties to set
             * @returns {dmcn.relay.GetRelayDescriptorResponse} GetRelayDescriptorResponse instance
             */
            GetRelayDescriptorResponse.create = function create(properties) {
                return new GetRelayDescriptorResponse(properties);
            };

            /**
             * Encodes the specified GetRelayDescriptorResponse message. Does not implicitly {@link dmcn.relay.GetRelayDescriptorResponse.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.GetRelayDescriptorResponse
             * @static
             * @param {dmcn.relay.IGetRelayDescriptorResponse} message GetRelayDescriptorResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetRelayDescriptorResponse.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.found != null && Object.hasOwnProperty.call(message, "found"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.found);
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.record);
                return writer;
            };

            /**
             * Encodes the specified GetRelayDescriptorResponse message, length delimited. Does not implicitly {@link dmcn.relay.GetRelayDescriptorResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.GetRelayDescriptorResponse
             * @static
             * @param {dmcn.relay.IGetRelayDescriptorResponse} message GetRelayDescriptorResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetRelayDescriptorResponse.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a GetRelayDescriptorResponse message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.GetRelayDescriptorResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.GetRelayDescriptorResponse} GetRelayDescriptorResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetRelayDescriptorResponse.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.GetRelayDescriptorResponse();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.found = reader.bool();
                            break;
                        }
                    case 2: {
                            message.record = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a GetRelayDescriptorResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.GetRelayDescriptorResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.GetRelayDescriptorResponse} GetRelayDescriptorResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetRelayDescriptorResponse.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetRelayDescriptorResponse message.
             * @function verify
             * @memberof dmcn.relay.GetRelayDescriptorResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetRelayDescriptorResponse.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.found != null && Object.hasOwnProperty.call(message, "found"))
                    if (typeof message.found !== "boolean")
                        return "found: boolean expected";
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    if (!(message.record && typeof message.record.length === "number" || $util.isString(message.record)))
                        return "record: buffer expected";
                return null;
            };

            /**
             * Creates a GetRelayDescriptorResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.GetRelayDescriptorResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.GetRelayDescriptorResponse} GetRelayDescriptorResponse
             */
            GetRelayDescriptorResponse.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.GetRelayDescriptorResponse)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.GetRelayDescriptorResponse: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.GetRelayDescriptorResponse();
                if (object.found != null)
                    message.found = Boolean(object.found);
                if (object.record != null)
                    if (typeof object.record === "string")
                        $util.base64.decode(object.record, message.record = $util.newBuffer($util.base64.length(object.record)), 0);
                    else if (object.record.length >= 0)
                        message.record = object.record;
                return message;
            };

            /**
             * Creates a plain object from a GetRelayDescriptorResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.GetRelayDescriptorResponse
             * @static
             * @param {dmcn.relay.GetRelayDescriptorResponse} message GetRelayDescriptorResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetRelayDescriptorResponse.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.found = false;
                    if (options.bytes === String)
                        object.record = "";
                    else {
                        object.record = [];
                        if (options.bytes !== Array)
                            object.record = $util.newBuffer(object.record);
                    }
                }
                if (message.found != null && Object.hasOwnProperty.call(message, "found"))
                    object.found = message.found;
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    object.record = options.bytes === String ? $util.base64.encode(message.record, 0, message.record.length) : options.bytes === Array ? Array.prototype.slice.call(message.record) : message.record;
                return object;
            };

            /**
             * Converts this GetRelayDescriptorResponse to JSON.
             * @function toJSON
             * @memberof dmcn.relay.GetRelayDescriptorResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetRelayDescriptorResponse.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for GetRelayDescriptorResponse
             * @function getTypeUrl
             * @memberof dmcn.relay.GetRelayDescriptorResponse
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            GetRelayDescriptorResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.GetRelayDescriptorResponse";
            };

            return GetRelayDescriptorResponse;
        })();

        relay.ErrorResponse = (function() {

            /**
             * Properties of an ErrorResponse.
             * @memberof dmcn.relay
             * @interface IErrorResponse
             * @property {string|null} [code] ErrorResponse code
             * @property {string|null} [message] ErrorResponse message
             */

            /**
             * Constructs a new ErrorResponse.
             * @memberof dmcn.relay
             * @classdesc Represents an ErrorResponse.
             * @implements IErrorResponse
             * @constructor
             * @param {dmcn.relay.IErrorResponse=} [properties] Properties to set
             */
            function ErrorResponse(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ErrorResponse code.
             * @member {string} code
             * @memberof dmcn.relay.ErrorResponse
             * @instance
             */
            ErrorResponse.prototype.code = "";

            /**
             * ErrorResponse message.
             * @member {string} message
             * @memberof dmcn.relay.ErrorResponse
             * @instance
             */
            ErrorResponse.prototype.message = "";

            /**
             * Creates a new ErrorResponse instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.ErrorResponse
             * @static
             * @param {dmcn.relay.IErrorResponse=} [properties] Properties to set
             * @returns {dmcn.relay.ErrorResponse} ErrorResponse instance
             */
            ErrorResponse.create = function create(properties) {
                return new ErrorResponse(properties);
            };

            /**
             * Encodes the specified ErrorResponse message. Does not implicitly {@link dmcn.relay.ErrorResponse.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.ErrorResponse
             * @static
             * @param {dmcn.relay.IErrorResponse} message ErrorResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ErrorResponse.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.code != null && Object.hasOwnProperty.call(message, "code"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.code);
                if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.message);
                return writer;
            };

            /**
             * Encodes the specified ErrorResponse message, length delimited. Does not implicitly {@link dmcn.relay.ErrorResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.ErrorResponse
             * @static
             * @param {dmcn.relay.IErrorResponse} message ErrorResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ErrorResponse.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes an ErrorResponse message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.ErrorResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.ErrorResponse} ErrorResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ErrorResponse.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.ErrorResponse();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.code = reader.string();
                            break;
                        }
                    case 2: {
                            message.message = reader.string();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an ErrorResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.ErrorResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.ErrorResponse} ErrorResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ErrorResponse.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an ErrorResponse message.
             * @function verify
             * @memberof dmcn.relay.ErrorResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ErrorResponse.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.code != null && Object.hasOwnProperty.call(message, "code"))
                    if (!$util.isString(message.code))
                        return "code: string expected";
                if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                    if (!$util.isString(message.message))
                        return "message: string expected";
                return null;
            };

            /**
             * Creates an ErrorResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.ErrorResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.ErrorResponse} ErrorResponse
             */
            ErrorResponse.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.ErrorResponse)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.ErrorResponse: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.ErrorResponse();
                if (object.code != null)
                    message.code = String(object.code);
                if (object.message != null)
                    message.message = String(object.message);
                return message;
            };

            /**
             * Creates a plain object from an ErrorResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.ErrorResponse
             * @static
             * @param {dmcn.relay.ErrorResponse} message ErrorResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ErrorResponse.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.code = "";
                    object.message = "";
                }
                if (message.code != null && Object.hasOwnProperty.call(message, "code"))
                    object.code = message.code;
                if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                    object.message = message.message;
                return object;
            };

            /**
             * Converts this ErrorResponse to JSON.
             * @function toJSON
             * @memberof dmcn.relay.ErrorResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ErrorResponse.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ErrorResponse
             * @function getTypeUrl
             * @memberof dmcn.relay.ErrorResponse
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ErrorResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.ErrorResponse";
            };

            return ErrorResponse;
        })();

        relay.PeersRequest = (function() {

            /**
             * Properties of a PeersRequest.
             * @memberof dmcn.relay
             * @interface IPeersRequest
             */

            /**
             * Constructs a new PeersRequest.
             * @memberof dmcn.relay
             * @classdesc Represents a PeersRequest.
             * @implements IPeersRequest
             * @constructor
             * @param {dmcn.relay.IPeersRequest=} [properties] Properties to set
             */
            function PeersRequest(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Creates a new PeersRequest instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.PeersRequest
             * @static
             * @param {dmcn.relay.IPeersRequest=} [properties] Properties to set
             * @returns {dmcn.relay.PeersRequest} PeersRequest instance
             */
            PeersRequest.create = function create(properties) {
                return new PeersRequest(properties);
            };

            /**
             * Encodes the specified PeersRequest message. Does not implicitly {@link dmcn.relay.PeersRequest.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.PeersRequest
             * @static
             * @param {dmcn.relay.IPeersRequest} message PeersRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PeersRequest.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                return writer;
            };

            /**
             * Encodes the specified PeersRequest message, length delimited. Does not implicitly {@link dmcn.relay.PeersRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.PeersRequest
             * @static
             * @param {dmcn.relay.IPeersRequest} message PeersRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PeersRequest.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a PeersRequest message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.PeersRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.PeersRequest} PeersRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PeersRequest.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.PeersRequest();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a PeersRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.PeersRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.PeersRequest} PeersRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PeersRequest.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PeersRequest message.
             * @function verify
             * @memberof dmcn.relay.PeersRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PeersRequest.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                return null;
            };

            /**
             * Creates a PeersRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.PeersRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.PeersRequest} PeersRequest
             */
            PeersRequest.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.PeersRequest)
                    return object;
                return new $root.dmcn.relay.PeersRequest();
            };

            /**
             * Creates a plain object from a PeersRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.PeersRequest
             * @static
             * @param {dmcn.relay.PeersRequest} message PeersRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PeersRequest.toObject = function toObject() {
                return {};
            };

            /**
             * Converts this PeersRequest to JSON.
             * @function toJSON
             * @memberof dmcn.relay.PeersRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PeersRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PeersRequest
             * @function getTypeUrl
             * @memberof dmcn.relay.PeersRequest
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PeersRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.PeersRequest";
            };

            return PeersRequest;
        })();

        relay.PeersResponse = (function() {

            /**
             * Properties of a PeersResponse.
             * @memberof dmcn.relay
             * @interface IPeersResponse
             * @property {Array.<string>|null} [peers] PeersResponse peers
             */

            /**
             * Constructs a new PeersResponse.
             * @memberof dmcn.relay
             * @classdesc Represents a PeersResponse.
             * @implements IPeersResponse
             * @constructor
             * @param {dmcn.relay.IPeersResponse=} [properties] Properties to set
             */
            function PeersResponse(properties) {
                this.peers = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PeersResponse peers.
             * @member {Array.<string>} peers
             * @memberof dmcn.relay.PeersResponse
             * @instance
             */
            PeersResponse.prototype.peers = $util.emptyArray;

            /**
             * Creates a new PeersResponse instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.PeersResponse
             * @static
             * @param {dmcn.relay.IPeersResponse=} [properties] Properties to set
             * @returns {dmcn.relay.PeersResponse} PeersResponse instance
             */
            PeersResponse.create = function create(properties) {
                return new PeersResponse(properties);
            };

            /**
             * Encodes the specified PeersResponse message. Does not implicitly {@link dmcn.relay.PeersResponse.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.PeersResponse
             * @static
             * @param {dmcn.relay.IPeersResponse} message PeersResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PeersResponse.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.peers != null && message.peers.length)
                    for (let i = 0; i < message.peers.length; ++i)
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.peers[i]);
                return writer;
            };

            /**
             * Encodes the specified PeersResponse message, length delimited. Does not implicitly {@link dmcn.relay.PeersResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.PeersResponse
             * @static
             * @param {dmcn.relay.IPeersResponse} message PeersResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PeersResponse.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a PeersResponse message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.PeersResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.PeersResponse} PeersResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PeersResponse.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.PeersResponse();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            if (!(message.peers && message.peers.length))
                                message.peers = [];
                            message.peers.push(reader.string());
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a PeersResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.PeersResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.PeersResponse} PeersResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PeersResponse.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PeersResponse message.
             * @function verify
             * @memberof dmcn.relay.PeersResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PeersResponse.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.peers != null && Object.hasOwnProperty.call(message, "peers")) {
                    if (!Array.isArray(message.peers))
                        return "peers: array expected";
                    for (let i = 0; i < message.peers.length; ++i)
                        if (!$util.isString(message.peers[i]))
                            return "peers: string[] expected";
                }
                return null;
            };

            /**
             * Creates a PeersResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.PeersResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.PeersResponse} PeersResponse
             */
            PeersResponse.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.PeersResponse)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.PeersResponse: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.PeersResponse();
                if (object.peers) {
                    if (!Array.isArray(object.peers))
                        throw TypeError(".dmcn.relay.PeersResponse.peers: array expected");
                    message.peers = [];
                    for (let i = 0; i < object.peers.length; ++i)
                        message.peers[i] = String(object.peers[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a PeersResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.PeersResponse
             * @static
             * @param {dmcn.relay.PeersResponse} message PeersResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PeersResponse.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.peers = [];
                if (message.peers && message.peers.length) {
                    object.peers = [];
                    for (let j = 0; j < message.peers.length; ++j)
                        object.peers[j] = message.peers[j];
                }
                return object;
            };

            /**
             * Converts this PeersResponse to JSON.
             * @function toJSON
             * @memberof dmcn.relay.PeersResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PeersResponse.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PeersResponse
             * @function getTypeUrl
             * @memberof dmcn.relay.PeersResponse
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PeersResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.PeersResponse";
            };

            return PeersResponse;
        })();

        relay.MailboxEntry = (function() {

            /**
             * Properties of a MailboxEntry.
             * @memberof dmcn.relay
             * @interface IMailboxEntry
             * @property {Uint8Array|null} [hash] MailboxEntry hash
             * @property {number|Long|null} [storedAt] MailboxEntry storedAt
             * @property {number|Long|null} [bodySize] MailboxEntry bodySize
             * @property {Array.<dmcn.message.IRecipientRecord>|null} [recipients] MailboxEntry recipients
             * @property {Uint8Array|null} [encryptedHeader] MailboxEntry encryptedHeader
             * @property {Uint8Array|null} [headerNonce] MailboxEntry headerNonce
             * @property {Uint8Array|null} [headerTag] MailboxEntry headerTag
             * @property {number|null} [headerSizeClass] MailboxEntry headerSizeClass
             * @property {Uint8Array|null} [bodyContentAddress] MailboxEntry bodyContentAddress
             */

            /**
             * Constructs a new MailboxEntry.
             * @memberof dmcn.relay
             * @classdesc Represents a MailboxEntry.
             * @implements IMailboxEntry
             * @constructor
             * @param {dmcn.relay.IMailboxEntry=} [properties] Properties to set
             */
            function MailboxEntry(properties) {
                this.recipients = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MailboxEntry hash.
             * @member {Uint8Array} hash
             * @memberof dmcn.relay.MailboxEntry
             * @instance
             */
            MailboxEntry.prototype.hash = $util.newBuffer([]);

            /**
             * MailboxEntry storedAt.
             * @member {number|Long} storedAt
             * @memberof dmcn.relay.MailboxEntry
             * @instance
             */
            MailboxEntry.prototype.storedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * MailboxEntry bodySize.
             * @member {number|Long} bodySize
             * @memberof dmcn.relay.MailboxEntry
             * @instance
             */
            MailboxEntry.prototype.bodySize = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * MailboxEntry recipients.
             * @member {Array.<dmcn.message.IRecipientRecord>} recipients
             * @memberof dmcn.relay.MailboxEntry
             * @instance
             */
            MailboxEntry.prototype.recipients = $util.emptyArray;

            /**
             * MailboxEntry encryptedHeader.
             * @member {Uint8Array} encryptedHeader
             * @memberof dmcn.relay.MailboxEntry
             * @instance
             */
            MailboxEntry.prototype.encryptedHeader = $util.newBuffer([]);

            /**
             * MailboxEntry headerNonce.
             * @member {Uint8Array} headerNonce
             * @memberof dmcn.relay.MailboxEntry
             * @instance
             */
            MailboxEntry.prototype.headerNonce = $util.newBuffer([]);

            /**
             * MailboxEntry headerTag.
             * @member {Uint8Array} headerTag
             * @memberof dmcn.relay.MailboxEntry
             * @instance
             */
            MailboxEntry.prototype.headerTag = $util.newBuffer([]);

            /**
             * MailboxEntry headerSizeClass.
             * @member {number} headerSizeClass
             * @memberof dmcn.relay.MailboxEntry
             * @instance
             */
            MailboxEntry.prototype.headerSizeClass = 0;

            /**
             * MailboxEntry bodyContentAddress.
             * @member {Uint8Array} bodyContentAddress
             * @memberof dmcn.relay.MailboxEntry
             * @instance
             */
            MailboxEntry.prototype.bodyContentAddress = $util.newBuffer([]);

            /**
             * Creates a new MailboxEntry instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.MailboxEntry
             * @static
             * @param {dmcn.relay.IMailboxEntry=} [properties] Properties to set
             * @returns {dmcn.relay.MailboxEntry} MailboxEntry instance
             */
            MailboxEntry.create = function create(properties) {
                return new MailboxEntry(properties);
            };

            /**
             * Encodes the specified MailboxEntry message. Does not implicitly {@link dmcn.relay.MailboxEntry.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.MailboxEntry
             * @static
             * @param {dmcn.relay.IMailboxEntry} message MailboxEntry message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MailboxEntry.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.hash != null && Object.hasOwnProperty.call(message, "hash"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.hash);
                if (message.storedAt != null && Object.hasOwnProperty.call(message, "storedAt"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.storedAt);
                if (message.bodySize != null && Object.hasOwnProperty.call(message, "bodySize"))
                    writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.bodySize);
                if (message.recipients != null && message.recipients.length)
                    for (let i = 0; i < message.recipients.length; ++i)
                        $root.dmcn.message.RecipientRecord.encode(message.recipients[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.encryptedHeader != null && Object.hasOwnProperty.call(message, "encryptedHeader"))
                    writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.encryptedHeader);
                if (message.headerNonce != null && Object.hasOwnProperty.call(message, "headerNonce"))
                    writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.headerNonce);
                if (message.headerTag != null && Object.hasOwnProperty.call(message, "headerTag"))
                    writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.headerTag);
                if (message.headerSizeClass != null && Object.hasOwnProperty.call(message, "headerSizeClass"))
                    writer.uint32(/* id 8, wireType 0 =*/64).uint32(message.headerSizeClass);
                if (message.bodyContentAddress != null && Object.hasOwnProperty.call(message, "bodyContentAddress"))
                    writer.uint32(/* id 9, wireType 2 =*/74).bytes(message.bodyContentAddress);
                return writer;
            };

            /**
             * Encodes the specified MailboxEntry message, length delimited. Does not implicitly {@link dmcn.relay.MailboxEntry.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.MailboxEntry
             * @static
             * @param {dmcn.relay.IMailboxEntry} message MailboxEntry message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MailboxEntry.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a MailboxEntry message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.MailboxEntry
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.MailboxEntry} MailboxEntry
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MailboxEntry.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.MailboxEntry();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.hash = reader.bytes();
                            break;
                        }
                    case 2: {
                            message.storedAt = reader.int64();
                            break;
                        }
                    case 3: {
                            message.bodySize = reader.uint64();
                            break;
                        }
                    case 4: {
                            if (!(message.recipients && message.recipients.length))
                                message.recipients = [];
                            message.recipients.push($root.dmcn.message.RecipientRecord.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 5: {
                            message.encryptedHeader = reader.bytes();
                            break;
                        }
                    case 6: {
                            message.headerNonce = reader.bytes();
                            break;
                        }
                    case 7: {
                            message.headerTag = reader.bytes();
                            break;
                        }
                    case 8: {
                            message.headerSizeClass = reader.uint32();
                            break;
                        }
                    case 9: {
                            message.bodyContentAddress = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a MailboxEntry message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.MailboxEntry
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.MailboxEntry} MailboxEntry
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MailboxEntry.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MailboxEntry message.
             * @function verify
             * @memberof dmcn.relay.MailboxEntry
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MailboxEntry.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.hash != null && Object.hasOwnProperty.call(message, "hash"))
                    if (!(message.hash && typeof message.hash.length === "number" || $util.isString(message.hash)))
                        return "hash: buffer expected";
                if (message.storedAt != null && Object.hasOwnProperty.call(message, "storedAt"))
                    if (!$util.isInteger(message.storedAt) && !(message.storedAt && $util.isInteger(message.storedAt.low) && $util.isInteger(message.storedAt.high)))
                        return "storedAt: integer|Long expected";
                if (message.bodySize != null && Object.hasOwnProperty.call(message, "bodySize"))
                    if (!$util.isInteger(message.bodySize) && !(message.bodySize && $util.isInteger(message.bodySize.low) && $util.isInteger(message.bodySize.high)))
                        return "bodySize: integer|Long expected";
                if (message.recipients != null && Object.hasOwnProperty.call(message, "recipients")) {
                    if (!Array.isArray(message.recipients))
                        return "recipients: array expected";
                    for (let i = 0; i < message.recipients.length; ++i) {
                        let error = $root.dmcn.message.RecipientRecord.verify(message.recipients[i], long + 1);
                        if (error)
                            return "recipients." + error;
                    }
                }
                if (message.encryptedHeader != null && Object.hasOwnProperty.call(message, "encryptedHeader"))
                    if (!(message.encryptedHeader && typeof message.encryptedHeader.length === "number" || $util.isString(message.encryptedHeader)))
                        return "encryptedHeader: buffer expected";
                if (message.headerNonce != null && Object.hasOwnProperty.call(message, "headerNonce"))
                    if (!(message.headerNonce && typeof message.headerNonce.length === "number" || $util.isString(message.headerNonce)))
                        return "headerNonce: buffer expected";
                if (message.headerTag != null && Object.hasOwnProperty.call(message, "headerTag"))
                    if (!(message.headerTag && typeof message.headerTag.length === "number" || $util.isString(message.headerTag)))
                        return "headerTag: buffer expected";
                if (message.headerSizeClass != null && Object.hasOwnProperty.call(message, "headerSizeClass"))
                    if (!$util.isInteger(message.headerSizeClass))
                        return "headerSizeClass: integer expected";
                if (message.bodyContentAddress != null && Object.hasOwnProperty.call(message, "bodyContentAddress"))
                    if (!(message.bodyContentAddress && typeof message.bodyContentAddress.length === "number" || $util.isString(message.bodyContentAddress)))
                        return "bodyContentAddress: buffer expected";
                return null;
            };

            /**
             * Creates a MailboxEntry message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.MailboxEntry
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.MailboxEntry} MailboxEntry
             */
            MailboxEntry.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.MailboxEntry)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.MailboxEntry: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.MailboxEntry();
                if (object.hash != null)
                    if (typeof object.hash === "string")
                        $util.base64.decode(object.hash, message.hash = $util.newBuffer($util.base64.length(object.hash)), 0);
                    else if (object.hash.length >= 0)
                        message.hash = object.hash;
                if (object.storedAt != null)
                    if ($util.Long)
                        message.storedAt = $util.Long.fromValue(object.storedAt, false);
                    else if (typeof object.storedAt === "string")
                        message.storedAt = parseInt(object.storedAt, 10);
                    else if (typeof object.storedAt === "number")
                        message.storedAt = object.storedAt;
                    else if (typeof object.storedAt === "object")
                        message.storedAt = new $util.LongBits(object.storedAt.low >>> 0, object.storedAt.high >>> 0).toNumber();
                if (object.bodySize != null)
                    if ($util.Long)
                        message.bodySize = $util.Long.fromValue(object.bodySize, true);
                    else if (typeof object.bodySize === "string")
                        message.bodySize = parseInt(object.bodySize, 10);
                    else if (typeof object.bodySize === "number")
                        message.bodySize = object.bodySize;
                    else if (typeof object.bodySize === "object")
                        message.bodySize = new $util.LongBits(object.bodySize.low >>> 0, object.bodySize.high >>> 0).toNumber(true);
                if (object.recipients) {
                    if (!Array.isArray(object.recipients))
                        throw TypeError(".dmcn.relay.MailboxEntry.recipients: array expected");
                    message.recipients = [];
                    for (let i = 0; i < object.recipients.length; ++i) {
                        if (!$util.isObject(object.recipients[i]))
                            throw TypeError(".dmcn.relay.MailboxEntry.recipients: object expected");
                        message.recipients[i] = $root.dmcn.message.RecipientRecord.fromObject(object.recipients[i], long + 1);
                    }
                }
                if (object.encryptedHeader != null)
                    if (typeof object.encryptedHeader === "string")
                        $util.base64.decode(object.encryptedHeader, message.encryptedHeader = $util.newBuffer($util.base64.length(object.encryptedHeader)), 0);
                    else if (object.encryptedHeader.length >= 0)
                        message.encryptedHeader = object.encryptedHeader;
                if (object.headerNonce != null)
                    if (typeof object.headerNonce === "string")
                        $util.base64.decode(object.headerNonce, message.headerNonce = $util.newBuffer($util.base64.length(object.headerNonce)), 0);
                    else if (object.headerNonce.length >= 0)
                        message.headerNonce = object.headerNonce;
                if (object.headerTag != null)
                    if (typeof object.headerTag === "string")
                        $util.base64.decode(object.headerTag, message.headerTag = $util.newBuffer($util.base64.length(object.headerTag)), 0);
                    else if (object.headerTag.length >= 0)
                        message.headerTag = object.headerTag;
                if (object.headerSizeClass != null)
                    message.headerSizeClass = object.headerSizeClass >>> 0;
                if (object.bodyContentAddress != null)
                    if (typeof object.bodyContentAddress === "string")
                        $util.base64.decode(object.bodyContentAddress, message.bodyContentAddress = $util.newBuffer($util.base64.length(object.bodyContentAddress)), 0);
                    else if (object.bodyContentAddress.length >= 0)
                        message.bodyContentAddress = object.bodyContentAddress;
                return message;
            };

            /**
             * Creates a plain object from a MailboxEntry message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.MailboxEntry
             * @static
             * @param {dmcn.relay.MailboxEntry} message MailboxEntry
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MailboxEntry.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.recipients = [];
                if (options.defaults) {
                    if (options.bytes === String)
                        object.hash = "";
                    else {
                        object.hash = [];
                        if (options.bytes !== Array)
                            object.hash = $util.newBuffer(object.hash);
                    }
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.storedAt = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.storedAt = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.bodySize = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.bodySize = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if (options.bytes === String)
                        object.encryptedHeader = "";
                    else {
                        object.encryptedHeader = [];
                        if (options.bytes !== Array)
                            object.encryptedHeader = $util.newBuffer(object.encryptedHeader);
                    }
                    if (options.bytes === String)
                        object.headerNonce = "";
                    else {
                        object.headerNonce = [];
                        if (options.bytes !== Array)
                            object.headerNonce = $util.newBuffer(object.headerNonce);
                    }
                    if (options.bytes === String)
                        object.headerTag = "";
                    else {
                        object.headerTag = [];
                        if (options.bytes !== Array)
                            object.headerTag = $util.newBuffer(object.headerTag);
                    }
                    object.headerSizeClass = 0;
                    if (options.bytes === String)
                        object.bodyContentAddress = "";
                    else {
                        object.bodyContentAddress = [];
                        if (options.bytes !== Array)
                            object.bodyContentAddress = $util.newBuffer(object.bodyContentAddress);
                    }
                }
                if (message.hash != null && Object.hasOwnProperty.call(message, "hash"))
                    object.hash = options.bytes === String ? $util.base64.encode(message.hash, 0, message.hash.length) : options.bytes === Array ? Array.prototype.slice.call(message.hash) : message.hash;
                if (message.storedAt != null && Object.hasOwnProperty.call(message, "storedAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.storedAt = typeof message.storedAt === "number" ? BigInt(message.storedAt) : $util.Long.fromBits(message.storedAt.low >>> 0, message.storedAt.high >>> 0, false).toBigInt();
                    else if (typeof message.storedAt === "number")
                        object.storedAt = options.longs === String ? String(message.storedAt) : message.storedAt;
                    else
                        object.storedAt = options.longs === String ? $util.Long.prototype.toString.call(message.storedAt) : options.longs === Number ? new $util.LongBits(message.storedAt.low >>> 0, message.storedAt.high >>> 0).toNumber() : message.storedAt;
                if (message.bodySize != null && Object.hasOwnProperty.call(message, "bodySize"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.bodySize = typeof message.bodySize === "number" ? BigInt(message.bodySize) : $util.Long.fromBits(message.bodySize.low >>> 0, message.bodySize.high >>> 0, true).toBigInt();
                    else if (typeof message.bodySize === "number")
                        object.bodySize = options.longs === String ? String(message.bodySize) : message.bodySize;
                    else
                        object.bodySize = options.longs === String ? $util.Long.prototype.toString.call(message.bodySize) : options.longs === Number ? new $util.LongBits(message.bodySize.low >>> 0, message.bodySize.high >>> 0).toNumber(true) : message.bodySize;
                if (message.recipients && message.recipients.length) {
                    object.recipients = [];
                    for (let j = 0; j < message.recipients.length; ++j)
                        object.recipients[j] = $root.dmcn.message.RecipientRecord.toObject(message.recipients[j], options, q + 1);
                }
                if (message.encryptedHeader != null && Object.hasOwnProperty.call(message, "encryptedHeader"))
                    object.encryptedHeader = options.bytes === String ? $util.base64.encode(message.encryptedHeader, 0, message.encryptedHeader.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedHeader) : message.encryptedHeader;
                if (message.headerNonce != null && Object.hasOwnProperty.call(message, "headerNonce"))
                    object.headerNonce = options.bytes === String ? $util.base64.encode(message.headerNonce, 0, message.headerNonce.length) : options.bytes === Array ? Array.prototype.slice.call(message.headerNonce) : message.headerNonce;
                if (message.headerTag != null && Object.hasOwnProperty.call(message, "headerTag"))
                    object.headerTag = options.bytes === String ? $util.base64.encode(message.headerTag, 0, message.headerTag.length) : options.bytes === Array ? Array.prototype.slice.call(message.headerTag) : message.headerTag;
                if (message.headerSizeClass != null && Object.hasOwnProperty.call(message, "headerSizeClass"))
                    object.headerSizeClass = message.headerSizeClass;
                if (message.bodyContentAddress != null && Object.hasOwnProperty.call(message, "bodyContentAddress"))
                    object.bodyContentAddress = options.bytes === String ? $util.base64.encode(message.bodyContentAddress, 0, message.bodyContentAddress.length) : options.bytes === Array ? Array.prototype.slice.call(message.bodyContentAddress) : message.bodyContentAddress;
                return object;
            };

            /**
             * Converts this MailboxEntry to JSON.
             * @function toJSON
             * @memberof dmcn.relay.MailboxEntry
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MailboxEntry.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MailboxEntry
             * @function getTypeUrl
             * @memberof dmcn.relay.MailboxEntry
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MailboxEntry.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.MailboxEntry";
            };

            return MailboxEntry;
        })();

        relay.MailboxBody = (function() {

            /**
             * Properties of a MailboxBody.
             * @memberof dmcn.relay
             * @interface IMailboxBody
             * @property {Uint8Array|null} [encryptedBody] MailboxBody encryptedBody
             * @property {Uint8Array|null} [bodyNonce] MailboxBody bodyNonce
             * @property {Uint8Array|null} [bodyTag] MailboxBody bodyTag
             * @property {number|null} [bodySizeClass] MailboxBody bodySizeClass
             * @property {Uint8Array|null} [bodyContentAddress] MailboxBody bodyContentAddress
             */

            /**
             * Constructs a new MailboxBody.
             * @memberof dmcn.relay
             * @classdesc Represents a MailboxBody.
             * @implements IMailboxBody
             * @constructor
             * @param {dmcn.relay.IMailboxBody=} [properties] Properties to set
             */
            function MailboxBody(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MailboxBody encryptedBody.
             * @member {Uint8Array} encryptedBody
             * @memberof dmcn.relay.MailboxBody
             * @instance
             */
            MailboxBody.prototype.encryptedBody = $util.newBuffer([]);

            /**
             * MailboxBody bodyNonce.
             * @member {Uint8Array} bodyNonce
             * @memberof dmcn.relay.MailboxBody
             * @instance
             */
            MailboxBody.prototype.bodyNonce = $util.newBuffer([]);

            /**
             * MailboxBody bodyTag.
             * @member {Uint8Array} bodyTag
             * @memberof dmcn.relay.MailboxBody
             * @instance
             */
            MailboxBody.prototype.bodyTag = $util.newBuffer([]);

            /**
             * MailboxBody bodySizeClass.
             * @member {number} bodySizeClass
             * @memberof dmcn.relay.MailboxBody
             * @instance
             */
            MailboxBody.prototype.bodySizeClass = 0;

            /**
             * MailboxBody bodyContentAddress.
             * @member {Uint8Array} bodyContentAddress
             * @memberof dmcn.relay.MailboxBody
             * @instance
             */
            MailboxBody.prototype.bodyContentAddress = $util.newBuffer([]);

            /**
             * Creates a new MailboxBody instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.MailboxBody
             * @static
             * @param {dmcn.relay.IMailboxBody=} [properties] Properties to set
             * @returns {dmcn.relay.MailboxBody} MailboxBody instance
             */
            MailboxBody.create = function create(properties) {
                return new MailboxBody(properties);
            };

            /**
             * Encodes the specified MailboxBody message. Does not implicitly {@link dmcn.relay.MailboxBody.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.MailboxBody
             * @static
             * @param {dmcn.relay.IMailboxBody} message MailboxBody message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MailboxBody.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.encryptedBody != null && Object.hasOwnProperty.call(message, "encryptedBody"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedBody);
                if (message.bodyNonce != null && Object.hasOwnProperty.call(message, "bodyNonce"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.bodyNonce);
                if (message.bodyTag != null && Object.hasOwnProperty.call(message, "bodyTag"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.bodyTag);
                if (message.bodySizeClass != null && Object.hasOwnProperty.call(message, "bodySizeClass"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.bodySizeClass);
                if (message.bodyContentAddress != null && Object.hasOwnProperty.call(message, "bodyContentAddress"))
                    writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.bodyContentAddress);
                return writer;
            };

            /**
             * Encodes the specified MailboxBody message, length delimited. Does not implicitly {@link dmcn.relay.MailboxBody.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.MailboxBody
             * @static
             * @param {dmcn.relay.IMailboxBody} message MailboxBody message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MailboxBody.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a MailboxBody message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.MailboxBody
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.MailboxBody} MailboxBody
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MailboxBody.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.MailboxBody();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.encryptedBody = reader.bytes();
                            break;
                        }
                    case 2: {
                            message.bodyNonce = reader.bytes();
                            break;
                        }
                    case 3: {
                            message.bodyTag = reader.bytes();
                            break;
                        }
                    case 4: {
                            message.bodySizeClass = reader.uint32();
                            break;
                        }
                    case 5: {
                            message.bodyContentAddress = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a MailboxBody message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.MailboxBody
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.MailboxBody} MailboxBody
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MailboxBody.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MailboxBody message.
             * @function verify
             * @memberof dmcn.relay.MailboxBody
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MailboxBody.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.encryptedBody != null && Object.hasOwnProperty.call(message, "encryptedBody"))
                    if (!(message.encryptedBody && typeof message.encryptedBody.length === "number" || $util.isString(message.encryptedBody)))
                        return "encryptedBody: buffer expected";
                if (message.bodyNonce != null && Object.hasOwnProperty.call(message, "bodyNonce"))
                    if (!(message.bodyNonce && typeof message.bodyNonce.length === "number" || $util.isString(message.bodyNonce)))
                        return "bodyNonce: buffer expected";
                if (message.bodyTag != null && Object.hasOwnProperty.call(message, "bodyTag"))
                    if (!(message.bodyTag && typeof message.bodyTag.length === "number" || $util.isString(message.bodyTag)))
                        return "bodyTag: buffer expected";
                if (message.bodySizeClass != null && Object.hasOwnProperty.call(message, "bodySizeClass"))
                    if (!$util.isInteger(message.bodySizeClass))
                        return "bodySizeClass: integer expected";
                if (message.bodyContentAddress != null && Object.hasOwnProperty.call(message, "bodyContentAddress"))
                    if (!(message.bodyContentAddress && typeof message.bodyContentAddress.length === "number" || $util.isString(message.bodyContentAddress)))
                        return "bodyContentAddress: buffer expected";
                return null;
            };

            /**
             * Creates a MailboxBody message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.MailboxBody
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.MailboxBody} MailboxBody
             */
            MailboxBody.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.MailboxBody)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.MailboxBody: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.MailboxBody();
                if (object.encryptedBody != null)
                    if (typeof object.encryptedBody === "string")
                        $util.base64.decode(object.encryptedBody, message.encryptedBody = $util.newBuffer($util.base64.length(object.encryptedBody)), 0);
                    else if (object.encryptedBody.length >= 0)
                        message.encryptedBody = object.encryptedBody;
                if (object.bodyNonce != null)
                    if (typeof object.bodyNonce === "string")
                        $util.base64.decode(object.bodyNonce, message.bodyNonce = $util.newBuffer($util.base64.length(object.bodyNonce)), 0);
                    else if (object.bodyNonce.length >= 0)
                        message.bodyNonce = object.bodyNonce;
                if (object.bodyTag != null)
                    if (typeof object.bodyTag === "string")
                        $util.base64.decode(object.bodyTag, message.bodyTag = $util.newBuffer($util.base64.length(object.bodyTag)), 0);
                    else if (object.bodyTag.length >= 0)
                        message.bodyTag = object.bodyTag;
                if (object.bodySizeClass != null)
                    message.bodySizeClass = object.bodySizeClass >>> 0;
                if (object.bodyContentAddress != null)
                    if (typeof object.bodyContentAddress === "string")
                        $util.base64.decode(object.bodyContentAddress, message.bodyContentAddress = $util.newBuffer($util.base64.length(object.bodyContentAddress)), 0);
                    else if (object.bodyContentAddress.length >= 0)
                        message.bodyContentAddress = object.bodyContentAddress;
                return message;
            };

            /**
             * Creates a plain object from a MailboxBody message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.MailboxBody
             * @static
             * @param {dmcn.relay.MailboxBody} message MailboxBody
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MailboxBody.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    if (options.bytes === String)
                        object.encryptedBody = "";
                    else {
                        object.encryptedBody = [];
                        if (options.bytes !== Array)
                            object.encryptedBody = $util.newBuffer(object.encryptedBody);
                    }
                    if (options.bytes === String)
                        object.bodyNonce = "";
                    else {
                        object.bodyNonce = [];
                        if (options.bytes !== Array)
                            object.bodyNonce = $util.newBuffer(object.bodyNonce);
                    }
                    if (options.bytes === String)
                        object.bodyTag = "";
                    else {
                        object.bodyTag = [];
                        if (options.bytes !== Array)
                            object.bodyTag = $util.newBuffer(object.bodyTag);
                    }
                    object.bodySizeClass = 0;
                    if (options.bytes === String)
                        object.bodyContentAddress = "";
                    else {
                        object.bodyContentAddress = [];
                        if (options.bytes !== Array)
                            object.bodyContentAddress = $util.newBuffer(object.bodyContentAddress);
                    }
                }
                if (message.encryptedBody != null && Object.hasOwnProperty.call(message, "encryptedBody"))
                    object.encryptedBody = options.bytes === String ? $util.base64.encode(message.encryptedBody, 0, message.encryptedBody.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedBody) : message.encryptedBody;
                if (message.bodyNonce != null && Object.hasOwnProperty.call(message, "bodyNonce"))
                    object.bodyNonce = options.bytes === String ? $util.base64.encode(message.bodyNonce, 0, message.bodyNonce.length) : options.bytes === Array ? Array.prototype.slice.call(message.bodyNonce) : message.bodyNonce;
                if (message.bodyTag != null && Object.hasOwnProperty.call(message, "bodyTag"))
                    object.bodyTag = options.bytes === String ? $util.base64.encode(message.bodyTag, 0, message.bodyTag.length) : options.bytes === Array ? Array.prototype.slice.call(message.bodyTag) : message.bodyTag;
                if (message.bodySizeClass != null && Object.hasOwnProperty.call(message, "bodySizeClass"))
                    object.bodySizeClass = message.bodySizeClass;
                if (message.bodyContentAddress != null && Object.hasOwnProperty.call(message, "bodyContentAddress"))
                    object.bodyContentAddress = options.bytes === String ? $util.base64.encode(message.bodyContentAddress, 0, message.bodyContentAddress.length) : options.bytes === Array ? Array.prototype.slice.call(message.bodyContentAddress) : message.bodyContentAddress;
                return object;
            };

            /**
             * Converts this MailboxBody to JSON.
             * @function toJSON
             * @memberof dmcn.relay.MailboxBody
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MailboxBody.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MailboxBody
             * @function getTypeUrl
             * @memberof dmcn.relay.MailboxBody
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MailboxBody.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.MailboxBody";
            };

            return MailboxBody;
        })();

        relay.MailboxOp = (function() {

            /**
             * Properties of a MailboxOp.
             * @memberof dmcn.relay
             * @interface IMailboxOp
             * @property {Uint8Array|null} [nonce] MailboxOp nonce
             * @property {Uint8Array|null} [signature] MailboxOp signature
             * @property {dmcn.relay.IMailboxListOp|null} [list] MailboxOp list
             * @property {dmcn.relay.IMailboxBodyOp|null} [body] MailboxOp body
             * @property {dmcn.relay.IMailboxDeleteOp|null} ["delete"] MailboxOp delete
             */

            /**
             * Constructs a new MailboxOp.
             * @memberof dmcn.relay
             * @classdesc Represents a MailboxOp.
             * @implements IMailboxOp
             * @constructor
             * @param {dmcn.relay.IMailboxOp=} [properties] Properties to set
             */
            function MailboxOp(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MailboxOp nonce.
             * @member {Uint8Array} nonce
             * @memberof dmcn.relay.MailboxOp
             * @instance
             */
            MailboxOp.prototype.nonce = $util.newBuffer([]);

            /**
             * MailboxOp signature.
             * @member {Uint8Array} signature
             * @memberof dmcn.relay.MailboxOp
             * @instance
             */
            MailboxOp.prototype.signature = $util.newBuffer([]);

            /**
             * MailboxOp list.
             * @member {dmcn.relay.IMailboxListOp|null|undefined} list
             * @memberof dmcn.relay.MailboxOp
             * @instance
             */
            MailboxOp.prototype.list = null;

            /**
             * MailboxOp body.
             * @member {dmcn.relay.IMailboxBodyOp|null|undefined} body
             * @memberof dmcn.relay.MailboxOp
             * @instance
             */
            MailboxOp.prototype.body = null;

            /**
             * MailboxOp delete.
             * @member {dmcn.relay.IMailboxDeleteOp|null|undefined} delete
             * @memberof dmcn.relay.MailboxOp
             * @instance
             */
            MailboxOp.prototype["delete"] = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * MailboxOp op.
             * @member {"list"|"body"|"delete"|undefined} op
             * @memberof dmcn.relay.MailboxOp
             * @instance
             */
            Object.defineProperty(MailboxOp.prototype, "op", {
                get: $util.oneOfGetter($oneOfFields = ["list", "body", "delete"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new MailboxOp instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.MailboxOp
             * @static
             * @param {dmcn.relay.IMailboxOp=} [properties] Properties to set
             * @returns {dmcn.relay.MailboxOp} MailboxOp instance
             */
            MailboxOp.create = function create(properties) {
                return new MailboxOp(properties);
            };

            /**
             * Encodes the specified MailboxOp message. Does not implicitly {@link dmcn.relay.MailboxOp.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.MailboxOp
             * @static
             * @param {dmcn.relay.IMailboxOp} message MailboxOp message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MailboxOp.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.nonce);
                if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.signature);
                if (message.list != null && Object.hasOwnProperty.call(message, "list"))
                    $root.dmcn.relay.MailboxListOp.encode(message.list, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.body != null && Object.hasOwnProperty.call(message, "body"))
                    $root.dmcn.relay.MailboxBodyOp.encode(message.body, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message["delete"] != null && Object.hasOwnProperty.call(message, "delete"))
                    $root.dmcn.relay.MailboxDeleteOp.encode(message["delete"], writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified MailboxOp message, length delimited. Does not implicitly {@link dmcn.relay.MailboxOp.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.MailboxOp
             * @static
             * @param {dmcn.relay.IMailboxOp} message MailboxOp message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MailboxOp.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a MailboxOp message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.MailboxOp
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.MailboxOp} MailboxOp
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MailboxOp.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.MailboxOp();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.nonce = reader.bytes();
                            break;
                        }
                    case 2: {
                            message.signature = reader.bytes();
                            break;
                        }
                    case 3: {
                            message.list = $root.dmcn.relay.MailboxListOp.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 4: {
                            message.body = $root.dmcn.relay.MailboxBodyOp.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 5: {
                            message["delete"] = $root.dmcn.relay.MailboxDeleteOp.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a MailboxOp message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.MailboxOp
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.MailboxOp} MailboxOp
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MailboxOp.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MailboxOp message.
             * @function verify
             * @memberof dmcn.relay.MailboxOp
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MailboxOp.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                let properties = {};
                if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                    if (!(message.nonce && typeof message.nonce.length === "number" || $util.isString(message.nonce)))
                        return "nonce: buffer expected";
                if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                    if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                        return "signature: buffer expected";
                if (message.list != null && Object.hasOwnProperty.call(message, "list")) {
                    properties.op = 1;
                    {
                        let error = $root.dmcn.relay.MailboxListOp.verify(message.list, long + 1);
                        if (error)
                            return "list." + error;
                    }
                }
                if (message.body != null && Object.hasOwnProperty.call(message, "body")) {
                    if (properties.op === 1)
                        return "op: multiple values";
                    properties.op = 1;
                    {
                        let error = $root.dmcn.relay.MailboxBodyOp.verify(message.body, long + 1);
                        if (error)
                            return "body." + error;
                    }
                }
                if (message["delete"] != null && Object.hasOwnProperty.call(message, "delete")) {
                    if (properties.op === 1)
                        return "op: multiple values";
                    properties.op = 1;
                    {
                        let error = $root.dmcn.relay.MailboxDeleteOp.verify(message["delete"], long + 1);
                        if (error)
                            return "delete." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a MailboxOp message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.MailboxOp
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.MailboxOp} MailboxOp
             */
            MailboxOp.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.MailboxOp)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.MailboxOp: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.MailboxOp();
                if (object.nonce != null)
                    if (typeof object.nonce === "string")
                        $util.base64.decode(object.nonce, message.nonce = $util.newBuffer($util.base64.length(object.nonce)), 0);
                    else if (object.nonce.length >= 0)
                        message.nonce = object.nonce;
                if (object.signature != null)
                    if (typeof object.signature === "string")
                        $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                    else if (object.signature.length >= 0)
                        message.signature = object.signature;
                if (object.list != null) {
                    if (!$util.isObject(object.list))
                        throw TypeError(".dmcn.relay.MailboxOp.list: object expected");
                    message.list = $root.dmcn.relay.MailboxListOp.fromObject(object.list, long + 1);
                }
                if (object.body != null) {
                    if (!$util.isObject(object.body))
                        throw TypeError(".dmcn.relay.MailboxOp.body: object expected");
                    message.body = $root.dmcn.relay.MailboxBodyOp.fromObject(object.body, long + 1);
                }
                if (object["delete"] != null) {
                    if (!$util.isObject(object["delete"]))
                        throw TypeError(".dmcn.relay.MailboxOp.delete: object expected");
                    message["delete"] = $root.dmcn.relay.MailboxDeleteOp.fromObject(object["delete"], long + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a MailboxOp message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.MailboxOp
             * @static
             * @param {dmcn.relay.MailboxOp} message MailboxOp
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MailboxOp.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    if (options.bytes === String)
                        object.nonce = "";
                    else {
                        object.nonce = [];
                        if (options.bytes !== Array)
                            object.nonce = $util.newBuffer(object.nonce);
                    }
                    if (options.bytes === String)
                        object.signature = "";
                    else {
                        object.signature = [];
                        if (options.bytes !== Array)
                            object.signature = $util.newBuffer(object.signature);
                    }
                }
                if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                    object.nonce = options.bytes === String ? $util.base64.encode(message.nonce, 0, message.nonce.length) : options.bytes === Array ? Array.prototype.slice.call(message.nonce) : message.nonce;
                if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                    object.signature = options.bytes === String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === Array ? Array.prototype.slice.call(message.signature) : message.signature;
                if (message.list != null && Object.hasOwnProperty.call(message, "list")) {
                    object.list = $root.dmcn.relay.MailboxListOp.toObject(message.list, options, q + 1);
                    if (options.oneofs)
                        object.op = "list";
                }
                if (message.body != null && Object.hasOwnProperty.call(message, "body")) {
                    object.body = $root.dmcn.relay.MailboxBodyOp.toObject(message.body, options, q + 1);
                    if (options.oneofs)
                        object.op = "body";
                }
                if (message["delete"] != null && Object.hasOwnProperty.call(message, "delete")) {
                    object["delete"] = $root.dmcn.relay.MailboxDeleteOp.toObject(message["delete"], options, q + 1);
                    if (options.oneofs)
                        object.op = "delete";
                }
                return object;
            };

            /**
             * Converts this MailboxOp to JSON.
             * @function toJSON
             * @memberof dmcn.relay.MailboxOp
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MailboxOp.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MailboxOp
             * @function getTypeUrl
             * @memberof dmcn.relay.MailboxOp
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MailboxOp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.MailboxOp";
            };

            return MailboxOp;
        })();

        relay.MailboxListOp = (function() {

            /**
             * Properties of a MailboxListOp.
             * @memberof dmcn.relay
             * @interface IMailboxListOp
             * @property {number|null} [limit] MailboxListOp limit
             * @property {Uint8Array|null} [cursor] MailboxListOp cursor
             */

            /**
             * Constructs a new MailboxListOp.
             * @memberof dmcn.relay
             * @classdesc Represents a MailboxListOp.
             * @implements IMailboxListOp
             * @constructor
             * @param {dmcn.relay.IMailboxListOp=} [properties] Properties to set
             */
            function MailboxListOp(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MailboxListOp limit.
             * @member {number} limit
             * @memberof dmcn.relay.MailboxListOp
             * @instance
             */
            MailboxListOp.prototype.limit = 0;

            /**
             * MailboxListOp cursor.
             * @member {Uint8Array} cursor
             * @memberof dmcn.relay.MailboxListOp
             * @instance
             */
            MailboxListOp.prototype.cursor = $util.newBuffer([]);

            /**
             * Creates a new MailboxListOp instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.MailboxListOp
             * @static
             * @param {dmcn.relay.IMailboxListOp=} [properties] Properties to set
             * @returns {dmcn.relay.MailboxListOp} MailboxListOp instance
             */
            MailboxListOp.create = function create(properties) {
                return new MailboxListOp(properties);
            };

            /**
             * Encodes the specified MailboxListOp message. Does not implicitly {@link dmcn.relay.MailboxListOp.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.MailboxListOp
             * @static
             * @param {dmcn.relay.IMailboxListOp} message MailboxListOp message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MailboxListOp.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.limit != null && Object.hasOwnProperty.call(message, "limit"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.limit);
                if (message.cursor != null && Object.hasOwnProperty.call(message, "cursor"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.cursor);
                return writer;
            };

            /**
             * Encodes the specified MailboxListOp message, length delimited. Does not implicitly {@link dmcn.relay.MailboxListOp.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.MailboxListOp
             * @static
             * @param {dmcn.relay.IMailboxListOp} message MailboxListOp message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MailboxListOp.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a MailboxListOp message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.MailboxListOp
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.MailboxListOp} MailboxListOp
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MailboxListOp.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.MailboxListOp();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.limit = reader.uint32();
                            break;
                        }
                    case 2: {
                            message.cursor = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a MailboxListOp message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.MailboxListOp
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.MailboxListOp} MailboxListOp
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MailboxListOp.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MailboxListOp message.
             * @function verify
             * @memberof dmcn.relay.MailboxListOp
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MailboxListOp.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.limit != null && Object.hasOwnProperty.call(message, "limit"))
                    if (!$util.isInteger(message.limit))
                        return "limit: integer expected";
                if (message.cursor != null && Object.hasOwnProperty.call(message, "cursor"))
                    if (!(message.cursor && typeof message.cursor.length === "number" || $util.isString(message.cursor)))
                        return "cursor: buffer expected";
                return null;
            };

            /**
             * Creates a MailboxListOp message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.MailboxListOp
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.MailboxListOp} MailboxListOp
             */
            MailboxListOp.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.MailboxListOp)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.MailboxListOp: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.MailboxListOp();
                if (object.limit != null)
                    message.limit = object.limit >>> 0;
                if (object.cursor != null)
                    if (typeof object.cursor === "string")
                        $util.base64.decode(object.cursor, message.cursor = $util.newBuffer($util.base64.length(object.cursor)), 0);
                    else if (object.cursor.length >= 0)
                        message.cursor = object.cursor;
                return message;
            };

            /**
             * Creates a plain object from a MailboxListOp message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.MailboxListOp
             * @static
             * @param {dmcn.relay.MailboxListOp} message MailboxListOp
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MailboxListOp.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.limit = 0;
                    if (options.bytes === String)
                        object.cursor = "";
                    else {
                        object.cursor = [];
                        if (options.bytes !== Array)
                            object.cursor = $util.newBuffer(object.cursor);
                    }
                }
                if (message.limit != null && Object.hasOwnProperty.call(message, "limit"))
                    object.limit = message.limit;
                if (message.cursor != null && Object.hasOwnProperty.call(message, "cursor"))
                    object.cursor = options.bytes === String ? $util.base64.encode(message.cursor, 0, message.cursor.length) : options.bytes === Array ? Array.prototype.slice.call(message.cursor) : message.cursor;
                return object;
            };

            /**
             * Converts this MailboxListOp to JSON.
             * @function toJSON
             * @memberof dmcn.relay.MailboxListOp
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MailboxListOp.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MailboxListOp
             * @function getTypeUrl
             * @memberof dmcn.relay.MailboxListOp
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MailboxListOp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.MailboxListOp";
            };

            return MailboxListOp;
        })();

        relay.MailboxBodyOp = (function() {

            /**
             * Properties of a MailboxBodyOp.
             * @memberof dmcn.relay
             * @interface IMailboxBodyOp
             * @property {Uint8Array|null} [hash] MailboxBodyOp hash
             */

            /**
             * Constructs a new MailboxBodyOp.
             * @memberof dmcn.relay
             * @classdesc Represents a MailboxBodyOp.
             * @implements IMailboxBodyOp
             * @constructor
             * @param {dmcn.relay.IMailboxBodyOp=} [properties] Properties to set
             */
            function MailboxBodyOp(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MailboxBodyOp hash.
             * @member {Uint8Array} hash
             * @memberof dmcn.relay.MailboxBodyOp
             * @instance
             */
            MailboxBodyOp.prototype.hash = $util.newBuffer([]);

            /**
             * Creates a new MailboxBodyOp instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.MailboxBodyOp
             * @static
             * @param {dmcn.relay.IMailboxBodyOp=} [properties] Properties to set
             * @returns {dmcn.relay.MailboxBodyOp} MailboxBodyOp instance
             */
            MailboxBodyOp.create = function create(properties) {
                return new MailboxBodyOp(properties);
            };

            /**
             * Encodes the specified MailboxBodyOp message. Does not implicitly {@link dmcn.relay.MailboxBodyOp.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.MailboxBodyOp
             * @static
             * @param {dmcn.relay.IMailboxBodyOp} message MailboxBodyOp message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MailboxBodyOp.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.hash != null && Object.hasOwnProperty.call(message, "hash"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.hash);
                return writer;
            };

            /**
             * Encodes the specified MailboxBodyOp message, length delimited. Does not implicitly {@link dmcn.relay.MailboxBodyOp.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.MailboxBodyOp
             * @static
             * @param {dmcn.relay.IMailboxBodyOp} message MailboxBodyOp message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MailboxBodyOp.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a MailboxBodyOp message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.MailboxBodyOp
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.MailboxBodyOp} MailboxBodyOp
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MailboxBodyOp.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.MailboxBodyOp();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.hash = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a MailboxBodyOp message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.MailboxBodyOp
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.MailboxBodyOp} MailboxBodyOp
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MailboxBodyOp.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MailboxBodyOp message.
             * @function verify
             * @memberof dmcn.relay.MailboxBodyOp
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MailboxBodyOp.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.hash != null && Object.hasOwnProperty.call(message, "hash"))
                    if (!(message.hash && typeof message.hash.length === "number" || $util.isString(message.hash)))
                        return "hash: buffer expected";
                return null;
            };

            /**
             * Creates a MailboxBodyOp message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.MailboxBodyOp
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.MailboxBodyOp} MailboxBodyOp
             */
            MailboxBodyOp.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.MailboxBodyOp)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.MailboxBodyOp: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.MailboxBodyOp();
                if (object.hash != null)
                    if (typeof object.hash === "string")
                        $util.base64.decode(object.hash, message.hash = $util.newBuffer($util.base64.length(object.hash)), 0);
                    else if (object.hash.length >= 0)
                        message.hash = object.hash;
                return message;
            };

            /**
             * Creates a plain object from a MailboxBodyOp message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.MailboxBodyOp
             * @static
             * @param {dmcn.relay.MailboxBodyOp} message MailboxBodyOp
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MailboxBodyOp.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    if (options.bytes === String)
                        object.hash = "";
                    else {
                        object.hash = [];
                        if (options.bytes !== Array)
                            object.hash = $util.newBuffer(object.hash);
                    }
                if (message.hash != null && Object.hasOwnProperty.call(message, "hash"))
                    object.hash = options.bytes === String ? $util.base64.encode(message.hash, 0, message.hash.length) : options.bytes === Array ? Array.prototype.slice.call(message.hash) : message.hash;
                return object;
            };

            /**
             * Converts this MailboxBodyOp to JSON.
             * @function toJSON
             * @memberof dmcn.relay.MailboxBodyOp
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MailboxBodyOp.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MailboxBodyOp
             * @function getTypeUrl
             * @memberof dmcn.relay.MailboxBodyOp
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MailboxBodyOp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.MailboxBodyOp";
            };

            return MailboxBodyOp;
        })();

        relay.MailboxDeleteOp = (function() {

            /**
             * Properties of a MailboxDeleteOp.
             * @memberof dmcn.relay
             * @interface IMailboxDeleteOp
             * @property {Uint8Array|null} [hash] MailboxDeleteOp hash
             */

            /**
             * Constructs a new MailboxDeleteOp.
             * @memberof dmcn.relay
             * @classdesc Represents a MailboxDeleteOp.
             * @implements IMailboxDeleteOp
             * @constructor
             * @param {dmcn.relay.IMailboxDeleteOp=} [properties] Properties to set
             */
            function MailboxDeleteOp(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MailboxDeleteOp hash.
             * @member {Uint8Array} hash
             * @memberof dmcn.relay.MailboxDeleteOp
             * @instance
             */
            MailboxDeleteOp.prototype.hash = $util.newBuffer([]);

            /**
             * Creates a new MailboxDeleteOp instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.MailboxDeleteOp
             * @static
             * @param {dmcn.relay.IMailboxDeleteOp=} [properties] Properties to set
             * @returns {dmcn.relay.MailboxDeleteOp} MailboxDeleteOp instance
             */
            MailboxDeleteOp.create = function create(properties) {
                return new MailboxDeleteOp(properties);
            };

            /**
             * Encodes the specified MailboxDeleteOp message. Does not implicitly {@link dmcn.relay.MailboxDeleteOp.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.MailboxDeleteOp
             * @static
             * @param {dmcn.relay.IMailboxDeleteOp} message MailboxDeleteOp message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MailboxDeleteOp.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.hash != null && Object.hasOwnProperty.call(message, "hash"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.hash);
                return writer;
            };

            /**
             * Encodes the specified MailboxDeleteOp message, length delimited. Does not implicitly {@link dmcn.relay.MailboxDeleteOp.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.MailboxDeleteOp
             * @static
             * @param {dmcn.relay.IMailboxDeleteOp} message MailboxDeleteOp message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MailboxDeleteOp.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a MailboxDeleteOp message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.MailboxDeleteOp
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.MailboxDeleteOp} MailboxDeleteOp
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MailboxDeleteOp.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.MailboxDeleteOp();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.hash = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a MailboxDeleteOp message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.MailboxDeleteOp
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.MailboxDeleteOp} MailboxDeleteOp
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MailboxDeleteOp.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MailboxDeleteOp message.
             * @function verify
             * @memberof dmcn.relay.MailboxDeleteOp
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MailboxDeleteOp.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.hash != null && Object.hasOwnProperty.call(message, "hash"))
                    if (!(message.hash && typeof message.hash.length === "number" || $util.isString(message.hash)))
                        return "hash: buffer expected";
                return null;
            };

            /**
             * Creates a MailboxDeleteOp message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.MailboxDeleteOp
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.MailboxDeleteOp} MailboxDeleteOp
             */
            MailboxDeleteOp.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.MailboxDeleteOp)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.MailboxDeleteOp: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.MailboxDeleteOp();
                if (object.hash != null)
                    if (typeof object.hash === "string")
                        $util.base64.decode(object.hash, message.hash = $util.newBuffer($util.base64.length(object.hash)), 0);
                    else if (object.hash.length >= 0)
                        message.hash = object.hash;
                return message;
            };

            /**
             * Creates a plain object from a MailboxDeleteOp message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.MailboxDeleteOp
             * @static
             * @param {dmcn.relay.MailboxDeleteOp} message MailboxDeleteOp
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MailboxDeleteOp.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    if (options.bytes === String)
                        object.hash = "";
                    else {
                        object.hash = [];
                        if (options.bytes !== Array)
                            object.hash = $util.newBuffer(object.hash);
                    }
                if (message.hash != null && Object.hasOwnProperty.call(message, "hash"))
                    object.hash = options.bytes === String ? $util.base64.encode(message.hash, 0, message.hash.length) : options.bytes === Array ? Array.prototype.slice.call(message.hash) : message.hash;
                return object;
            };

            /**
             * Converts this MailboxDeleteOp to JSON.
             * @function toJSON
             * @memberof dmcn.relay.MailboxDeleteOp
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MailboxDeleteOp.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MailboxDeleteOp
             * @function getTypeUrl
             * @memberof dmcn.relay.MailboxDeleteOp
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MailboxDeleteOp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.MailboxDeleteOp";
            };

            return MailboxDeleteOp;
        })();

        relay.MailboxListResponse = (function() {

            /**
             * Properties of a MailboxListResponse.
             * @memberof dmcn.relay
             * @interface IMailboxListResponse
             * @property {Array.<dmcn.relay.IMailboxEntry>|null} [entries] MailboxListResponse entries
             * @property {Uint8Array|null} [nextCursor] MailboxListResponse nextCursor
             */

            /**
             * Constructs a new MailboxListResponse.
             * @memberof dmcn.relay
             * @classdesc Represents a MailboxListResponse.
             * @implements IMailboxListResponse
             * @constructor
             * @param {dmcn.relay.IMailboxListResponse=} [properties] Properties to set
             */
            function MailboxListResponse(properties) {
                this.entries = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MailboxListResponse entries.
             * @member {Array.<dmcn.relay.IMailboxEntry>} entries
             * @memberof dmcn.relay.MailboxListResponse
             * @instance
             */
            MailboxListResponse.prototype.entries = $util.emptyArray;

            /**
             * MailboxListResponse nextCursor.
             * @member {Uint8Array} nextCursor
             * @memberof dmcn.relay.MailboxListResponse
             * @instance
             */
            MailboxListResponse.prototype.nextCursor = $util.newBuffer([]);

            /**
             * Creates a new MailboxListResponse instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.MailboxListResponse
             * @static
             * @param {dmcn.relay.IMailboxListResponse=} [properties] Properties to set
             * @returns {dmcn.relay.MailboxListResponse} MailboxListResponse instance
             */
            MailboxListResponse.create = function create(properties) {
                return new MailboxListResponse(properties);
            };

            /**
             * Encodes the specified MailboxListResponse message. Does not implicitly {@link dmcn.relay.MailboxListResponse.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.MailboxListResponse
             * @static
             * @param {dmcn.relay.IMailboxListResponse} message MailboxListResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MailboxListResponse.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.entries != null && message.entries.length)
                    for (let i = 0; i < message.entries.length; ++i)
                        $root.dmcn.relay.MailboxEntry.encode(message.entries[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.nextCursor != null && Object.hasOwnProperty.call(message, "nextCursor"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.nextCursor);
                return writer;
            };

            /**
             * Encodes the specified MailboxListResponse message, length delimited. Does not implicitly {@link dmcn.relay.MailboxListResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.MailboxListResponse
             * @static
             * @param {dmcn.relay.IMailboxListResponse} message MailboxListResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MailboxListResponse.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a MailboxListResponse message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.MailboxListResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.MailboxListResponse} MailboxListResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MailboxListResponse.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.MailboxListResponse();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            if (!(message.entries && message.entries.length))
                                message.entries = [];
                            message.entries.push($root.dmcn.relay.MailboxEntry.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 2: {
                            message.nextCursor = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a MailboxListResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.MailboxListResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.MailboxListResponse} MailboxListResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MailboxListResponse.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MailboxListResponse message.
             * @function verify
             * @memberof dmcn.relay.MailboxListResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MailboxListResponse.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.entries != null && Object.hasOwnProperty.call(message, "entries")) {
                    if (!Array.isArray(message.entries))
                        return "entries: array expected";
                    for (let i = 0; i < message.entries.length; ++i) {
                        let error = $root.dmcn.relay.MailboxEntry.verify(message.entries[i], long + 1);
                        if (error)
                            return "entries." + error;
                    }
                }
                if (message.nextCursor != null && Object.hasOwnProperty.call(message, "nextCursor"))
                    if (!(message.nextCursor && typeof message.nextCursor.length === "number" || $util.isString(message.nextCursor)))
                        return "nextCursor: buffer expected";
                return null;
            };

            /**
             * Creates a MailboxListResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.MailboxListResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.MailboxListResponse} MailboxListResponse
             */
            MailboxListResponse.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.MailboxListResponse)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.MailboxListResponse: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.MailboxListResponse();
                if (object.entries) {
                    if (!Array.isArray(object.entries))
                        throw TypeError(".dmcn.relay.MailboxListResponse.entries: array expected");
                    message.entries = [];
                    for (let i = 0; i < object.entries.length; ++i) {
                        if (!$util.isObject(object.entries[i]))
                            throw TypeError(".dmcn.relay.MailboxListResponse.entries: object expected");
                        message.entries[i] = $root.dmcn.relay.MailboxEntry.fromObject(object.entries[i], long + 1);
                    }
                }
                if (object.nextCursor != null)
                    if (typeof object.nextCursor === "string")
                        $util.base64.decode(object.nextCursor, message.nextCursor = $util.newBuffer($util.base64.length(object.nextCursor)), 0);
                    else if (object.nextCursor.length >= 0)
                        message.nextCursor = object.nextCursor;
                return message;
            };

            /**
             * Creates a plain object from a MailboxListResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.MailboxListResponse
             * @static
             * @param {dmcn.relay.MailboxListResponse} message MailboxListResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MailboxListResponse.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.entries = [];
                if (options.defaults)
                    if (options.bytes === String)
                        object.nextCursor = "";
                    else {
                        object.nextCursor = [];
                        if (options.bytes !== Array)
                            object.nextCursor = $util.newBuffer(object.nextCursor);
                    }
                if (message.entries && message.entries.length) {
                    object.entries = [];
                    for (let j = 0; j < message.entries.length; ++j)
                        object.entries[j] = $root.dmcn.relay.MailboxEntry.toObject(message.entries[j], options, q + 1);
                }
                if (message.nextCursor != null && Object.hasOwnProperty.call(message, "nextCursor"))
                    object.nextCursor = options.bytes === String ? $util.base64.encode(message.nextCursor, 0, message.nextCursor.length) : options.bytes === Array ? Array.prototype.slice.call(message.nextCursor) : message.nextCursor;
                return object;
            };

            /**
             * Converts this MailboxListResponse to JSON.
             * @function toJSON
             * @memberof dmcn.relay.MailboxListResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MailboxListResponse.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MailboxListResponse
             * @function getTypeUrl
             * @memberof dmcn.relay.MailboxListResponse
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MailboxListResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.MailboxListResponse";
            };

            return MailboxListResponse;
        })();

        relay.MailboxBodyHeader = (function() {

            /**
             * Properties of a MailboxBodyHeader.
             * @memberof dmcn.relay
             * @interface IMailboxBodyHeader
             * @property {Uint8Array|null} [bodyNonce] MailboxBodyHeader bodyNonce
             * @property {Uint8Array|null} [bodyTag] MailboxBodyHeader bodyTag
             * @property {number|null} [bodySizeClass] MailboxBodyHeader bodySizeClass
             * @property {number|Long|null} [bodyTotalSize] MailboxBodyHeader bodyTotalSize
             */

            /**
             * Constructs a new MailboxBodyHeader.
             * @memberof dmcn.relay
             * @classdesc Represents a MailboxBodyHeader.
             * @implements IMailboxBodyHeader
             * @constructor
             * @param {dmcn.relay.IMailboxBodyHeader=} [properties] Properties to set
             */
            function MailboxBodyHeader(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MailboxBodyHeader bodyNonce.
             * @member {Uint8Array} bodyNonce
             * @memberof dmcn.relay.MailboxBodyHeader
             * @instance
             */
            MailboxBodyHeader.prototype.bodyNonce = $util.newBuffer([]);

            /**
             * MailboxBodyHeader bodyTag.
             * @member {Uint8Array} bodyTag
             * @memberof dmcn.relay.MailboxBodyHeader
             * @instance
             */
            MailboxBodyHeader.prototype.bodyTag = $util.newBuffer([]);

            /**
             * MailboxBodyHeader bodySizeClass.
             * @member {number} bodySizeClass
             * @memberof dmcn.relay.MailboxBodyHeader
             * @instance
             */
            MailboxBodyHeader.prototype.bodySizeClass = 0;

            /**
             * MailboxBodyHeader bodyTotalSize.
             * @member {number|Long} bodyTotalSize
             * @memberof dmcn.relay.MailboxBodyHeader
             * @instance
             */
            MailboxBodyHeader.prototype.bodyTotalSize = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * Creates a new MailboxBodyHeader instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.MailboxBodyHeader
             * @static
             * @param {dmcn.relay.IMailboxBodyHeader=} [properties] Properties to set
             * @returns {dmcn.relay.MailboxBodyHeader} MailboxBodyHeader instance
             */
            MailboxBodyHeader.create = function create(properties) {
                return new MailboxBodyHeader(properties);
            };

            /**
             * Encodes the specified MailboxBodyHeader message. Does not implicitly {@link dmcn.relay.MailboxBodyHeader.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.MailboxBodyHeader
             * @static
             * @param {dmcn.relay.IMailboxBodyHeader} message MailboxBodyHeader message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MailboxBodyHeader.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.bodyNonce != null && Object.hasOwnProperty.call(message, "bodyNonce"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.bodyNonce);
                if (message.bodyTag != null && Object.hasOwnProperty.call(message, "bodyTag"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.bodyTag);
                if (message.bodySizeClass != null && Object.hasOwnProperty.call(message, "bodySizeClass"))
                    writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.bodySizeClass);
                if (message.bodyTotalSize != null && Object.hasOwnProperty.call(message, "bodyTotalSize"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.bodyTotalSize);
                return writer;
            };

            /**
             * Encodes the specified MailboxBodyHeader message, length delimited. Does not implicitly {@link dmcn.relay.MailboxBodyHeader.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.MailboxBodyHeader
             * @static
             * @param {dmcn.relay.IMailboxBodyHeader} message MailboxBodyHeader message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MailboxBodyHeader.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a MailboxBodyHeader message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.MailboxBodyHeader
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.MailboxBodyHeader} MailboxBodyHeader
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MailboxBodyHeader.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.MailboxBodyHeader();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.bodyNonce = reader.bytes();
                            break;
                        }
                    case 2: {
                            message.bodyTag = reader.bytes();
                            break;
                        }
                    case 3: {
                            message.bodySizeClass = reader.uint32();
                            break;
                        }
                    case 4: {
                            message.bodyTotalSize = reader.uint64();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a MailboxBodyHeader message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.MailboxBodyHeader
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.MailboxBodyHeader} MailboxBodyHeader
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MailboxBodyHeader.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MailboxBodyHeader message.
             * @function verify
             * @memberof dmcn.relay.MailboxBodyHeader
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MailboxBodyHeader.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.bodyNonce != null && Object.hasOwnProperty.call(message, "bodyNonce"))
                    if (!(message.bodyNonce && typeof message.bodyNonce.length === "number" || $util.isString(message.bodyNonce)))
                        return "bodyNonce: buffer expected";
                if (message.bodyTag != null && Object.hasOwnProperty.call(message, "bodyTag"))
                    if (!(message.bodyTag && typeof message.bodyTag.length === "number" || $util.isString(message.bodyTag)))
                        return "bodyTag: buffer expected";
                if (message.bodySizeClass != null && Object.hasOwnProperty.call(message, "bodySizeClass"))
                    if (!$util.isInteger(message.bodySizeClass))
                        return "bodySizeClass: integer expected";
                if (message.bodyTotalSize != null && Object.hasOwnProperty.call(message, "bodyTotalSize"))
                    if (!$util.isInteger(message.bodyTotalSize) && !(message.bodyTotalSize && $util.isInteger(message.bodyTotalSize.low) && $util.isInteger(message.bodyTotalSize.high)))
                        return "bodyTotalSize: integer|Long expected";
                return null;
            };

            /**
             * Creates a MailboxBodyHeader message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.MailboxBodyHeader
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.MailboxBodyHeader} MailboxBodyHeader
             */
            MailboxBodyHeader.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.MailboxBodyHeader)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.MailboxBodyHeader: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.MailboxBodyHeader();
                if (object.bodyNonce != null)
                    if (typeof object.bodyNonce === "string")
                        $util.base64.decode(object.bodyNonce, message.bodyNonce = $util.newBuffer($util.base64.length(object.bodyNonce)), 0);
                    else if (object.bodyNonce.length >= 0)
                        message.bodyNonce = object.bodyNonce;
                if (object.bodyTag != null)
                    if (typeof object.bodyTag === "string")
                        $util.base64.decode(object.bodyTag, message.bodyTag = $util.newBuffer($util.base64.length(object.bodyTag)), 0);
                    else if (object.bodyTag.length >= 0)
                        message.bodyTag = object.bodyTag;
                if (object.bodySizeClass != null)
                    message.bodySizeClass = object.bodySizeClass >>> 0;
                if (object.bodyTotalSize != null)
                    if ($util.Long)
                        message.bodyTotalSize = $util.Long.fromValue(object.bodyTotalSize, true);
                    else if (typeof object.bodyTotalSize === "string")
                        message.bodyTotalSize = parseInt(object.bodyTotalSize, 10);
                    else if (typeof object.bodyTotalSize === "number")
                        message.bodyTotalSize = object.bodyTotalSize;
                    else if (typeof object.bodyTotalSize === "object")
                        message.bodyTotalSize = new $util.LongBits(object.bodyTotalSize.low >>> 0, object.bodyTotalSize.high >>> 0).toNumber(true);
                return message;
            };

            /**
             * Creates a plain object from a MailboxBodyHeader message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.MailboxBodyHeader
             * @static
             * @param {dmcn.relay.MailboxBodyHeader} message MailboxBodyHeader
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MailboxBodyHeader.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    if (options.bytes === String)
                        object.bodyNonce = "";
                    else {
                        object.bodyNonce = [];
                        if (options.bytes !== Array)
                            object.bodyNonce = $util.newBuffer(object.bodyNonce);
                    }
                    if (options.bytes === String)
                        object.bodyTag = "";
                    else {
                        object.bodyTag = [];
                        if (options.bytes !== Array)
                            object.bodyTag = $util.newBuffer(object.bodyTag);
                    }
                    object.bodySizeClass = 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.bodyTotalSize = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.bodyTotalSize = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                }
                if (message.bodyNonce != null && Object.hasOwnProperty.call(message, "bodyNonce"))
                    object.bodyNonce = options.bytes === String ? $util.base64.encode(message.bodyNonce, 0, message.bodyNonce.length) : options.bytes === Array ? Array.prototype.slice.call(message.bodyNonce) : message.bodyNonce;
                if (message.bodyTag != null && Object.hasOwnProperty.call(message, "bodyTag"))
                    object.bodyTag = options.bytes === String ? $util.base64.encode(message.bodyTag, 0, message.bodyTag.length) : options.bytes === Array ? Array.prototype.slice.call(message.bodyTag) : message.bodyTag;
                if (message.bodySizeClass != null && Object.hasOwnProperty.call(message, "bodySizeClass"))
                    object.bodySizeClass = message.bodySizeClass;
                if (message.bodyTotalSize != null && Object.hasOwnProperty.call(message, "bodyTotalSize"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.bodyTotalSize = typeof message.bodyTotalSize === "number" ? BigInt(message.bodyTotalSize) : $util.Long.fromBits(message.bodyTotalSize.low >>> 0, message.bodyTotalSize.high >>> 0, true).toBigInt();
                    else if (typeof message.bodyTotalSize === "number")
                        object.bodyTotalSize = options.longs === String ? String(message.bodyTotalSize) : message.bodyTotalSize;
                    else
                        object.bodyTotalSize = options.longs === String ? $util.Long.prototype.toString.call(message.bodyTotalSize) : options.longs === Number ? new $util.LongBits(message.bodyTotalSize.low >>> 0, message.bodyTotalSize.high >>> 0).toNumber(true) : message.bodyTotalSize;
                return object;
            };

            /**
             * Converts this MailboxBodyHeader to JSON.
             * @function toJSON
             * @memberof dmcn.relay.MailboxBodyHeader
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MailboxBodyHeader.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MailboxBodyHeader
             * @function getTypeUrl
             * @memberof dmcn.relay.MailboxBodyHeader
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MailboxBodyHeader.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.MailboxBodyHeader";
            };

            return MailboxBodyHeader;
        })();

        relay.MailboxDeleteResponse = (function() {

            /**
             * Properties of a MailboxDeleteResponse.
             * @memberof dmcn.relay
             * @interface IMailboxDeleteResponse
             * @property {boolean|null} [success] MailboxDeleteResponse success
             */

            /**
             * Constructs a new MailboxDeleteResponse.
             * @memberof dmcn.relay
             * @classdesc Represents a MailboxDeleteResponse.
             * @implements IMailboxDeleteResponse
             * @constructor
             * @param {dmcn.relay.IMailboxDeleteResponse=} [properties] Properties to set
             */
            function MailboxDeleteResponse(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MailboxDeleteResponse success.
             * @member {boolean} success
             * @memberof dmcn.relay.MailboxDeleteResponse
             * @instance
             */
            MailboxDeleteResponse.prototype.success = false;

            /**
             * Creates a new MailboxDeleteResponse instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.MailboxDeleteResponse
             * @static
             * @param {dmcn.relay.IMailboxDeleteResponse=} [properties] Properties to set
             * @returns {dmcn.relay.MailboxDeleteResponse} MailboxDeleteResponse instance
             */
            MailboxDeleteResponse.create = function create(properties) {
                return new MailboxDeleteResponse(properties);
            };

            /**
             * Encodes the specified MailboxDeleteResponse message. Does not implicitly {@link dmcn.relay.MailboxDeleteResponse.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.MailboxDeleteResponse
             * @static
             * @param {dmcn.relay.IMailboxDeleteResponse} message MailboxDeleteResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MailboxDeleteResponse.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.success != null && Object.hasOwnProperty.call(message, "success"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.success);
                return writer;
            };

            /**
             * Encodes the specified MailboxDeleteResponse message, length delimited. Does not implicitly {@link dmcn.relay.MailboxDeleteResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.MailboxDeleteResponse
             * @static
             * @param {dmcn.relay.IMailboxDeleteResponse} message MailboxDeleteResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MailboxDeleteResponse.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a MailboxDeleteResponse message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.MailboxDeleteResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.MailboxDeleteResponse} MailboxDeleteResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MailboxDeleteResponse.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.MailboxDeleteResponse();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.success = reader.bool();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a MailboxDeleteResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.MailboxDeleteResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.MailboxDeleteResponse} MailboxDeleteResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MailboxDeleteResponse.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MailboxDeleteResponse message.
             * @function verify
             * @memberof dmcn.relay.MailboxDeleteResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MailboxDeleteResponse.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.success != null && Object.hasOwnProperty.call(message, "success"))
                    if (typeof message.success !== "boolean")
                        return "success: boolean expected";
                return null;
            };

            /**
             * Creates a MailboxDeleteResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.MailboxDeleteResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.MailboxDeleteResponse} MailboxDeleteResponse
             */
            MailboxDeleteResponse.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.MailboxDeleteResponse)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.MailboxDeleteResponse: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.MailboxDeleteResponse();
                if (object.success != null)
                    message.success = Boolean(object.success);
                return message;
            };

            /**
             * Creates a plain object from a MailboxDeleteResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.MailboxDeleteResponse
             * @static
             * @param {dmcn.relay.MailboxDeleteResponse} message MailboxDeleteResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MailboxDeleteResponse.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    object.success = false;
                if (message.success != null && Object.hasOwnProperty.call(message, "success"))
                    object.success = message.success;
                return object;
            };

            /**
             * Converts this MailboxDeleteResponse to JSON.
             * @function toJSON
             * @memberof dmcn.relay.MailboxDeleteResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MailboxDeleteResponse.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MailboxDeleteResponse
             * @function getTypeUrl
             * @memberof dmcn.relay.MailboxDeleteResponse
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MailboxDeleteResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.MailboxDeleteResponse";
            };

            return MailboxDeleteResponse;
        })();

        relay.StoreInit = (function() {

            /**
             * Properties of a StoreInit.
             * @memberof dmcn.relay
             * @interface IStoreInit
             * @property {string|null} [senderAddress] StoreInit senderAddress
             * @property {Uint8Array|null} [senderSignature] StoreInit senderSignature
             * @property {number|null} [version] StoreInit version
             * @property {Uint8Array|null} [messageId] StoreInit messageId
             * @property {number|Long|null} [createdAt] StoreInit createdAt
             * @property {Array.<dmcn.message.IRecipientRecord>|null} [recipients] StoreInit recipients
             * @property {Uint8Array|null} [encryptedHeader] StoreInit encryptedHeader
             * @property {Uint8Array|null} [headerNonce] StoreInit headerNonce
             * @property {Uint8Array|null} [headerTag] StoreInit headerTag
             * @property {number|null} [headerSizeClass] StoreInit headerSizeClass
             * @property {Uint8Array|null} [bodyNonce] StoreInit bodyNonce
             * @property {Uint8Array|null} [bodyTag] StoreInit bodyTag
             * @property {number|null} [bodySizeClass] StoreInit bodySizeClass
             * @property {number|Long|null} [bodyTotalSize] StoreInit bodyTotalSize
             * @property {Uint8Array|null} [bodyContentAddress] StoreInit bodyContentAddress
             */

            /**
             * Constructs a new StoreInit.
             * @memberof dmcn.relay
             * @classdesc Represents a StoreInit.
             * @implements IStoreInit
             * @constructor
             * @param {dmcn.relay.IStoreInit=} [properties] Properties to set
             */
            function StoreInit(properties) {
                this.recipients = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * StoreInit senderAddress.
             * @member {string} senderAddress
             * @memberof dmcn.relay.StoreInit
             * @instance
             */
            StoreInit.prototype.senderAddress = "";

            /**
             * StoreInit senderSignature.
             * @member {Uint8Array} senderSignature
             * @memberof dmcn.relay.StoreInit
             * @instance
             */
            StoreInit.prototype.senderSignature = $util.newBuffer([]);

            /**
             * StoreInit version.
             * @member {number} version
             * @memberof dmcn.relay.StoreInit
             * @instance
             */
            StoreInit.prototype.version = 0;

            /**
             * StoreInit messageId.
             * @member {Uint8Array} messageId
             * @memberof dmcn.relay.StoreInit
             * @instance
             */
            StoreInit.prototype.messageId = $util.newBuffer([]);

            /**
             * StoreInit createdAt.
             * @member {number|Long} createdAt
             * @memberof dmcn.relay.StoreInit
             * @instance
             */
            StoreInit.prototype.createdAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * StoreInit recipients.
             * @member {Array.<dmcn.message.IRecipientRecord>} recipients
             * @memberof dmcn.relay.StoreInit
             * @instance
             */
            StoreInit.prototype.recipients = $util.emptyArray;

            /**
             * StoreInit encryptedHeader.
             * @member {Uint8Array} encryptedHeader
             * @memberof dmcn.relay.StoreInit
             * @instance
             */
            StoreInit.prototype.encryptedHeader = $util.newBuffer([]);

            /**
             * StoreInit headerNonce.
             * @member {Uint8Array} headerNonce
             * @memberof dmcn.relay.StoreInit
             * @instance
             */
            StoreInit.prototype.headerNonce = $util.newBuffer([]);

            /**
             * StoreInit headerTag.
             * @member {Uint8Array} headerTag
             * @memberof dmcn.relay.StoreInit
             * @instance
             */
            StoreInit.prototype.headerTag = $util.newBuffer([]);

            /**
             * StoreInit headerSizeClass.
             * @member {number} headerSizeClass
             * @memberof dmcn.relay.StoreInit
             * @instance
             */
            StoreInit.prototype.headerSizeClass = 0;

            /**
             * StoreInit bodyNonce.
             * @member {Uint8Array} bodyNonce
             * @memberof dmcn.relay.StoreInit
             * @instance
             */
            StoreInit.prototype.bodyNonce = $util.newBuffer([]);

            /**
             * StoreInit bodyTag.
             * @member {Uint8Array} bodyTag
             * @memberof dmcn.relay.StoreInit
             * @instance
             */
            StoreInit.prototype.bodyTag = $util.newBuffer([]);

            /**
             * StoreInit bodySizeClass.
             * @member {number} bodySizeClass
             * @memberof dmcn.relay.StoreInit
             * @instance
             */
            StoreInit.prototype.bodySizeClass = 0;

            /**
             * StoreInit bodyTotalSize.
             * @member {number|Long} bodyTotalSize
             * @memberof dmcn.relay.StoreInit
             * @instance
             */
            StoreInit.prototype.bodyTotalSize = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * StoreInit bodyContentAddress.
             * @member {Uint8Array} bodyContentAddress
             * @memberof dmcn.relay.StoreInit
             * @instance
             */
            StoreInit.prototype.bodyContentAddress = $util.newBuffer([]);

            /**
             * Creates a new StoreInit instance using the specified properties.
             * @function create
             * @memberof dmcn.relay.StoreInit
             * @static
             * @param {dmcn.relay.IStoreInit=} [properties] Properties to set
             * @returns {dmcn.relay.StoreInit} StoreInit instance
             */
            StoreInit.create = function create(properties) {
                return new StoreInit(properties);
            };

            /**
             * Encodes the specified StoreInit message. Does not implicitly {@link dmcn.relay.StoreInit.verify|verify} messages.
             * @function encode
             * @memberof dmcn.relay.StoreInit
             * @static
             * @param {dmcn.relay.IStoreInit} message StoreInit message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StoreInit.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.senderAddress != null && Object.hasOwnProperty.call(message, "senderAddress"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.senderAddress);
                if (message.senderSignature != null && Object.hasOwnProperty.call(message, "senderSignature"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.senderSignature);
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.version);
                if (message.messageId != null && Object.hasOwnProperty.call(message, "messageId"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.messageId);
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.createdAt);
                if (message.recipients != null && message.recipients.length)
                    for (let i = 0; i < message.recipients.length; ++i)
                        $root.dmcn.message.RecipientRecord.encode(message.recipients[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
                if (message.encryptedHeader != null && Object.hasOwnProperty.call(message, "encryptedHeader"))
                    writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.encryptedHeader);
                if (message.headerNonce != null && Object.hasOwnProperty.call(message, "headerNonce"))
                    writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.headerNonce);
                if (message.headerTag != null && Object.hasOwnProperty.call(message, "headerTag"))
                    writer.uint32(/* id 9, wireType 2 =*/74).bytes(message.headerTag);
                if (message.headerSizeClass != null && Object.hasOwnProperty.call(message, "headerSizeClass"))
                    writer.uint32(/* id 10, wireType 0 =*/80).uint32(message.headerSizeClass);
                if (message.bodyNonce != null && Object.hasOwnProperty.call(message, "bodyNonce"))
                    writer.uint32(/* id 11, wireType 2 =*/90).bytes(message.bodyNonce);
                if (message.bodyTag != null && Object.hasOwnProperty.call(message, "bodyTag"))
                    writer.uint32(/* id 12, wireType 2 =*/98).bytes(message.bodyTag);
                if (message.bodySizeClass != null && Object.hasOwnProperty.call(message, "bodySizeClass"))
                    writer.uint32(/* id 13, wireType 0 =*/104).uint32(message.bodySizeClass);
                if (message.bodyTotalSize != null && Object.hasOwnProperty.call(message, "bodyTotalSize"))
                    writer.uint32(/* id 14, wireType 0 =*/112).uint64(message.bodyTotalSize);
                if (message.bodyContentAddress != null && Object.hasOwnProperty.call(message, "bodyContentAddress"))
                    writer.uint32(/* id 16, wireType 2 =*/130).bytes(message.bodyContentAddress);
                return writer;
            };

            /**
             * Encodes the specified StoreInit message, length delimited. Does not implicitly {@link dmcn.relay.StoreInit.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dmcn.relay.StoreInit
             * @static
             * @param {dmcn.relay.IStoreInit} message StoreInit message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StoreInit.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a StoreInit message from the specified reader or buffer.
             * @function decode
             * @memberof dmcn.relay.StoreInit
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dmcn.relay.StoreInit} StoreInit
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StoreInit.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmcn.relay.StoreInit();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.senderAddress = reader.string();
                            break;
                        }
                    case 2: {
                            message.senderSignature = reader.bytes();
                            break;
                        }
                    case 3: {
                            message.version = reader.uint32();
                            break;
                        }
                    case 4: {
                            message.messageId = reader.bytes();
                            break;
                        }
                    case 5: {
                            message.createdAt = reader.int64();
                            break;
                        }
                    case 6: {
                            if (!(message.recipients && message.recipients.length))
                                message.recipients = [];
                            message.recipients.push($root.dmcn.message.RecipientRecord.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 7: {
                            message.encryptedHeader = reader.bytes();
                            break;
                        }
                    case 8: {
                            message.headerNonce = reader.bytes();
                            break;
                        }
                    case 9: {
                            message.headerTag = reader.bytes();
                            break;
                        }
                    case 10: {
                            message.headerSizeClass = reader.uint32();
                            break;
                        }
                    case 11: {
                            message.bodyNonce = reader.bytes();
                            break;
                        }
                    case 12: {
                            message.bodyTag = reader.bytes();
                            break;
                        }
                    case 13: {
                            message.bodySizeClass = reader.uint32();
                            break;
                        }
                    case 14: {
                            message.bodyTotalSize = reader.uint64();
                            break;
                        }
                    case 16: {
                            message.bodyContentAddress = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a StoreInit message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dmcn.relay.StoreInit
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dmcn.relay.StoreInit} StoreInit
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StoreInit.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a StoreInit message.
             * @function verify
             * @memberof dmcn.relay.StoreInit
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StoreInit.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.senderAddress != null && Object.hasOwnProperty.call(message, "senderAddress"))
                    if (!$util.isString(message.senderAddress))
                        return "senderAddress: string expected";
                if (message.senderSignature != null && Object.hasOwnProperty.call(message, "senderSignature"))
                    if (!(message.senderSignature && typeof message.senderSignature.length === "number" || $util.isString(message.senderSignature)))
                        return "senderSignature: buffer expected";
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    if (!$util.isInteger(message.version))
                        return "version: integer expected";
                if (message.messageId != null && Object.hasOwnProperty.call(message, "messageId"))
                    if (!(message.messageId && typeof message.messageId.length === "number" || $util.isString(message.messageId)))
                        return "messageId: buffer expected";
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
                        return "createdAt: integer|Long expected";
                if (message.recipients != null && Object.hasOwnProperty.call(message, "recipients")) {
                    if (!Array.isArray(message.recipients))
                        return "recipients: array expected";
                    for (let i = 0; i < message.recipients.length; ++i) {
                        let error = $root.dmcn.message.RecipientRecord.verify(message.recipients[i], long + 1);
                        if (error)
                            return "recipients." + error;
                    }
                }
                if (message.encryptedHeader != null && Object.hasOwnProperty.call(message, "encryptedHeader"))
                    if (!(message.encryptedHeader && typeof message.encryptedHeader.length === "number" || $util.isString(message.encryptedHeader)))
                        return "encryptedHeader: buffer expected";
                if (message.headerNonce != null && Object.hasOwnProperty.call(message, "headerNonce"))
                    if (!(message.headerNonce && typeof message.headerNonce.length === "number" || $util.isString(message.headerNonce)))
                        return "headerNonce: buffer expected";
                if (message.headerTag != null && Object.hasOwnProperty.call(message, "headerTag"))
                    if (!(message.headerTag && typeof message.headerTag.length === "number" || $util.isString(message.headerTag)))
                        return "headerTag: buffer expected";
                if (message.headerSizeClass != null && Object.hasOwnProperty.call(message, "headerSizeClass"))
                    if (!$util.isInteger(message.headerSizeClass))
                        return "headerSizeClass: integer expected";
                if (message.bodyNonce != null && Object.hasOwnProperty.call(message, "bodyNonce"))
                    if (!(message.bodyNonce && typeof message.bodyNonce.length === "number" || $util.isString(message.bodyNonce)))
                        return "bodyNonce: buffer expected";
                if (message.bodyTag != null && Object.hasOwnProperty.call(message, "bodyTag"))
                    if (!(message.bodyTag && typeof message.bodyTag.length === "number" || $util.isString(message.bodyTag)))
                        return "bodyTag: buffer expected";
                if (message.bodySizeClass != null && Object.hasOwnProperty.call(message, "bodySizeClass"))
                    if (!$util.isInteger(message.bodySizeClass))
                        return "bodySizeClass: integer expected";
                if (message.bodyTotalSize != null && Object.hasOwnProperty.call(message, "bodyTotalSize"))
                    if (!$util.isInteger(message.bodyTotalSize) && !(message.bodyTotalSize && $util.isInteger(message.bodyTotalSize.low) && $util.isInteger(message.bodyTotalSize.high)))
                        return "bodyTotalSize: integer|Long expected";
                if (message.bodyContentAddress != null && Object.hasOwnProperty.call(message, "bodyContentAddress"))
                    if (!(message.bodyContentAddress && typeof message.bodyContentAddress.length === "number" || $util.isString(message.bodyContentAddress)))
                        return "bodyContentAddress: buffer expected";
                return null;
            };

            /**
             * Creates a StoreInit message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dmcn.relay.StoreInit
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dmcn.relay.StoreInit} StoreInit
             */
            StoreInit.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dmcn.relay.StoreInit)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dmcn.relay.StoreInit: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dmcn.relay.StoreInit();
                if (object.senderAddress != null)
                    message.senderAddress = String(object.senderAddress);
                if (object.senderSignature != null)
                    if (typeof object.senderSignature === "string")
                        $util.base64.decode(object.senderSignature, message.senderSignature = $util.newBuffer($util.base64.length(object.senderSignature)), 0);
                    else if (object.senderSignature.length >= 0)
                        message.senderSignature = object.senderSignature;
                if (object.version != null)
                    message.version = object.version >>> 0;
                if (object.messageId != null)
                    if (typeof object.messageId === "string")
                        $util.base64.decode(object.messageId, message.messageId = $util.newBuffer($util.base64.length(object.messageId)), 0);
                    else if (object.messageId.length >= 0)
                        message.messageId = object.messageId;
                if (object.createdAt != null)
                    if ($util.Long)
                        message.createdAt = $util.Long.fromValue(object.createdAt, false);
                    else if (typeof object.createdAt === "string")
                        message.createdAt = parseInt(object.createdAt, 10);
                    else if (typeof object.createdAt === "number")
                        message.createdAt = object.createdAt;
                    else if (typeof object.createdAt === "object")
                        message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
                if (object.recipients) {
                    if (!Array.isArray(object.recipients))
                        throw TypeError(".dmcn.relay.StoreInit.recipients: array expected");
                    message.recipients = [];
                    for (let i = 0; i < object.recipients.length; ++i) {
                        if (!$util.isObject(object.recipients[i]))
                            throw TypeError(".dmcn.relay.StoreInit.recipients: object expected");
                        message.recipients[i] = $root.dmcn.message.RecipientRecord.fromObject(object.recipients[i], long + 1);
                    }
                }
                if (object.encryptedHeader != null)
                    if (typeof object.encryptedHeader === "string")
                        $util.base64.decode(object.encryptedHeader, message.encryptedHeader = $util.newBuffer($util.base64.length(object.encryptedHeader)), 0);
                    else if (object.encryptedHeader.length >= 0)
                        message.encryptedHeader = object.encryptedHeader;
                if (object.headerNonce != null)
                    if (typeof object.headerNonce === "string")
                        $util.base64.decode(object.headerNonce, message.headerNonce = $util.newBuffer($util.base64.length(object.headerNonce)), 0);
                    else if (object.headerNonce.length >= 0)
                        message.headerNonce = object.headerNonce;
                if (object.headerTag != null)
                    if (typeof object.headerTag === "string")
                        $util.base64.decode(object.headerTag, message.headerTag = $util.newBuffer($util.base64.length(object.headerTag)), 0);
                    else if (object.headerTag.length >= 0)
                        message.headerTag = object.headerTag;
                if (object.headerSizeClass != null)
                    message.headerSizeClass = object.headerSizeClass >>> 0;
                if (object.bodyNonce != null)
                    if (typeof object.bodyNonce === "string")
                        $util.base64.decode(object.bodyNonce, message.bodyNonce = $util.newBuffer($util.base64.length(object.bodyNonce)), 0);
                    else if (object.bodyNonce.length >= 0)
                        message.bodyNonce = object.bodyNonce;
                if (object.bodyTag != null)
                    if (typeof object.bodyTag === "string")
                        $util.base64.decode(object.bodyTag, message.bodyTag = $util.newBuffer($util.base64.length(object.bodyTag)), 0);
                    else if (object.bodyTag.length >= 0)
                        message.bodyTag = object.bodyTag;
                if (object.bodySizeClass != null)
                    message.bodySizeClass = object.bodySizeClass >>> 0;
                if (object.bodyTotalSize != null)
                    if ($util.Long)
                        message.bodyTotalSize = $util.Long.fromValue(object.bodyTotalSize, true);
                    else if (typeof object.bodyTotalSize === "string")
                        message.bodyTotalSize = parseInt(object.bodyTotalSize, 10);
                    else if (typeof object.bodyTotalSize === "number")
                        message.bodyTotalSize = object.bodyTotalSize;
                    else if (typeof object.bodyTotalSize === "object")
                        message.bodyTotalSize = new $util.LongBits(object.bodyTotalSize.low >>> 0, object.bodyTotalSize.high >>> 0).toNumber(true);
                if (object.bodyContentAddress != null)
                    if (typeof object.bodyContentAddress === "string")
                        $util.base64.decode(object.bodyContentAddress, message.bodyContentAddress = $util.newBuffer($util.base64.length(object.bodyContentAddress)), 0);
                    else if (object.bodyContentAddress.length >= 0)
                        message.bodyContentAddress = object.bodyContentAddress;
                return message;
            };

            /**
             * Creates a plain object from a StoreInit message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dmcn.relay.StoreInit
             * @static
             * @param {dmcn.relay.StoreInit} message StoreInit
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StoreInit.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.recipients = [];
                if (options.defaults) {
                    object.senderAddress = "";
                    if (options.bytes === String)
                        object.senderSignature = "";
                    else {
                        object.senderSignature = [];
                        if (options.bytes !== Array)
                            object.senderSignature = $util.newBuffer(object.senderSignature);
                    }
                    object.version = 0;
                    if (options.bytes === String)
                        object.messageId = "";
                    else {
                        object.messageId = [];
                        if (options.bytes !== Array)
                            object.messageId = $util.newBuffer(object.messageId);
                    }
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.createdAt = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.createdAt = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if (options.bytes === String)
                        object.encryptedHeader = "";
                    else {
                        object.encryptedHeader = [];
                        if (options.bytes !== Array)
                            object.encryptedHeader = $util.newBuffer(object.encryptedHeader);
                    }
                    if (options.bytes === String)
                        object.headerNonce = "";
                    else {
                        object.headerNonce = [];
                        if (options.bytes !== Array)
                            object.headerNonce = $util.newBuffer(object.headerNonce);
                    }
                    if (options.bytes === String)
                        object.headerTag = "";
                    else {
                        object.headerTag = [];
                        if (options.bytes !== Array)
                            object.headerTag = $util.newBuffer(object.headerTag);
                    }
                    object.headerSizeClass = 0;
                    if (options.bytes === String)
                        object.bodyNonce = "";
                    else {
                        object.bodyNonce = [];
                        if (options.bytes !== Array)
                            object.bodyNonce = $util.newBuffer(object.bodyNonce);
                    }
                    if (options.bytes === String)
                        object.bodyTag = "";
                    else {
                        object.bodyTag = [];
                        if (options.bytes !== Array)
                            object.bodyTag = $util.newBuffer(object.bodyTag);
                    }
                    object.bodySizeClass = 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.bodyTotalSize = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.bodyTotalSize = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if (options.bytes === String)
                        object.bodyContentAddress = "";
                    else {
                        object.bodyContentAddress = [];
                        if (options.bytes !== Array)
                            object.bodyContentAddress = $util.newBuffer(object.bodyContentAddress);
                    }
                }
                if (message.senderAddress != null && Object.hasOwnProperty.call(message, "senderAddress"))
                    object.senderAddress = message.senderAddress;
                if (message.senderSignature != null && Object.hasOwnProperty.call(message, "senderSignature"))
                    object.senderSignature = options.bytes === String ? $util.base64.encode(message.senderSignature, 0, message.senderSignature.length) : options.bytes === Array ? Array.prototype.slice.call(message.senderSignature) : message.senderSignature;
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    object.version = message.version;
                if (message.messageId != null && Object.hasOwnProperty.call(message, "messageId"))
                    object.messageId = options.bytes === String ? $util.base64.encode(message.messageId, 0, message.messageId.length) : options.bytes === Array ? Array.prototype.slice.call(message.messageId) : message.messageId;
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.createdAt = typeof message.createdAt === "number" ? BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
                    else if (typeof message.createdAt === "number")
                        object.createdAt = options.longs === String ? String(message.createdAt) : message.createdAt;
                    else
                        object.createdAt = options.longs === String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
                if (message.recipients && message.recipients.length) {
                    object.recipients = [];
                    for (let j = 0; j < message.recipients.length; ++j)
                        object.recipients[j] = $root.dmcn.message.RecipientRecord.toObject(message.recipients[j], options, q + 1);
                }
                if (message.encryptedHeader != null && Object.hasOwnProperty.call(message, "encryptedHeader"))
                    object.encryptedHeader = options.bytes === String ? $util.base64.encode(message.encryptedHeader, 0, message.encryptedHeader.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedHeader) : message.encryptedHeader;
                if (message.headerNonce != null && Object.hasOwnProperty.call(message, "headerNonce"))
                    object.headerNonce = options.bytes === String ? $util.base64.encode(message.headerNonce, 0, message.headerNonce.length) : options.bytes === Array ? Array.prototype.slice.call(message.headerNonce) : message.headerNonce;
                if (message.headerTag != null && Object.hasOwnProperty.call(message, "headerTag"))
                    object.headerTag = options.bytes === String ? $util.base64.encode(message.headerTag, 0, message.headerTag.length) : options.bytes === Array ? Array.prototype.slice.call(message.headerTag) : message.headerTag;
                if (message.headerSizeClass != null && Object.hasOwnProperty.call(message, "headerSizeClass"))
                    object.headerSizeClass = message.headerSizeClass;
                if (message.bodyNonce != null && Object.hasOwnProperty.call(message, "bodyNonce"))
                    object.bodyNonce = options.bytes === String ? $util.base64.encode(message.bodyNonce, 0, message.bodyNonce.length) : options.bytes === Array ? Array.prototype.slice.call(message.bodyNonce) : message.bodyNonce;
                if (message.bodyTag != null && Object.hasOwnProperty.call(message, "bodyTag"))
                    object.bodyTag = options.bytes === String ? $util.base64.encode(message.bodyTag, 0, message.bodyTag.length) : options.bytes === Array ? Array.prototype.slice.call(message.bodyTag) : message.bodyTag;
                if (message.bodySizeClass != null && Object.hasOwnProperty.call(message, "bodySizeClass"))
                    object.bodySizeClass = message.bodySizeClass;
                if (message.bodyTotalSize != null && Object.hasOwnProperty.call(message, "bodyTotalSize"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.bodyTotalSize = typeof message.bodyTotalSize === "number" ? BigInt(message.bodyTotalSize) : $util.Long.fromBits(message.bodyTotalSize.low >>> 0, message.bodyTotalSize.high >>> 0, true).toBigInt();
                    else if (typeof message.bodyTotalSize === "number")
                        object.bodyTotalSize = options.longs === String ? String(message.bodyTotalSize) : message.bodyTotalSize;
                    else
                        object.bodyTotalSize = options.longs === String ? $util.Long.prototype.toString.call(message.bodyTotalSize) : options.longs === Number ? new $util.LongBits(message.bodyTotalSize.low >>> 0, message.bodyTotalSize.high >>> 0).toNumber(true) : message.bodyTotalSize;
                if (message.bodyContentAddress != null && Object.hasOwnProperty.call(message, "bodyContentAddress"))
                    object.bodyContentAddress = options.bytes === String ? $util.base64.encode(message.bodyContentAddress, 0, message.bodyContentAddress.length) : options.bytes === Array ? Array.prototype.slice.call(message.bodyContentAddress) : message.bodyContentAddress;
                return object;
            };

            /**
             * Converts this StoreInit to JSON.
             * @function toJSON
             * @memberof dmcn.relay.StoreInit
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StoreInit.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for StoreInit
             * @function getTypeUrl
             * @memberof dmcn.relay.StoreInit
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            StoreInit.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dmcn.relay.StoreInit";
            };

            return StoreInit;
        })();

        return relay;
    })();

    return dmcn;
})();

export { $root as default };
